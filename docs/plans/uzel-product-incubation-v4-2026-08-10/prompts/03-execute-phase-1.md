# Prompt — execute only corrected Uzel Phase 1

Execute the independently reviewed Phase 1 plans with state validation in the clean manual Phase 1 worktree.
GSD automatic worktrees remain disabled; execution is sequential. Do not advance to
Phase 2.

Before execution confirm the expected branch/clean state, current reviewed plans, zero Critical/High or blocking Medium
findings and explicit dispositions for the remainder, `b185ad1` worktree/safety ref/portable archive, unchanged source and
locks, and the reconciled future roadmap through 7.9.

Rules:

1. Inventory package manager, lock, toolchain, Vite and conformance-tool origins first.
2. Realize only exact lock/integrity/hash-bound bytes.
3. Disable lifecycle scripts by default; allowlist only exact reviewed necessities.
4. Use disposable replay checkout, `HOME`, XDG, temp and cache paths.
5. Deny external egress/public DNS; allow only documented local fixtures and prove denial.
6. Do not modify historical source/lock, use globals/dynamic downloads, copy arbitrary
   dependency trees or substitute packages.
7. Enforce one inventory, one closure path, one attempt and at most one proven harness-
   only correction.
8. Record exact replay verdict and claim-by-claim impacts.
9. Build/test current Nix package separately from store paths with clean state.
10. Inspect/test `b185ad1`; assign retain/amend/supersede/discard with provenance.
11. Produce current authority/schema/threat and CI evidence without feature/refactor work.
12. Reverify ecosystem sources and capture immutable source identities; produce the
    canonical machine upstream registry and RCP, generated human rendering, package/hash
    binding, manifest/build-identity SIR, capability-ledger baseline, clean-room fixture
    plan, independent-peer blocking rule, interop/version-skew backlog, canonical term
    registry, executable-learning witness schema and visibility-aware knowledge system.
13. Define and test the required/optional capability-negotiation and canonical transcript
    contract, including fail-before-guest, no implicit downgrade and no mid-session profile
    mutation.
14. Establish global/per-principal admission, fairness and anti-starvation baselines and
    tests for guest/runtime work.
15. Establish separate local-patch/upstream merge/release/adoption/removal states plus
    named-human/AI-policy/authorship controls without contacting upstream automatically.
16. Make an explicit Phase 2 go/no-go verdict for exact executing-byte identity and launch
    negotiation.
17. Commit atomically and preserve evidence paths/checksums.

After execution run `$gsd-verify-work 1` and `$gsd-progress --forensic`. Present separate
verdicts for historical replay, replacement invariants, current package/native
acceptance, `b185ad1`, authority/schema, compatibility/profile/upstream/maturity/knowledge and CI. Stop for human review before Phase 2.
