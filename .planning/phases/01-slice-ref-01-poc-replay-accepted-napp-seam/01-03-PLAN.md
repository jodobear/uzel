---
phase: 01-slice-ref-01-poc-replay-accepted-napp-seam
plan: 03
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - docs/architecture/phase-01-authority.md
  - docs/architecture/phase-01-identity.md
  - evidence/phase-01/current-source-citations.json
  - docs/architecture/phase-01-interaction.md
  - docs/architecture/durable-formats.toml
  - docs/architecture/instance-profile-paths.toml
  - docs/architecture/storage-retention.md
  - docs/security/phase-01-threat-model.md
  - docs/architecture/phase-01-structure.json
  - .planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-03-SUMMARY.md
autonomous: true
requirements:
  - M0-03
user_setup: []
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-02 through D-05: authority, profile/instance identity, schema floors, and source-grounding are explicit; documents never silently outrank executable source."
    - "D-04: schema/version floors, unknown-version behavior, canonicalization, and migration authority are recorded only where sources support them."
    - "D-02 through D-05: Uzel shell, product service, runtime mediation, and exact-pinned canonical engine/provider responsibilities follow v4 authority; no stale external Napp blocker or ownership is invented."
    - "D-24: view identity, viewed subject, signing identity, instance, profile, runtime, project, and workbench state are distinct axes."
    - "D-27: Phase 1 executes strictly in plan order; Plan 03 consumes Plan-02 current package/native evidence only as a separately labeled measured evidence class and never lets it repair historical replay truth."
    - "D-28: UI/runtime/plugin/helper/filesystem/media/process/automation trust boundaries and mitigations are source-grounded at ASVS level 1."
    - "D-29 and D-30: authority and threat claims cite current source lines plus exact baseline evidence, with uncertainty retained."
    - "D-31: this contract does not expand product scope or authorize implementation beyond Phase 1."
  artifacts:
    - path: docs/architecture/phase-01-authority.md
      provides: "Normative versus executable authority tiers, contradictions, confidence, and Uzel/runtime/engine ownership"
    - path: docs/architecture/phase-01-identity.md
      provides: "Non-conflated identity and state-axis definitions"
    - path: evidence/phase-01/current-source-citations.json
      provides: "Current-head source anchors and hashes for every executable authority claim"
    - path: docs/architecture/durable-formats.toml
      provides: "Versioned durable-format registry with compatibility floors and migration authority"
    - path: docs/architecture/instance-profile-paths.toml
      provides: "Instance/profile selection, persistence, failure, restart, and concurrency paths"
    - path: docs/architecture/storage-retention.md
      provides: "Storage ownership, retention, deletion, export, audit, and synthetic-data rules"
    - path: docs/security/phase-01-threat-model.md
      provides: "Scoped STRIDE/ASVS-L1 trust-boundary and validation baseline"
    - path: docs/architecture/phase-01-structure.json
      provides: "Parsed cross-reference registry plus the immutable architecture-baseline handoff expected by Plan 06 evidence projection"
  key_links:
    - from: evidence/phase-01/current-source-citations.json
      to: docs/architecture/phase-01-authority.md
      via: "Every executable claim carries current head/blob/line anchor and confidence"
      pattern: "source_claim_id"
    - from: docs/architecture/phase-01-identity.md
      to: docs/architecture/instance-profile-paths.toml
      via: "Identity axes map to explicit storage and selection keys without conflation"
      pattern: "axis_id"
    - from: docs/architecture/durable-formats.toml
      to: docs/architecture/storage-retention.md
      via: "Every durable format names owner, location, retention, migration, and deletion authority"
      pattern: "format_id"
    - from: docs/architecture/phase-01-interaction.md
      to: docs/security/phase-01-threat-model.md
      via: "Each side effect and boundary maps to a threat row and evidence validation"
      pattern: "boundary_id"
    - from: docs/architecture/phase-01-structure.json
      to: Plan 06 evidence projection
      via: "Registry records the source-coupled architecture-baseline handoff, its source tree, and disclosure classification; Plan 06 must project only the declared public-safe subset."
      pattern: "downstream_expectations.plan_id=01-06"
  assumptions:
    - id: EDGE-M0-03-CONCURRENCY
      requirement: M0-03
      category: concurrency
      statement: "The spec-less edge probe identified unresolved interrupted and parallel-operation guarantees; manual review must verify cancel/restart behavior, simultaneous profiles/projects, storage conflicts, stale writes, and evidence-preserving recovery against current source and observed tests."
      status: unresolved
      flagged: true
  prohibitions:
    - id: PROHIB-M0-03-AUTHORITY
      requirement: M0-03
      category: transparency
      statement: "MUST NOT overstate source isolation or authority, conflate read identity/viewed subject/signing identity, assign Uzel product authority to its replaceable engine/provider, or revive a stale external Napp candidate gate."
      status: unverified
      flagged: true
---

## Phase Goal

**As a** Uzel owner, **I want to** preserve the current POC and incident evidence while separating historical replay truth from current evidence, **so that** Phase 1 can reset execution without inventing a green baseline.

<objective>
Create a current-source-grounded authority, identity, schema, instance/profile, storage, trust, and threat baseline without inventing final architecture.

Purpose: Satisfy M0-03 by making executable truth, normative ownership, durable data, side effects, and uncertainty reviewable before product work resumes.

Output: Authority/identity/interaction contracts, source citations, durable-format and instance/profile registries, storage policy, and an ASVS-L1 threat baseline.
</objective>

<execution_context>
@/home/at/.codex/gsd-core/workflows/execute-plan.md
@/home/at/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-CONTEXT.md
@docs/plans/uzel-product-incubation-v4-2026-08-10/00-GSD-INGEST.md
@docs/plans/uzel-product-incubation-v4-2026-08-10/00-GSD-INGEST-ADR.md
@docs/plans/uzel-product-incubation-v4-2026-08-10/02-PRODUCT-ARCHITECTURE.md
@docs/plans/uzel-product-incubation-v4-2026-08-10/09-PRODUCTION-MATURITY.md
@apps/uzel/src
@apps/uzel/src-tauri/src
@apps/uzel-napd/src
@crates/napd/src
@crates/napd-protocol/src
@contracts
@napplets
@scripts/ref-candidate-check.py
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Trace one user action from current source through authority and distinct identity axes</name>
  <files>docs/architecture/phase-01-authority.md, docs/architecture/phase-01-identity.md, evidence/phase-01/current-source-citations.json</files>
  <execution_contract>`authoritative_claim_ids` is deterministic and exact, not author-selected: four ownership IDs (`ownership/shell`, `ownership/product_service`, `ownership/runtime_mediation`, `ownership/canonical_engine`), eleven identity IDs (`identity/view`, `identity/viewed_subject`, `identity/signing`, `identity/app_instance`, `identity/profile`, `identity/runtime`, `identity/project`, `identity/workbench_state`, `identity/actor`, `identity/role`, `identity/signer`), nine `trace/&lt;required-segment-id&gt;` IDs, five schema IDs (`schema/source_of_truth`, `schema/version_floor`, `schema/unknown_version`, `schema/canonicalization`, `schema/migration_authority`), and eight `workflow/&lt;Plan-02-workflow-id&gt;` IDs. Claim IDs equal this universe bijectively. Each trace segment names its matching trace claim and uses exactly that claim’s citations. Every source-test registry row carries exact sorted nonempty `claim_ids`; those IDs equal the claims whose citations actually reference the row, preventing an unrelated test/command/evidence row from being reused without an explicit exact semantic subject link. Every registry row is referenced; no unused/self-selected evidence row. If `workflow/reopen` is cited, its Plan-02 workflow row must reference one executed command and carry an exact nonempty `reopen_outcome_pointer` plus `observed_reopen_outcome`; recompute that value from the strict command evidence envelope stdout and require its cited source-test rows to bind that same command or artifact.</execution_contract>
  <semantic_fact_contract>Every claim owns exactly one unique `expected_fact`; every citation repeats its `fact_id`. Pin a deterministic parser registry for each admitted source/test language with exact executable path/version/SHA-256, parse argv, retained version evidence, and supported extensions. Parser output is canonical strict JSON AST and every parsed node carries exact source span plus stable structural node ID. A cited fact names exact source node kind, selector, operand/field path, operator, and canonical expected value. `current_test` support names exact test declaration node ID and one assertion node whose actual operand structurally references the cited source symbol/behavior coordinate and whose operator/expected operand equals the fact; string/comment co-occurrence is rejected. Command/evidence facts bind exact JSON pointer/value as before. An absent claim names an exact finite structural universe (`declaration`, `call`, `field`, or `command` nodes below explicit tracked roots), selector, and `expected_nodes: []`; parse every applicable tracked blob, require at least one parsed file and the exact filtered node set empty. Regex/substrings cannot prove absence. Registry rows carry exact sorted nonempty `fact_ids`; trace-segment fact, claim fact, source node, assertion node, and expected operand/value all agree.</semantic_fact_contract>
  <read_first>
    - .planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-CONTEXT.md — D-02 through D-05, D-11, D-24, D-28 through D-31
    - docs/plans/uzel-product-incubation-v4-2026-08-10/00-GSD-INGEST.md — immutable programme authority and exact locked decisions
    - docs/plans/uzel-product-incubation-v4-2026-08-10/00-GSD-INGEST-ADR.md — parser-safe decision projection
    - docs/plans/uzel-product-incubation-v4-2026-08-10/02-PRODUCT-ARCHITECTURE.md — ownership, seams, identity axes, formats, and storage contracts
    - evidence/phase-01/current-output-inventory.json, evidence/phase-01/current-package-verdict.json, evidence/phase-01/current-state-paths.json, evidence/phase-01/current-native-baseline.json, evidence/phase-01/current-measurements.json — completed Plan-02 measured-current inputs; preserve their evidence class and do not use them as source or historical-replay truth
    - apps/uzel/src, apps/uzel/src-tauri/src, apps/uzel-napd/src, crates/napd/src, crates/napd-protocol/src, contracts, napplets — current executable source map; inspect only files reached by Graphify/source search
    - scripts/ref-candidate-check.py — current repository-qualified candidate/authority evidence tracer located by Graphify
  </read_first>
  <action>Implement D-02 through D-05, D-11, D-24, D-27, D-29, and D-30 as a source-grounded tracer. Declare each claim's stable fact ID, concrete subject, predicate, expected source literal, and exact test-body literal, observed JSON value, or absence regex before selecting evidence. Start with one existing select/compose/preview/reopen path found in current source and use the current Graphify graph plus focused `rg` to identify its shell entry, Tauri/native boundary, `apps/uzel-napd` entry, `crates/napd` runtime behavior, `crates/napd-protocol` contract, exact napplet/contract input, side effect, persistence path, and error/cancel behavior. `current-source-citations.json` has a versioned schema and binds its single `source_head`/`source_tree`; those values must exactly equal the completed Plan-02 authoritative current-source identity in `current-output-inventory.json`, and `source_head^{tree}` resolved by Git must equal JSON `source_tree` before accepting any citation. Give the JSON one nonempty `full_tracer_claim_id` naming exactly one claim with `trace_complete: true` and exactly the nine required `trace_segments` IDs `shell_entry`, `native_boundary`, `napd_entry`, `runtime_behavior`, `protocol_contract`, `napplet_or_contract_input`, `side_effect`, `persistence`, and `error_or_cancel`; every segment has a nonempty `citations[]` array. Each claim has either a non-empty `citations[]` array or a strict `explicit_absence` object with `status`, `reason`, `owner`, and `evidence_need`; it cannot have both or neither, and the designated tracer claim must use citations rather than absence. Every claim and segment citation repeats that exact head/tree plus immutable `blob_oid`, repo-relative `path`, positive `line_start`/`line_end`, and a source-test ID. Add one nonempty, unique-ID `source_test_registry`: every cited `source_test_id` resolves exactly once to a row of kind `current_test`, `current_command`, or `current_evidence`. A `current_test` row repeats the authoritative head/tree and names a repo-relative source path, blob OID, and positive in-range line span resolved from that head; a `current_command` row repeats the same head/tree and names an existing Plan-02 `current-native-baseline.json` command row; a `current_evidence` row repeats the same head/tree and names an exact path/digest pair in that baseline's retained-artifact registry. No citation may point to a historical, proposed, or unknown test row. Resolve `source_head:path` with Git, require its blob OID to equal `blob_oid`, count the resolved blob's lines, and reject any absent/out-of-range line, mismatched path/blob/head/tree, unresolved source-test ID, or malformed absence/source-test evidence. Expand only after the tracer is complete to every current executable authority claim required by M0-03. In the authority document distinguish immutable programme ingest, ADR/context projection, supporting contracts, current plans/requirements, executable source, source-grounded test evidence, Plan-02 measured package/native evidence, and stale brownfield evidence; record contradiction resolution and change authority. Preserve v4 ownership: trusted Uzel shell owns presentation/human decisions; Uzel product service owns durable workflow semantics; runtime mediation owns guest/runtime truth; the exact-pinned canonical engine/provider owns its protocol semantics behind a narrow Uzel-private adapter. No external Napp candidate is a v4 Phase-1 entry condition. In the identity document define and separately trace view identity, viewed subject, signing identity, app instance, profile, runtime, project, workbench state, actor, role, and signer; record which are present, absent, inferred, or prohibited from conflation. Document schema source-of-truth, version floor, unknown-field/version behavior, canonicalization, migration authority, and source drift. Before accepting any `workflow/reopen` citation, reopen the Plan-02 ignored raw evidence by its retained registry path, recheck ignore ownership, regular-file/component-symlink safety and byte digest, parse its strict envelope, and recompute the observed reopen outcome; a copied claim or digest alone is insufficient. Read Plan-02 artifacts because strict D-27 ordering makes them prior evidence, but label their observations separately: they may inform current package/native authority and paths, never repair Track-A replay or replace current-source citations. No document claim may upgrade missing code or unmeasured behavior; mark it `not_implemented`, `not_observed`, or `contradicted` with owner and evidence need. Write JSON atomically; on citation-head drift stop and restart the source scan rather than mixing heads.</action>
  <verify>
    <automated>python3 -m json.tool evidence/phase-01/current-source-citations.json &gt;/dev/null &amp;&amp; python3 -c 'import json,subprocess; d=json.load(open("evidence/phase-01/current-source-citations.json")); o=json.load(open("evidence/phase-01/current-output-inventory.json")); h=d["source_head"]; t=d["source_tree"]; claims=d["claims"]; required={"shell_entry","native_boundary","napd_entry","runtime_behavior","protocol_contract","napplet_or_contract_input","side_effect","persistence","error_or_cancel"}; assert d.get("schema") and h==o["source_head"] and t==o["source_tree"] and subprocess.check_output(["git","rev-parse",h+"^{tree}"],text=True).strip()==t; assert claims and all(isinstance(x.get("id"),str) and x["id"] for x in claims) and len({x["id"] for x in claims})==len(claims); assert all((bool(x.get("citations")) != bool(x.get("explicit_absence"))) and (not x.get("explicit_absence") or (set(x["explicit_absence"])=={"status","reason","owner","evidence_need"} and all(isinstance(x["explicit_absence"].get(k),str) and x["explicit_absence"][k] for k in ("status","reason","owner","evidence_need")))) for x in claims); tracer=[x for x in claims if x["id"]==d.get("full_tracer_claim_id")]; assert len(tracer)==1 and tracer[0].get("trace_complete") is True and bool(tracer[0].get("citations")) and not tracer[0].get("explicit_absence"); segments=tracer[0].get("trace_segments"); assert isinstance(segments,list) and len(segments)==len(required) and {x.get("id") for x in segments}==required and all(isinstance(x.get("citations"),list) and x["citations"] for x in segments); cs=[c for x in claims for c in x.get("citations",[])]+[c for x in segments for c in x["citations"]]; assert cs and all(c["head"]==h and c["tree"]==t and c.get("source_test_id") for c in cs); assert all(subprocess.check_output(["git","rev-parse","%s:%s" % (h,c["path"])],text=True).strip()==c["blob_oid"] and len(subprocess.check_output(["git","show","%s:%s" % (h,c["path"])],text=True).splitlines())&gt;=c["line_end"]&gt;=c["line_start"]&gt;0 for c in cs)' &amp;&amp; python3 -c 'import json,pathlib,re,subprocess; d=json.load(open("evidence/phase-01/current-source-citations.json")); b=json.load(open("evidence/phase-01/current-native-baseline.json")); h=d["source_head"]; t=d["source_tree"]; rows=d["source_test_registry"]; assert isinstance(rows,list) and rows and all(isinstance(x.get("id"),str) and x["id"] for x in rows) and len({x["id"] for x in rows})==len(rows); by={x["id"]:x for x in rows}; commands={x["row_id"] for x in b["commands"]}; retained={x["path"]:x["sha256"] for x in b["retained_artifacts"]}; valid_test=lambda x: x.get("kind")=="current_test" and x.get("head")==h and x.get("tree")==t and isinstance(x.get("path"),str) and bool(x["path"]) and not pathlib.PurePosixPath(x["path"]).is_absolute() and ".." not in pathlib.PurePosixPath(x["path"]).parts and re.fullmatch(r"[0-9a-f]{40}",x.get("blob_oid","")) and subprocess.check_output(["git","rev-parse",h+":"+x["path"]],text=True).strip()==x["blob_oid"] and isinstance(x.get("line_start"),int) and isinstance(x.get("line_end"),int) and len(subprocess.check_output(["git","show",h+":"+x["path"]],text=True).splitlines())&gt;=x["line_end"]&gt;=x["line_start"]&gt;0; valid_command=lambda x: x.get("kind")=="current_command" and x.get("head")==h and x.get("tree")==t and x.get("command_row_id") in commands; valid_evidence=lambda x: x.get("kind")=="current_evidence" and x.get("head")==h and x.get("tree")==t and isinstance(x.get("evidence_path"),str) and retained.get(x["evidence_path"])==x.get("evidence_sha256") and re.fullmatch(r"[0-9a-f]{64}",x.get("evidence_sha256","")); assert all(valid_test(x) or valid_command(x) or valid_evidence(x) for x in rows); tracer=next(x for x in d["claims"] if x["id"]==d["full_tracer_claim_id"]); cs=[c for x in d["claims"] for c in x.get("citations",[])]+[c for x in tracer["trace_segments"] for c in x["citations"]]; assert cs and all(c.get("source_test_id") in by for c in cs)' &amp;&amp; rg -q 'Trusted.*shell.*owns|shell ownership' docs/architecture/phase-01-authority.md &amp;&amp; rg -q 'product service.*owns|product-service ownership' docs/architecture/phase-01-authority.md &amp;&amp; rg -q 'runtime mediation.*owns|runtime ownership' docs/architecture/phase-01-authority.md &amp;&amp; rg -q 'canonical.*engine.*owns|engine ownership' docs/architecture/phase-01-authority.md &amp;&amp; rg -q 'view identity' docs/architecture/phase-01-identity.md &amp;&amp; rg -q 'signing identity' docs/architecture/phase-01-identity.md</automated>
    <automated>python3 -c 'import json,pathlib
def strict(pairs):
 d={}; [(_ for _ in ()).throw(ValueError("duplicate key "+k)) if k in d else d.__setitem__(k,v) for k,v in pairs]; return d
raw=pathlib.Path("evidence/phase-01/current-source-citations.json").read_bytes(); d=json.loads(raw,object_pairs_hook=strict,parse_constant=lambda x:(_ for _ in ()).throw(ValueError(x))); assert raw==json.dumps(d,sort_keys=True,separators=(",",":"),ensure_ascii=False).encode(); seg={"shell_entry","native_boundary","napd_entry","runtime_behavior","protocol_contract","napplet_or_contract_input","side_effect","persistence","error_or_cancel"}; ids={"ownership/shell","ownership/product_service","ownership/runtime_mediation","ownership/canonical_engine"}|{"identity/"+x for x in ("view","viewed_subject","signing","app_instance","profile","runtime","project","workbench_state","actor","role","signer")}|{"trace/"+x for x in seg}|{"schema/"+x for x in ("source_of_truth","version_floor","unknown_version","canonicalization","migration_authority")}|{"workflow/"+x for x in ("import","select","edit","compose","preview","export","save","reopen")}; assert d["authoritative_claim_ids"]==sorted(ids) and {x["id"] for x in d["claims"]}==ids; by={x["id"]:x for x in d["claims"]}; tracer=by[d["full_tracer_claim_id"]]; assert all(x["claim_id"]=="trace/"+x["id"] and x["citations"]==by[x["claim_id"]]["citations"] for x in tracer["trace_segments"]); used={c["source_test_id"] for x in d["claims"] for c in x.get("citations",[])}|{c["source_test_id"] for x in tracer["trace_segments"] for c in x["citations"]}; assert used=={x["id"] for x in d["source_test_registry"]}; assert all(x.get("explicit_absence") is None or x["explicit_absence"]["status"] in ("not_implemented","not_observed","contradicted") for x in d["claims"])'</automated>
    <automated>python3 -c 'import json
d=json.load(open("evidence/phase-01/current-source-citations.json")); registry={x["id"]:x for x in d["source_test_registry"]}; pairs=[(x["id"],c) for x in d["claims"] for c in x.get("citations",[])]; tracer=next(x for x in d["claims"] if x["id"]==d["full_tracer_claim_id"]); pairs += [(x["claim_id"],c) for x in tracer["trace_segments"] for c in x["citations"]]; expected={k:set() for k in registry}
for claim_id,c in pairs: expected[c["source_test_id"]].add(claim_id)
for rid,row in registry.items():
 assert row["claim_ids"]==sorted(expected[rid]) and row["claim_ids"]'</automated>
    <automated>python3 -c 'import hashlib,json,pathlib,subprocess
def strict(pairs):
 d={}
 for k,v in pairs:
  if k in d: raise ValueError("duplicate key "+k)
  d[k]=v
 return d
def ptr(obj,p):
 assert isinstance(p,str) and p.startswith("/")
 for part in p.split("/")[1:]: obj=obj[int(part)] if isinstance(obj,list) else obj[part.replace("~1","/").replace("~0","~")]
 return obj
d=json.load(open("evidence/phase-01/current-source-citations.json"),object_pairs_hook=strict,parse_constant=lambda x:(_ for _ in ()).throw(ValueError(x))); b=json.load(open("evidence/phase-01/current-native-baseline.json"),object_pairs_hook=strict,parse_constant=lambda x:(_ for _ in ()).throw(ValueError(x))); h=d["source_head"]; registry={x["id"]:x for x in d["source_test_registry"]}; commands={x["row_id"]:x for x in b["commands"]}; parsers={x["parser_id"]:x for x in d["parser_registry"]}; assert parsers and len(parsers)==len(d["parser_registry"]); facts={}; refs={k:set() for k in registry}; cache={}
for p in parsers.values():
 exe=pathlib.Path(p["path"]); ver=pathlib.Path(p["version_artifact_path"]); assert set(p)=={"parser_id","path","sha256","version","version_artifact_path","version_artifact_sha256","extensions","parse_stdin_argv"} and exe.is_file() and not exe.is_symlink() and hashlib.sha256(exe.read_bytes()).hexdigest()==p["sha256"] and p["version"] and ver.is_file() and hashlib.sha256(ver.read_bytes()).hexdigest()==p["version_artifact_sha256"] and p["extensions"] and p["parse_stdin_argv"]
def ast(path):
 key=(h,path)
 if key in cache: return cache[key]
 ext=pathlib.PurePosixPath(path).suffix; ps=[x for x in parsers.values() if ext in x["extensions"]]; assert len(ps)==1; p=ps[0]; raw=subprocess.check_output(["git","show",h+":"+path]); out=subprocess.run([p["path"]]+p["parse_stdin_argv"]+[ext],input=raw,capture_output=True,check=True).stdout; obj=json.loads(out,object_pairs_hook=strict,parse_constant=lambda x:(_ for _ in ()).throw(ValueError(x))); assert out==json.dumps(obj,sort_keys=True,separators=(",",":"),ensure_ascii=False).encode() and obj["schema"]=="phase1-source-ast-v1" and isinstance(obj["nodes"],list) and len({x["node_id"] for x in obj["nodes"]})==len(obj["nodes"]) and all(set(x)&gt;={"node_id","kind","line_start","line_end"} and x["line_end"]&gt;=x["line_start"]&gt;0 for x in obj["nodes"]); cache[key]=obj["nodes"]; return obj["nodes"]
def select(nodes,s):
 return [n for n in nodes if n["kind"]==s["kind"] and all(ptr(n,k)==v for k,v in s["fields"].items())]
for claim in d["claims"]:
 f=claim["expected_fact"]; assert set(f)&gt;={"fact_id","subject","predicate"} and all(isinstance(f[k],str) and f[k] for k in ("fact_id","subject","predicate")) and f["fact_id"] not in facts; facts[f["fact_id"]]=f; citations=claim.get("citations",[]); assert all(c["fact_id"]==f["fact_id"] for c in citations)
 if citations:
  assert f["predicate"] in ("source_test_asserts","source_observation_equals") and isinstance(f.get("source_selector"),dict); assert set(f["source_selector"])=={"kind","fields"} and isinstance(f["source_selector"]["fields"],dict) and f["source_selector"]["fields"] and "expected_value" in f
  for c in citations:
   source_nodes=select(ast(c["path"]),f["source_selector"]); assert len(source_nodes)==1 and source_nodes[0]["node_id"]==c["source_node_id"] and c["line_start"]&lt;=source_nodes[0]["line_start"]&lt;=source_nodes[0]["line_end"]&lt;=c["line_end"] and ptr(source_nodes[0],f["source_value_pointer"])==f["expected_value"]; row=registry[c["source_test_id"]]; refs[row["id"]].add(f["fact_id"])
   if f["predicate"]=="source_test_asserts":
    assert row["kind"]=="current_test" and set(f["assertion"] )=={"test_declaration_node_id","assertion_node_id","actual_source_node_id","operator","expected_value"}; nodes=ast(row["path"]); decl=[x for x in nodes if x["node_id"]==f["assertion"]["test_declaration_node_id"] and x["kind"]=="test_declaration"]; assertion=[x for x in nodes if x["node_id"]==f["assertion"]["assertion_node_id"] and x["kind"]=="assertion"]; assert len(decl)==len(assertion)==1; a=assertion[0]; assert a["enclosing_test_node_id"]==decl[0]["node_id"] and a["actual_source_node_id"]==source_nodes[0]["node_id"]==f["assertion"]["actual_source_node_id"] and a["operator"]==f["assertion"]["operator"] and a["expected_value"]==f["assertion"]["expected_value"]==f["expected_value"] and row["line_start"]&lt;=a["line_start"]&lt;=a["line_end"]&lt;=row["line_end"]
   else:
    assert row["kind"] in ("current_command","current_evidence") and isinstance(f.get("json_pointer"),str); raw=pathlib.Path(commands[row["command_row_id"]]["artifact_path"]).read_bytes() if row["kind"]=="current_command" else pathlib.Path(row["evidence_path"]).read_bytes(); digest=commands[row["command_row_id"]]["artifact_sha256"] if row["kind"]=="current_command" else row["evidence_sha256"]; assert hashlib.sha256(raw).hexdigest()==digest; ev=json.loads(raw,object_pairs_hook=strict,parse_constant=lambda x:(_ for _ in ()).throw(ValueError(x))); observed=json.loads(ev["stdout"],object_pairs_hook=strict,parse_constant=lambda x:(_ for _ in ()).throw(ValueError(x))) if row["kind"]=="current_command" else ev; assert ptr(observed,f["json_pointer"])==f["expected_value"]
 else:
  assert claim["explicit_absence"] and f["predicate"]=="absence_structural" and f.get("expected_nodes")==[] and isinstance(f.get("source_roots"),list) and f["source_roots"] and isinstance(f.get("absence_selector"),dict) and set(f["absence_selector"])=={"kind","fields"} and f["absence_selector"]["kind"] in ("declaration","call","field","command") and isinstance(f["absence_selector"]["fields"],dict) and f["absence_selector"]["fields"]; paths=subprocess.check_output(["git","ls-tree","-r","--name-only",h]).decode().splitlines(); applicable=[p for p in paths if any(p==root or p.startswith(root.rstrip("/")+"/") for root in f["source_roots"]) and any(pathlib.PurePosixPath(p).suffix in x["extensions"] for x in parsers.values())]; assert applicable; matches=[(p,n["node_id"]) for p in applicable for n in select(ast(p),f["absence_selector"])]; assert matches==[]
assert len(facts)==len(d["claims"]) and all(row["fact_ids"]==sorted(refs[rid]) and row["fact_ids"] for rid,row in registry.items()); tracer=next(x for x in d["claims"] if x["id"]==d["full_tracer_claim_id"]); assert all(x["fact_id"]==next(c for c in d["claims"] if c["id"]==x["claim_id"])["expected_fact"]["fact_id"] and all(c["fact_id"]==x["fact_id"] for c in x["citations"]) for x in tracer["trace_segments"])'</automated>
    <automated>python3 -c 'import hashlib,json,pathlib,subprocess
def strict(pairs):
 d={}; [(_ for _ in ()).throw(ValueError("duplicate key "+k)) if k in d else d.__setitem__(k,v) for k,v in pairs]; return d
def load(p):
 raw=pathlib.Path(p).read_bytes(); obj=json.loads(raw,object_pairs_hook=strict,parse_constant=lambda x:(_ for _ in ()).throw(ValueError(x))); assert raw==json.dumps(obj,sort_keys=True,separators=(",",":"),ensure_ascii=False).encode(); return obj
def ptr(obj,p):
 assert isinstance(p,str) and p.startswith("/")
 for part in p.split("/")[1:]: obj=obj[int(part)] if isinstance(obj,list) else obj[part.replace("~1","/").replace("~0","~")]
 return obj
d=load("evidence/phase-01/current-source-citations.json"); b=load("evidence/phase-01/current-native-baseline.json"); claim=next(x for x in d["claims"] if x["id"]=="workflow/reopen")
if claim.get("citations"):
 workflow=next(x for x in b["workflow_coverage"] if x["workflow_id"]=="reopen"); assert isinstance(workflow.get("command_row_id"),str) and workflow["command_row_id"] and isinstance(workflow.get("reopen_outcome_pointer"),str) and workflow["reopen_outcome_pointer"] and "observed_reopen_outcome" in workflow; command=next(x for x in b["commands"] if x["row_id"]==workflow["command_row_id"]); retained={x["path"]:x["sha256"] for x in b["retained_artifacts"]}; path=command["artifact_path"]; root=pathlib.Path("evidence/phase-01/raw/current-native").resolve(); q=pathlib.Path(path); assert pathlib.Path(".gitignore").read_text().splitlines().count("/evidence/phase-01/raw/current-native/")==1 and subprocess.run(["git","check-ignore","--quiet",path]).returncode==0 and retained.get(path)==command["artifact_sha256"]==workflow["evidence_sha256"] and workflow["evidence_path"]==path and not q.is_absolute() and q.parts[:4]==("evidence","phase-01","raw","current-native") and ".." not in q.parts and all(not pathlib.Path(*q.parts[:n]).is_symlink() for n in range(1,len(q.parts)+1)) and q.resolve().is_relative_to(root) and q.is_file() and hashlib.sha256(q.read_bytes()).hexdigest()==command["artifact_sha256"]; ev=load(path); assert ev["schema"]=="phase1-command-evidence-v1" and ev["argv"]==command["argv"] and ev["status"]==command["status"] and ev["exit_status"]==command["exit_status"] and ptr(json.loads(ev["stdout"],object_pairs_hook=strict,parse_constant=lambda x:(_ for _ in ()).throw(ValueError(x))),workflow["reopen_outcome_pointer"])==workflow["observed_reopen_outcome"]; registry={x["id"]:x for x in d["source_test_registry"]}; cited=[registry[x["source_test_id"]] for x in claim["citations"]]; assert cited and all((x["kind"]=="current_command" and x["command_row_id"]==command["row_id"]) or (x["kind"]=="current_evidence" and x["evidence_path"]==path and x["evidence_sha256"]==command["artifact_sha256"]) for x in cited)'</automated>
  </verify>
  <acceptance_criteria>
    - One complete user path links current source and source tests to authority tier, identity axes, side effects, and uncertainty; any Plan-02 evidence consumed remains a separate measured-current class.
    - Every executable claim has current-head source citations or an explicit non-implemented/non-observed/contradicted status.
    - Normative, executable, measured-current, historical, and stale-evidence authority classes remain distinct.
    - Uzel shell/product-service, runtime-mediation, and replaceable engine/provider responsibilities are explicit and match v4 authority.
    - Identity axes cannot be read as aliases for each other.
  </acceptance_criteria>
  <done>One full source-grounded authority path and the complete authority/identity baseline exist at one immutable current head.</done>
</task>

<task type="auto">
  <name>Task 2: Baseline durable formats, profiles, storage, side effects, and scoped threats</name>
  <files>docs/architecture/phase-01-interaction.md, docs/architecture/durable-formats.toml, docs/architecture/instance-profile-paths.toml, docs/architecture/storage-retention.md, docs/security/phase-01-threat-model.md, docs/architecture/phase-01-structure.json</files>
  <execution_contract>`phase-01-structure.json` is projection, never authority. Interaction, retention, and threat Markdown each contain exactly one canonical fenced `phase1-structure-source-v1` JSON object; TOML registries expose exact arrays. Every projected row carries `source_path`, `source_format`, RFC-6901 `object_pointer`, source-byte SHA-256, source-object canonical SHA-256, and exact source object. Strictly parse each source, resolve pointer, require object equality/digest, and require projected registry sets equal authoritative source arrays. Missing, duplicate, nonfinite, malformed, substring-only, or extra source/projected row fails.</execution_contract>
  <read_first>
    - docs/architecture/phase-01-authority.md — accepted authority order and ownership
    - docs/architecture/phase-01-identity.md — identity/state axes and non-conflation rules
    - evidence/phase-01/current-source-citations.json — source-grounded executable facts
    - package.json, apps/uzel/src, apps/uzel/src-tauri/src, apps/uzel-napd/src, crates/napd/src, crates/napd-protocol/src, contracts, napplets — current declared commands, workflow/storage paths, failure handling, and test surfaces
    - docs/plans/uzel-product-incubation-v4-2026-08-10/09-PRODUCTION-MATURITY.md — maturity, retention, deletion, and scoped security evidence contract
  </read_first>
  <action>Implement D-02 through D-05, D-24, D-28, D-29, and D-30 across the remaining M0-03 baseline. Give each produced architecture, interaction, retention, and threat document an explicit schema/version header and stable IDs. Create `phase-01-structure.json` as the parsed structural registry for all interaction, format, boundary, storage, and threat records: its five nonempty registries (`interactions`, `formats`, `boundaries`, `storage`, `threats`) must each name their required schema, and every row must carry all `source_claim_ids`, `format_ids`, `boundary_ids`, `storage_ids`, `threat_ids`, and `interaction_ids` cross-link fields plus a nonempty unique `applicable_cross_links` list and an `inapplicable_cross_link_reasons` object. Every field named applicable is a nonempty array of unique nonempty IDs; every field not named applicable is an empty array and has exactly one nonempty reason keyed by that field. Thus no applicable registry relationship can silently pass as an empty array. Require unique nonempty row IDs, reject duplicate or unknown targets in every cross-link class including interaction, and require the `source_claims` registry ID set to equal—not merely be a subset of—the claim IDs in `current-source-citations.json`. In the interaction contract enumerate each current UI/runtime/plugin/helper/filesystem/media/process/automation boundary reached by the POC workflow, naming stable boundary ID, caller, callee, input schema, authority, side effect, cancellation, restart, failure propagation, observed test/evidence row, and unknowns. In `durable-formats.toml`, register current project/composition/export/config/profile/instance/evidence formats only when source or measured paths prove them; for each record format ID, normative owner, executable owner, schema/version location, minimum reader/writer, canonicalization, unknown-version behavior, migration/rollback authority, storage class, compatibility scope, and confidence. In `instance-profile-paths.toml`, define instance/profile selection and persistence separately from runtime/project/workbench state, including stable path ID, defaults, collision rules, parallel access, interrupted writes, restart/resume, stale data, failure recovery, and source evidence. In the retention document map each observed storage path through a stable storage ID to data class, owner, sensitivity, retention clock, deletion/export authority, auditability, backup/recovery claim, cleanup, and synthetic fixture policy; do not promise encryption, isolation, concurrency, portability, or deletion not proven by source/evidence. Create the scoped ASVS-L1 threat baseline for declared boundaries and assets: synthetic user content, project data, identities, signing authority if present, plugin/helper inputs, filesystem/media paths, child processes, local services, automation, raw evidence, and dependency/runtime inputs. Every threat has stable threat ID, STRIDE category, severity, disposition, source/evidence citation, validation, residual risk, owner, and a boundary/storage/interaction cross-link. Include interrupted/parallel-operation tests or evidence needs under EDGE-M0-03-CONCURRENCY. This is a baseline and validation contract, not a new sandbox, auth system, storage engine, or final architecture. Validate TOML/links before atomic replacement; on source-head drift cancel and regenerate all source-coupled rows from one head.</action>
  <verify>
    <automated>python3 -c 'import json,tomllib; [tomllib.load(open(p,"rb")) for p in ("docs/architecture/durable-formats.toml","docs/architecture/instance-profile-paths.toml")]; r=json.load(open("docs/architecture/phase-01-structure.json")); c=json.load(open("evidence/phase-01/current-source-citations.json")); assert r.get("schema"); groups=("source_claims","interactions","formats","boundaries","storage","threats"); assert all(isinstance(r.get(g),list) and r[g] and all(isinstance(x.get("id"),str) and x["id"] for x in r[g]) and len({x["id"] for x in r[g]})==len(r[g]) for g in groups); assert isinstance(r.get("registries"),list) and len(r["registries"])==5 and {x.get("id") for x in r["registries"]}==set(groups[1:]) and all(isinstance(x.get("schema"),str) and x["schema"] for x in r["registries"]); ids={g:{x["id"] for x in r[g]} for g in groups}; refs={"source_claim_ids":"source_claims","interaction_ids":"interactions","format_ids":"formats","boundary_ids":"boundaries","storage_ids":"storage","threat_ids":"threats"}; assert all(k in x and isinstance(x[k],list) and all(isinstance(y,str) and y for y in x[k]) and len(x[k])==len(set(x[k])) and set(x[k])&lt;=ids[v] for g in groups for x in r[g] for k,v in refs.items()); assert ids["source_claims"]=={x["id"] for x in c["claims"]}' &amp;&amp; python3 -c 'import json; r=json.load(open("docs/architecture/phase-01-structure.json")); groups=("source_claims","interactions","formats","boundaries","storage","threats"); keys={"source_claim_ids","interaction_ids","format_ids","boundary_ids","storage_ids","threat_ids"}; rows=[x for g in groups for x in r[g]]; assert all(isinstance(x.get("applicable_cross_links"),list) and x["applicable_cross_links"] and len(x["applicable_cross_links"])==len(set(x["applicable_cross_links"])) and set(x["applicable_cross_links"])&lt;=keys and isinstance(x.get("inapplicable_cross_link_reasons"),dict) and set(x["inapplicable_cross_link_reasons"])==keys-set(x["applicable_cross_links"]) and all(isinstance(v,str) and v for v in x["inapplicable_cross_link_reasons"].values()) and all(isinstance(x[k],list) and ((k in x["applicable_cross_links"] and len(x[k])&gt;0) or (k not in x["applicable_cross_links"] and len(x[k])==0)) for k in keys) for x in rows)' &amp;&amp; rg -q 'schema' docs/architecture/phase-01-interaction.md &amp;&amp; rg -q 'schema' docs/architecture/storage-retention.md &amp;&amp; rg -q 'schema' docs/security/phase-01-threat-model.md &amp;&amp; rg -q 'interrupted' docs/architecture/instance-profile-paths.toml &amp;&amp; rg -q 'parallel' docs/architecture/instance-profile-paths.toml &amp;&amp; rg -q 'ASVS.*1|Level 1' docs/security/phase-01-threat-model.md &amp;&amp; rg -q 'retention' docs/architecture/storage-retention.md &amp;&amp; rg -q 'deletion' docs/architecture/storage-retention.md</automated>
    <automated>python3 -c 'import hashlib,json,pathlib,re,tomllib
def strict(pairs):
 d={}; [(_ for _ in ()).throw(ValueError("duplicate key "+k)) if k in d else d.__setitem__(k,v) for k,v in pairs]; return d
loads=lambda raw:json.loads(raw,object_pairs_hook=strict,parse_constant=lambda x:(_ for _ in ()).throw(ValueError(x)))
raw=pathlib.Path("docs/architecture/phase-01-structure.json").read_bytes(); out=loads(raw); assert raw==json.dumps(out,sort_keys=True,separators=(",",":"),ensure_ascii=False).encode()
def pointer(obj,p):
 for q in p.split("/")[1:]: obj=obj[int(q)] if isinstance(obj,list) else obj[q.replace("~1","/").replace("~0","~")]
 return obj
cache={}
for p in ("docs/architecture/phase-01-interaction.md","docs/architecture/storage-retention.md","docs/security/phase-01-threat-model.md"):
 b=pathlib.Path(p).read_bytes(); blocks=re.findall(rb"```json phase1-structure-source-v1\n([^`]+)\n```",b); assert len(blocks)==1; obj=loads(blocks[0]); assert blocks[0]==json.dumps(obj,sort_keys=True,separators=(",",":"),ensure_ascii=False).encode(); cache[p]=(b,obj)
for p in ("docs/architecture/durable-formats.toml","docs/architecture/instance-profile-paths.toml"):
 b=pathlib.Path(p).read_bytes(); cache[p]=(b,tomllib.loads(b.decode()))
groups=("interactions","formats","boundaries","storage","threats"); seen={g:[] for g in groups}
for g in groups:
 for x in out[g]:
  b,obj=cache[x["source_path"]]; val=pointer(obj,x["object_pointer"]); assert x["source_format"] in ("json","toml") and x["source_sha256"]==hashlib.sha256(b).hexdigest() and x["source_object"]==val and x["source_object_sha256"]==hashlib.sha256(json.dumps(val,sort_keys=True,separators=(",",":"),ensure_ascii=False).encode()).hexdigest(); seen[g].append(val)
 expected=[]
 for b,obj in cache.values():
  if isinstance(obj,dict): expected.extend(obj.get(g,[]))
 assert seen[g]==expected and len({x["id"] for x in seen[g]})==len(seen[g])'</automated>
  </verify>
  <acceptance_criteria>
    - Every recorded interaction, durable format, instance/profile path, and storage claim cites current source or measured evidence and records uncertainty.
    - Schema floors, version behavior, canonicalization, migration/rollback authority, storage ownership, retention, deletion, and export are explicit where applicable.
    - Interrupted and parallel guarantees are proven, contradicted, or retained as flagged evidence needs; none are inferred.
    - Threat model covers only current/declared Phase-1 boundaries at ASVS Level 1 with severity, disposition, mitigation/validation, residual risk, and owner.
    - Uzel shell/product-service, runtime, engine/provider, UI/plugin/helper, profile/instance, and identity responsibilities remain separate.
    - PROHIB-M0-03-AUTHORITY remains flagged for independent review.
  </acceptance_criteria>
  <done>M0-03 has a source-grounded, non-conflated authority/schema/instance/profile/storage/trust/threat baseline with explicit concurrency uncertainty and no invented architecture.</done>
</task>

</tasks>

## Artifacts Produced

- `docs/architecture/phase-01-authority.md` — authority tiers, contradictions, and ownership.
- `docs/architecture/phase-01-identity.md` — distinct identity and state axes.
- `evidence/phase-01/current-source-citations.json` — current executable-source evidence.
- `docs/architecture/phase-01-interaction.md` — side-effect and failure/cancellation boundary map.
- `docs/architecture/durable-formats.toml` — schema/version/compatibility format registry.
- `docs/architecture/instance-profile-paths.toml` — instance/profile and concurrency path registry.
- `docs/architecture/storage-retention.md` — storage lifecycle and authority baseline.
- `docs/security/phase-01-threat-model.md` — scoped ASVS-L1 threat and validation baseline.
- `docs/architecture/phase-01-structure.json` — parsed ID and cross-reference registry for the Phase-1 architecture baseline.

<threat_model>
Security validation scope: OWASP ASVS Level 1, limited to current declared authority, identity, data, and execution boundaries.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Normative documents to executable source | Human intent and source behavior can drift; neither may silently impersonate the other. |
| UI to runtime/engine/plugin/helper | User-controlled content and actions cross into replaceable execution components and side effects. |
| Runtime to filesystem/media/process/automation | Paths, codecs/helpers, child processes, and automation cross host authority boundaries. |
| Profile/instance/project state to durable storage | Distinct identities and concurrent/interrupted writes can collide or corrupt retained data. |
| Source citations to published baseline | Stale heads or selective citations could misrepresent executable truth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-01 | Spoofing | identity/profile/instance axes | high | mitigate | Separate axes, cite current selectors/consumers, record absent authentication/signing behavior, validate no aliasing claim. |
| T-03-02 | Tampering | durable project/profile data | high | mitigate | Register schema/version/canonicalization, observed paths, write ownership, interrupted/parallel behavior, rollback and residual gaps. |
| T-03-03 | Elevation | plugin/helper/process/automation boundary | high | mitigate | Enumerate call sites/argv/inputs/authority, current containment, ASVS-L1 validation, and unsupported isolation as residual risk. |
| T-03-04 | Information Disclosure | filesystem/media/storage/evidence paths | medium | mitigate | Classify data and paths, use synthetic fixtures, state retention/export/deletion authority, avoid unproven secrecy claims. |
| T-03-05 | Repudiation | source-grounded authority claims | medium | mitigate | Bind claims to one Git tree and blob/line anchors, record confidence/contradictions, stop on head drift. |
| T-03-SC | Tampering | existing source/runtime inputs | high | mitigate | No installs or source changes; bind claims to one current Git tree and current lock/source identities, keeping consumed Plan-02 measurements separately labeled and unable to replace source citations or Track-A truth. |
</threat_model>

<verification>
Run both automated gates; sample each authority tier, identity axis, durable format, storage class, trust boundary, and threat back to a current source citation or honest missing-evidence row. Validate that EDGE-M0-03-CONCURRENCY is still visible until interrupted/parallel evidence is independently reviewed.
</verification>

<success_criteria>
- M0-03 is covered without claiming a final architecture or implementing product/storage/security features.
- Current source, measured evidence, normative authority, and stale evidence remain distinguishable.
- Identity, instance, profile, runtime, project, and signing concepts remain non-conflated.
- ASVS-L1 validation is scoped to declared current boundaries and explicit residual risk.
</success_criteria>

<output>
Create `.planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-03-SUMMARY.md` when done.
</output>
