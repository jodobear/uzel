---
phase: 01-slice-ref-01-poc-replay-accepted-napp-seam
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - PROGRAMME_CONTRACT.md
  - NAPP_CONSUMER_PROFILE.md
  - SOURCE_BASELINE.md
  - docs/product-authority.json
  - ISSUE_PROCESS.md
  - .github/ISSUE_TEMPLATE/slice.yml
  - .github/PULL_REQUEST_TEMPLATE.md
  - tools/audit_repo_docs.py
  - tools/test_audit_repo_docs.py
  - AUDIT.md
autonomous: true
requirements:
  - REF-05
  - REF-07
must_haves:
  truths:
    - "D-16: One narrow root product-first authority set is selected, reconciled to committed planning decisions, auditable, and committed before implementation may depend on it."
    - "REF-05/D-03: Canonical documents preserve Uzel presentation/trusted-host ownership, Napp runtime authority, and NMP sole Nostr/state authority without copying programme text into implementation files."
    - "REF-07: Consumer policy admits only an exact committed candidate with source-backed client, product events, testkit, lifecycle/version, and pin evidence; current absence remains a dependency, not a private substitute."
    - "D-17/D-18: Issue and PR templates enforce one repository-qualified issue, one visible outcome, exact evidence, rollback/stop, and one review sequence per semantic candidate."
    - "Unselected pack files remain user-owned proposed inputs and are neither deleted nor silently promoted to normative authority."
    - "Review #17/#19: Pre-plan tracked/index/ref/porcelain snapshots and before/after hashes cover every proposed pack input; only the ten declared plan files may change or enter task commits."
    - "Review #18/#20/#21/#22/#24: Required authority, issue-form, PR-template, pin, and denial fields are parsed and negative-tested; required YAML/JSON parse failure is fatal while malformed non-allowlisted files remain ignored."
    - "Scope-sanity/D-16: The nine admitted authority paths plus their one auditor test are one docs-only acceptance unit: the final candidate passes one shared audit/staging invariant, changes no production path, and never exposes a half-admitted authority set."
    - "D-18: Execute-plan stops after creating one exact candidate commit and a machine-checkable pending review handoff; no push, PR, merge, remote-review claim, or publication occurs without separate authorization."
  artifacts:
    - path: docs/product-authority.json
      provides: "Machine-readable allowlist of canonical root product/process documents and templates"
    - path: PROGRAMME_CONTRACT.md
      provides: "Canonical product-first ownership and phase-boundary contract"
    - path: NAPP_CONSUMER_PROFILE.md
      provides: "Canonical accepted-candidate-only consumer admission rule"
    - path: SOURCE_BASELINE.md
      provides: "Exact current-green Uzel/nampplets/NMP/NAP/Tauri/Nix pins and revalidation trigger"
    - path: tools/audit_repo_docs.py
      provides: "Narrow authority-set auditor that does not scan unrelated repository Markdown"
    - path: tools/test_audit_repo_docs.py
      provides: "Field-level authority/template/pin tests and fail-closed negative fixtures"
    - path: .github/ISSUE_TEMPLATE/slice.yml
      provides: "One outcome, dependency, trust, evidence, failure, and rollback issue form"
    - path: .github/PULL_REQUEST_TEMPLATE.md
      provides: "Exact issue/head/gate/review evidence PR contract"
  key_links:
    - from: tools/audit_repo_docs.py
      to: docs/product-authority.json
      via: "Auditor reads the explicit allowlist instead of unrestricted repository recursion"
      pattern: "product-authority.json"
    - from: NAPP_CONSUMER_PROFILE.md
      to: .planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-CONTEXT.md
      via: "D-01/D-02/D-03 admission and stop rules remain exact"
      pattern: "client"
    - from: .github/ISSUE_TEMPLATE/slice.yml
      to: .github/PULL_REQUEST_TEMPLATE.md
      via: "Both carry the same repository-qualified issue, scope, exact-head evidence, failure, and rollback fields"
      pattern: "Outcome"
  assumptions:
    - "[FLAGGED-UNVERIFIED] The proposed root pack can be reduced to one non-conflicting authority set; any locked-decision conflict must stop admission and be reported, not auto-merged."
    - "[FLAGGED-UNVERIFIED] REF-05 edge classification is unresolved; document admission proves authority selection, not implementation completeness."
    - "[FLAGGED-UNVERIFIED] REF-07 edge classification is unresolved; current coverage is fail-closed admission/dependency disposition, not adapter parity."
  prohibitions:
    - "[FLAGGED-UNVERIFIED] No private Napp substitute or expanded legacy protocol."
    - "[FLAGGED-UNVERIFIED] No duplicated NMP, runtime, Nostr, or store authority."
    - "[FLAGGED-UNVERIFIED] No evidence-class conflation or fabricated metrics."
    - "[FLAGGED-UNVERIFIED] No pull-in of Nix packaging, CI architecture, Social rebuild, multi-instance implementation, or parked capabilities."
---

## Phase Goal

**As a** Uzel maintainer/reviewer, **I want to** replay current POC evidence and apply a fail-closed Napp admission gate, **so that** visible value survives without duplicating authority.

<objective>
Deliver repository-qualified issue slice `jodobear/uzel — REF-01C: admit product-first root authority set` as one bounded documentation-only PR.

Purpose: Turn selected user-owned proposed pack files into one committed, auditable source of product/process authority without touching product code or promoting competing copies, per D-03, D-16, D-17, and D-18.
Output: Three canonical root contracts, a machine-readable authority allowlist, aligned issue/PR templates, and a narrowed root auditor. Unselected user files and unrelated dirty work remain untouched.
</objective>

<execution_context>
@/home/at/.codex/gsd-core/workflows/execute-plan.md
@/home/at/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-CONTEXT.md
@.planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-REAUDIT.md
@.planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-RESEARCH.md
</context>

## Artifacts this phase produces

- `docs/product-authority.json`; new authority allowlist, no code symbols.
- `PROGRAMME_CONTRACT.md`, `NAPP_CONSUMER_PROFILE.md`, `SOURCE_BASELINE.md`; selected canonical root contracts.
- `ISSUE_PROCESS.md`, `.github/ISSUE_TEMPLATE/slice.yml`, `.github/PULL_REQUEST_TEMPLATE.md`; selected delivery contract.
- `tools/audit_repo_docs.py`; refactored into `load_authority`, `audit_authority`, and `main` entry points.
- `tools/test_audit_repo_docs.py`; standard-library tests for exact fields, required parser failure, denial concepts, pin drift, and allowlist confinement.
- `AUDIT.md`; operator instructions, admitted scope, and explicit non-normative-input treatment.

## Atomic docs-only scope and staging invariant

Plan 03 intentionally retains ten coupled paths in one bounded authority-admission candidate. `docs/product-authority.json` admits exactly nine mutually dependent paths: the three product/consumer/source contracts; the issue process and two templates that carry those decisions into delivery; the auditor that enforces the allowlist; the manifest itself; and `AUDIT.md`, which defines the operator-visible command/result contract. `tools/test_audit_repo_docs.py` is the tenth path and proves that exact nine-path contract fails closed. Current source confirms the proposed pack, templates, manifest, and broad auditor are one uncommitted input set; splitting would either publish normative contracts/templates before their validator and negative tests pass, or commit a validator/test without its stable admitted target. There is no independent acceptance boundary between those halves, so no extra plan/dependency is created.

The shared acceptance invariant is indivisible: `docs/product-authority.json.canonical` contains exactly nine admitted paths; `python3 -m unittest tools/test_audit_repo_docs.py` passes; `python3 tools/audit_repo_docs.py` returns `status=pass` and `admitted_file_count=9`; the final staged/committed union equals only the ten `files_modified` paths; every production Rust, Tauri, Svelte, napplet, fixture, protocol, dependency, lock, package, and CI path remains byte-identical; unrelated tracked/untracked inputs, refs, and index state remain unchanged. Exact task staging partitions are Task 1 = `PROGRAMME_CONTRACT.md`, `NAPP_CONSUMER_PROFILE.md`, `SOURCE_BASELINE.md`, `docs/product-authority.json`; Task 2 = `ISSUE_PROCESS.md`, `.github/ISSUE_TEMPLATE/slice.yml`, `.github/PULL_REQUEST_TEMPLATE.md`; Task 3 = `tools/audit_repo_docs.py`, `tools/test_audit_repo_docs.py`, `AUDIT.md`. Intermediate task commits are executor-local construction steps, not separately admissible/publishable authority candidates.

<tasks>

<task type="auto">
  <name>Task 1: Select and reconcile the canonical product authority</name>
  <read_first>
    .planning/PROJECT.md
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-CONTEXT.md
    .planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-REAUDIT.md
    PROGRAMME_CONTRACT.md
    NAPP_CONSUMER_PROFILE.md
    SOURCE_BASELINE.md
    CONTEXT.md
    QUALITY.md
    NIX_PACKAGE.md
    GSD_RUNBOOK.md
    docs/00-boundary.md
    docs/10-gsd-seed.md
  </read_first>
  <files>PROGRAMME_CONTRACT.md, NAPP_CONSUMER_PROFILE.md, SOURCE_BASELINE.md, docs/product-authority.json</files>
  <action>Before editing, capture a plan-base snapshot under ignored `.artifacts/phase-01/authority/`: full HEAD, index tree, sorted refs digest, NUL-delimited porcelain-v2 status, and SHA-256/Git tracked-untracked state for every entry in `manifest.json.files`, its `excluded` entries, the root archive/extracted-copy paths, and every additional proposed root input named by CONTEXT/REAUDIT. Store the complete selected/non-normative inventory and before hash/state in `docs/product-authority.json`; after each task, recalculate it and require every non-declared path byte/state/ref unchanged. Select only `PROGRAMME_CONTRACT.md`, `NAPP_CONSUMER_PROFILE.md`, and `SOURCE_BASELINE.md` as canonical product/consumer/source contracts for this slice; reconcile them against committed `.planning` decisions, with D-01 through D-18 and roadmap/requirements taking precedence over proposed text. `PROGRAMME_CONTRACT.md` must keep Tauri/Svelte/Uzel presentation and trusted-host integration in Uzel, Napp runtime/client/events/testkit authority in Napp, and all Nostr query/relay/store/freshness/provenance/signer/publication truth in NMP. `NAPP_CONSUMER_PROFILE.md` must require one exact reachable committed candidate, candidate-owned declared source and probes, version mismatch, lifecycle/recovery, testkit vectors, and later Cargo/Nix pin parity; it must stop on absence and state REF-01D is separately replanned only after qualification. Derive `SOURCE_BASELINE.md` fields from committed blobs/locks, not proposed prose: exact Uzel production commit/tree; nampplets and transitive NMP commits from Cargo files; every NAP package/version from pnpm files; Tauri Rust/JS pins; and nixpkgs from flake files. Record source path, commit, blob OID, and value for each, and explicitly refuse compatibility claims for moved remote heads. Create `docs/product-authority.json` with stable schema/revision, admitted revision/decision sources, canonical roles, complete proposed-input inventory, selected before/after hashes/state, complete non-normative inventory, and pin provenance. Do not delete, move, normalize, or stage any unselected pack file. Before each task commit, require the staged delta relative to the captured index to equal only that task's declared files; stop on a locked-decision conflict or unrelated mutation.</action>
  <verify>
    <automated>python3 -m json.tool docs/product-authority.json &gt;/dev/null &amp;&amp; jq -e '(.canonical|length) == 9 and (.proposed_inputs|type=="array" and length &gt; 0) and (.non_normative_inputs|type=="array" and length &gt; 0) and (.pin_provenance|type=="object") and (.locked_decision_source|endswith("01-CONTEXT.md"))' docs/product-authority.json &gt;/dev/null &amp;&amp; rg -q '19519c378c2e775c6ad4b042cfd9aadd89f766b9' SOURCE_BASELINE.md &amp;&amp; rg -q 'NMP' PROGRAMME_CONTRACT.md &amp;&amp; rg -q 'testkit' NAPP_CONSUMER_PROFILE.md</automated>
  </verify>
  <acceptance_criteria>Exactly one selected product/consumer/source authority exists in the allowlist; it matches locked decisions and exact current pins. Proposed alternatives remain present and explicitly non-normative.</acceptance_criteria>
  <done>D-16 product authority is selected and ready for exact-path commit without product-code dependency on uncommitted pack material.</done>
</task>

<task type="auto">
  <name>Task 2: Align one-issue and bounded-PR delivery contracts</name>
  <read_first>
    docs/product-authority.json
    ISSUE_PROCESS.md
    .github/ISSUE_TEMPLATE/slice.yml
    .github/PULL_REQUEST_TEMPLATE.md
    REVIEW_WORKFLOW.md
    REVIEW_EVIDENCE.md
    .planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-CONTEXT.md
  </read_first>
  <files>ISSUE_PROCESS.md, .github/ISSUE_TEMPLATE/slice.yml, .github/PULL_REQUEST_TEMPLATE.md</files>
  <action>Admit and align only the bounded issue/PR contract. `ISSUE_PROCESS.md` must define one repository-qualified issue, one observable outcome, explicit hard dependencies versus delivery order, exact source/pins, owned seam/trust boundary, resource budget, failure/cancel/restart behavior, acceptance evidence, rollback/stop, and exclusions. Rewrite `.github/ISSUE_TEMPLATE/slice.yml` as canonical JSON syntax (valid YAML 1.2) so Python standard-library `json` performs a complete parse without an optional parser or new dependency. Its exact field IDs are `owner_repository`, `slice_id`, `outcome`, `why`, `hard_dependencies`, `delivery_after`, `scope`, `exclusions`, `exact_refs`, `ci_class`, `trust_boundary`, `resource_budget`, `failure_recovery`, `acceptance_evidence`, `rollback_stop`, and `review_sensitivity`; all except delivery order are required, external dependencies require full owner/repository references in the field contract, and options are finite. Keep the PR template bound to required parseable labels for full owner/repository issue ref, slice ID, exact base/head, affected local evidence, result-class separation, rollback/stop, and D-18 local CodeRabbit/remote Codex/final CodeRabbit evidence once per immutable semantic candidate. This task does not open issues, create PRs, enforce GitHub CI, or alter review-provider configuration; it creates the canonical templates future slices consume. Preserve every unrelated `.github` and dirty path, recalculate the authority snapshot, and stage only the three task files.</action>
  <verify>
    <automated>python3 -c 'import json; from pathlib import Path; form=json.loads(Path(".github/ISSUE_TEMPLATE/slice.yml").read_text(encoding="utf-8")); ids=[item.get("id") for item in form["body"]]; required={"owner_repository","slice_id","outcome","why","hard_dependencies","scope","exclusions","exact_refs","ci_class","trust_boundary","resource_budget","failure_recovery","acceptance_evidence","rollback_stop","review_sensitivity"}; assert required &lt;= set(ids)' &amp;&amp; rg -q 'Closes .*full issue ref' .github/PULL_REQUEST_TEMPLATE.md &amp;&amp; rg -q 'exact base SHA' .github/PULL_REQUEST_TEMPLATE.md &amp;&amp; rg -q 'exact head SHA' .github/PULL_REQUEST_TEMPLATE.md</automated>
  </verify>
  <acceptance_criteria>Issue and PR contracts carry one outcome, exact refs, trust/resource/lifecycle evidence, rollback, and repository-qualified dependencies without creating a CI or review orchestration subsystem.</acceptance_criteria>
  <done>D-17 one-issue/one-PR mapping and D-18 once-per-candidate review evidence are canonical and auditable.</done>
</task>

<task type="auto">
  <name>Task 3: Narrow the root auditor to admitted authority</name>
  <read_first>
    docs/product-authority.json
    tools/audit_repo_docs.py
    AUDIT.md
    DIAGRAMS.md
    reports/mermaid-render.json
    manifest.json
    uzel-poc-validated-pack/scripts/audit_docs.py
  </read_first>
  <files>tools/audit_repo_docs.py, tools/test_audit_repo_docs.py, AUDIT.md</files>
  <action>Refactor `tools/audit_repo_docs.py` into small `load_authority`, `audit_authority`, and `main` functions. Read `docs/product-authority.json`; audit only its canonical paths and the explicit authority manifest itself. For each admitted file, require existence, UTF-8 readability, local-link resolution, duplicate-paragraph checks, and mandatory syntax/schema validation: standard-library JSON/TOML parsers for those formats; `.github/ISSUE_TEMPLATE/slice.yml` must parse as JSON-compatible YAML and match the exact field/type/required/options contract; PR Markdown must match the exact required heading/label contract. No required authority file may enter `skipped_optional_parsers`; parse/read/schema failure is an error and nonzero. Retain diagram-index/render checks only for admitted documents that contain Mermaid. Parse and validate stable field-level authority: schema/revision, admitted decision revision, complete proposed/non-normative inventory, before/after hash/state, canonical roles, exact committed Uzel/nampplets/transitive-NMP/NAP/Tauri/nixpkgs pins and blob provenance, Napp candidate admission gates, and issue/PR contracts. Explicitly reject normative authority granting raw frame network, native bridge, host path, secret, caller-selected principal, duplicate Nostr/runtime/store ownership, or private fallback. Emit canonical JSON with exactly `status`, sorted `errors`, `admitted_file_count`, `diagram_count`, and sorted `skipped_optional_parsers`; malformed non-allowlisted Markdown/YAML must be ignored and never counted/admitted.

Create `tools/test_audit_repo_docs.py` with standard-library temporary-tree tests for every required field and each negative class: missing/malformed authority JSON; invalid/non-JSON-compatible required issue YAML; missing issue/PR fields; owner/repository or base/head omissions; pin/blob mismatch; incomplete non-normative inventory; every raw-frame/native/path/secret/principal/duplicate-store/private-fallback denial; non-UTF-8 admitted input; and malformed non-allowlisted Markdown/YAML ignored without admission. Tests parse auditor JSON and assert stable fields/counts/nonzero results rather than grepping text. Update `AUDIT.md` with the exact command, scope, result schema, required-parser rule, non-normative-input rule, and limitation that this audit is product-document admission rather than production/test/package/CI evidence. Run tests/audit before the task commit, recalculate the plan-base mutation snapshot, require no unrelated change, and stage only these three task files.</action>
  <verify>
    <automated>python3 -m unittest tools/test_audit_repo_docs.py &amp;&amp; python3 tools/audit_repo_docs.py &amp;&amp; python3 -c 'import json,subprocess; result=json.loads(subprocess.check_output(["python3","tools/audit_repo_docs.py"],text=True)); assert list(result)==["status","errors","admitted_file_count","diagram_count","skipped_optional_parsers"]; assert result["status"]=="pass" and result["admitted_file_count"]==9' &amp;&amp; git diff --check -- PROGRAMME_CONTRACT.md NAPP_CONSUMER_PROFILE.md SOURCE_BASELINE.md docs/product-authority.json ISSUE_PROCESS.md .github/ISSUE_TEMPLATE/slice.yml .github/PULL_REQUEST_TEMPLATE.md tools/audit_repo_docs.py tools/test_audit_repo_docs.py AUDIT.md</automated>
  </verify>
  <acceptance_criteria>Root audit parses and field-validates all nine admitted files, exact pins/provenance, templates, and denial rules; every required parse/field fault fails, while malformed non-allowlisted input is ignored and uncounted. Plan-base snapshot proves only ten declared paths changed or entered task commits; all user-owned inputs remain byte/state-identical unless selected.</acceptance_criteria>
  <done>D-16 authority set is committed and audit-backed; REF-01D may cite it only after this plan's summary records the exact commit.</done>
</task>

</tasks>

<post_execution_review_gate>
## D-18 candidate handoff and separately authorized ship/review gate

- **Execute-plan ownership:** Capture the full 40-character plan-start HEAD as `candidate_base` before Task 1 and the final task commit as `candidate_head`. Only after the shared nine-path audit/ten-path staging invariant passes, write one `review_handoff` object in `01-03-SUMMARY.md` with `schema: uzel.review-handoff/v1`, `slice: ref-01c`, both SHAs, the exact task `<verify>` commands and statuses as `affected_local_gates`, `review_evidence_path: evidence/phase-01/reviews/ref-01c-&lt;candidate_head&gt;.md`, `external_review_status: pending-separate-authorization`, and `publication_status: not-authorized`. Stop there: execution does not push, open/update a PR, merge, or claim any external review passed.
- **Evidence owner and order:** A separately authorized `$gsd-review`/`$gsd-ship` workflow creates the head-specific evidence record and fills these ordered stages, each with exact base/head plus live command output, check URL, or evidence hash: `affected_local_gates=pass`; `local_coderabbit=pass` from one full `coderabbit review --committed --base-commit &lt;base&gt;` on the immutable pair; `remote_codex=pass`; `in_scope_fixes=none|batched`; any required affected-gate/review reruns; `final_github_coderabbit=pass` on the final exact head; `unresolved_blockers=[]`. No status may be claimed from planned or stale evidence, and no secret, credential, signer material, private host data, or unrelated private metadata may be uploaded.
- **Invalidation:** Any candidate-head change creates a new head-specific evidence path, invalidates all later review evidence tied to the old head, and restarts at affected local gates plus full local CodeRabbit before remote Codex/final GitHub CodeRabbit. Unchanged base/head and semantic inputs do not rerun expensive gates.
- **Authority boundary:** Publication, PR creation/update, and merge each require separate user authorization. Phase 3 later automates/enforces CI and exact-head aggregation; this Phase 1 contract only records the candidate/pending stop and the ordered ship gate.
</post_execution_review_gate>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-owned proposed pack → canonical project authority | Untracked documents are untrusted proposed input until selected, reconciled, audited, and committed. |
| canonical docs → executor/issue/PR | Normative text can authorize work; conflicting or broadened scope must fail admission. |
| issue/PR templates → external delivery state | Templates carry repository refs and evidence but do not themselves authorize merge, publication, or CI changes. |
| future Uzel consumer → Napp/NMP | Docs must preserve source binding, exact-build authority, one runtime/NMP owner, bounded cancellation/recovery, and no private fallback. |

## STRIDE Threat Register — ASVS L1, block high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | proposed root documents and Git staging | high | mitigate | Snapshot HEAD/index/refs/status plus every proposed-input hash/state; reconcile only selected paths, require exact per-task staged sets, and prove unrelated dirty/untracked inputs remain unchanged. |
| T-03-02 | Elevation | normative scope/ownership text | high | mitigate | Field-level auditor and negative tests require Uzel/Napp/NMP ownership, source binding, denied raw frame/native/path/secret/principal authority, no duplicate store, and no private fallback. |
| T-03-03 | Spoofing / Repudiation | issue and PR references | medium | mitigate | Parse JSON-compatible issue form and PR labels; require full owner/repository issue refs, slice ID, exact base/head, evidence paths, results, rollback, and review SHAs. |
| T-03-04 | Denial of service | unrestricted auditor scan | medium | mitigate | Replace repository-wide recursion with a finite manifest allowlist and report optional parser skips explicitly. |
| T-03-05 | Tampering | candidate/pin claims | high | mitigate | Auditor derives and verifies Uzel, nampplets, transitive NMP, NAP, Tauri, and nixpkgs values against committed source/lock blobs; moved remote heads confer no acceptance. |
| T-03-06 | Elevation / Information disclosure | future frame/runtime contract | high | mitigate | Canonical authority forbids raw network, Tauri bridge, host path, secret, caller principal, duplicate stores, and unbounded recovery/resource work. |
</threat_model>

<verification>
- `docs/product-authority.json` names exactly the selected canonical set and proposed non-normative inputs.
- Selected contracts match D-01 through D-18 and exact current source/lock pins.
- Issue/PR templates carry one bounded repository-qualified slice and exact evidence/rollback fields.
- `python3 tools/audit_repo_docs.py` passes and scopes itself to the authority allowlist.
- Plan commits/staging include only ten declared files (nine reviewed authority files plus new auditor test); unrelated dirty/untracked files, refs, and index entries remain untouched.
</verification>

<success_criteria>
- Implementation has one committed product/consumer/source authority set instead of competing untracked copies.
- Root auditor is usable, finite, and fail-closed for the admitted set.
- Napp/NMP/Uzel ownership, source-binding safety, exact-build admission, and fail-closed dependency behavior are normative.
- One issue/one PR and review evidence contracts are committed without creating CI/review enforcement.
- No user-owned unselected input is deleted, normalized, or silently accepted.
- All ten paths remain one docs-only authority-admission candidate because only their shared nine-path audit plus tenth test makes the set admissible; no intermediate task commit is publishable authority.
</success_criteria>

## Review Feedback Disposition

| Review item | Status | Reason / executable disposition |
|---:|---|---|
| 17 | Accepted | HEAD/index/refs/porcelain-v2 plan-base snapshot, exact per-task staged sets, and unrelated-path invariant; declared count is ten after adding tests. |
| 18 | Accepted | Auditor negative tests cover raw frame, native, path, secret, principal, duplicate-store, and private-fallback authority. |
| 19 | Accepted | Complete manifest/proposed-input selected/non-normative inventory with before/after SHA-256 and Git state. |
| 20 | Accepted | Stable JSON output fields/counts plus malformed non-allowlisted input isolation tests. |
| 21 | Accepted | JSON-compatible YAML issue form and structured PR labels are parsed for fields, repo owner, issue/slice, base/head, and review evidence. |
| 22 | Accepted | Admitted revision, decision reconciliation, complete non-normative inventory, and exact Uzel/nampplets/NMP/NAP/Tauri/nixpkgs committed pin provenance validated. |
| 23 | Rejected | D-02/D-16, ROADMAP, and REAUDIT make ownership (01-02) and authority admission (01-03) independent after shared replay prerequisite 01-01; no artifact contract joins them. |
| 24 | Accepted | Required issue YAML is JSON-compatible and mandatory-parsed; required parse failure cannot be an optional-parser skip. |
| Scope-sanity checker | Accepted | Retained as one bounded docs-only candidate: nine admitted authority paths and one enforcing test share one acceptance/staging invariant; splitting creates half-admitted authority or an untargeted validator. Exact 4+3+3 staging partitions and zero production changes are explicit. |
| D-18 checker | Accepted | Executor records exact candidate base/head and pending handoff only after the shared audit passes; separately authorized review/ship fills one head-bound ordered evidence record, with head-change invalidation and no automatic publication. |

<output>
Create `.planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-03-SUMMARY.md` when done. Record repository-qualified issue/branch/PR only if already available, before/after hashes for selected proposed inputs, exact admitted paths, audit command/result, authority commit, any conflict that stopped admission, and the required `review_handoff` pending-stop object without performing external publication.
</output>
