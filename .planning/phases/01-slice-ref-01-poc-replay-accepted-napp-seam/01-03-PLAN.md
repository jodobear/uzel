---
phase: 01-slice-ref-01-poc-replay-accepted-napp-seam
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - PROGRAMME_CONTRACT.md
  - NAPP_CONSUMER_PROFILE.md
  - SOURCE_BASELINE.md
  - docs/product-authority.json
  - ISSUE_PROCESS.md
  - .github/ISSUE_TEMPLATE/slice.yml
  - .github/PULL_REQUEST_TEMPLATE.md
  - tools/audit_repo_docs.py
  - AUDIT.md
autonomous: true
requirements:
  - REF-05
  - REF-07
must_haves:
  truths:
    - "D-16: One narrow root product-first authority set is selected, reconciled to committed planning decisions, auditable, and committed before implementation may depend on it."
    - "REF-05/D-03: Canonical documents preserve Uzel presentation/trusted-host ownership, Napp runtime authority, and NMP sole Nostr/state authority without copying programme text into implementation files."
    - "REF-07: Consumer policy admits only an exact committed candidate with source-backed client, product events, testkit, lifecycle/version, and pin evidence; current absence remains a dependency, not a private substitute."
    - "D-17/D-18: Issue and PR templates enforce one repository-qualified issue, one visible outcome, exact evidence, rollback/stop, and one review sequence per semantic candidate."
    - "Unselected pack files remain user-owned proposed inputs and are neither deleted nor silently promoted to normative authority."
  artifacts:
    - path: docs/product-authority.json
      provides: "Machine-readable allowlist of canonical root product/process documents and templates"
    - path: PROGRAMME_CONTRACT.md
      provides: "Canonical product-first ownership and phase-boundary contract"
    - path: NAPP_CONSUMER_PROFILE.md
      provides: "Canonical accepted-candidate-only consumer admission rule"
    - path: SOURCE_BASELINE.md
      provides: "Exact current-green Uzel/nampplets/NMP/NAP/Tauri/Nix pins and revalidation trigger"
    - path: tools/audit_repo_docs.py
      provides: "Narrow authority-set auditor that does not scan unrelated repository Markdown"
    - path: .github/ISSUE_TEMPLATE/slice.yml
      provides: "One outcome, dependency, trust, evidence, failure, and rollback issue form"
    - path: .github/PULL_REQUEST_TEMPLATE.md
      provides: "Exact issue/head/gate/review evidence PR contract"
  key_links:
    - from: tools/audit_repo_docs.py
      to: docs/product-authority.json
      via: "Auditor reads the explicit allowlist instead of unrestricted repository recursion"
      pattern: "product-authority.json"
    - from: NAPP_CONSUMER_PROFILE.md
      to: .planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-CONTEXT.md
      via: "D-01/D-02/D-03 admission and stop rules remain exact"
      pattern: "client"
    - from: .github/ISSUE_TEMPLATE/slice.yml
      to: .github/PULL_REQUEST_TEMPLATE.md
      via: "Both carry the same repository-qualified issue, scope, exact-head evidence, failure, and rollback fields"
      pattern: "Outcome"
  assumptions:
    - "[FLAGGED-UNVERIFIED] The proposed root pack can be reduced to one non-conflicting authority set; any locked-decision conflict must stop admission and be reported, not auto-merged."
    - "[FLAGGED-UNVERIFIED] REF-05 edge classification is unresolved; document admission proves authority selection, not implementation completeness."
    - "[FLAGGED-UNVERIFIED] REF-07 edge classification is unresolved; current coverage is fail-closed admission/dependency disposition, not adapter parity."
  prohibitions:
    - "[FLAGGED-UNVERIFIED] No private Napp substitute or expanded legacy protocol."
    - "[FLAGGED-UNVERIFIED] No duplicated NMP, runtime, Nostr, or store authority."
    - "[FLAGGED-UNVERIFIED] No evidence-class conflation or fabricated metrics."
    - "[FLAGGED-UNVERIFIED] No pull-in of Nix packaging, CI architecture, Social rebuild, multi-instance implementation, or parked capabilities."
---

## Phase Goal

**As a** Uzel maintainer/reviewer, **I want to** replay current POC evidence and apply a fail-closed Napp admission gate, **so that** visible value survives without duplicating authority.

<objective>
Deliver repository-qualified issue slice `jodobear/uzel — REF-01C: admit product-first root authority set` as one bounded documentation-only PR.

Purpose: Turn selected user-owned proposed pack files into one committed, auditable source of product/process authority without touching product code or promoting competing copies, per D-03, D-16, D-17, and D-18.
Output: Three canonical root contracts, a machine-readable authority allowlist, aligned issue/PR templates, and a narrowed root auditor. Unselected user files and unrelated dirty work remain untouched.
</objective>

<execution_context>
@/home/at/.codex/gsd-core/workflows/execute-plan.md
@/home/at/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-CONTEXT.md
@.planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-REAUDIT.md
@.planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-RESEARCH.md
</context>

## Artifacts this phase produces

- `docs/product-authority.json`; new authority allowlist, no code symbols.
- `PROGRAMME_CONTRACT.md`, `NAPP_CONSUMER_PROFILE.md`, `SOURCE_BASELINE.md`; selected canonical root contracts.
- `ISSUE_PROCESS.md`, `.github/ISSUE_TEMPLATE/slice.yml`, `.github/PULL_REQUEST_TEMPLATE.md`; selected delivery contract.
- `tools/audit_repo_docs.py`; refactored into `load_authority`, `audit_authority`, and `main` entry points.
- `AUDIT.md`; operator instructions, admitted scope, and explicit non-normative-input treatment.

<tasks>

<task type="auto">
  <name>Task 1: Select and reconcile the canonical product authority</name>
  <read_first>
    .planning/PROJECT.md
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-CONTEXT.md
    .planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-REAUDIT.md
    PROGRAMME_CONTRACT.md
    NAPP_CONSUMER_PROFILE.md
    SOURCE_BASELINE.md
    CONTEXT.md
    QUALITY.md
    NIX_PACKAGE.md
    GSD_RUNBOOK.md
    docs/00-boundary.md
    docs/10-gsd-seed.md
  </read_first>
  <files>PROGRAMME_CONTRACT.md, NAPP_CONSUMER_PROFILE.md, SOURCE_BASELINE.md, docs/product-authority.json</files>
  <action>Before editing, capture SHA-256 and Git tracked/untracked state for every proposed root input in the plan SUMMARY so user-owned provenance remains visible. Select only `PROGRAMME_CONTRACT.md`, `NAPP_CONSUMER_PROFILE.md`, and `SOURCE_BASELINE.md` as canonical product/consumer/source contracts for this slice; reconcile them against committed `.planning` decisions, with D-01 through D-18 and roadmap/requirements taking precedence over proposed text. `PROGRAMME_CONTRACT.md` must keep Tauri/Svelte/Uzel presentation and trusted-host integration in Uzel, Napp runtime/client/events/testkit authority in Napp, and all Nostr query/relay/store/freshness/provenance/signer/publication truth in NMP. `NAPP_CONSUMER_PROFILE.md` must require one exact reachable committed candidate, candidate-owned declared source and probes, version mismatch, lifecycle/recovery, testkit vectors, and later Cargo/Nix pin parity; it must stop on absence and state REF-01D is separately replanned only after qualification. `SOURCE_BASELINE.md` must refresh exact merged Uzel production commit `19519c378c2e775c6ad4b042cfd9aadd89f766b9` plus current-green nampplets, transitive NMP, NAP package, Tauri, and nixpkgs pins from committed source/locks, explicitly refusing compatibility claims for moved remote heads. Create `docs/product-authority.json` listing canonical files by role plus issue/PR templates and auditor; include the admitted revision, locked-decision source, and an explicit array of known proposed-but-non-normative inputs. Do not delete, move, normalize, or stage any unselected pack file. Stop on a locked-decision conflict that cannot be resolved without changing product scope.</action>
  <verify>
    <automated>python3 -m json.tool docs/product-authority.json &gt;/dev/null &amp;&amp; jq -e '(.canonical|length) == 8 and (.non_normative_inputs|type=="array") and (.locked_decision_source|endswith("01-CONTEXT.md"))' docs/product-authority.json &gt;/dev/null &amp;&amp; rg -q '19519c378c2e775c6ad4b042cfd9aadd89f766b9' SOURCE_BASELINE.md &amp;&amp; rg -q 'NMP' PROGRAMME_CONTRACT.md &amp;&amp; rg -q 'testkit' NAPP_CONSUMER_PROFILE.md</automated>
  </verify>
  <acceptance_criteria>Exactly one selected product/consumer/source authority exists in the allowlist; it matches locked decisions and exact current pins. Proposed alternatives remain present and explicitly non-normative.</acceptance_criteria>
  <done>D-16 product authority is selected and ready for exact-path commit without product-code dependency on uncommitted pack material.</done>
</task>

<task type="auto">
  <name>Task 2: Align one-issue and bounded-PR delivery contracts</name>
  <read_first>
    docs/product-authority.json
    ISSUE_PROCESS.md
    .github/ISSUE_TEMPLATE/slice.yml
    .github/PULL_REQUEST_TEMPLATE.md
    REVIEW_WORKFLOW.md
    REVIEW_EVIDENCE.md
    .planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-CONTEXT.md
  </read_first>
  <files>ISSUE_PROCESS.md, .github/ISSUE_TEMPLATE/slice.yml, .github/PULL_REQUEST_TEMPLATE.md</files>
  <action>Admit and align only the bounded issue/PR contract. `ISSUE_PROCESS.md` must define one repository-qualified issue, one observable outcome, explicit hard dependencies versus delivery order, exact source/pins, owned seam/trust boundary, resource budget, failure/cancel/restart behavior, acceptance evidence, rollback/stop, and exclusions. Keep the issue form fields for outcome, value, dependencies, scope, exclusions, exact refs, CI class, trust, performance, failure, acceptance, rollback, and review sensitivity; ensure owner/repository references are required when an external dependency such as Napp is named. Keep the PR template bound to one full issue ref and one slice ID, exact base/head, affected local evidence, result-class separation, rollback/stop, and D-18 review evidence once per immutable semantic candidate. This task does not open issues, create PRs, enforce GitHub CI, or alter review-provider configuration; it creates the canonical templates future slices consume. Preserve every unrelated `.github` and dirty path.</action>
  <verify>
    <automated>python3 -c 'from pathlib import Path; import sys; paths=[Path("ISSUE_PROCESS.md"),Path(".github/ISSUE_TEMPLATE/slice.yml"),Path(".github/PULL_REQUEST_TEMPLATE.md")]; sys.exit(0 if all(p.exists() and p.read_text(encoding="utf-8").strip() for p in paths) else 1)' &amp;&amp; rg -q 'Hard dependencies' .github/ISSUE_TEMPLATE/slice.yml &amp;&amp; rg -q 'Failure, cancellation and restart' .github/ISSUE_TEMPLATE/slice.yml &amp;&amp; rg -q 'Rollback or stop condition' .github/ISSUE_TEMPLATE/slice.yml &amp;&amp; rg -q 'Closes .*full issue ref' .github/PULL_REQUEST_TEMPLATE.md</automated>
  </verify>
  <acceptance_criteria>Issue and PR contracts carry one outcome, exact refs, trust/resource/lifecycle evidence, rollback, and repository-qualified dependencies without creating a CI or review orchestration subsystem.</acceptance_criteria>
  <done>D-17 one-issue/one-PR mapping and D-18 once-per-candidate review evidence are canonical and auditable.</done>
</task>

<task type="auto">
  <name>Task 3: Narrow the root auditor to admitted authority</name>
  <read_first>
    docs/product-authority.json
    tools/audit_repo_docs.py
    AUDIT.md
    DIAGRAMS.md
    reports/mermaid-render.json
    manifest.json
    uzel-poc-validated-pack/scripts/audit_docs.py
  </read_first>
  <files>tools/audit_repo_docs.py, AUDIT.md</files>
  <action>Refactor `tools/audit_repo_docs.py` into small `load_authority`, `audit_authority`, and `main` functions. Read `docs/product-authority.json`; audit only its canonical paths and the explicit authority manifest itself. For each admitted file, require existence, UTF-8 readability, local-link resolution, duplicate-paragraph checks, and syntax validation for JSON/TOML/YAML when the already-available standard/runtime modules support it. Retain diagram-index/render checks only for admitted documents that contain Mermaid. Validate required ownership/admission concepts from the canonical docs and exact issue/PR fields, but do not recursively scan unrelated repository Markdown or binary-derived paths. Emit stable JSON with status, errors, admitted file count, diagram count, and skipped optional parser capabilities; exit nonzero on missing, malformed, conflicting, non-UTF-8, or broken admitted authority. Update `AUDIT.md` with the exact command, scope, expected result shape, non-normative-input rule, and limitation that this audit is product-document admission rather than production/test/package/CI evidence. Run it before the task commit; preserve every unselected/unrelated dirty file and report exact selected paths in SUMMARY.</action>
  <verify>
    <automated>python3 tools/audit_repo_docs.py &amp;&amp; python3 -c 'from pathlib import Path; compile(Path("tools/audit_repo_docs.py").read_text(encoding="utf-8"), "tools/audit_repo_docs.py", "exec")' &amp;&amp; rg -q 'docs/product-authority.json' tools/audit_repo_docs.py AUDIT.md &amp;&amp; git diff --check -- PROGRAMME_CONTRACT.md NAPP_CONSUMER_PROFILE.md SOURCE_BASELINE.md docs/product-authority.json ISSUE_PROCESS.md .github/ISSUE_TEMPLATE/slice.yml .github/PULL_REQUEST_TEMPLATE.md tools/audit_repo_docs.py AUDIT.md</automated>
  </verify>
  <acceptance_criteria>Root audit passes only the admitted allowlist, fails closed on faults inside it, and ignores unrelated user-owned inputs without calling them accepted. Selected paths are the only paths committed for REF-01C.</acceptance_criteria>
  <done>D-16 authority set is committed and audit-backed; REF-01D may cite it only after this plan's summary records the exact commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-owned proposed pack → canonical project authority | Untracked documents are untrusted proposed input until selected, reconciled, audited, and committed. |
| canonical docs → executor/issue/PR | Normative text can authorize work; conflicting or broadened scope must fail admission. |
| issue/PR templates → external delivery state | Templates carry repository refs and evidence but do not themselves authorize merge, publication, or CI changes. |
| future Uzel consumer → Napp/NMP | Docs must preserve source binding, exact-build authority, one runtime/NMP owner, bounded cancellation/recovery, and no private fallback. |

## STRIDE Threat Register — ASVS L1, block high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | proposed root documents | high | mitigate | Snapshot hashes/state, reconcile only selected paths to committed locked decisions, allowlist authority, and retain unselected inputs as non-normative. |
| T-03-02 | Elevation | normative scope/ownership text | high | mitigate | Auditor requires Uzel/Napp/NMP ownership, source binding, denied raw frame/native/path/secret/principal authority, and no private fallback. |
| T-03-03 | Spoofing / Repudiation | issue and PR references | medium | mitigate | Require full owner/repository issue refs, slice ID, exact base/head, evidence paths, results, rollback, and review SHAs. |
| T-03-04 | Denial of service | unrestricted auditor scan | medium | mitigate | Replace repository-wide recursion with a finite manifest allowlist and report optional parser skips explicitly. |
| T-03-05 | Tampering | candidate/pin claims | high | mitigate | Consumer/source docs require exact committed source, declared probes, mismatch/lifecycle vectors, and current-green pin evidence; moved remote heads confer no acceptance. |
| T-03-06 | Elevation / Information disclosure | future frame/runtime contract | high | mitigate | Canonical authority forbids raw network, Tauri bridge, host path, secret, caller principal, duplicate stores, and unbounded recovery/resource work. |
</threat_model>

<verification>
- `docs/product-authority.json` names exactly the selected canonical set and proposed non-normative inputs.
- Selected contracts match D-01 through D-18 and exact current source/lock pins.
- Issue/PR templates carry one bounded repository-qualified slice and exact evidence/rollback fields.
- `python3 tools/audit_repo_docs.py` passes and scopes itself to the authority allowlist.
- Plan commits/staging include only nine declared files; unrelated dirty/untracked files remain untouched.
</verification>

<success_criteria>
- Implementation has one committed product/consumer/source authority set instead of competing untracked copies.
- Root auditor is usable, finite, and fail-closed for the admitted set.
- Napp/NMP/Uzel ownership, source-binding safety, exact-build admission, and fail-closed dependency behavior are normative.
- One issue/one PR and review evidence contracts are committed without creating CI/review enforcement.
- No user-owned unselected input is deleted, normalized, or silently accepted.
</success_criteria>

<output>
Create `.planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-03-SUMMARY.md` when done. Record repository-qualified issue/branch/PR if available, before/after hashes for selected proposed inputs, exact admitted paths, audit command/result, authority commit, and any conflict that stopped admission.
</output>
