---
phase: 01-slice-ref-01-poc-replay-accepted-napp-seam
plan: 04
type: execute
wave: 4
depends_on:
  - "01-03"
files_modified:
  - evidence/phase-01/orchestration-profile.toml
  - evidence/phase-01/command-inventory.json
  - evidence/phase-01/test-claim-map.toml
  - evidence/phase-01/command-artifacts.sha256
  - evidence/phase-01/artifact-manifest.json
  - evidence/phase-01/ci-baseline.json
  - evidence/phase-01/review-baseline.json
  - docs/delivery/phase-01-review-sequence.md
  - docs/delivery/phase-01-ci-profile.md
  - .planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-04-SUMMARY.md
autonomous: true
requirements:
  - M0-04
user_setup: []
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-07: CI/test/review/orchestration maturity is measured with exact commands, runtimes, hashes, durations, artifacts, coverage, and limitations."
    - "D-25: after pushing the immutable exact PR SHA, local CodeRabbit and GitHub Codex review that same SHA serially or concurrently; acceptance joins both dispositions, while only a recorded local-CodeRabbit rate limit before findings permits green GitHub Codex to satisfy the fallback gate, and any later commit invalidates review acceptance for that SHA."
    - "D-27: command-profile inventory precedes measured CI/review conclusions and no duplicate speculative gate is invented."
    - "D-29 and D-30: absent workflows, failed commands, unavailable agents, stale review, and unsupported coverage remain visible evidence."
    - "D-31: review evidence is valid only for the recorded SHA; any later change invalidates final-review acceptance."
  artifacts:
    - path: evidence/phase-01/orchestration-profile.toml
      provides: "Pinned executable paths, versions, hashes, help fingerprints, env policy, timeouts, cancellation, restart, and review order"
    - path: evidence/phase-01/command-inventory.json
      provides: "Declared and discovered command surface without fabricated commands"
    - path: evidence/phase-01/test-claim-map.toml
      provides: "Requirement/claim to existing test and artifact coverage"
    - path: evidence/phase-01/artifact-manifest.json
      provides: "Exact command, reviewer, and claim artifact ownership/coverage manifest"
    - path: evidence/phase-01/ci-baseline.json
      provides: "Measured local/declared-CI truth, including absence of tracked workflows"
    - path: evidence/phase-01/review-baseline.json
      provides: "Measured review/agent capability and final-SHA validity baseline"
    - path: docs/delivery/phase-01-review-sequence.md
      provides: "Non-ambiguous local-CodeRabbit/GitHub-Codex ordering, SHA binding, and whole-chain invalidation rules"
    - path: docs/delivery/phase-01-ci-profile.md
      provides: "One evidence-derived later CI command profile and known gaps"
  key_links:
    - from: evidence/phase-01/orchestration-profile.toml
      to: evidence/phase-01/command-inventory.json
      via: "Every executable row binds to a pinned path/version/hash/help fingerprint and environment policy"
      pattern: "tool_id"
    - from: evidence/phase-01/command-inventory.json
      to: evidence/phase-01/ci-baseline.json
      via: "Only declared or discovered existing commands may be measured"
      pattern: "command_id"
    - from: evidence/phase-01/test-claim-map.toml
      to: evidence/phase-01/ci-baseline.json
      via: "Coverage conclusions cite exact command/artifact rows, not command names alone"
      pattern: "claim_id"
    - from: evidence/phase-01/review-baseline.json
      to: docs/delivery/phase-01-review-sequence.md
      via: "Review stages, actors, inputs, outputs, and SHA invalidation derive from measured capability evidence"
      pattern: "review_stage_id"
  assumptions:
    - id: EDGE-M0-04-UNCLASSIFIED
      requirement: M0-04
      category: unclassified
      statement: "The spec-less edge probe could not classify M0-04; manual review must confirm command discovery, test-claim coverage, unavailable/failed tool handling, review sequencing, SHA invalidation, cancellation, and restart criteria cover every material delivery edge."
      status: unresolved
      flagged: true
  prohibitions:
    - id: PROHIB-M0-04-REVIEW-INDEPENDENCE
      requirement: M0-04
      category: transparency
      statement: "MUST NOT use Claude, OpenCode, remote CodeRabbit or local Codex self-review; treat any CodeRabbit failure other than recorded rate limiting as fallback authorization; reuse review evidence after a commit; or claim a gate not measured on its recorded SHA."
      status: unverified
      flagged: true
---

## Phase Goal

**As a** Uzel owner, **I want to** preserve the current POC and incident evidence while separating historical replay truth from current evidence, **so that** Phase 1 can reset execution without inventing a green baseline.

<objective>
Measure the current command, test, CI, review, and orchestration surface and pin a reproducible command/review profile with final-SHA validity.

Purpose: Satisfy M0-04 by replacing assumed delivery maturity with exact observed capability and one non-duplicative evidence-derived gate profile.

Output: Pinned orchestration and command inventories, test-claim and raw-artifact maps, measured CI/review baselines, and explicit delivery/review contracts.
</objective>

<execution_context>
@/home/at/.codex/gsd-core/workflows/execute-plan.md
@/home/at/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-CONTEXT.md
@docs/plans/uzel-product-incubation-v4-2026-08-10/04-DELIVERY-QUALITY.md
@docs/plans/uzel-product-incubation-v4-2026-08-10/06-START-RUNBOOK.md
@docs/plans/uzel-product-incubation-v4-2026-08-10/prompts/02-review-phase-1.md
@.planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-02-SUMMARY.md
@.planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-03-SUMMARY.md
@package.json
@pnpm-workspace.yaml
@uzel-poc-validated-pack/package.json
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Trace one claim through a pinned command, artifact, and review-capability row</name>
  <files>evidence/phase-01/orchestration-profile.toml, evidence/phase-01/command-inventory.json, evidence/phase-01/test-claim-map.toml, evidence/phase-01/command-artifacts.sha256, evidence/phase-01/artifact-manifest.json</files>
  <execution_contract>Derive `authoritative_command_rows` exactly from every `scripts` member of every strict-parsed tracked `package.json` at source head plus every tracked mode-100755 repository script; bind path/blob/SHA/script key/value or mode. Inventory covers that universe bijectively; any extra row must use exactly one fixed approved capability authority ID (`managed_codex`, `bare_codex_collision`, `local_coderabbit`, `github_codex`, `nix_flake_show`, or `nix_eval`). For a package-script authority, derive the canonical cwd as repository root and argv exactly as `[tool.absolute_path,"--dir",package_directory,"run",script_name]`; for an executable-script authority derive cwd as repository root and argv exactly as `[absolute_source_path]`. Aliases repeat their canonical argv/cwd exactly. Every raw artifact is canonical `phase1-command-evidence-v1`; parse and bind its exact argv/cwd/status/exit/stdout/stderr fields to its command row. Every covered/partial claim artifact is canonical `phase1-claim-evidence-v1` and binds the claim ID/status/rationale plus selected command ID and that command artifact ID/digest. Every tool has distinct nonempty `version_artifact_id` and `help_artifact_id`; artifact-manifest owner rows `tool_version` and `tool_help` bind those IDs, paths, kinds, and byte digests, and join the exact command/reviewer/claim artifact bijection. Local command `argv[0]` equals referenced tool `absolute_path`. Attempted reviewer evidence is a canonical content-addressed actual-capture envelope retaining verbatim base64 command/API response bytes; its exact argv equals the referenced canonical reviewer command, its integer `exit_status` repeats the reviewer row, pass requires zero, and rate-limit/failed require nonzero. Status/findings/pre-finding are derived only from decoded bytes, never accepted from a synthesized normalized summary. Unavailable/not-attempted rows have null exit status. CodeRabbit identity is exact `/home/at/.local/bin/coderabbit`, version `0.7.2`, SHA-256 `f9f61ecdb385d3c8d5001ee652dee1d95b3282fd740bd023dc89b18a191d3e97`. GitHub Codex is exact reviewer `GitHub Codex`, tool `github-codex-review`, with no local executable/tool metadata.</execution_contract>
  <read_first>
    - .planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-CONTEXT.md — D-07, D-25, D-27, D-29 through D-31
    - docs/plans/uzel-product-incubation-v4-2026-08-10/04-DELIVERY-QUALITY.md — command profile, test taxonomy, CI/review ordering, and evidence requirements
    - docs/plans/uzel-product-incubation-v4-2026-08-10/06-START-RUNBOOK.md — tool/runtime path discipline and stop rules
    - package.json, pnpm-workspace.yaml, uzel-poc-validated-pack/package.json — declared existing command surfaces
    - evidence/phase-01/current-output-inventory.json — authoritative Plan-02 source head/tree and discovery identity
    - evidence/phase-01/current-native-baseline.json — already measured command rows; reuse evidence rather than rerunning without reason
    - evidence/phase-01/current-source-citations.json — claim IDs and current source head
  </read_first>
  <action>Implement D-07, D-25, D-27, D-29, and D-30 as a tracer beginning with one M0 claim that already has current-source and Plan-02 command evidence. Give `orchestration-profile.toml`, `command-inventory.json`, and `test-claim-map.toml` explicit schema/version fields. The command inventory's single `source_head`/`source_tree` must exactly equal both Plan-02 `current-output-inventory.json` and Plan-03 `current-source-citations.json`; resolve `source_head^{tree}` with Git and reject any mismatch. Every command row repeats that exact head/tree and every local executable row has a unique stable `tool_id`/`command_id`, absolute path whose resolved target is a regular executable with matching byte SHA-256, exact version output plus `version_artifact_path`/digest, bounded `help_artifact_path`/fingerprint SHA-256, exact argv, canonical command ID, environment allowlist, working-directory policy, timeout, owned-child cancellation, raw-output artifact SHA-256, redaction, and restart-on-hash-drift rule. A canonical command is one executable command row; aliases are separate unique command IDs that reference that canonical ID, so duplicate alias `canonical_id` values are allowed and must resolve to one canonical row. Reject duplicate command/tool IDs, a missing canonical target, missing artifact references, or claim rows that do not bind a command/artifact ID. Create versioned `artifact-manifest.json` as the sole exact artifact-bijection registry: `artifacts[]` has unique `artifact_id`, repo-relative path, SHA-256, owner kind/ID, and kind; every command row with raw output, every reviewer row with retained evidence, and every covered/partial claim row has exactly one `raw_artifact_id`/`artifact_id` whose manifest row has matching owner and digest, while unavailable/not-attempted rows explicitly have no artifact and a reason. Parse and validate the manifest against command inventory, orchestration reviewer rows, and TOML claim rows; reject duplicate manifest IDs, an orphan manifest row, duplicate ownership references, or a missing command/reviewer/claim artifact. The TOML claim map uses stable `claim_rows[]` with coverage status and command/artifact IDs so this check is parsed rather than token-based. Include managed Codex and the bare-path collision as distinct tool rows with exact roles `managed_codex` and `bare_codex_collision`. Probe exactly two nonempty reviewer stage records with distinct `record_kind` values, `local_coderabbit` as `local_executable_reviewer` and `github_codex` as `remote_github_reviewer`. Every reviewer row has `review_scope = "capability_probe_only"`, `acceptance = false`, nonempty exact identity, finding disposition, status reason, a true canonical command ID, and one allowed status. For local CodeRabbit, every attempted `pass`, `rate_limit`, or `failed` probe carries `tool_id`, absolute executable path, nonempty version, executable/version/help SHA-256 values, and those fields exactly equal the referenced local tool row; unavailable/not-attempted rows carry no invented executable link. For GitHub Codex, prohibit executable/tool fields and require nonempty reviewer/request/response/evidence locators for pass/rate-limit evidence. Any pass/rate-limit review-shaped capability evidence binds candidate/input/effective/reviewed-head to the same authoritative source head, but remains non-acceptance evidence. A CodeRabbit `rate_limit` is valid only before findings. Every version/help artifact path resolves to an exact artifact-manifest row and matching bytes. Both rows bind retained evidence to the artifact manifest when present; failed rows retain immutable artifact evidence and digest plus explicit reason; unavailable/not-attempted rows remain honest non-acceptance capability data with explicit no-artifact reason. Record supported commands/requests, owner-approved outbound boundary, unavailable/failed capability honestly, and never treat presence as successful Prompt-02 review. Record Claude, OpenCode, remote CodeRabbit and local Codex self-review as prohibited routes, not fallback capabilities. Discover commands from tracked manifests, workspace config, existing scripts, and tracked workflow definitions; record the absence of `.github/workflows` if still observed rather than creating one. Classify commands by build, type/check, unit, integration, conformance, hostile-boundary, smoke, lint, format, docs, fallow, measurement, review, and orchestration; link aliases to one canonical argv so CI gates are not duplicated. Review stages are ordered evidence consumers of the same canonical command gates, not extra CI gates. Map every M0 claim and current-source authority claim to exact existing tests/commands/artifacts and mark `covered`, `partial`, `uncovered`, `unavailable`, or `not_applicable` with rationale. Reuse valid Plan-02 artifacts by digest and run only discovery/capability probes missing there. Write task-owned files atomically; cancellation retains checksummed completed probes and restart requires unchanged head/tool hashes.</action>
  <verify>
    <automated>python3 -c 'import hashlib,json,pathlib,re,tomllib; p=tomllib.load(open("evidence/phase-01/orchestration-profile.toml","rb")); m=tomllib.load(open("evidence/phase-01/test-claim-map.toml","rb")); i=json.load(open("evidence/phase-01/command-inventory.json")); a=json.load(open("evidence/phase-01/artifact-manifest.json")); h40=lambda v:isinstance(v,str) and re.fullmatch(r"[0-9a-f]{40}",v) is not None; h64=lambda v:isinstance(v,str) and re.fullmatch(r"[0-9a-f]{64}",v) is not None; tools=p["tools"]; reviewers=p["reviewers"]; commands=i["commands"]; claims=m["claim_rows"]; artifacts=a["artifacts"]; assert all(x.get("schema") for x in (p,m,i,a)) and h40(i.get("source_head")) and h40(i.get("source_tree")); assert tools and commands and claims and artifacts; tids=[x["tool_id"] for x in tools]; cids=[x["id"] for x in commands]; clids=[x["id"] for x in claims]; aids=[x["artifact_id"] for x in artifacts]; assert len(tids)==len(set(tids)) and len(cids)==len(set(cids)) and len(clids)==len(set(clids)) and len(aids)==len(set(aids)); assert {"managed_codex","bare_codex_collision"}.issubset({x.get("role") for x in tools}); assert all(isinstance(x.get("absolute_path"),str) and pathlib.Path(x["absolute_path"]).is_absolute() and isinstance(x.get("version_output"),str) and x["version_output"] and h64(x.get("version_output_sha256")) and h64(x.get("executable_sha256")) and h64(x.get("help_fingerprint_sha256")) and isinstance(x.get("version_artifact_id"),str) and x["version_artifact_id"] and isinstance(x.get("help_artifact_id"),str) and x["help_artifact_id"] and x["version_artifact_id"]!=x["help_artifact_id"] for x in tools); canonical={x["id"] for x in commands if x.get("canonical_id")==x["id"]}; assert canonical and all(isinstance(x.get("argv"),list) and x["argv"] and all(isinstance(v,str) and v for v in x["argv"]) and isinstance(x.get("origin"),str) and x["origin"] and x.get("canonical_id") in canonical and x.get("tool_id") in set(tids) and h40(x.get("source_head")) and h40(x.get("source_tree")) and ((isinstance(x.get("raw_artifact_id"),str) and x["raw_artifact_id"] and h64(x.get("raw_artifact_sha256"))) or (x.get("raw_artifact_id") is None and x.get("status") in ("unavailable","not_attempted") and isinstance(x.get("reason"),str) and x["reason"])) for x in commands); assert {x.get("id") for x in reviewers}=={"local_coderabbit","github_codex"} and len(reviewers)==2; rb={x["id"]:x for x in reviewers}; l=rb["local_coderabbit"]; g=rb["github_codex"]; assert l.get("record_kind")=="local_executable_reviewer" and g.get("record_kind")=="remote_github_reviewer" and isinstance(l.get("identity"),str) and l["identity"] and isinstance(g.get("reviewer"),str) and g["reviewer"] and g.get("executable_path") is None; assert all(isinstance(x.get("schema"),str) and x["schema"] and x.get("canonical_command_id") in set(cids) and x.get("status") in ("pass","rate_limit","failed","unavailable","not_attempted") and ((isinstance(x.get("artifact_id"),str) and x["artifact_id"] and h64(x.get("artifact_sha256"))) or (x.get("artifact_id") is None and x.get("status") in ("failed","unavailable","not_attempted") and isinstance(x.get("reason"),str) and x["reason"])) for x in reviewers); assert any(x.get("status") in ("covered","partial") for x in claims) and all(x.get("command_id") in set(cids) and (isinstance(x.get("rationale"),str) and x["rationale"]) and ((x.get("status") in ("covered","partial") and isinstance(x.get("artifact_id"),str) and x["artifact_id"] and h64(x.get("artifact_sha256"))) or (x.get("status") in ("uncovered","unavailable","not_applicable") and x.get("artifact_id") is None and isinstance(x.get("reason"),str) and x["reason"])) for x in claims); rows=[("command",x["id"],x["raw_artifact_id"],x["raw_artifact_sha256"]) for x in commands if x.get("raw_artifact_id")]+[("reviewer",x["id"],x["artifact_id"],x["artifact_sha256"]) for x in reviewers if x.get("artifact_id")]+[("claim",x["id"],x["artifact_id"],x["artifact_sha256"]) for x in claims if x["status"] in ("covered","partial")]+[("tool_version",x["tool_id"],x["version_artifact_id"],x["version_output_sha256"]) for x in tools]+[("tool_help",x["tool_id"],x["help_artifact_id"],x["help_fingerprint_sha256"]) for x in tools]; assert {x[0] for x in rows}=={"command","reviewer","claim","tool_version","tool_help"} and len({(k,o) for k,o,_,_ in rows})==len(rows); by={x["artifact_id"]:x for x in artifacts}; assert len(by)==len(artifacts)==len(rows) and len({x.get("path") for x in artifacts})==len(artifacts); assert all(aid in by and by[aid].get("owner_kind")==k and by[aid].get("owner_id")==o and by[aid].get("sha256")==sha and isinstance(by[aid].get("kind"),str) and by[aid]["kind"] for k,o,aid,sha in rows); paths=[pathlib.Path(x["path"]) for x in artifacts]; assert all(isinstance(x.get("path"),str) and x["path"] and not pth.is_absolute() and ".." not in pth.parts and all(not pathlib.Path(*pth.parts[:n]).is_symlink() for n in range(1,len(pth.parts)+1)) and pth.is_file() and hashlib.sha256(pth.read_bytes()).hexdigest()==x["sha256"] for x,pth in zip(artifacts,paths)); lines=pathlib.Path("evidence/phase-01/command-artifacts.sha256").read_text().splitlines(); want=sorted(x["sha256"]+"  "+x["path"] for x in artifacts); assert lines==want' &amp;&amp; python3 -c 'import hashlib,json,os,pathlib,re,stat,subprocess,tomllib; p=tomllib.load(open("evidence/phase-01/orchestration-profile.toml","rb")); i=json.load(open("evidence/phase-01/command-inventory.json")); a=json.load(open("evidence/phase-01/artifact-manifest.json")); o=json.load(open("evidence/phase-01/current-output-inventory.json")); s=json.load(open("evidence/phase-01/current-source-citations.json")); req=pathlib.Path(".planning/REQUIREMENTS.md").read_text(); m=tomllib.load(open("evidence/phase-01/test-claim-map.toml","rb")); authoritative={x["id"] for x in s["claims"]}|set(re.findall(r"(?m)^- \[[ xX]\] \*\*(M0-[0-9]{2}):",req)); assert authoritative and {x["id"] for x in m["claim_rows"]}==authoritative; h=i["source_head"]; t=i["source_tree"]; assert h==o["source_head"]==s["source_head"] and t==o["source_tree"]==s["source_tree"] and subprocess.check_output(["git","rev-parse",h+"^{tree}"],text=True).strip()==t; commands=i["commands"]; assert all(x.get("source_head")==h and x.get("source_tree")==t for x in commands); canonical={x["id"] for x in commands if x.get("canonical_id")==x["id"]}; tools={x["tool_id"]:x for x in p["tools"]}; reviewers={x["id"]:x for x in p["reviewers"]}; bypath={x["path"]:x for x in a["artifacts"]}; assert all((q:=pathlib.Path(x["absolute_path"])).is_absolute() and (actual:=q.resolve(strict=True)) and stat.S_ISREG(actual.stat().st_mode) and os.access(actual,os.X_OK) and hashlib.sha256(actual.read_bytes()).hexdigest()==x["executable_sha256"] and isinstance(x.get("version_artifact_path"),str) and (vp:=pathlib.Path(x["version_artifact_path"])).is_file() and bypath.get(x["version_artifact_path"],{}).get("sha256")==x["version_output_sha256"]==hashlib.sha256(vp.read_bytes()).hexdigest() and vp.read_text().rstrip("\n")==x["version_output"].rstrip("\n") and isinstance(x.get("help_artifact_path"),str) and (hp:=pathlib.Path(x["help_artifact_path"])).is_file() and bypath.get(x["help_artifact_path"],{}).get("sha256")==x["help_fingerprint_sha256"]==hashlib.sha256(hp.read_bytes()).hexdigest() for x in p["tools"]); assert set(reviewers)=={"local_coderabbit","github_codex"} and all(x.get("review_scope")=="capability_probe_only" and x.get("acceptance") is False and isinstance(x.get("finding_disposition"),str) and x["finding_disposition"] and isinstance(x.get("reason"),str) and x["reason"] and x.get("canonical_command_id") in canonical for x in reviewers.values()); l=reviewers["local_coderabbit"]; g=reviewers["github_codex"]; attempted=l["status"] in ("pass","rate_limit","failed"); assert ((attempted and l.get("tool_id") in tools and (z:=tools[l["tool_id"]]) and l.get("executable_path")==z.get("absolute_path") and pathlib.Path(l["executable_path"]).is_absolute() and l.get("version_output")==z.get("version_output") and l.get("version_output_sha256")==z.get("version_output_sha256") and l.get("executable_sha256")==z.get("executable_sha256") and l.get("help_fingerprint_sha256")==z.get("help_fingerprint_sha256") and all(re.fullmatch(r"[0-9a-f]{64}",l[k]) for k in ("version_output_sha256","executable_sha256","help_fingerprint_sha256"))) or ((not attempted) and l.get("tool_id") is None and l.get("executable_path") is None)); assert l["status"]!="rate_limit" or (l.get("findings_emitted") is False and l.get("finding_count")==0); assert g.get("tool_id") is None and g.get("executable_path") is None and isinstance(g.get("reviewer"),str) and g["reviewer"]; assert all(x["status"] not in ("pass","rate_limit","failed") or (all(re.fullmatch(r"[0-9a-f]{40}",x.get(k,"")) for k in ("candidate_sha","input_sha","effective_sha","reviewed_head_sha")) and x["candidate_sha"]==x["input_sha"]==x["effective_sha"]==x["reviewed_head_sha"]==h) for x in reviewers.values()); assert g["status"] not in ("pass","rate_limit") or (all(isinstance(g.get(k),str) and g[k] for k in ("request","response","external_evidence")) and re.fullmatch(r"[0-9a-f]{64}",g.get("external_evidence_sha256","")) and g["external_evidence_sha256"]==g.get("artifact_sha256"))' &amp;&amp; sha256sum -c evidence/phase-01/command-artifacts.sha256</automated>
    <automated>python3 -c 'import hashlib,json,pathlib,subprocess,tomllib
def strict(pairs):
 d={}; [(_ for _ in ()).throw(ValueError("duplicate key "+k)) if k in d else d.__setitem__(k,v) for k,v in pairs]; return d
def load(p):
 raw=pathlib.Path(p).read_bytes(); obj=json.loads(raw,object_pairs_hook=strict,parse_constant=lambda x:(_ for _ in ()).throw(ValueError(x))); assert raw==json.dumps(obj,sort_keys=True,separators=(",",":"),ensure_ascii=False).encode(); return obj
i=load("evidence/phase-01/command-inventory.json"); a=load("evidence/phase-01/artifact-manifest.json"); p=tomllib.load(open("evidence/phase-01/orchestration-profile.toml","rb")); h=i["source_head"]; derived=[]
for path in subprocess.check_output(["git","ls-tree","-r","--name-only",h],text=True).splitlines():
 if pathlib.PurePosixPath(path).name=="package.json":
  raw=subprocess.check_output(["git","show",h+":"+path]); obj=json.loads(raw,object_pairs_hook=strict,parse_constant=lambda x:(_ for _ in ()).throw(ValueError(x))); blob=subprocess.check_output(["git","rev-parse",h+":"+path],text=True).strip()
  for k,v in sorted(obj.get("scripts",{}).items()): derived.append({"authority_id":"package_script:"+path+":"+k,"kind":"package_script","source_path":path,"source_blob_oid":blob,"source_sha256":hashlib.sha256(raw).hexdigest(),"script_name":k,"script_value":v})
for z in subprocess.check_output(["git","ls-tree","-r","-z",h]).decode().split("\0"):
 if z and z.startswith("100755 blob "):
  oid,path=z.split(None,2)[2].split("\t",1); derived.append({"authority_id":"executable_script:"+path,"kind":"executable_script","source_path":path,"source_blob_oid":oid,"source_sha256":hashlib.sha256(subprocess.check_output(["git","show",h+":"+path])).hexdigest()})
expected=sorted(derived,key=lambda x:(x["source_path"],x.get("script_name",""))); assert i["authoritative_command_rows"]==expected; source_ids={x["authority_id"] for x in expected}; canonical=[x for x in i["commands"] if x["id"]==x["canonical_id"]]; command_ids={x["authority_id"] for x in canonical}; approved={"managed_codex","bare_codex_collision","local_coderabbit","github_codex","nix_flake_show","nix_eval"}; assert source_ids&lt;=command_ids and command_ids-source_ids&lt;=approved and all(sum(x["authority_id"]==a for x in canonical)==1 for a in source_ids) and all(x["authority_id"]==next(z for z in canonical if z["id"]==x["canonical_id"])["authority_id"] for x in i["commands"]); tools={x["tool_id"]:x for x in p["tools"]}; assert all(x["argv"][0]==tools[x["tool_id"]]["absolute_path"] for x in i["commands"]); rb={x["id"]:x for x in p["reviewers"]}; cr=rb["local_coderabbit"]; gh=rb["github_codex"]; assert cr["executable_path"]=="/home/at/.local/bin/coderabbit" and cr["version_output"]=="0.7.2" and cr["executable_sha256"]=="f9f61ecdb385d3c8d5001ee652dee1d95b3282fd740bd023dc89b18a191d3e97"; assert gh["reviewer"]=="GitHub Codex" and gh["tool_identity"]=="github-codex-review" and not any(k in gh for k in ("tool_id","executable_path","absolute_path","version_output","version_output_sha256","executable_sha256","help_fingerprint_sha256"))'</automated>
    <automated>python3 -c 'import hashlib,json,pathlib,tomllib
i=json.load(open("evidence/phase-01/command-inventory.json")); p=tomllib.load(open("evidence/phase-01/orchestration-profile.toml","rb")); a=json.load(open("evidence/phase-01/artifact-manifest.json")); root=pathlib.Path.cwd().resolve(); tools={x["tool_id"]:x for x in p["tools"]}; auth={x["authority_id"]:x for x in i["authoritative_command_rows"]}; canonical={x["id"]:x for x in i["commands"] if x["id"]==x["canonical_id"]}
for c in canonical.values():
 if c["authority_id"] not in auth: continue
 row=auth[c["authority_id"]]; tool=tools[c["tool_id"]]; expected_cwd=str(root)
 if row["kind"]=="package_script": expected=[tool["absolute_path"],"--dir",str(pathlib.PurePosixPath(row["source_path"]).parent),"run",row["script_name"]]
 else: expected=[str(root/pathlib.PurePosixPath(row["source_path"]))]; assert pathlib.Path(tool["absolute_path"])==pathlib.Path(expected[0])
 assert c["argv"]==expected and c["cwd"]==expected_cwd
assert all(x["argv"]==canonical[x["canonical_id"]]["argv"] and x["cwd"]==canonical[x["canonical_id"]]["cwd"] for x in i["commands"]); by={x["artifact_id"]:x for x in a["artifacts"]}; refs=[]
for t in tools.values(): refs += [("tool_version",t["tool_id"],t["version_artifact_id"],t["version_artifact_path"],t["version_output_sha256"],"version_output"),("tool_help",t["tool_id"],t["help_artifact_id"],t["help_artifact_path"],t["help_fingerprint_sha256"],"help_output")]
assert len({aid for k,o,aid,path,digest,kind in refs})==len(refs) and all((z:=by[aid])["owner_kind"]==k and z["owner_id"]==o and z["path"]==path and z["sha256"]==digest and z["kind"]==kind and hashlib.sha256(pathlib.Path(path).read_bytes()).hexdigest()==digest for k,o,aid,path,digest,kind in refs)'</automated>
    <automated>python3 -c 'import json,pathlib,tomllib
def strict(pairs):
 d={}; [(_ for _ in ()).throw(ValueError("duplicate key "+k)) if k in d else d.__setitem__(k,v) for k,v in pairs]; return d
def load(p):
 raw=pathlib.Path(p).read_bytes(); obj=json.loads(raw,object_pairs_hook=strict,parse_constant=lambda x:(_ for _ in ()).throw(ValueError(x))); assert raw==json.dumps(obj,sort_keys=True,separators=(",",":"),ensure_ascii=False).encode(); return obj
i=load("evidence/phase-01/command-inventory.json"); a=load("evidence/phase-01/artifact-manifest.json"); m=tomllib.load(open("evidence/phase-01/test-claim-map.toml","rb")); artifacts={x["artifact_id"]:x for x in a["artifacts"]}; commands={x["id"]:x for x in i["commands"]}
for x in commands.values():
 if x.get("raw_artifact_id"):
  row=artifacts[x["raw_artifact_id"]]; ev=load(row["path"]); assert set(ev)=={"schema","argv","cwd","status","exit_status","stdout","stderr"} and ev["schema"]=="phase1-command-evidence-v1" and ev["argv"]==x["argv"] and ev["cwd"]==x["cwd"] and ev["status"]==x["status"] and ev["exit_status"]==x["exit_status"]
for x in m["claim_rows"]:
 if x["status"] in ("covered","partial"):
  command=commands[x["command_id"]]; assert command.get("raw_artifact_id"); row=artifacts[x["artifact_id"]]; ev=load(row["path"]); assert set(ev)=={"schema","claim_id","status","rationale","command_id","command_artifact_id","command_artifact_sha256"} and ev["schema"]=="phase1-claim-evidence-v1" and ev["claim_id"]==x["id"] and ev["status"]==x["status"] and ev["rationale"]==x["rationale"] and ev["command_id"]==command["id"] and ev["command_artifact_id"]==command["raw_artifact_id"] and ev["command_artifact_sha256"]==command["raw_artifact_sha256"]'</automated>
    <automated>python3 -c 'import base64,json,pathlib,tomllib
def strict(pairs):
 d={}; [(_ for _ in ()).throw(ValueError("duplicate key "+k)) if k in d else d.__setitem__(k,v) for k,v in pairs]; return d
load=lambda b:json.loads(b,object_pairs_hook=strict,parse_constant=lambda x:(_ for _ in ()).throw(ValueError(x)))
p=tomllib.load(open("evidence/phase-01/orchestration-profile.toml","rb")); i=json.load(open("evidence/phase-01/command-inventory.json")); commands={x["id"]:x for x in i["commands"]}; profile={x["id"]:x for x in p["reviewers"]}; r=profile
for reviewer_id in ("local_coderabbit","github_codex"):
 x=r[reviewer_id]; assert all(x.get(k)==profile[reviewer_id].get(k) for k in ("status","candidate_sha","artifact_id","artifact_sha256","external_evidence","external_evidence_sha256"))
 if x["status"] not in ("pass","rate_limit","failed"): assert x.get("exit_status") is None; continue
 cap=load(pathlib.Path(x["external_evidence"]).read_bytes()); common={"capture_schema","reviewer_id","candidate_sha","argv","exit_status","stderr_base64"}; assert cap["capture_schema"]=="phase1-reviewer-actual-capture-v1" and cap["reviewer_id"]==reviewer_id and cap["candidate_sha"]==x["candidate_sha"] and cap["argv"]==commands[x["canonical_command_id"]]["argv"] and type(cap["exit_status"]) is int and cap["exit_status"]==x["exit_status"] and ((x["status"]=="pass" and cap["exit_status"]==0) or (x["status"] in ("rate_limit","failed") and cap["exit_status"]!=0)); base64.b64decode(cap["stderr_base64"],validate=True)
 if reviewer_id=="local_coderabbit":
  assert set(cap)==common|{"stdout_base64"}; facts=load(base64.b64decode(cap["stdout_base64"],validate=True)); assert set(facts)=={"status","findings","pre_finding"} and facts["status"]==x["status"] and len(facts["findings"])==x["finding_count"] and bool(facts["findings"])==x["findings_emitted"] and facts["pre_finding"]==x["pre_finding"]
 else:
  assert set(cap)==common|{"responses_base64"} and ((x["status"]=="pass" and len(cap["responses_base64"])==2) or (x["status"] in ("rate_limit","failed") and len(cap["responses_base64"]) in (0,1)));
  if len(cap["responses_base64"])==2: review=load(base64.b64decode(cap["responses_base64"][0],validate=True)); comments=load(base64.b64decode(cap["responses_base64"][1],validate=True)); assert review["commit_id"]==x["candidate_sha"] and review["state"]=="COMMENTED" and isinstance(comments,list) and x["finding_count"]==len(comments) and x["findings_emitted"]==bool(comments) and x["pre_finding"] is False'</automated>
  </verify>
  <acceptance_criteria>
    - One claim is linked end-to-end to current source, canonical argv, pinned runtime identity, raw artifact digest, and review capability.
    - All discovered commands have tracked origins; aliases cannot create duplicate gates.
    - Tool path/version/hash/help and bare-versus-managed collisions are visible.
    - Every M0/current-source claim has an honest coverage status and evidence need.
    - Missing tracked CI workflows remain an observation, not a request to create CI in this phase.
    - EDGE-M0-04-UNCLASSIFIED remains flagged for manual review.
  </acceptance_criteria>
  <done>The exact command/reviewer profile and test-claim map are pinned to one source head and ready for measured maturity conclusions.</done>
</task>

<task type="auto">
  <name>Task 2: Measure CI/review maturity and lock the local-CodeRabbit-to-GitHub-Codex sequence</name>
  <files>evidence/phase-01/ci-baseline.json, evidence/phase-01/review-baseline.json, evidence/phase-01/artifact-manifest.json, docs/delivery/phase-01-review-sequence.md, docs/delivery/phase-01-ci-profile.md</files>
  <execution_contract>Derive tracked workflow set exactly from `git ls-tree -r` at authoritative head below `.github/workflows/`; require exact set equality including empty. Recompute each gate from referenced canonical command evidence envelope; gaps cannot cite passing output. Parse reviewer status, finding count, pre-finding flag, and disposition from immutable content-addressed raw output. GitHub row forbids all local-tool metadata. All normative JSON rejects duplicate keys and NaN/Infinity and uses canonical UTF-8 bytes.</execution_contract>
  <read_first>
    - evidence/phase-01/orchestration-profile.toml — sole pinned tool and execution profile
    - evidence/phase-01/command-inventory.json — canonical current command set
    - evidence/phase-01/test-claim-map.toml — claim coverage and missing-evidence rows
    - evidence/phase-01/artifact-manifest.json — exact raw/evidence artifact ownership registry
    - evidence/phase-01/current-native-baseline.json — reusable measured outcomes
    - docs/plans/uzel-product-incubation-v4-2026-08-10/04-DELIVERY-QUALITY.md — exact review ordering and final-SHA contract
    - docs/plans/uzel-product-incubation-v4-2026-08-10/prompts/02-review-phase-1.md — local CodeRabbit plus GitHub Codex plan review remains a separate pre-execution gate
  </read_first>
  <action>Implement D-07, D-25, D-27, D-29, D-30, and D-31. Derive `ci-baseline.json` from current tracked workflow presence/absence, canonical command rows, Plan-02 measured artifacts, environment parity, duration, flake/retry behavior if observed, coverage, artifact retention, cancellation, restart, and missing gates. Bind its nonempty exact `source_sha` to the command inventory source head. Give every canonical command exactly one nonempty gate row or explicit gap row (`gap_for_command_id`, reason). Every row has a unique nonempty `gate_id`, exactly one of `canonical_command_id` or `gap_for_command_id`, explicit `subject_command_id` equal to that selected canonical command, and exact `evidence_artifact_id`, `evidence_path`, SHA-256, `evidence_owner_kind`, and `evidence_owner_id` resolving field-for-field to one artifact-manifest row with matching retained bytes and that same subject; reject missing fields, duplicate gate IDs, duplicate subjects, unknown subjects, extra command coverage, or any manifest mismatch. Do not rerun an already valid command solely to populate a second name; reference its digest. If a required measurement is absent, run the canonical pinned command once under its recorded timeout and cancellation policy, retain raw output checksums, and classify the result honestly. Complete the atomically updated artifact manifest after these rows exist, then rerun its complete owner/reference/path/byte/checksum validation after the final mutation: it must be an exact bijection, not a best-effort checksum list, across every retained command raw artifact, reviewer evidence artifact, and covered/partial claim artifact; unavailable/not-attempted rows instead carry explicit no-artifact reason. Write `phase-01-ci-profile.md` as one ordered evidence-derived command profile using existing canonical commands and explicit gaps; it must not claim that a proposed profile currently runs in hosted CI. Review stages consume those canonical gates and must not duplicate them. Probe approved tools only enough to record capability, version/path/hash, input/output contract and failure status; do not perform or claim the later Prompt-02 plan review. `review-baseline.json` has exact stage order `["local_coderabbit", "github_codex"]` and exactly those two nonempty stage rows: the former is a local executable row (identity, executable path/version/hash/help fingerprint, command/evidence); the latter is a remote reviewer row (reviewer identity, PR/request locator, response/review locator, evidence locator/SHA-256, disposition) and must not claim a local executable path. The corresponding orchestration-profile and review-baseline stage rows must exactly agree on record kind, scope, acceptance, status, identity, disposition, reason, canonical command, artifact, tool/executable/probe identity, candidate head, request/response, and finding fields. Each stage has `review_scope = "capability_probe_only"`, `acceptance = false`, one status from `pass`, `rate_limit`, `failed`, `unavailable`, or `not_attempted`, and nonempty reviewer identity, finding disposition, and status reason. Any attempted local pass/rate-limit/failed probe repeats executable path, version, executable/version/help hashes, and tool ID exactly from the orchestration profile; unavailable/not-attempted local rows carry no executable link. Candidate/input/effective/reviewed-head SHA must all equal the authoritative source head. Every attempted pass/rate-limit/failed row requires nonempty immutable external-evidence path/digest plus exact artifact-manifest ownership; failed rows additionally retain an explicit reason and remain `acceptance = false`. Unavailable/not-attempted rows record the honest reason, no artifact/evidence link, and `acceptance = false`. A `rate_limit` row is valid only before findings (`findings_emitted = false`, `finding_count = 0`); `github_codex_rate_limit_fallback` is valid only when local CodeRabbit has that pre-finding rate limit and GitHub Codex is `pass`. Command aliases keep their own IDs but reference one canonical command ID; aliases do not create additional canonical commands. This Wave-4 record is strictly `acceptance_scope = "capability_probe_only"` and `final_phase_acceptance = false`: it does not require or claim successful Prompt-02 review. Plans 06/07 own real E/P and final-SHA transition review. In `phase-01-review-sequence.md`, include exactly one canonical compact `ReviewContractJSON: {...}` line whose parsed schema records the exact order, exact-A-before-lanes binding, overlap permission, required two-disposition join, normal and sole fallback combinations, later-commit invalidation, capability-only Wave-4 scope, Plans-06/07 acceptance ownership, and exact prohibited reviewer array; then document D-25's later actual-review protocol: implementation agents produce candidate SHA A; A is pushed as the exact PR SHA before either review stage; local CodeRabbit then attempts the immutable diff ending at A and GitHub Codex reviews exact A. CodeRabbit `pass` plus GitHub Codex `pass` is the normal later path; pre-finding `rate_limit` plus GitHub Codex `pass` is the only fallback; all other outcomes fail closed when that later acceptance is attempted. Any commit invalidates later review acceptance although the Wave-4 capability observation remains evidence. Claude, OpenCode, remote CodeRabbit and local Codex self-review remain prohibited. Record bounded cancellation/restart at each stage and atomically validate all links.</action>
  <verify>
    <automated>python3 -m json.tool evidence/phase-01/ci-baseline.json &gt;/dev/null &amp;&amp; python3 -m json.tool evidence/phase-01/review-baseline.json &gt;/dev/null &amp;&amp; jq -e '(.source_sha | test("^[0-9a-f]{40}$")) and (.tracked_workflows | type == "array") and (.gates | type == "array" and length &gt; 0) and (all(.gates[]; (.status | IN("pass","fail","blocked","unavailable","not_configured")) and (.evidence_artifact_id | type == "string" and length &gt; 0) and (.evidence_path | type == "string" and length &gt; 0) and (.evidence_sha256 | test("^[0-9a-f]{64}$"))))' evidence/phase-01/ci-baseline.json &gt;/dev/null &amp;&amp; jq -e '. as $root | (.acceptance_scope == "capability_probe_only") and (.final_phase_acceptance == false) and (.order == ["implementation_agents","push_exact_pr_sha","local_coderabbit_and_github_codex_may_overlap","join_both_dispositions"]) and (.all_review_evidence_invalidated_on_later_commit == true) and (.fail_closed == true) and (.prohibited_reviewers == ["claude","opencode","remote_coderabbit","local_codex_self_review"]) and ((.stages | length) == 2) and (.local_coderabbit | (.identity | type == "string") and (.finding_disposition | type == "string") and (.status | IN("pass","rate_limit","failed","unavailable","not_attempted")) and ((.executable_path | type == "string") or (.status | IN("unavailable","not_attempted"))) and (((.status == "pass" or .status == "rate_limit" or .status == "failed") | not) or ((.candidate_sha | test("^[0-9a-f]{40}$")) and (.input_sha == .candidate_sha) and (.effective_sha == .candidate_sha) and (.external_evidence | type == "string") and (.external_evidence_sha256 | test("^[0-9a-f]{64}$")))) and ((.status != "rate_limit") or ((.findings_emitted == false) and (.finding_count == 0))) and (((.status | IN("failed","unavailable","not_attempted")) | not) or ((.reason | type == "string") and (.acceptance == false)))) and ($root.github_codex | (.reviewer | type == "string") and (.finding_disposition | type == "string") and (.status | IN("pass","rate_limit","failed","unavailable","not_attempted")) and (.executable_path? == null) and (((.status == "pass" or .status == "rate_limit" or .status == "failed") | not) or ((.candidate_sha | test("^[0-9a-f]{40}$")) and (.input_sha == .candidate_sha) and (.effective_sha == .candidate_sha) and (.request | type == "string") and (.response | type == "string") and (.external_evidence | type == "string") and (.external_evidence_sha256 | test("^[0-9a-f]{64}$")))) and ((.status != "rate_limit") or ((.findings_emitted == false) and (.finding_count == 0))) and (((.status | IN("failed","unavailable","not_attempted")) | not) or ((.reason | type == "string") and (.acceptance == false)))) and (($root.mode != "github_codex_rate_limit_fallback") or (($root.local_coderabbit.status == "rate_limit") and ($root.local_coderabbit.findings_emitted == false) and ($root.local_coderabbit.finding_count == 0) and ($root.github_codex.status == "pass")))' evidence/phase-01/review-baseline.json &gt;/dev/null &amp;&amp; python3 -c 'import json,pathlib,re,tomllib; r=json.load(open("evidence/phase-01/review-baseline.json")); i=json.load(open("evidence/phase-01/command-inventory.json")); ci=json.load(open("evidence/phase-01/ci-baseline.json")); a=json.load(open("evidence/phase-01/artifact-manifest.json")); p=tomllib.load(open("evidence/phase-01/orchestration-profile.toml","rb")); h=i["source_head"]; canonical={x["id"] for x in i["commands"] if x.get("canonical_id")==x["id"]}; artifacts={x["artifact_id"]:x for x in a["artifacts"]}; gates=ci["gates"]; assert ci.get("source_sha")==h and canonical and gates; represented=[]; assert all(((isinstance(x.get("canonical_command_id"),str) and x["canonical_command_id"] in canonical and x.get("gap_for_command_id") is None and represented.append(x["canonical_command_id"]) is None) or (x.get("canonical_command_id") is None and isinstance(x.get("gap_for_command_id"),str) and x["gap_for_command_id"] in canonical and isinstance(x.get("reason"),str) and x["reason"] and represented.append(x["gap_for_command_id"]) is None)) for x in gates); assert len(represented)==len(set(represented)) and set(represented)==canonical and all(x.get("evidence_artifact_id") in artifacts and (z:=artifacts[x["evidence_artifact_id"]]) and z.get("path")==x.get("evidence_path") and z.get("sha256")==x.get("evidence_sha256") and z.get("owner_kind")==x.get("evidence_owner_kind") and z.get("owner_id")==x.get("evidence_owner_id") and z.get("subject_command_id")==(x.get("canonical_command_id") if x.get("canonical_command_id") is not None else x.get("gap_for_command_id")) for x in gates); assert r.get("stages")==["local_coderabbit","github_codex"]; rows={k:r[k] for k in r["stages"]}; profile={x["id"]:x for x in p["reviewers"]}; tools={x["tool_id"]:x for x in p["tools"]}; consistent=("record_kind","review_scope","acceptance","status","identity","reviewer","finding_disposition","reason","canonical_command_id","artifact_id","artifact_sha256","tool_id","executable_path","version_output","version_output_sha256","executable_sha256","help_fingerprint_sha256","candidate_sha","input_sha","effective_sha","reviewed_head_sha","request","response","external_evidence","external_evidence_sha256","findings_emitted","finding_count","exit_status"); assert set(rows)==set(profile)=={"local_coderabbit","github_codex"} and all(all(x.get(f)==profile[k].get(f) for f in consistent) and x.get("review_scope")=="capability_probe_only" and x.get("acceptance") is False and x.get("status") in ("pass","rate_limit","failed","unavailable","not_attempted") and isinstance(x.get("finding_disposition"),str) and x["finding_disposition"] and isinstance(x.get("reason"),str) and x["reason"] and x.get("artifact_id")==profile[k].get("artifact_id") and x.get("artifact_sha256")==profile[k].get("artifact_sha256") for k,x in rows.items()); l=rows["local_coderabbit"]; g=rows["github_codex"]; assert isinstance(l.get("identity"),str) and l["identity"] and isinstance(g.get("reviewer"),str) and g["reviewer"]; attempted=l["status"] in ("pass","rate_limit","failed"); assert ((attempted and l.get("tool_id") in tools and (z:=tools[l["tool_id"]]) and l.get("executable_path")==z.get("absolute_path") and pathlib.Path(l["executable_path"]).is_absolute() and l.get("version_output")==z.get("version_output") and l.get("version_output_sha256")==z.get("version_output_sha256") and l.get("executable_sha256")==z.get("executable_sha256") and l.get("help_fingerprint_sha256")==z.get("help_fingerprint_sha256") and all(re.fullmatch(r"[0-9a-f]{64}",l[k]) for k in ("version_output_sha256","executable_sha256","help_fingerprint_sha256"))) or ((not attempted) and l.get("tool_id") is None and l.get("executable_path") is None)); assert g.get("tool_id") is None and g.get("executable_path") is None; assert all(x["status"] not in ("pass","rate_limit","failed") or (all(re.fullmatch(r"[0-9a-f]{40}",x.get(k,"")) for k in ("candidate_sha","input_sha","effective_sha","reviewed_head_sha")) and x["candidate_sha"]==x["input_sha"]==x["effective_sha"]==x["reviewed_head_sha"]==h and isinstance(x.get("external_evidence"),str) and x["external_evidence"] and re.fullmatch(r"[0-9a-f]{64}",x.get("external_evidence_sha256","")) and x["external_evidence_sha256"]==x.get("artifact_sha256")) for x in rows.values()); assert g["status"] not in ("pass","rate_limit") or (isinstance(g.get("request"),str) and g["request"] and isinstance(g.get("response"),str) and g["response"]); assert l["status"]!="rate_limit" or (l.get("findings_emitted") is False and l.get("finding_count")==0)' &amp;&amp; python3 -c 'import hashlib,json,pathlib,tomllib; p=tomllib.load(open("evidence/phase-01/orchestration-profile.toml","rb")); m=tomllib.load(open("evidence/phase-01/test-claim-map.toml","rb")); i=json.load(open("evidence/phase-01/command-inventory.json")); a=json.load(open("evidence/phase-01/artifact-manifest.json")); ci=json.load(open("evidence/phase-01/ci-baseline.json")); commands=i["commands"]; reviewers=p["reviewers"]; claims=m["claim_rows"]; artifacts=a["artifacts"]; refs=[("command",x["id"],x["raw_artifact_id"],x["raw_artifact_sha256"]) for x in commands if x.get("raw_artifact_id")]+[("reviewer",x["id"],x["artifact_id"],x["artifact_sha256"]) for x in reviewers if x.get("artifact_id")]+[("claim",x["id"],x["artifact_id"],x["artifact_sha256"]) for x in claims if x["status"] in ("covered","partial")]+[("tool_version",x["tool_id"],x["version_artifact_id"],x["version_output_sha256"]) for x in p["tools"]]+[("tool_help",x["tool_id"],x["help_artifact_id"],x["help_fingerprint_sha256"]) for x in p["tools"]]; by={x["artifact_id"]:x for x in artifacts}; assert len(by)==len(artifacts)==len(refs) and len({x["path"] for x in artifacts})==len(artifacts) and all(aid in by and by[aid].get("owner_kind")==k and by[aid].get("owner_id")==o and by[aid].get("sha256")==h for k,o,aid,h in refs); assert all((aid:=x.get("evidence_artifact_id")) in by and (z:=by[aid]).get("path")==x.get("evidence_path") and z.get("sha256")==x.get("evidence_sha256") and z.get("owner_kind")==x.get("evidence_owner_kind") and z.get("owner_id")==x.get("evidence_owner_id") and z.get("subject_command_id")==(x.get("canonical_command_id") if x.get("canonical_command_id") is not None else x.get("gap_for_command_id")) for x in ci["gates"]); paths=[pathlib.Path(x["path"]) for x in artifacts]; assert all(not q.is_absolute() and ".." not in q.parts and all(not pathlib.Path(*q.parts[:n]).is_symlink() for n in range(1,len(q.parts)+1)) and q.is_file() and hashlib.sha256(q.read_bytes()).hexdigest()==x["sha256"] for x,q in zip(artifacts,paths)); lines=pathlib.Path("evidence/phase-01/command-artifacts.sha256").read_text().splitlines(); assert lines==sorted(x["sha256"]+"  "+x["path"] for x in artifacts)' &amp;&amp; python3 -c 'import hashlib,json,pathlib,re; ci=json.load(open("evidence/phase-01/ci-baseline.json")); a=json.load(open("evidence/phase-01/artifact-manifest.json")); i=json.load(open("evidence/phase-01/command-inventory.json")); canonical={x["id"] for x in i["commands"] if x.get("canonical_id")==x["id"]}; gates=ci["gates"]; by={x["artifact_id"]:x for x in a["artifacts"]}; required=("gate_id","subject_command_id","evidence_artifact_id","evidence_path","evidence_sha256","evidence_owner_kind","evidence_owner_id"); assert gates and all(all(isinstance(x.get(k),str) and x[k] for k in required) for x in gates) and len({x["gate_id"] for x in gates})==len(gates); ci_refs=[(x["gate_id"],x["subject_command_id"],x["evidence_artifact_id"],x["evidence_path"],x["evidence_sha256"],x["evidence_owner_kind"],x["evidence_owner_id"]) for x in gates]; assert len(ci_refs)==len(gates) and len({x[1] for x in ci_refs})==len(ci_refs) and {x[1] for x in ci_refs}==canonical; assert all(((x.get("canonical_command_id") in canonical and x.get("gap_for_command_id") is None and x["subject_command_id"]==x["canonical_command_id"]) or (x.get("canonical_command_id") is None and x.get("gap_for_command_id") in canonical and isinstance(x.get("reason"),str) and x["reason"] and x["subject_command_id"]==x["gap_for_command_id"])) for x in gates); assert all((z:=by.get(aid)) is not None and z.get("path")==path and z.get("sha256")==digest and z.get("owner_kind")==ok and z.get("owner_id")==oid and z.get("subject_command_id")==subject and (q:=pathlib.Path(path)) and not q.is_absolute() and ".." not in q.parts and all(not pathlib.Path(*q.parts[:n]).is_symlink() for n in range(1,len(q.parts)+1)) and q.is_file() and hashlib.sha256(q.read_bytes()).hexdigest()==digest for gate,subject,aid,path,digest,ok,oid in ci_refs)' &amp;&amp; sha256sum -c evidence/phase-01/command-artifacts.sha256 &amp;&amp; jq -e '[.commands[] | select(.id == .canonical_id) | .id] as $canonical | [.commands[].id] as $ids | (($ids | length) == ($ids | unique | length)) and ([.commands[].canonical_id | IN($canonical[])] | all)' evidence/phase-01/command-inventory.json &gt;/dev/null &amp;&amp; python3 -c 'import json,pathlib; prefix="ReviewContractJSON: "; rows=[x for x in pathlib.Path("docs/delivery/phase-01-review-sequence.md").read_text().splitlines() if x.startswith(prefix)]; assert len(rows)==1; raw=rows[0][len(prefix):]; obj=json.loads(raw); expected={"schema":"phase1-review-sequence-v1","order":["implementation_agents","push_exact_pr_sha","local_coderabbit_and_github_codex_may_overlap","join_both_dispositions"],"candidate_binding":"exact_pushed_sha_A_before_lanes","lanes_may_overlap":True,"join_required":"both_dispositions","normal":["coderabbit_pass","github_codex_pass"],"fallback":["coderabbit_pre_finding_rate_limit_zero_findings","github_codex_pass"],"invalidated_on_later_commit":True,"wave4_scope":"capability_probe_only","later_acceptance_owner":"plans_06_07","prohibited_reviewers":["claude","opencode","remote_coderabbit","local_codex_self_review"]}; assert obj==expected and raw==json.dumps(obj,sort_keys=True,separators=(",",":"))'</automated>
    <automated>python3 -c 'import json,pathlib,subprocess
def strict(pairs):
 d={}; [(_ for _ in ()).throw(ValueError("duplicate key "+k)) if k in d else d.__setitem__(k,v) for k,v in pairs]; return d
def load(p):
 raw=pathlib.Path(p).read_bytes(); obj=json.loads(raw,object_pairs_hook=strict,parse_constant=lambda x:(_ for _ in ()).throw(ValueError(x))); assert raw==json.dumps(obj,sort_keys=True,separators=(",",":"),ensure_ascii=False).encode(); return obj
ci=load("evidence/phase-01/ci-baseline.json"); r=load("evidence/phase-01/review-baseline.json"); load("evidence/phase-01/artifact-manifest.json"); inventory=load("evidence/phase-01/command-inventory.json"); commands={x["id"]:x for x in inventory["commands"] if x["id"]==x["canonical_id"]}; expected=sorted(x for x in subprocess.check_output(["git","ls-tree","-r","--name-only",ci["source_sha"],"--",".github/workflows"],text=True).splitlines() if x.startswith(".github/workflows/")); assert ci["tracked_workflows"]==expected
for g in ci["gates"]:
 ev=load(g["evidence_path"]); assert ev["schema"]=="phase1-command-evidence-v1"
 if g.get("canonical_command_id") is not None: command=commands[g["canonical_command_id"]]; assert g["subject_command_id"]==command["id"] and ev["argv"]==g["argv"]==command["argv"] and ev["cwd"]==g["cwd"]==command["cwd"] and ev["status"]==command["status"] and ev["exit_status"]==g["exit_status"]==command["exit_status"] and g["status"]==("pass" if ev["exit_status"]==0 else "fail")
 else: assert g["status"] in ("blocked","unavailable","not_configured") and ev["exit_status"] is None and ev["status"]==g["status"] and ev["reason"]==g["reason"]
for k in ("local_coderabbit","github_codex"):
 x=r[k]
 if x.get("artifact_id"):
  ev=load(x["external_evidence"]); assert ev["capture_schema"]=="phase1-reviewer-actual-capture-v1" and ev["reviewer_id"]==k
g=r["github_codex"]; assert g["reviewer"]=="GitHub Codex" and g["tool_identity"]=="github-codex-review" and not any(k in g for k in ("tool_id","executable_path","absolute_path","version_output","version_output_sha256","executable_sha256","help_fingerprint_sha256"))'</automated>
    <automated>python3 -c 'import hashlib,json,pathlib,re
def strict(pairs):
 d={}; [(_ for _ in ()).throw(ValueError("duplicate key "+k)) if k in d else d.__setitem__(k,v) for k,v in pairs]; return d
def load(path):
 raw=pathlib.Path(path).read_bytes(); obj=json.loads(raw,object_pairs_hook=strict,parse_constant=lambda x:(_ for _ in ()).throw(ValueError(x))); assert raw==json.dumps(obj,sort_keys=True,separators=(",",":"),ensure_ascii=False).encode(); return obj
r=load("evidence/phase-01/review-baseline.json"); manifest=load("evidence/phase-01/artifact-manifest.json"); by={x["artifact_id"]:x for x in manifest["artifacts"]}; assert len(by)==len(manifest["artifacts"])
for reviewer_id in ("local_coderabbit","github_codex"):
 x=r[reviewer_id]; attempted=x["status"] in ("pass","rate_limit","failed")
 if attempted:
  assert isinstance(x.get("artifact_id"),str) and x["artifact_id"] and isinstance(x.get("external_evidence"),str) and x["external_evidence"] and re.fullmatch(r"[0-9a-f]{64}",x.get("external_evidence_sha256","")) and x["external_evidence_sha256"]==x.get("artifact_sha256"); row=by[x["artifact_id"]]; assert row.get("owner_kind")=="reviewer" and row.get("owner_id")==reviewer_id and row.get("path")==x["external_evidence"] and row.get("sha256")==x["external_evidence_sha256"]; raw=pathlib.Path(x["external_evidence"]).read_bytes(); assert hashlib.sha256(raw).hexdigest()==x["external_evidence_sha256"]; ev=load(x["external_evidence"]); assert ev["capture_schema"]=="phase1-reviewer-actual-capture-v1" and ev["reviewer_id"]==reviewer_id
 else:
  assert x["status"] in ("unavailable","not_attempted") and x.get("artifact_id") is None and x.get("artifact_sha256") is None and x.get("external_evidence") is None and x.get("external_evidence_sha256") is None and isinstance(x.get("reason"),str) and x["reason"]'</automated>
    <automated>python3 -c 'import base64,hashlib,json,pathlib,re
def strict(pairs):
 d={}; [(_ for _ in ()).throw(ValueError("duplicate key "+k)) if k in d else d.__setitem__(k,v) for k,v in pairs]; return d
def parse(raw): return json.loads(raw,object_pairs_hook=strict,parse_constant=lambda x:(_ for _ in ()).throw(ValueError(x)))
def load(path):
 raw=pathlib.Path(path).read_bytes(); obj=parse(raw); assert raw==json.dumps(obj,sort_keys=True,separators=(",",":"),ensure_ascii=False).encode(); return obj
r=load("evidence/phase-01/review-baseline.json"); inventory=load("evidence/phase-01/command-inventory.json"); manifest=load("evidence/phase-01/artifact-manifest.json"); commands={x["id"]:x for x in inventory["commands"]}; artifacts={x["artifact_id"]:x for x in manifest["artifacts"]}; assert len(artifacts)==len(manifest["artifacts"])
for reviewer_id in ("local_coderabbit","github_codex"):
 x=r[reviewer_id]; attempted=x["status"] in ("pass","rate_limit","failed")
 if not attempted:
  assert x["status"] in ("unavailable","not_attempted") and x.get("exit_status") is None; continue
 assert x.get("canonical_command_id") in commands and isinstance(x.get("artifact_id"),str) and x["artifact_id"] and isinstance(x.get("external_evidence"),str) and x["external_evidence"] and re.fullmatch(r"[0-9a-f]{64}",x.get("external_evidence_sha256","") or "") and x["external_evidence_sha256"]==x.get("artifact_sha256"); row=artifacts[x["artifact_id"]]; assert row.get("owner_kind")=="reviewer" and row.get("owner_id")==reviewer_id and row.get("path")==x["external_evidence"] and row.get("sha256")==x["external_evidence_sha256"]; raw=pathlib.Path(row["path"]).read_bytes(); assert hashlib.sha256(raw).hexdigest()==row["sha256"]; cap=parse(raw); common={"capture_schema","reviewer_id","candidate_sha","argv","exit_status","stderr_base64"}; assert cap["capture_schema"]=="phase1-reviewer-actual-capture-v1" and cap["reviewer_id"]==reviewer_id and cap["candidate_sha"]==x["candidate_sha"] and cap["argv"]==commands[x["canonical_command_id"]]["argv"] and type(cap["exit_status"]) is int and cap["exit_status"]==x["exit_status"] and ((x["status"]=="pass" and cap["exit_status"]==0) or (x["status"] in ("rate_limit","failed") and cap["exit_status"]!=0)); base64.b64decode(cap["stderr_base64"],validate=True)
 if reviewer_id=="local_coderabbit":
  assert set(cap)==common|{"stdout_base64"}; facts=parse(base64.b64decode(cap["stdout_base64"],validate=True)); assert set(facts)=={"status","findings","pre_finding"} and facts["status"]==x["status"] and isinstance(facts["findings"],list) and len(facts["findings"])==x["finding_count"] and bool(facts["findings"])==x["findings_emitted"] and facts["pre_finding"]==x["pre_finding"]
 else:
  assert set(cap)==common|{"responses_base64"} and isinstance(cap["responses_base64"],list) and all(isinstance(y,str) for y in cap["responses_base64"]) and ((x["status"]=="pass" and len(cap["responses_base64"])==2) or (x["status"] in ("rate_limit","failed") and len(cap["responses_base64"]) in (0,1)))
  decoded=[parse(base64.b64decode(y,validate=True)) for y in cap["responses_base64"]]
  if len(decoded)==2: review,comments=decoded; assert isinstance(review,dict) and review.get("commit_id")==x["candidate_sha"] and review.get("state")=="COMMENTED" and isinstance(comments,list) and x["finding_count"]==len(comments) and x["findings_emitted"]==bool(comments) and x["pre_finding"] is False'</automated>
  </verify>
  <acceptance_criteria>
    - CI baseline distinguishes current tracked automation, local measured commands, and proposed later profile.
    - Each gate is canonical, non-duplicated, timed, artifact-linked, and honest about coverage/flakiness/unavailability.
    - Capability stages are explicit and nonempty; pass/rate-limit, failed, unavailable, and not-attempted outcomes are honestly distinguished without pretending Wave 4 completed Prompt-02 review.
    - Only actual review evidence binds candidate/input/effective SHA and external evidence; non-pass capability states record a reason and cannot confer acceptance.
    - Any later commit invalidates actual review acceptance for that candidate; capability history remains non-acceptance evidence and Plans 06/07 own transition review.
    - Claude, OpenCode, remote CodeRabbit and local Codex self-review are explicitly prohibited.
    - PROHIB-M0-04-REVIEW-INDEPENDENCE remains flagged until GitHub Codex verifies the exact pushed SHA.
  </acceptance_criteria>
  <done>M0-04 has a measured delivery baseline, one pinned non-duplicative command profile, and the complete fail-closed local-CodeRabbit/GitHub-Codex exact-SHA review contract.</done>
</task>

</tasks>

## Artifacts Produced

- `evidence/phase-01/orchestration-profile.toml` — pinned runtimes, reviewers, cancellation, and restart rules.
- `evidence/phase-01/command-inventory.json` — canonical declared/discovered commands.
- `evidence/phase-01/test-claim-map.toml` — claim-to-test/artifact coverage.
- `evidence/phase-01/command-artifacts.sha256` — raw command/reviewer probe checksums.
- `evidence/phase-01/artifact-manifest.json` — exact non-orphaned ownership and digest bijection for command, reviewer, and claim artifacts.
- `evidence/phase-01/ci-baseline.json` — measured current CI/test maturity.
- `evidence/phase-01/review-baseline.json` — measured reviewer capability and exact-SHA order.
- `docs/delivery/phase-01-review-sequence.md` — mandatory implementation/local-CodeRabbit/push-exact-SHA/GitHub-Codex sequence with whole-chain invalidation.
- `docs/delivery/phase-01-ci-profile.md` — non-duplicative evidence-derived CI profile.

<threat_model>
Security validation scope: OWASP ASVS Level 1, limited to command execution, evidence handling, and reviewer identity/SHA boundaries.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Manifests/scripts to command inventory | Untrusted or stale command names can create false gates unless tied to tracked origins and canonical argv. |
| Runtime/reviewer executable to evidence | PATH collisions, version drift, or opaque reviewer output can misattribute a result. |
| Implementation SHA to approved review stages | Reviews become stale after any byte change and reviewer identity can be falsely claimed. |
| Raw logs/review output to tracked baseline | Host paths, source excerpts, or sensitive material may cross into durable evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-01 | Spoofing | Codex/CodeRabbit/runtime identity | high | mitigate | Pin absolute path, version, executable hash, help fingerprint, invocation contract, and primary-versus-independent role. |
| T-04-02 | Tampering | staged review and final-SHA validity | high | mitigate | Bind each stage to input/effective SHA and evidence digest; any later commit invalidates all downstream evidence and reruns from the earliest invalid stage. |
| T-04-03 | Repudiation | CI/test coverage claims | medium | mitigate | Canonical command IDs, exact argv/status/duration/artifact digests, claim map, and explicit absent/unavailable rows. |
| T-04-04 | Denial of Service | hung command/reviewer | medium | mitigate | Bounded timeout, owned-child termination, partial artifact checksums, cancellation state, hash-bound restart. |
| T-04-05 | Information Disclosure | logs/review packets | medium | mitigate | Synthetic inputs, bounded excerpts, redaction, private raw retention, public checksums. |
| T-04-SC | Tampering | existing command/runtime closure | high | mitigate | No installs; reuse verified locks/runtime hashes from Plan 02 and reject command execution on drift. |
</threat_model>

<verification>
Run both automated gates, reconcile every test-claim row with one canonical command/artifact or explicit gap, and verify review sequence/state transitions against `04-DELIVERY-QUALITY.md`. Do not count the plan-set's later Prompt-02 review as performed by this baseline task.
</verification>

<success_criteria>
- M0-04 current maturity is measured rather than inferred from manifests or tool presence.
- One canonical command profile exists; aliases and proposed hosted CI do not duplicate gates.
- After the immutable exact PR SHA is pushed, local CodeRabbit and GitHub Codex may run serially or concurrently against that same SHA; acceptance joins both dispositions, only a recorded pre-finding local-CodeRabbit `rate_limit` permits green GitHub Codex fallback, and any later commit invalidates review evidence.
- All failures, missing workflows, unavailable reviewers, cancellations, and stale-SHA states remain visible.
</success_criteria>

<output>
Create `.planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-04-SUMMARY.md` when done.
</output>
