---
phase: 01-slice-ref-01-poc-replay-accepted-napp-seam
plan: 04
status: superseded
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ref-candidate-check.py
  - evidence/phase-01/candidate-qualification.md
  - evidence/phase-01/napp-dependency.md
autonomous: true
requirements:
  - REF-07
must_haves:
  truths:
    - "REF-07/D-01: Qualification inspects one exact reachable committed Napp tree and accepts no dirty sibling work, documentation-only promise, inferred symbol, or undeclared probe."
    - "D-02/D-11: Missing client, product events, testkit vectors, version mismatch behavior, lifecycle/recovery vectors, declared probes, or pin evidence produces a repository-qualified stop record and no Uzel source change."
    - "D-03/D-14: Any future candidate must leave runtime authority in Napp and Nostr query/relay/store/freshness/provenance/signer/publication truth in NMP."
    - "D-12/D-17: Reusable upstream work belongs to a dedicated `jodobear/napp` branch/issue and contribution ledger; this Uzel plan records only consumer evidence and handoff."
    - "D-15/D-16: REF-01D remains outside current tasks until the root authority set is committed and a candidate qualifies; then research/planning reruns for one narrow Rust/Tauri adapter and the same parity matrix."
    - "Review #1/#28/#30: Candidate material is read from pinned Git blobs, every required category/reachability/tree/probe outcome is schema-validated, and candidate commands must match one finite literal-argv grammar under controlled Git config/environment or be recorded skipped and block qualification."
    - "Review #26/#27/#29: Handoff fields equal qualification fields structurally, bind the committed Plan-01 parity contract digest, and prove HEAD/read-only index serialization/refs/status/filesystem/object-store invariants in both repositories."
    - "Artifact-path checker: The byte-preserving NUL-delimited committed-tree inventory has one exact filename, `.artifacts/phase-01/napp/&lt;commit&gt;/tree.bin`, across creation, qualification schema, digest validation, handoff equality, and review disposition."
    - "D-18: Execute-plan stops after creating one exact candidate commit and a machine-checkable pending review handoff; no push, PR, merge, remote-review claim, or publication occurs without separate authorization."
  artifacts:
    - path: scripts/ref-candidate-check.py
      provides: "Fixed-schema committed-candidate, safe-probe, cross-document equality, parity-binding, and mutation validator"
    - path: evidence/phase-01/candidate-qualification.md
      provides: "Exact committed-tree qualification matrix and current stop/qualified-for-research result"
    - path: evidence/phase-01/napp-dependency.md
      provides: "Repository-qualified missing-contract handoff and explicit adapter resume prerequisites"
    - path: ".artifacts/phase-01/napp/<commit>/tree.bin"
      provides: "Mode-0600 byte-preserving NUL-delimited committed-tree inventory bound by path and SHA-256"
  key_links:
    - from: evidence/phase-01/candidate-qualification.md
      to: /workspace/projects/napplets/napp-uzel/napp
      via: "Controlled literal-argv `git rev-parse`, `git ls-tree`, committed-blob `git show`, and safe `git cat-file` existence/type/size forms against one full SHA"
      pattern: "observed_commit"
    - from: evidence/phase-01/napp-dependency.md
      to: evidence/phase-01/candidate-qualification.md
      via: "Handoff copies exact observed facts and missing evidence categories, not proposed API names"
      pattern: "resume_when"
    - from: evidence/phase-01/candidate-qualification.md
      to: ".artifacts/phase-01/napp/<commit>/tree.bin"
      via: "Qualification records the fixed binary inventory path and SHA-256; validator recomputes both from canonical NUL-delimited ls-tree bytes"
      pattern: "tree_inventory_path"
    - from: evidence/phase-01/napp-dependency.md
      to: .planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-CONTEXT.md
      via: "D-16 requires a committed root authority set independently of candidate qualification"
      pattern: "D-16"
  assumptions:
    - "[FLAGGED-UNVERIFIED] REF-07 edge classification is unresolved; this plan proves fail-closed qualification/dependency disposition only, not adapter parity."
    - "[FLAGGED-UNVERIFIED] A future Napp source may expose a client/events/testkit shape suitable for Uzel, but no path, symbol, event name, version type, or test vector may be assumed now."
  prohibitions:
    - "[FLAGGED-UNVERIFIED] No private Napp substitute or expanded legacy protocol."
    - "[FLAGGED-UNVERIFIED] No duplicated NMP, runtime, Nostr, or store authority."
    - "[FLAGGED-UNVERIFIED] No evidence-class conflation or fabricated metrics."
    - "[FLAGGED-UNVERIFIED] No pull-in of Nix packaging, CI architecture, Social rebuild, multi-instance implementation, or parked capabilities."
---

## Phase Goal

**As a** Uzel maintainer/reviewer, **I want to** replay current POC evidence and apply a fail-closed Napp admission gate, **so that** visible value survives without duplicating authority.

<objective>
Deliver bounded dependency slice `jodobear/uzel — Napp qualification/handoff for REF-01D` as one evidence-only PR, paired with the repository-qualified upstream dependency `jodobear/napp`.

Purpose: Turn current Napp absence into an executable, exact, fail-closed admission record without inventing APIs or changing Uzel, per D-01, D-02, D-03, D-11, D-12, D-15, D-17, and D-18.
Output: Candidate qualification and dependency handoff documents. No Napp/Uzel production source, private protocol, dependency, lock, fixture, branch, PR, or remote state change.
</objective>

<execution_context>
@/home/at/.codex/gsd-core/workflows/execute-plan.md
@/home/at/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-CONTEXT.md
@.planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-REAUDIT.md
@.planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-RESEARCH.md
@.planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-PATTERNS.md
</context>

## Artifacts this phase produces

- `evidence/phase-01/candidate-qualification.md`; no source symbols, only exact committed-tree facts and result.
- `evidence/phase-01/napp-dependency.md`; no source symbols, only repository-qualified ownership, missing evidence, resume prerequisites, and next command.
- `scripts/ref-candidate-check.py`; Python standard-library validator for embedded canonical JSON records and both-repository mutation snapshots.
- `.artifacts/phase-01/napp/<commit>/tree.bin`; ignored mode-`0600` byte-preserving NUL-delimited committed-tree inventory referenced by exact path and SHA-256 from qualification and handoff evidence.
- No `COVERAGE.md`: this plan reads a local committed Git tree and declares non-integration; it does not integrate or call an external API.

<tasks>

<task type="auto">
  <name>Task 1: Qualify the exact committed Napp candidate or stop</name>
  <precondition>Candidate `0b75b6b4a9ba83598ef8be5ff95dbd40faaf128e` resolves as a commit in the Napp repository through the controlled fixed-environment, literal-argv `cat-file -e` form defined below; otherwise halt before evidence changes.</precondition>
  <reversibility rating="costly">D-01 candidate selection is costly: changing an admitted commit later requires repinning and replaying trust, lifecycle, fixture, package, and platform evidence.</reversibility>
  <read_first>
    .planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-REAUDIT.md
    .planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-CONTEXT.md
  </read_first>
  <files>scripts/ref-candidate-check.py, evidence/phase-01/candidate-qualification.md</files>
  <action>Inspect only the committed object graph of local repository `/workspace/projects/napplets/napp-uzel/napp` at `0b75b6b4a9ba83598ef8be5ff95dbd40faaf128e`. Resolve the absolute Git executable and canonical repository roots once; neither may come from candidate data. Before inspection, capture a non-mutating snapshot for both Uzel and Napp: exact HEAD bytes/object; raw index-file SHA-256 and `lstat` metadata; exact stdout bytes/SHA-256 from fixed `git ls-files -s -z` without decoding, shell variables, line splitting, or newline normalization; exact sorted-ref bytes/SHA-256 from fixed `for-each-ref --sort=refname`; exact NUL-delimited porcelain-v2 bytes/SHA-256; resolved Git/common/object directories; `HEAD`, `packed-refs`, `refs`, `logs`, `worktrees`, and index filesystem metadata; and a recursive object-store manifest of raw relative-path bytes, entry type/mode/size/mtime-nanoseconds, symlink target, and SHA-256 for regular files. Exclude access time so the snapshot itself cannot self-invalidate. Hash subprocess stdout as bytes directly in Python. No snapshot command may create an object, lock, ref, index entry, worktree, maintenance record, or config change. Resolve canonical owner/repository from the configured origin, candidate full SHA/tree, containment in explicitly recorded approved refs, and canonical `git ls-tree -rz --full-tree` bytes/SHA-256 into mode-`0600` `.artifacts/phase-01/napp/<commit>/tree.bin`. Read `AGENTS.md`, `README.md`, `.planning/PROJECT.md`, `.planning/ROADMAP.md`, and `.planning/REQUIREMENTS.md` only as pinned committed blobs; record blob OID/content hash or `missing-in-committed-tree`. Never read candidate facts from sibling working-tree paths.

Implement one Git runner using literal argv lists, `subprocess` with `shell=False`, a fixed canonical `cwd`, stdin closed unless the safe batch form below supplies validator-built object IDs, and captured byte stdout/stderr. Start from a fresh environment allowlist: `LC_ALL=C`, `LANG=C`, an empty mode-`0700` HOME/XDG config root, `GIT_CONFIG_NOSYSTEM=1`, `GIT_CONFIG_GLOBAL=/dev/null`, `GIT_OPTIONAL_LOCKS=0`, `GIT_NO_REPLACE_OBJECTS=1`, `GIT_NO_LAZY_FETCH=1`, `GIT_TERMINAL_PROMPT=0`, `GIT_PROTOCOL_FROM_USER=0`, `GIT_PAGER=cat`, `PAGER=cat`, and `GIT_ATTR_NOSYSTEM=1`. Omit every caller-supplied Git/config/credential/SSH/proxy/path/index/object/alternate-object environment variable. The trusted runner—not candidate data—prepends absolute Git plus `--no-pager`, `--no-replace-objects`, and fixed config disabling hooks, fsmonitor, auto-maintenance/GC, external diff, pager, attributes, LFS clean/smudge/process filters, credentials, and all protocols. Candidate argv may contain neither that trusted prefix nor any config/environment override.

Define the candidate-probe grammar as an exhaustive tagged union, not a subcommand-name allowlist. Let `C` equal the one expected full candidate SHA, `O` equal a full object ID independently emitted by the tree inventory, and `P` equal a strict non-absolute committed-tree path with no empty/dot/dot-dot component, control byte, leading dash, pathspec magic, glob, revision syntax, or symlink/working-tree resolution. Admit only these exact arrays: `['git','rev-parse','--verify','--end-of-options',C+'^{commit}']`; `['git','rev-parse','--verify','--end-of-options',C+'^{tree}']`; `['git','ls-tree','-rz','--full-tree',C]`; `['git','ls-tree','-rz','--full-tree',C,'--',P...]`; `['git','show','--no-ext-diff','--no-textconv','--no-renames','--format=',C+':'+P]`; `['git','cat-file','-e',O]` or the same exact existence form for `C+'^{commit}'`; `['git','cat-file','-t',O]`; `['git','cat-file','-s',O]`; and `['git','cat-file','--batch-check=%(objectname) %(objecttype) %(objectsize)']` with stdin constructed solely as newline-terminated validated `O` values and no candidate-supplied stdin. The batch form returns existence/type/size only. No other option order, optional flag, revision, stdin, or argument shape is admissible. Full SHAs/object IDs and strict `P` values remove ambiguous revisions; the wrapper disables replacement refs, mailmap, pagers, hooks, filters, external diff, and network before any admitted argv executes.

Reject every unlisted global option, subcommand, option, argument, environment key, or stdin before process creation. Explicitly reject `-c`, `--config-env`, `-C`, `--git-dir`, `--work-tree`, aliases, declaration-supplied env/cwd, `--textconv`, `--filters`, `--use-mailmap`, `--ext-diff`, external diff/filter/process execution, replacement refs, hooks, pagers, URLs/protocol/network access, and shell operators/metacharacters/redirection/substitution/newlines. Explicitly forbid Git-writing or state-mutating commands and forms including `write-tree`, `hash-object -w`, `update-index`, `commit-tree`, `update-ref`, `symbolic-ref`, `checkout`, `switch`, `reset`, `clean`, `add`, `commit`, `merge`, `rebase`, `cherry-pick`, `am`, `apply`, `fetch`, `pull`, `push`, `gc`, `repack`, `prune`, `maintenance`, config writes, worktree mutations, `submodule update`, Git LFS, and filter execution. Candidate-declared project probes are a separate `project` variant and may run only through an already-existing, separately approved no-network/no-secret/read-only-repository/resource-limited sandbox contract with exact candidate-owned argv; the validator never runs them directly. The current candidate declares no such admitted contract, so any project probe is `skipped-unsafe` and forces `result: stop`; do not invent a sandbox, dependency, path, API, or probe command in this plan.

From candidate-owned committed blobs only, record whether source-backed product client, product event stream/types, committed testkit vectors, runtime/protocol version authority and mismatch result, review/confirm/launch/cancel/lost-reply/status/reconcile/cleanup lifecycle vectors, one-instance/profile/XDG/socket scope, NMP ownership projection, pin-parity input/fixture, and candidate-declared executable probes are present. Record every unsafe, unknown, malformed, or project-without-sandbox command with `probe_status: skipped-unsafe`, rejection code, and raw declaration; any required skipped/missing probe forces `result: stop`. No dependency installation or sandbox weakening is allowed.

Create `scripts/ref-candidate-check.py` using Python standard library. Both Markdown artifacts contain exactly one marker-delimited canonical JSON record. Qualification schema requires repository/origin, observed commit/tree, `tree_inventory_path` fixed to `.artifacts/phase-01/napp/&lt;observed_commit&gt;/tree.bin`, tree inventory SHA-256, approved-ref reachability results, pinned-blob records, `working_tree_evidence: excluded`, one object per admission category with status/evidence/hash/owner, declared-probe records, controlled-runner identity/config, all mutation snapshots, missing categories, and result. Validator requires that exact `.bin` path, mode `0600`, regular non-symlink confinement beneath the resolved Napp artifact root, then recomputes the file bytes and SHA-256 from canonical `git ls-tree -rz --full-tree` stdout without NUL/newline conversion. It rejects any `.txt` alias or alternate inventory path, duplicate/unknown/missing fields, requires current commit to record every absent category and `result: stop`, and permits `qualified-for-research` only when every category is present, every required probe safely passed, Napp/NMP ownership holds, and no working-tree evidence contributed. Its negative self-tests bracket every rejection with complete before/after snapshots and cover at least `write-tree`, `hash-object -w`, `cat-file --textconv`, `cat-file --filters`, `show --textconv`, `show --ext-diff`, alias/`-c`/`--config-env`/environment injection, shell metacharacters, network commands/URLs, `update-ref`, symbolic-ref mutation, index/worktree mutation, maintenance/GC, LFS/filter execution, and hooks. They require rejection before spawn plus byte-identical HEAD, refs, raw index file, `ls-files -s -z`, porcelain-v2, protected filesystem metadata, and object-store manifest; positive tests admit only the exact grammar forms. After writing, recapture both repositories and require all Napp snapshots byte-identical; Uzel may differ only by the two declared Task-1 files and ignored `tree.bin` inventory, while its HEAD, refs, index/index serialization, protected metadata, and object store remain unchanged. Do not search for or name guessed future paths/symbols/events/vectors, and make no production/dependency/upstream change.</action>
  <verify>
    <automated>python3 -c 'from pathlib import Path; compile(Path("scripts/ref-candidate-check.py").read_text(encoding="utf-8"), "scripts/ref-candidate-check.py", "exec")' &amp;&amp; python3 scripts/ref-candidate-check.py self-test &amp;&amp; python3 scripts/ref-candidate-check.py qualification --repo /workspace/projects/napplets/napp-uzel/napp --expected-repository jodobear/napp --expected-commit 0b75b6b4a9ba83598ef8be5ff95dbd40faaf128e --record evidence/phase-01/candidate-qualification.md --expected-result stop</automated>
  </verify>
  <acceptance_criteria>Structured validator proves canonical repository, approved-ref reachability status, exact commit/tree and the single fixed mode-`0600` `tree.bin` path/bytes/SHA-256, pinned instruction/context blobs or committed-tree absence, every admission category, exhaustive argv-grammar decisions, controlled Git execution, hashes/owners, `result: stop`, and working-tree exclusion. Malicious argv/env/stdin cases are rejected before spawn and cannot qualify. Both-repository HEAD/raw-index/index-serialization/refs/status/protected-filesystem/object-store snapshots prove no undeclared mutation or Git-object write.</acceptance_criteria>
  <done>REF-07 current coverage is a reproducible fail-closed qualification result, not a private seam or parity claim.</done>
</task>

<task type="auto">
  <name>Task 2: Publish repository-qualified dependency and adapter resume contract</name>
  <read_first>
    evidence/phase-01/candidate-qualification.md
    .planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-REAUDIT.md
    .planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-CONTEXT.md
    uzel-poc-validated-pack/docs/08-upstream-contributions.md
  </read_first>
  <files>evidence/phase-01/napp-dependency.md</files>
  <action>Create a concise handoff owned by `jodobear/napp` and consumed by `jodobear/uzel`, with one marker-delimited canonical JSON record plus human explanation. Copy structurally—not by prose—the exact repository, origin, observed commit/tree, fixed `tree_inventory_path` ending in `/tree.bin`, inventory SHA-256, approved-ref reachability, qualification result, every admission category/status/evidence/hash/owner, declared-probe disposition, missing categories, working-tree exclusion, and both-repository pre/post snapshots from Task 1. `ref-candidate-check.py handoff` parses both records and requires field-by-field equality for those objects plus blocker, owner, next probe, rollback, exact heads, and D-18 rule; text coincidence, a `.txt` path, or an alternate inventory filename cannot pass.

Bind parity to the committed Plan-01 contract without making qualification depend on Plan-01 execution: resolve the committed blob of `.planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-01-PLAN.superseded.md` at the current Uzel HEAD and record path, commit, blob OID, content SHA-256, fixed production commit, and declared replay-manifest path. If Plan 01 has already completed, also record its admitted manifest SHA-256/execution head; otherwise record `replay_manifest_status: pending-plan-01` and make exact admitted manifest digest a REF-01D precondition. State the required Napp-owned deliverables by behavior only. Record D-12 routing: reusable fixes use a dedicated `jodobear/napp` branch/issue and enter the contribution ledger only after Uzel validation; this plan performs no such mutation. Record three independent REF-01D preconditions: Plan 03 authority set at a committed exact SHA; an exact reachable Napp commit validated as `qualified-for-research`; and Plan-01 replay evidence admitted at the recorded contract plus exact manifest digest. Resume action is `$gsd-plan-phase 1 --research`, followed by a new narrow Rust/Tauri adapter plan and D-15 pin/adapter revert; qualification never auto-starts implementation. Bind one repository-qualified Uzel issue/PR and corresponding Napp dependency, exact heads, blocker, next probe, and D-18 once-per-semantic-candidate rule. Recheck both repositories after writing with the same snapshot contract.</action>
  <verify>
    <automated>python3 scripts/ref-candidate-check.py handoff --repo . --napp-repo /workspace/projects/napplets/napp-uzel/napp --qualification evidence/phase-01/candidate-qualification.md --handoff evidence/phase-01/napp-dependency.md --plan .planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-01-PLAN.superseded.md &amp;&amp; git diff --exit-code -- Cargo.toml Cargo.lock flake.nix flake.lock apps crates contracts fixtures napplets</automated>
  </verify>
  <acceptance_criteria>Validator proves qualification/handoff structural equality, committed Plan-01 parity-contract binding, three exact adapter preconditions, safe-probe dispositions, preserved-Poc rollback, and both-repository mutation invariants. Handoff is sufficient for Napp owners/future Uzel replanning without naming unknown APIs; no production, dependency, or upstream state changed.</acceptance_criteria>
  <done>Napp absence is a durable repository-qualified dependency; REF-01D remains blocked and resumable rather than silently omitted.</done>
</task>

</tasks>

<post_execution_review_gate>
## D-18 candidate handoff and separately authorized ship/review gate

- **Execute-plan ownership:** Capture the full 40-character plan-start HEAD as `candidate_base` before Task 1 and the final task commit as `candidate_head`. In `01-04-SUMMARY.md`, write one `review_handoff` object with `schema: uzel.review-handoff/v1`, `slice: napp-qualification`, both SHAs, the exact task `<verify>` commands and statuses as `affected_local_gates`, `review_evidence_path: evidence/phase-01/reviews/napp-qualification-&lt;candidate_head&gt;.md`, `external_review_status: pending-separate-authorization`, and `publication_status: not-authorized`. Stop there: execution does not push, open/update a PR or upstream issue, merge, or claim any external review passed.
- **Evidence owner and order:** A separately authorized `$gsd-review`/`$gsd-ship` workflow creates the head-specific evidence record and fills these ordered stages, each with exact base/head plus live command output, check URL, or evidence hash: `affected_local_gates=pass`; `local_coderabbit=pass` from one full `coderabbit review --committed --base-commit &lt;base&gt;` on the immutable pair; `remote_codex=pass`; `in_scope_fixes=none|batched`; any required affected-gate/review reruns; `final_github_coderabbit=pass` on the final exact head; `unresolved_blockers=[]`. No status may be claimed from planned or stale evidence, and no secret, credential, signer material, private host data, sibling-repository private metadata, or unrelated private metadata may be uploaded.
- **Invalidation:** Any candidate-head change creates a new head-specific evidence path, invalidates all later review evidence tied to the old head, and restarts at affected local gates plus full local CodeRabbit before remote Codex/final GitHub CodeRabbit. Unchanged base/head and semantic inputs do not rerun expensive gates.
- **Authority boundary:** Publication, PR/upstream-issue creation or update, and merge each require separate user authorization. Phase 3 later automates/enforces CI and exact-head aggregation; this Phase 1 contract only records the candidate/pending stop and the ordered ship gate.
</post_execution_review_gate>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dirty sibling checkout → committed candidate | Only exact reachable Git objects can supply admission evidence; working-tree material is untrusted. |
| candidate declarations → Uzel qualification | Candidate-owned source, vectors, versions, lifecycle, pins, and commands must be concrete before Uzel relies on them. |
| candidate-declared command → local process | Command declarations are untrusted; only finite read-only Git argv may execute, with all other probes skipped and qualification stopped. |
| untrusted frame → future Napp adapter | Source-created surface binding must precede runtime routing; frames receive no raw network, native bridge, host paths, secrets, or caller principal. |
| future Napp runtime → NMP | One runtime/NMP owner must retain all Nostr/store authority and bounded cancellation/recovery. |

## STRIDE Threat Register — ASVS L1, block high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Spoofing / Tampering | candidate provenance | high | mitigate | Resolve repository and full commit, inspect committed tree only, hash inventory/output, and exclude dirty sibling files. |
| T-04-02 | Tampering | client/events/testkit claims | high | mitigate | Require candidate-owned committed source/vectors/declarations; missing or ambiguous evidence returns stop with no invented path or symbol. |
| T-04-03 | Elevation | future source/principal routing | high | mitigate | Handoff requires trusted host source binding, runtime-derived surface/principal, and denial of raw frame network/native/path/secret authority. |
| T-04-04 | Elevation / Information disclosure | Napp/NMP ownership | high | mitigate | Require Napp runtime authority and NMP sole Nostr/store/freshness/provenance/signer/publication ownership; prohibit private fallback/dual state. |
| T-04-05 | Denial of service | lifecycle/recovery contract | high | mitigate | Qualification requires bounded review/launch/cancel/lost-reply/status/reconcile/cleanup vectors before adapter research. |
| T-04-06 | Repudiation | qualification/handoff | medium | mitigate | Record exact commands, output hashes, missing categories, result, blocker, owner, next probe, and resume command. |
| T-04-07 | Elevation / Tampering / DoS | candidate-declared probes | high | mitigate | Match literal argv against the exhaustive rev-parse/ls-tree/committed-blob-show/cat-file-existence-type-size grammar, execute through controlled Git config/environment with no shell/network/filter/hook path, and reject all other commands before spawn; skipped required probes force stop. |
| T-04-08 | Tampering | Uzel and Napp repositories | high | mitigate | Compare HEAD, raw index plus read-only `ls-files -s -z` serialization, sorted refs, NUL porcelain-v2, protected Git filesystem metadata, and recursive object-store manifest before/after; allow only declared Uzel evidence/validator paths and ignored inventory, no Napp or Git-object mutation. |
</threat_model>

<verification>
- Current exact Napp commit is reachable and its committed tree is independently inventoried.
- Qualification records all required behavioral categories without future path/symbol invention.
- Candidate probe self-tests reject every malicious argv/env/network/ref/index/object-store case before spawn, admit only exact grammar forms, and prove rejected probes leave both repositories byte/state-identical.
- Current commit yields `result: stop`; working-tree evidence is excluded.
- Handoff structurally equals qualification facts and names repository, commit, missing contracts, owner, all three adapter preconditions, committed Plan-01 parity-contract digest, rollback, and resume command.
- Uzel/Napp production, pins, locks, fixtures, protocol, and remote state remain unchanged.
</verification>

<success_criteria>
- Maintainer can reproduce why current Napp commit is not consumable.
- Napp owners receive exact behavioral deliverables and evidence requirements without a Uzel-authored API design.
- Missing/falsified evidence stops only qualification/adaptation; replay/ownership/admission/Debian lanes remain independent.
- REF-01D does not exist as an executable task until committed authority and qualified candidate gates pass.
- Future migration remains one-axis with the preserved POC pin/adapter revert path.
</success_criteria>

## Review Feedback Disposition

| Review item | Status | Reason / executable disposition |
|---:|---|---|
| 1 | Accepted | Candidate commands use an exhaustive literal-argv grammar, controlled Git config/environment, no shell evaluation, and malicious-command self-tests; unsafe/unknown/required skipped probes record evidence and force stop. |
| 25 | Rejected | D-02 and ROADMAP permit qualification independently of authority admission; only REF-01D requires both committed authority and qualified candidate. |
| 26 | Accepted | Handoff binds committed `01-01-PLAN.superseded.md` blob/content digest now and requires exact admitted replay-manifest digest before adapter work. |
| 27 | Accepted | Validator parses canonical JSON blocks and compares repository/commit/tree/result/categories/preconditions/heads/blocker/next probe/rollback/D-18 field by field. |
| 28 | Accepted | Validator recomputes canonical repo, approved-ref reachability result, tree digest, every category/hash/owner/probe, stop result, and working-tree exclusion. |
| 29 | Accepted | HEAD, raw index, read-only NUL `ls-files -s` index serialization, refs, NUL porcelain-v2, protected Git filesystem metadata, and recursive object-store manifests are compared before/after; negative probe tests prove no Git write occurs. |
| 30 | Accepted | Candidate AGENTS/README/planning context read only via pinned committed blobs; missing blobs recorded. |
| 37 | Accepted | Research candidate example now contains reachability, clean-tree, version, lifecycle, pin, client/events/testkit, and safe declared-probe outcomes. |
| Artifact-path checker | Accepted | `.artifacts/phase-01/napp/&lt;commit&gt;/tree.bin` is the only inventory path; qualification validator recomputes canonical NUL bytes/SHA-256 and handoff equality rejects `.txt` or alternate paths. |
| D-18 checker | Accepted | Executor records exact candidate base/head and pending handoff; separately authorized review/ship fills one head-bound ordered evidence record, with head-change invalidation and no automatic publication/upstream mutation. |

<output>
Create `.planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-04-SUMMARY.md` when done. Record repository-qualified Uzel issue/PR and Napp dependency only if already available, exact candidate commit/tree hash, fixed `tree.bin` path/digest, result, missing evidence, blocker owner, next probe, resume command, and the required `review_handoff` pending-stop object without performing external publication or upstream mutation.
</output>
