---
phase: 01-slice-ref-01-poc-replay-accepted-napp-seam
plan: 05
type: execute
wave: 5
depends_on:
  - "01-04"
files_modified:
  - docs/ecosystem/upstreams.toml
  - docs/runtime/capabilities/m0-index.toml
  - docs/knowledge/m0-governance.md
  - docs/knowledge/terms.toml
  - uzel-poc-validated-pack/docs/08-upstream-contributions.md
  - uzel-poc-validated-pack/STATUS.md
  - uzel-poc-validated-pack/audit-results.json
  - uzel-poc-validated-pack/manifest.json
  - evidence/phase-01/upstream-discovery-observations.json
  - docs/compat/profiles/uzel-m0.toml
  - docs/compat/generated/uzel-m0.md
  - docs/specs/interpretations/SIR-0001-manifest-build-identity.md
  - docs/compat/negotiation-vectors.json
  - docs/knowledge/index.internal.json
  - .planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-05-SUMMARY.md
autonomous: true
requirements:
  - M0-05
user_setup: []
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: medium
must_haves:
  truths:
    - "D-01 and D-03: programme authority is traceable; Uzel shell/product-service owns product workflow while the canonical engine/provider remains replaceable behind an explicit private adapter and upstream relationships."
    - "D-06, D-08 through D-10, and D-26: ecosystem, compatibility, SIR, negotiation, capability, fairness, knowledge, terms, visibility, and learning artifacts form one evidence-linked closure."
    - "D-08, D-09, and D-26: source-profile hash and installed packaged-RCP digest are separate non-aliased identities; verified packages bind independently read bytes before guest code, while `not_yet_packaged` explicitly omits the packaged digest and blocks packaged-runtime acceptance."
    - "D-08: the finalized per-principal capability/fairness ledger exists before Task 2 computes its whole-file digest and creates the immutable compatibility profile or any generated projection."
    - "D-11: upstream work has explicit ownership, dedicated jodobear fork/branch/worktree policy, contribution record, named human submitter/approver, truthful AI/authorship/signoff/DCO/CLA/public-text dispositions, and human publication authority."
    - "D-11 and D-26: every guest-influenced queue has both global and per-principal limits, a named anti-starvation owner/policy, and executable noisy/quiet, many-principal, and adversarial pressure vectors."
    - "D-29 and D-30: exact-build identity, interop/version-skew, local-patch status, evidence, limitations, disagreement, and negative results are first-class."
    - "D-31: this autonomous compatibility-closure plan creates no Phase-2 decision or transition authority; evidence freeze and transition closeout remain isolated in dependent Plans 06-07."
  artifacts:
    - path: docs/ecosystem/upstreams.toml
      provides: "Source-of-truth upstream registry, ownership, pins, fork/worktree policy, contribution state, and drift"
    - path: docs/compat/profiles/uzel-m0.toml
      provides: "Immutable evidence-bound compatibility profile atomically finalized after the capability-ledger digest exists"
    - path: docs/specs/interpretations/SIR-0001-manifest-build-identity.md
      provides: "Spec interpretation record for manifest and exact-build identity"
    - path: docs/compat/negotiation-vectors.json
      provides: "Canonical transcript gate plus positive and pre-guest-code rejection vectors for identity/build/principal/capability/skew mismatches"
    - path: docs/runtime/capabilities/m0-index.toml
      provides: "Per-principal capability/admission/fairness ledger with evidence and residual gaps"
    - path: docs/knowledge/m0-governance.md
      provides: "Decision, disagreement, learning, visibility, education, upstream, and closeout workflow"
    - path: docs/knowledge/index.internal.json
      provides: "Task-2-owned internal index over finalized ledger/profile/SIR/transcript/governance records, including machine-verifiable Phase-1 closeout and teaching witness"
  key_links:
    - from: docs/ecosystem/upstreams.toml
      to: docs/compat/profiles/uzel-m0.toml
      via: "Every compatibility dependency binds to exact registry IDs and immutable upstream/source identities"
      pattern: "upstream_id"
    - from: docs/runtime/capabilities/m0-index.toml
      to: docs/compat/profiles/uzel-m0.toml
      via: "Task 1 finalizes immutable ledger bytes; Task 2 computes their digest before it creates and publishes the immutable profile"
      pattern: "capability_ledger_sha256"
    - from: docs/specs/interpretations/SIR-0001-manifest-build-identity.md
      to: docs/compat/negotiation-vectors.json
      via: "Normative ambiguity and exact-build policy become executable positive/negative/skew/local-patch vectors"
      pattern: "SIR-0001"
    - from: docs/compat/profiles/uzel-m0.toml
      to: docs/compat/negotiation-vectors.json
      via: "Each transcript binds source profile ID/hash plus separate packaged-RCP status/digest, exact-build identity, finalized ledger digest, and session principals before admission"
      pattern: "canonical_transcript_sha256"
    - from: docs/knowledge/index.internal.json
      to: evidence/phase-01/phase-2-evidence-set.json
      via: "Plan 06 freezes internal-index transition sources and Phase-1 closeout/teaching witness into E"
      pattern: "phase-01-closeout-teaching-witness"
  assumptions:
    - id: EDGE-M0-05-UNCLASSIFIED
      requirement: M0-05
      category: unclassified
      statement: "The spec-less edge probe could not classify M0-05; manual review must confirm registry completeness, immutable-profile transitions, exact-build ambiguity, negotiation/skew/local-patch cases, per-principal capability/fairness, disclosure projections, learning closure, and human transition authority cover every material ecosystem edge."
      status: unresolved
      flagged: true
  prohibitions:
    - id: PROHIB-M0-05-FAIRNESS-INDEPENDENCE
      requirement: M0-05
      category: values
      statement: "MUST NOT claim universal conformance, manufacture an independent peer result, contact or publish upstream without human authority, conceal asymmetric principal treatment, or advance automatically into Phase 2."
      status: unverified
      flagged: true
---

## Phase Goal

**As a** Uzel owner, **I want to** preserve the current POC and incident evidence while separating historical replay truth from current evidence, **so that** Phase 1 can reset execution without inventing a green baseline.

<objective>
Close the ecosystem, compatibility, exact-build interpretation, negotiation, capability, fairness, upstream, visibility, and learning baselines as immutable inputs for dependent transition closeout.

Purpose: Satisfy the autonomous compatibility-closure portion of M0-05 with immutable evidence-linked governance artifacts and no transition decision, upstream contact, public claim, or product implementation.

Output: Ecosystem/compatibility/SIR/negotiation/capability/knowledge registries and internal index ready for Plan 06 evidence freeze and Plan 07 transition closeout.
</objective>

<execution_context>
@/home/at/.codex/gsd-core/workflows/execute-plan.md
@/home/at/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-CONTEXT.md
@docs/plans/uzel-product-incubation-v4-2026-08-10/00-GSD-INGEST.md
@docs/plans/uzel-product-incubation-v4-2026-08-10/02-PRODUCT-ARCHITECTURE.md
@docs/plans/uzel-product-incubation-v4-2026-08-10/03-ROADMAP.md
@docs/plans/uzel-product-incubation-v4-2026-08-10/04-DELIVERY-QUALITY.md
@docs/plans/uzel-product-incubation-v4-2026-08-10/07-ECOSYSTEM-UPSTREAM.md
@docs/plans/uzel-product-incubation-v4-2026-08-10/08-DECISIONS-LEARNING.md
@docs/plans/uzel-product-incubation-v4-2026-08-10/09-PRODUCTION-MATURITY.md
@.planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-03-SUMMARY.md
@.planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-04-SUMMARY.md
@uzel-poc-validated-pack/docs/08-upstream-contributions.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Trace one principal and upstream into the finalized capability, fairness, and governance ledgers</name>
  <files>docs/ecosystem/upstreams.toml, docs/runtime/capabilities/m0-index.toml, docs/knowledge/m0-governance.md, docs/knowledge/terms.toml, uzel-poc-validated-pack/docs/08-upstream-contributions.md, uzel-poc-validated-pack/STATUS.md, uzel-poc-validated-pack/audit-results.json, uzel-poc-validated-pack/manifest.json, evidence/phase-01/upstream-discovery-observations.json</files>
  <read_first>
    - .planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-CONTEXT.md — D-01, D-03, D-06, D-08, D-09, D-11, D-26, D-29 through D-31
    - docs/plans/uzel-product-incubation-v4-2026-08-10/07-ECOSYSTEM-UPSTREAM.md — registry, fork/worktree, patch, contribution, and drift authority
    - docs/plans/uzel-product-incubation-v4-2026-08-10/08-DECISIONS-LEARNING.md — SIR, disagreement, immutable decision, and visibility contracts
    - docs/plans/uzel-product-incubation-v4-2026-08-10/09-PRODUCTION-MATURITY.md — capability, fairness, visibility, education, and maturity closure
    - docs/plans/uzel-product-incubation-v4-2026-08-10/templates/UPSTREAM-REGISTRY.toml — registry field contract
    - docs/plans/uzel-product-incubation-v4-2026-08-10/templates/CAPABILITY-LEDGER.md — per-principal capability/admission/fairness field contract
    - docs/plans/uzel-product-incubation-v4-2026-08-10/templates/TERM-REGISTRY.toml — term-registry field contract
    - docs/architecture/phase-01-authority.md, docs/architecture/phase-01-identity.md — owners, principals, actors, roles, instances, profiles, and non-conflation rules
    - evidence/phase-01/current-source-citations.json, evidence/phase-01/current-package-verdict.json, evidence/phase-01/current-native-baseline.json — current source/package/native evidence identities
    - evidence/phase-01/ci-baseline.json, evidence/phase-01/review-baseline.json — delivery maturity and SHA-bound review evidence
    - uzel-poc-validated-pack/docs/08-upstream-contributions.md — canonical upstream contribution ledger required by repository policy
  </read_first>
  <action>Implement D-01, D-03, D-06, D-08, D-10, D-11, D-26, D-29, and D-30 as the prerequisite tracer for profile finalization. Derive the complete upstream universe independently before writing the registry, using only source-specific semantic identities. Strict-load the exact immutable Task-1-parent `closure-candidate-inventory.json` (whose Plan-01 contract already binds the pinned YAML-1.2 parser, rejects duplicate/alias/merge/tag/non-string/non-finite pnpm YAML, and derives semantic pnpm importer/package/snapshot members), and require its source rows/blob digests against Git. Derive package identities only from its canonical lock candidates plus dependency/override tables and exact `packageManager` pnpm name/version in workspace `package.json`/`Cargo.toml`; derive exact Rust channel identity from every applicable `rust-toolchain.toml`; derive Flake inputs only from `inputs.&lt;name&gt;.(url|follows)` plus canonical flake lock candidates; derive compatibility package/spec/upstream identities only from the named semantic tables and identity fields in `compatibility.lock`; derive citation-backed specification identities only from claim ID plus the citation's exact head/tree/blob/path/line span/source-test tuple; and derive local patches only from tracked `.patch`/`.diff` bytes. Never turn arbitrary citation contents, scripts, descriptions, booleans, timestamps, test prose, or generic scalars into upstreams, and never label an arbitrary cited file as a patch. Create canonical strict `phase1-upstream-discovery-observations-v1` JSON that freezes the one exact successful `git remote -v` observation and immutable-parent `.gitmodules` observation: exact shell-free argv/cwd/source-head, integer exit, stdout/stderr UTF-8 bytes and SHA-256, plus parsed sorted unique remote/submodule rows. Registry discovery binds this artifact path/digest and derives remotes/submodules only from its retained bytes; validation never re-runs mutable `git remote -v`. Canonical discovery keys are source-kind plus normalized URL/name/path/revision, sorted unique. Store the exact source path/blob OID/SHA-256 manifest and require registry `discovery_keys` set equality with the independently recomputed universe; an author-selected registry list cannot define its own completeness. Inventory every derived upstream dependency, fork, specification, schema, build input, engine seam, and maintained local patch using exact canonical URL/repository, source-of-truth authority, upstream/fork owner, pinned revision and tree/lock identity, consumed path, licence/status, drift, divergence, local-patch state, contribution suitability, and evidence digest. For every material local patch or upstream interaction, add a machine-readable record that keeps `local_patch`, `thread`, `pr_accepted`, `pr_merged`, `release_contains_change`, `uzel_adoption`, and `patch_removal` as seven separate keyed state objects. Every state object records its own status, SHA-256 evidence, owner, and next trigger even when blocked or not applicable; no state, missing field, or shared evidence pointer implies another state. Record the required dedicated branch in the corresponding jodobear fork, a dedicated clean worktree, exact upstream base/head, tests, contribution-policy review, and update the canonical `uzel-poc-validated-pack/docs/08-upstream-contributions.md` ledger with the same upstream IDs and independently stated lifecycle dispositions. Add exactly one fenced `uzel-upstream-lifecycle-v1` JSON block to that Markdown ledger. Its canonical compact/sorted JSON has exactly `schema` and `upstreams`; each upstream row has exactly `upstream_id`, `states`, and `publication_status`, its ID set equals the TOML registry, its seven state keys/statuses equal that registry record, and `publication_status` is one of `not_requested`, `blocked`, `draft_local`, or `human_authorization_required`—never a pushed/published/merged claim inferred by this task. Also record a named human submitter and named human approver (or explicit `blocked_unassigned`), repository AI-contribution policy, truthful AI assistance, authorship, signoff, DCO, CLA, public-text-review and disclosure dispositions. Contacting, pushing, or publishing remains separately human-authorized later work. Update `uzel-poc-validated-pack/STATUS.md` in the same Task-1 commit with an exact `phase-01-upstream-baseline-v1` canonical JSON block binding the registry SHA-256, upstream-contribution-ledger SHA-256, sorted upstream IDs, exact Task-1-parent SHA, intended exact commit message `feat(phase-01): finalize upstream capability baseline` and trailers `GSD-Plan: 01-05`, `GSD-Task: 01-05-task-1-upstream-capability-baseline`, exact validation command arrays, evidence artifact IDs/digests, known limitations, and next trigger; it records planned commit identity as `pending_task_commit` and never invents the containing commit SHA.

Create and finalize the M0 capability index before any compatibility profile exists: enumerate every declared current capability and distinct principal/actor/role from the identity baseline, then record each pair's requested, advertised, supportable, denied, failed, unavailable, or not-applicable state; whether it is runtime-required or guest-optional; authority; source/test/evidence; constraints; side effect; audit signal; cancellation/retry behavior; limitation; and residual risk. The universe is not author-selected: identity IDs equal the eleven programme-authority axes `view-identity`, `viewed-subject`, `signing-identity`, `app-instance`, `profile`, `runtime`, `project`, `workbench-state`, `actor`, `role`, and `signer`; capability IDs equal `source-claim/` plus every sorted citation-backed current-source claim ID, excluding only claims carrying the exact validated `explicit_absence` object. Require set equality in the identity inventory, capability definitions, matrix, and profile binding, so no authority item can be omitted by leaving it out of a ledger-defined list. Do not insert a profile ID/hash or negotiation transcript that does not yet exist. Build one exhaustive `guest_influenced_queues` inventory covering every queue/cache/batch/retry/subscription/composition/diagnostics buffer. Give every queue a stable `queue_id`; named admission principal; numeric global and per-principal item, byte, in-flight, wakeup, and retained-result limits; typed overload behavior; named anti-starvation owner; scheduling algorithm or bounded-wait policy; privacy-safe metrics; cancellation/cleanup; and crash/restart behavior. Require exactly one admission-class row per queue ID, with no orphan or duplicate queue. For every queue ID add machine-readable noisy-producer/quiet-peer, many-principal, adversarial-pressure, cancellation, and overload vectors with bounded expected outcomes and evidence digests; aggregate vectors or a soak alone are insufficient. Add fairness rows comparing like-for-like principals and inputs, explaining asymmetric treatment and unmeasured cases without inventing multi-user guarantees.

Create one authoritative `evidence_registry` in `m0-index.toml` before any Task-1 evidence reference. Each row has exactly `artifact_id`, repository-relative `path`, and `sha256`; IDs and paths are sorted/unique and every path resolves through component-wise `lstat` to tracked regular retained bytes whose digest is recomputed. Every upstream lifecycle state names one registry `evidence_artifact_id` and repeats that row's digest. Every capability-matrix row has exactly `capability_id`, `identity_id`, `state`, `necessity`, `authority`, `source_claim_ids`, `evidence_artifact_ids`, `constraints`, `side_effect`, `audit_signal`, `cancellation`, `retry`, `limitation`, and `residual_risk`; evidence and source-claim arrays are nonempty sorted/unique and resolve exactly. Each pressure vector and fairness row records exact `test_id`, shell-free `argv`, absolute component-symlink-free task-owned `cwd`, sorted nonempty registry-backed `input_artifact_ids`, expected zero exit/pass, `guest_code_started`, sorted `exposed_domains`, structured `observed_result`, exactly one `execution_receipt_artifact_id`, and distinct retained stdout/stderr artifact IDs, in addition to its exact known queue/vector/fairness fields. That registry artifact parses as canonical duplicate-key-free `phase1-vector-execution-receipt-v2` JSON with exact keys `receipt_schema`, `test_id`, `argv`, `cwd`, `input_artifact_ids`, `vector_id`, `queue_id`, `transcript_id`, `exit_status`, `status`, `stdout_artifact_id`, `stdout_sha256`, `stderr_artifact_id`, `stderr_sha256`, `guest_code_started`, `exposed_domains`, and `observed_result`. Require every receipt field to equal its row, including exact vector/test/queue (`transcript_id: not_applicable` for pressure/fairness), cwd, exit/status, captured-byte artifacts, guest start, domains, and result. Execute every `argv` with `subprocess.run(argv, cwd=cwd, shell=False, capture_output=True)`, require actual integer exit/stdout/stderr bytes and SHA-256 equal the retained artifacts and receipt, and reject explicit shell launchers (`sh`, `bash`, `zsh`, `fish`, `cmd`, `powershell`) as argv0. A stored receipt cannot substitute for execution. Failed/blocked/nonzero receipts remain truthful evidence but cannot satisfy a claimed completed vector. Require exactly one of each required pressure kind per known queue, no duplicate pair, and no unknown queue. A merely 64-hex or shared unresolvable digest is invalid.

Record the M0/M1 acquisition or commission plan for a separately authored clean-room peer: owner, acquisition route, allowed public-kit/black-box inputs, prohibited Uzel internals/private tests/implementation coaching, provenance requirements, target journey, evidence due phase, current status, and blocking effect. Until acquired it is exactly `blocked_no_independent_peer`; this blocks L4 runtime-composability promotion and M5, not M0 or Phase 2, and the Uzel-authored external-source fixture remains explicitly non-independent. The finalized ledger exposes a stable capability-set ID and immutable bytes; after Task 1 succeeds, Task 2 computes their whole-file SHA-256, treats those bytes as immutable input, and forces ledger review plus a new profile candidate on any drift. In the governance document define immutable decision/SIR updates, dissent, correction, supersession, milestone closeout, learning extraction, teaching/education ownership, visibility tiers, disclosure, provenance, restart/cancellation, Uzel shell/product-service direction with a replaceable canonical engine/provider adapter, independent-peer acquisition/blocking, and the dedicated jodobear upstream contribution workflow. Populate the term registry from programme authority without silent redefinition. Validate the three TOML registries, unique IDs, cross-links, source/evidence digests, separate upstream lifecycle states, contribution-policy dispositions, bounds/anti-starvation fields and pressure-vector coverage before atomic replacement. On cancellation retain only independently validated task-owned artifacts, mark ledger finalization incomplete, and refuse Task 2.</action>
  <cycle12_ownership_override>The pack-integrity and STATUS contract here supersedes stale Task-1 wording above. The STATUS block uses exact audit-safe trailers `Phase-Plan: 01-05` and `Phase-Task: 01-05-task-1-upstream-capability-baseline`; it MUST NOT contain the pack-auditor-forbidden literal workflow acronym. `validation_commands` is exactly the two typed argv arrays `[["python3","uzel-poc-validated-pack/scripts/audit_docs.py"],["python3","-m","json.tool","uzel-poc-validated-pack/audit-results.json"]]`. `evidence_artifacts` is a nonempty sorted unique list of exact objects `{artifact_id,path,sha256}` equal to rows in `m0-index.toml`'s evidence registry. After updating STATUS and `docs/08-upstream-contributions.md`, execute the first argv, require zero `errors`, then commit regenerated `audit-results.json` and `manifest.json` in Task 1. Recompute the manifest's complete eligible path universe exactly as `audit_docs.py` does from Git cached-plus-untracked, nonignored files below the pack, excluding only `manifest.json`, `__pycache__`, and `.pyc`; require exact sorted row-set equality and actual bytes/digest parity for every eligible file, not a three-row spot check. Independently validate the closure candidate's complete `members` source rows and each lock candidate's `lock_path`/`lock_sha256` against the immutable WIP Git tree before using any candidate identity. Citations use Plan-03's exact nine-key schema including `fact_id` and `source_node_id`, and bind the citation document's own authoritative earlier `source_head`/`source_tree`; they never pretend that earlier source identity equals the later Plan-05 Task parent.</cycle12_ownership_override>
  <verify>
    <automated>python3 -c 'import re,tomllib; u=tomllib.load(open("docs/ecosystem/upstreams.toml","rb")); c=tomllib.load(open("docs/runtime/capabilities/m0-index.toml","rb")); tomllib.load(open("docs/knowledge/terms.toml","rb")); hex64=lambda x:isinstance(x,str) and re.fullmatch(r"[0-9a-f]{64}",x); required_states={"local_patch","thread","pr_accepted","pr_merged","release_contains_change","uzel_adoption","patch_removal"}; records=u.get("upstream_records",[]); assert records and all(set(r["states"]) == required_states and all(s.get("status") and hex64(s.get("evidence_sha256")) and s.get("owner") and s.get("next_trigger") for s in r["states"].values()) and all(r.get(k) for k in ("human_submitter","human_approver","ai_assistance","repository_ai_policy","authorship","signoff","dco","cla","public_text_review")) for r in records); queues=c.get("guest_influenced_queues",[]); admissions=c.get("admission_classes",[]); qids=[q["queue_id"] for q in queues]; aids=[a["queue_id"] for a in admissions]; limit_keys=("global_item_limit","per_principal_item_limit","global_byte_limit","per_principal_byte_limit","global_in_flight_limit","per_principal_in_flight_limit","global_wakeup_limit","per_principal_wakeup_limit","global_retained_result_limit","per_principal_retained_result_limit"); assert qids and len(qids)==len(set(qids)) and sorted(qids)==sorted(aids) and len(aids)==len(set(aids)); assert all(a.get("admission_principal") and all(type(a.get(k)) is int and a[k]&gt;=0 for k in limit_keys) and a.get("typed_overload") and a.get("anti_starvation_owner") and a.get("anti_starvation_policy") and a.get("privacy_safe_metrics") and a.get("cancellation_cleanup") and a.get("crash_restart") for a in admissions); required_kinds={"noisy_quiet","many_principal","adversarial_pressure","cancellation","overload"}; vectors=c.get("pressure_vectors",[]); assert all({v["kind"] for v in vectors if v.get("queue_id")==q} == required_kinds and all(v.get("bounded_expected_outcome") and hex64(v.get("evidence_sha256")) for v in vectors if v.get("queue_id")==q) for q in qids); p=c["independent_peer"]; assert p["status"] == "blocked_no_independent_peer" and p["blocks_l4_runtime_composability"] and p["blocks_m5"] and not p["blocks_phase_2"] and p.get("acquisition_owner") and p.get("acquisition_route")' &amp;&amp; python3 -c 'import json,re,tomllib; from pathlib import Path
def strict(pairs):
	d={}; [(_ for _ in ()).throw(ValueError("duplicate JSON key: "+k)) if k in d else d.__setitem__(k,v) for k,v in pairs]; return d
registry=tomllib.load(open("docs/ecosystem/upstreams.toml","rb"))["upstream_records"]; text=Path("uzel-poc-validated-pack/docs/08-upstream-contributions.md").read_text(); blocks=re.findall(r"```uzel-upstream-lifecycle-v1\n([^\n]+)\n```",text); assert len(blocks)==1; ledger=json.loads(blocks[0],object_pairs_hook=strict,parse_constant=lambda x:(_ for _ in ()).throw(ValueError("non-finite JSON constant: "+x))); assert set(ledger)=={"schema","upstreams"} and ledger["schema"]=="uzel-upstream-lifecycle-v1" and blocks[0]==json.dumps(ledger,sort_keys=True,separators=(",",":"),ensure_ascii=False); rows=ledger["upstreams"]; assert isinstance(rows,list) and len(rows)==len(registry); lm={x["upstream_id"]:x for x in rows}; rm={x["id"]:x for x in registry}; states={"local_patch","thread","pr_accepted","pr_merged","release_contains_change","uzel_adoption","patch_removal"}; safe={"not_requested","blocked","draft_local","human_authorization_required"}; assert len(lm)==len(rows) and set(lm)==set(rm); assert all(set(x)=={"upstream_id","states","publication_status"} and set(x["states"])==states and x["publication_status"] in safe and all(type(x["states"][k]) is str and x["states"][k]==rm[x["upstream_id"]]["states"][k]["status"] for k in states) for x in rows)' &amp;&amp; rg -q 'jodobear' docs/ecosystem/upstreams.toml &amp;&amp; rg -q 'dedicated.*worktree|worktree.*dedicated' docs/ecosystem/upstreams.toml &amp;&amp; rg -q 'finalized = true' docs/runtime/capabilities/m0-index.toml &amp;&amp; rg -q 'capability_set_id' docs/runtime/capabilities/m0-index.toml &amp;&amp; rg -q 'uzel-poc-validated-pack/docs/08-upstream-contributions.md' docs/knowledge/m0-governance.md &amp;&amp; rg -q 'human.*authoriz|authoriz.*human' docs/knowledge/m0-governance.md</automated>
    <automated>python3 -c 'import hashlib,json,pathlib,re,stat,subprocess,tomllib
root=pathlib.Path.cwd().resolve(); c=tomllib.load(open("docs/runtime/capabilities/m0-index.toml","rb")); u=tomllib.load(open("docs/ecosystem/upstreams.toml","rb")); rows=c["evidence_registry"]; assert isinstance(rows,list) and rows; ids=[x["artifact_id"] for x in rows]; paths=[x["path"] for x in rows]; assert ids==sorted(set(ids)) and paths==sorted(set(paths)); reg={x["artifact_id"]:x for x in rows}
for x in rows:
 assert set(x)=={"artifact_id","path","sha256"} and re.fullmatch(r"[A-Za-z0-9_.:-]+",x["artifact_id"]) and re.fullmatch(r"[0-9a-f]{64}",x["sha256"]); rel=pathlib.PurePosixPath(x["path"]); assert not rel.is_absolute() and ".." not in rel.parts and subprocess.run(["git","ls-files","--error-unmatch","--",str(rel)],capture_output=True).returncode==0; cur=root
 for part in rel.parts: cur=cur/part; assert not stat.S_ISLNK(cur.lstat().st_mode)
 assert cur.is_file() and stat.S_ISREG(cur.stat().st_mode) and hashlib.sha256(cur.read_bytes()).hexdigest()==x["sha256"]
state_keys={"status","evidence_artifact_id","evidence_sha256","owner","next_trigger"}
for upstream in u["upstream_records"]:
 for state in upstream["states"].values(): assert set(state)==state_keys and state["evidence_artifact_id"] in reg and state["evidence_sha256"]==reg[state["evidence_artifact_id"]]["sha256"]
claim_ids={x["id"] for x in json.load(open("evidence/phase-01/current-source-citations.json"))["claims"]}; identities=sorted({x["identity_id"] for x in c["capability_matrix"]}); caps=sorted(x["capability_id"] for x in c["capability_definitions"]); matrix=c["capability_matrix"]; assert [(x["capability_id"],x["identity_id"]) for x in matrix]==[(cap,identity) for cap in caps for identity in identities]
matrix_keys={"capability_id","identity_id","state","necessity","authority","source_claim_ids","evidence_artifact_ids","constraints","side_effect","audit_signal","cancellation","retry","limitation","residual_risk"}; states={"requested","advertised","supportable","denied","failed","unavailable","not_applicable"}; necessities={"runtime_required","guest_optional","not_applicable"}
for x in matrix:
 assert set(x)==matrix_keys and x["state"] in states and x["necessity"] in necessities and all(isinstance(x[k],str) and x[k] for k in matrix_keys-{"source_claim_ids","evidence_artifact_ids"}); assert x["source_claim_ids"]==sorted(set(x["source_claim_ids"])) and x["source_claim_ids"] and set(x["source_claim_ids"])&lt;=claim_ids; assert x["evidence_artifact_ids"]==sorted(set(x["evidence_artifact_ids"])) and x["evidence_artifact_ids"] and set(x["evidence_artifact_ids"])&lt;=set(reg)
qids={x["queue_id"] for x in c["guest_influenced_queues"]}; kinds={"noisy_quiet","many_principal","adversarial_pressure","cancellation","overload"}; vectors=c["pressure_vectors"]; assert len(vectors)==len(qids)*len(kinds) and {(x["queue_id"],x["kind"]) for x in vectors}=={(q,k) for q in qids for k in kinds}
for x in vectors: assert set(x)=={"queue_id","kind","test_id","argv","cwd","input_artifact_ids","expected_exit_status","expected_status","bounded_expected_outcome","guest_code_started","exposed_domains","observed_result","evidence_artifact_ids","evidence_sha256","execution_receipt_artifact_id","stdout_artifact_id","stderr_artifact_id"} and x["queue_id"] in qids and x["kind"] in kinds and x["test_id"]==x["queue_id"]+":"+x["kind"] and isinstance(x["argv"],list) and x["argv"] and pathlib.Path(x["cwd"]).is_absolute() and x["input_artifact_ids"]==sorted(set(x["input_artifact_ids"])) and x["input_artifact_ids"] and set(x["input_artifact_ids"])&lt;=set(reg) and x["expected_exit_status"]==0 and x["expected_status"]=="pass" and type(x["guest_code_started"]) is bool and x["exposed_domains"]==sorted(set(x["exposed_domains"])) and x["observed_result"]==x["bounded_expected_outcome"] and isinstance(x["bounded_expected_outcome"],str) and x["bounded_expected_outcome"] and x["evidence_artifact_ids"]==sorted(set(x["evidence_artifact_ids"])) and x["evidence_artifact_ids"] and set(x["evidence_artifact_ids"])&lt;=set(reg) and all(x[k] in reg for k in ("execution_receipt_artifact_id","stdout_artifact_id","stderr_artifact_id")) and len({x["execution_receipt_artifact_id"],x["stdout_artifact_id"],x["stderr_artifact_id"]})==3 and re.fullmatch(r"[0-9a-f]{64}",x["evidence_sha256"]) and x["evidence_sha256"] in {reg[i]["sha256"] for i in x["evidence_artifact_ids"]}
fair=c["fairness_rows"]; assert isinstance(fair,list) and fair
for x in fair: assert set(x)=={"fairness_id","queue_id","test_id","argv","cwd","input_artifact_ids","expected_exit_status","expected_status","principal_a","principal_b","input_identity","guest_code_started","exposed_domains","observed_result","observed_comparison","limitation","evidence_artifact_ids","execution_receipt_artifact_id","stdout_artifact_id","stderr_artifact_id"} and x["queue_id"] in qids and x["test_id"]==x["fairness_id"] and x["principal_a"]!=x["principal_b"] and isinstance(x["argv"],list) and x["argv"] and pathlib.Path(x["cwd"]).is_absolute() and x["input_artifact_ids"]==sorted(set(x["input_artifact_ids"])) and x["input_artifact_ids"] and set(x["input_artifact_ids"])&lt;=set(reg) and x["expected_exit_status"]==0 and x["expected_status"]=="pass" and type(x["guest_code_started"]) is bool and x["exposed_domains"]==sorted(set(x["exposed_domains"])) and x["observed_result"]==x["observed_comparison"] and x["evidence_artifact_ids"]==sorted(set(x["evidence_artifact_ids"])) and x["evidence_artifact_ids"] and set(x["evidence_artifact_ids"])&lt;=set(reg) and all(x[k] in reg for k in ("execution_receipt_artifact_id","stdout_artifact_id","stderr_artifact_id")) and len({x["execution_receipt_artifact_id"],x["stdout_artifact_id"],x["stderr_artifact_id"]})==3'</automated>
    <automated>python3 -c 'import hashlib,json,pathlib,subprocess,tomllib
def strict(pairs):
 d={}; [(_ for _ in ()).throw(ValueError("duplicate JSON key: "+k)) if k in d else d.__setitem__(k,v) for k,v in pairs]; return d
c=tomllib.load(open("docs/runtime/capabilities/m0-index.toml","rb")); reg={x["artifact_id"]:x for x in c["evidence_registry"]}; top={"receipt_schema","test_id","argv","cwd","input_artifact_ids","vector_id","queue_id","transcript_id","exit_status","status","stdout_artifact_id","stdout_sha256","stderr_artifact_id","stderr_sha256","guest_code_started","exposed_domains","observed_result"}; shells={"sh","bash","zsh","fish","cmd","powershell","pwsh"}
def receipt(row,row_id,queue,outcome):
 a=reg[row["execution_receipt_artifact_id"]]; raw=pathlib.Path(a["path"]).read_bytes(); assert hashlib.sha256(raw).hexdigest()==a["sha256"]; x=json.loads(raw,object_pairs_hook=strict,parse_constant=lambda q:(_ for _ in ()).throw(ValueError("non-finite JSON constant: "+q))); stdout=pathlib.Path(reg[row["stdout_artifact_id"]]["path"]).read_bytes(); stderr=pathlib.Path(reg[row["stderr_artifact_id"]]["path"]).read_bytes(); cwd=pathlib.Path(row["cwd"]); assert cwd.is_absolute() and cwd.is_dir() and cwd==cwd.resolve() and all(not p.is_symlink() for p in (cwd,*cwd.parents)); assert raw==json.dumps(x,sort_keys=True,separators=(",",":"),ensure_ascii=False).encode() and set(x)==top and x["receipt_schema"]=="phase1-vector-execution-receipt-v2" and x["test_id"]==x["vector_id"]==row_id==row["test_id"] and x["queue_id"]==queue and x["transcript_id"]=="not_applicable" and x["argv"]==row["argv"] and x["cwd"]==row["cwd"] and all(isinstance(v,str) and v for v in x["argv"]) and pathlib.Path(x["argv"][0]).name not in shells and x["input_artifact_ids"]==row["input_artifact_ids"] and row["execution_receipt_artifact_id"] not in x["input_artifact_ids"] and type(x["exit_status"]) is int and x["status"]==row["expected_status"]=="pass" and x["exit_status"]==row["expected_exit_status"]==0 and x["stdout_artifact_id"]==row["stdout_artifact_id"] and x["stderr_artifact_id"]==row["stderr_artifact_id"] and x["stdout_sha256"]==reg[x["stdout_artifact_id"]]["sha256"]==hashlib.sha256(stdout).hexdigest() and x["stderr_sha256"]==reg[x["stderr_artifact_id"]]["sha256"]==hashlib.sha256(stderr).hexdigest() and x["guest_code_started"]==row["guest_code_started"] and x["exposed_domains"]==row["exposed_domains"] and x["observed_result"]==row["observed_result"]==outcome; run=subprocess.run(x["argv"],cwd=x["cwd"],shell=False,capture_output=True); assert run.returncode==x["exit_status"] and run.stdout==stdout and run.stderr==stderr
for row in c["pressure_vectors"]: receipt(row,row["queue_id"]+":"+row["kind"],row["queue_id"],row["bounded_expected_outcome"])
for row in c["fairness_rows"]: receipt(row,row["fairness_id"],row["queue_id"],row["observed_comparison"])'</automated>
    <automated>python3 -c 'import hashlib,json,pathlib,re,subprocess,tomllib
def strict(pairs):
 d={}; [(_ for _ in ()).throw(ValueError("duplicate JSON key: "+k)) if k in d else d.__setitem__(k,v) for k,v in pairs]; return d
root=pathlib.Path.cwd().resolve()
enc=lambda x:json.dumps(x,sort_keys=True,separators=(",",":"),ensure_ascii=False).encode(); u=tomllib.load(open("docs/ecosystem/upstreams.toml","rb")); discovery=u["discovery"]; parent=discovery["task_parent_sha"]; assert re.fullmatch(r"[0-9a-f]{40}",parent); tree=set(subprocess.check_output(["git","ls-tree","-r","--name-only",parent],text=True).splitlines()); citations_path="evidence/phase-01/current-source-citations.json"; candidate_path="evidence/phase-01/closure-candidate-inventory.json"; citations=json.loads(subprocess.check_output(["git","show",parent+":"+citations_path]),object_pairs_hook=strict,parse_constant=lambda x:(_ for _ in ()).throw(ValueError("non-finite JSON constant: "+x))); cited={v for claim in citations["claims"] for v in ([claim.get("source_path")]+[z.get("path") for z in claim.get("citations",[]) if isinstance(z,dict)]) if isinstance(v,str) and v in tree}; base={p for p in tree if p in {"Cargo.toml","Cargo.lock","package.json","pnpm-workspace.yaml","pnpm-lock.yaml","flake.nix","flake.lock","rust-toolchain.toml","uzel-poc-validated-pack/compatibility.lock",citations_path,candidate_path} or p.endswith(("/Cargo.toml","/package.json","/rust-toolchain.toml",".patch",".diff"))}; expected=sorted(base|cited); sources=discovery["sources"]; assert [x["path"] for x in sources]==expected and all(set(x)=={"path","git_blob_oid","sha256"} and x["git_blob_oid"]==subprocess.check_output(["git","rev-parse",parent+":"+x["path"]],text=True).strip() and x["sha256"]==hashlib.sha256(subprocess.check_output(["git","show",parent+":"+x["path"]])).hexdigest() for x in sources)
obs_path=pathlib.Path("evidence/phase-01/upstream-discovery-observations.json"); obs_raw=obs_path.read_bytes(); obs=json.loads(obs_raw,object_pairs_hook=strict,parse_constant=lambda x:(_ for _ in ()).throw(ValueError("non-finite JSON constant: "+x))); obs_cwd=pathlib.Path(obs["cwd"]); assert obs_raw==enc(obs) and set(obs)=={"observation_schema","source_head","cwd","remote_command","remotes","submodules"} and obs["observation_schema"]=="phase1-upstream-discovery-observations-v1" and obs["source_head"]==parent and obs_cwd==root and obs_cwd.is_absolute() and obs_cwd.is_dir() and obs_cwd==obs_cwd.resolve() and all(not p.is_symlink() for p in (obs_cwd,*obs_cwd.parents)); cmd=obs["remote_command"]; assert set(cmd)=={"argv","cwd","exit_status","stdout","stderr","stdout_sha256","stderr_sha256"} and cmd["argv"]==["git","remote","-v"] and cmd["cwd"]==obs["cwd"] and type(cmd["exit_status"]) is int and cmd["exit_status"]==0 and isinstance(cmd["stdout"],str) and isinstance(cmd["stderr"],str) and hashlib.sha256(cmd["stdout"].encode()).hexdigest()==cmd["stdout_sha256"] and hashlib.sha256(cmd["stderr"].encode()).hexdigest()==cmd["stderr_sha256"]; parsed=[]
for line in cmd["stdout"].splitlines():
 m=re.fullmatch(r"(\S+)\s+(\S+)\s+\((fetch|push)\)",line); assert m; parsed.append({"name":m.group(1),"url":m.group(2),"kind":m.group(3)})
assert obs["remotes"]==sorted(parsed,key=lambda x:(x["name"],x["kind"],x["url"])) and len({(x["name"],x["kind"],x["url"]) for x in obs["remotes"]})==len(obs["remotes"]); expected_sub=[]
if ".gitmodules" in tree:
 gm=subprocess.check_output(["git","show",parent+":.gitmodules"],text=True); sections=re.findall(r"(?ms)^\[submodule \"([^\"]+)\"\]\s*$(.*?)(?=^\[|\Z)",gm)
 for name,body in sections:
  fields=dict(re.findall(r"(?m)^\s*(path|url|branch)\s*=\s*(.+?)\s*$",body)); assert {"path","url"}&lt;=set(fields); expected_sub.append({"name":name,"path":fields["path"],"url":fields["url"],"branch":fields.get("branch")})
assert obs["submodules"]==sorted(expected_sub,key=lambda x:x["name"]); assert discovery["observation_path"]==str(obs_path) and discovery["observation_sha256"]==hashlib.sha256(obs_raw).hexdigest()
candidate=json.loads(subprocess.check_output(["git","show",parent+":"+candidate_path]),object_pairs_hook=strict,parse_constant=lambda x:(_ for _ in ()).throw(ValueError("non-finite JSON constant: "+x))); incident=json.loads(subprocess.check_output(["git","show",parent+":"+"evidence/phase-01/incident-state.json"]),object_pairs_hook=strict,parse_constant=lambda x:(_ for _ in ()).throw(ValueError("non-finite JSON constant: "+x))); wip=incident["wip"]["commit"]; closures=candidate["closures"]; assert isinstance(closures,list) and len(closures)==1; closure=closures[0]; source_members=closure["members"]; source_by={x["path"]:x for x in source_members}; assert source_members==sorted(source_members,key=lambda x:x["path"]) and len(source_by)==len(source_members) and all(set(x)=={"path","source_kind","mode","blob","sha256"} and x["source_kind"] in {"manifest","executable_script"} and x["mode"] in {"100644","100755"} and x["blob"]==subprocess.check_output(["git","rev-parse",wip+":"+x["path"]],text=True).strip() and x["sha256"]==hashlib.sha256(subprocess.check_output(["git","show",wip+":"+x["path"]])).hexdigest() for x in source_members); members=closure["lock_candidate_members"]; assert members and members==sorted(members,key=lambda x:x["member_id"]) and len({x["member_id"] for x in members})==len(members) and all(set(x)=={"member_id","ecosystem","lock_path","lock_sha256","lock_pointer","locked_identity","identity_sha256"} and x["lock_path"] in source_by and source_by[x["lock_path"]]["source_kind"]=="manifest" and x["lock_sha256"]==source_by[x["lock_path"]]["sha256"] and x["identity_sha256"]==hashlib.sha256(enc(x["locked_identity"])).hexdigest() for x in members); assert closure["lock_candidate_members_sha256"]==hashlib.sha256(enc(members)).hexdigest() and closure["candidate_aggregate_sha256"]==hashlib.sha256(enc({"source_members":source_members,"lock_candidate_members":members})).hexdigest(); derived=["lock:{}:{}:{}".format(x["ecosystem"],x["member_id"],x["identity_sha256"]) for x in members]
dep_sections={"dependencies","devDependencies","peerDependencies","optionalDependencies","overrides"}
for source in sources:
 path=source["path"]; raw=subprocess.check_output(["git","show",parent+":"+path]); name=pathlib.PurePosixPath(path).name
 if name=="package.json":
  obj=json.loads(raw,object_pairs_hook=strict,parse_constant=lambda x:(_ for _ in ()).throw(ValueError("non-finite JSON constant: "+x)))
  manager=obj.get("packageManager")
  if manager is not None:
   assert isinstance(manager,str); mm=re.fullmatch(r"pnpm@([^+\s]+)(?:\+sha\d+\.[A-Za-z0-9+/=._-]+)?",manager); assert mm; derived.append("package-manager:{}#/packageManager=pnpm@{}".format(path,mm.group(1)))
  for section in sorted(dep_sections):
   table=obj.get(section,{})
   if table is not None: assert isinstance(table,dict)
   for package,value in sorted(table.items()): assert isinstance(package,str) and package and isinstance(value,(str,dict)); derived.append("package-json:{}#/{}/{}={}".format(path,section,package.replace("/","~1"),enc(value).decode()))
  pnpm=obj.get("pnpm",{}); assert isinstance(pnpm,dict)
  for package,value in sorted(pnpm.get("overrides",{}).items()): derived.append("package-json:{}#/pnpm/overrides/{}={}".format(path,package.replace("/","~1"),enc(value).decode()))
 elif name=="Cargo.toml":
  obj=tomllib.loads(raw.decode())
  def walk(node,pointer=""):
   if not isinstance(node,dict): return
   for key,value in sorted(node.items()):
    child=pointer+"/"+str(key).replace("~","~0").replace("/","~1")
    if key in ("dependencies","dev-dependencies","build-dependencies","workspace.dependencies") and isinstance(value,dict):
     for crate,spec in sorted(value.items()): derived.append("cargo-manifest:{}#{}={}".format(path,child+"/"+crate.replace("/","~1"),enc(spec).decode()))
    walk(value,child)
  walk(obj)
 elif path=="flake.nix":
  for input_name,field,value in re.findall(r"(?m)^\s*inputs\.([A-Za-z0-9_-]+)\.(url|follows)\s*=\s*\"([^\"]+)\";",raw.decode()): derived.append("flake-input:{}:{}={}".format(input_name,field,value))
 elif name=="rust-toolchain.toml":
  obj=tomllib.loads(raw.decode()); toolchain=obj.get("toolchain"); assert isinstance(toolchain,dict) and set(toolchain)&lt;={"channel","components","targets","profile"} and isinstance(toolchain.get("channel"),str) and toolchain["channel"]; derived.append("rust-toolchain:{}#/toolchain/channel={}".format(path,toolchain["channel"]))
 elif path=="uzel-poc-validated-pack/compatibility.lock":
  obj=tomllib.loads(raw.decode()); identity_fields={"repository","url","commit","head","merge_commit","upstream_base_commit","candidate_commit","version","integrity","shasum","pull_request","document_commit","repository_head"}
  def compat(node,pointer=""):
   if not isinstance(node,dict): return
   semantic=pointer.startswith("/packages/") or pointer.startswith("/specs/") or any(k in node for k in ("repository","upstream_base_commit"))
   if semantic:
    for key,value in sorted(node.items()):
     if key in identity_fields and isinstance(value,(str,int)): derived.append("compatibility:{}#{}={}".format(path,pointer+"/"+key,enc(value).decode()))
   for key,value in sorted(node.items()): compat(value,pointer+"/"+str(key).replace("~","~0").replace("/","~1"))
  compat(obj)
 elif path==citations_path:
  citation_keys={"fact_id","source_node_id","head","tree","blob_oid","path","line_start","line_end","source_test_id"}; citation_head=citations["source_head"]; citation_tree=citations["source_tree"]; assert re.fullmatch(r"[0-9a-f]{40}",citation_head) and re.fullmatch(r"[0-9a-f]{40}",citation_tree) and citation_tree==subprocess.check_output(["git","rev-parse",citation_head+"^{tree}"],text=True).strip(); citation_paths=set(subprocess.check_output(["git","ls-tree","-r","--name-only",citation_head],text=True).splitlines())
  for claim in citations["claims"]:
   for citation in claim.get("citations",[]):
    assert isinstance(citation,dict) and set(citation)==citation_keys and citation["head"]==citation_head and citation["tree"]==citation_tree and isinstance(citation["fact_id"],str) and citation["fact_id"] and citation["fact_id"]==claim["expected_fact"]["fact_id"] and isinstance(citation["source_node_id"],str) and citation["source_node_id"] and isinstance(citation["path"],str) and citation["path"] in citation_paths and isinstance(citation["line_start"],int) and isinstance(citation["line_end"],int) and citation["line_end"]>=citation["line_start"]>0 and citation["blob_oid"]==subprocess.check_output(["git","rev-parse",citation_head+":"+citation["path"]],text=True).strip() and isinstance(citation["source_test_id"],str) and citation["source_test_id"]
    selected={k:citation[k] for k in sorted(citation_keys)}; derived.append("spec-claim:{}:{}:{}".format(claim["id"],citation["source_test_id"],hashlib.sha256(enc(selected)).hexdigest()))
 elif path.endswith((".patch",".diff")): derived.append("local-patch:{}@{}".format(path,hashlib.sha256(raw).hexdigest()))
derived += ["git-remote:{name}:{kind}:{url}".format(**x) for x in obs["remotes"]]+["git-submodule:{}:{}:{}:{}".format(x["name"],x["path"],x["url"],x["branch"] or "") for x in obs["submodules"]]; derived=sorted(set(derived)); assert derived and discovery["derived_keys"]==derived and sorted(k for row in u["upstream_records"] for k in row["discovery_keys"])==derived
status_path="uzel-poc-validated-pack/STATUS.md"; parent_status=subprocess.check_output(["git","show",parent+":"+status_path]); current=pathlib.Path(status_path).read_bytes(); pattern=rb"\n\n```phase-01-upstream-baseline-v1\n([^\n]+)\n```\n\Z"; match=re.search(pattern,current); assert match and current[:match.start()]+b"\n"==parent_status.rstrip(b"\n")+b"\n" and b"Work 07 active" in parent_status and b"Work 07 active" in current; b=json.loads(match.group(1),object_pairs_hook=strict,parse_constant=lambda x:(_ for _ in ()).throw(ValueError("non-finite JSON constant: "+x))); expected_commands=[["python3","uzel-poc-validated-pack/scripts/audit_docs.py"],["python3","-m","json.tool","uzel-poc-validated-pack/audit-results.json"]]; cap=tomllib.load(open("docs/runtime/capabilities/m0-index.toml","rb")); evidence_reg={x["artifact_id"]:x for x in cap["evidence_registry"]}; artifacts=b["evidence_artifacts"]; assert match.group(1)==enc(b) and set(b)=={"schema","registry_sha256","contribution_ledger_sha256","upstream_ids","task_parent_sha","task_commit","task_commit_message","task_commit_trailers","validation_commands","evidence_artifacts","known_limitations","next_trigger"} and b["schema"]=="phase-01-upstream-baseline-v1" and b["task_parent_sha"]==parent and b["task_commit"]=="pending_task_commit" and b["task_commit_message"]=="feat(phase-01): finalize upstream capability baseline" and b["task_commit_trailers"]=={"Phase-Plan":"01-05","Phase-Task":"01-05-task-1-upstream-capability-baseline"} and b["validation_commands"]==expected_commands and isinstance(artifacts,list) and artifacts and [x["artifact_id"] for x in artifacts]==sorted({x["artifact_id"] for x in artifacts}) and all(set(x)=={"artifact_id","path","sha256"} and x["artifact_id"] in evidence_reg and x==evidence_reg[x["artifact_id"]] for x in artifacts) and b["upstream_ids"]==sorted(x["id"] for x in u["upstream_records"]) and b["registry_sha256"]==hashlib.sha256(pathlib.Path("docs/ecosystem/upstreams.toml").read_bytes()).hexdigest() and b["contribution_ledger_sha256"]==hashlib.sha256(pathlib.Path("uzel-poc-validated-pack/docs/08-upstream-contributions.md").read_bytes()).hexdigest() and b["known_limitations"] and b["next_trigger"]
audit_path=pathlib.Path("uzel-poc-validated-pack/audit-results.json"); pack=pathlib.Path("uzel-poc-validated-pack"); manifest_path=pack/"manifest.json"; audit=json.loads(audit_path.read_bytes(),object_pairs_hook=strict,parse_constant=lambda x:(_ for _ in ()).throw(ValueError("non-finite JSON constant: "+x))); manifest=json.loads(manifest_path.read_bytes(),object_pairs_hook=strict,parse_constant=lambda x:(_ for _ in ()).throw(ValueError("non-finite JSON constant: "+x))); assert audit["errors"]==[] and set(manifest)=={"files"}; listed=subprocess.run(["git","ls-files","--cached","--others","--exclude-standard","-z","--",str(pack)],capture_output=True); assert listed.returncode==0; eligible=[]
for item in listed.stdout.split(b"\0"):
 if not item: continue
 path=pathlib.Path(item.decode()); assert path.is_file() and path.is_relative_to(pack); rel=path.relative_to(pack); assert path.name!="manifest.json" and "__pycache__" not in path.parts and path.suffix!=".pyc"; eligible.append(rel.as_posix())
eligible=sorted(set(eligible)); rows=manifest["files"]; assert [x["path"] for x in rows]==eligible and len(rows)==len(eligible); by_path={x["path"]:x for x in rows}; assert len(by_path)==len(rows)
for rel in eligible:
 raw=(pack/rel).read_bytes(); row=by_path[rel]; assert set(row)=={"path","bytes","sha256"} and row["bytes"]==len(raw) and row["sha256"]==hashlib.sha256(raw).hexdigest()
assert not any(bad in "\n".join(p.read_text() for p in pathlib.Path("uzel-poc-validated-pack").rglob("*.md") if p.name!="AUDIT.md") for bad in ("GSD","work/08","templates/fact-record"))'</automated>
  </verify>
  <acceptance_criteria>
    - Identity IDs derive exactly from the canonical identity inventory and current-source claim IDs; capability matrix equals the complete capability-by-identity Cartesian product and the profile binds its exact sets/digest.
    - Every blocker/replacement packet-row template equals its authoritative manifest and resolved source object; informational classification requires an exact source pointer/digest and unknown classification fails closed.
    - One principal/capability path links identity, source evidence, fairness/admission status, stable capability-set ID, and finalized immutable ledger bytes before profile creation.
    - Registry covers upstream/fork ownership, pins, drift/divergence/local patches, dedicated contribution branch/worktree, policy, tests, named human submitter/approver, AI/authorship/signoff/DCO/CLA/public-text dispositions, and human publication authority.
    - Canonical validated-pack contribution ledger is updated in-task and matches upstream lifecycle IDs without implying publication.
    - Upstream records keep local patch, thread, acceptance, merge, release, exact Uzel adoption, and patch removal as exactly seven keyed states; every state independently has status/evidence/owner/trigger and no state implies another.
    - Every guest-influenced queue has exactly one admission row, no orphan/duplicate ID, all global/per-principal item/byte/in-flight/wakeup/retained-result bounds, and owned anti-starvation behavior proven by all five per-queue pressure-vector kinds.
    - Every current capability is recorded per distinct principal with required/optional semantics, honest support/failure/unavailable state, and like-for-like fairness comparison.
    - Independent-peer acquisition has an owner, clean-room input/provenance rules, target journey and honest `blocked_no_independent_peer` record; it blocks L4/M5 rather than being manufactured or silently deleted.
    - Ledger finalization contains no profile ID/hash or transcript reference to data created by Task 2.
    - Governance/terms/upstream records have single ownership, provenance, cancellation, restart, learning, visibility, and human authority rules.
  </acceptance_criteria>
  <done>The upstream, capability/fairness, governance, and term registries are finalized; Task 2 can now hash immutable ledger bytes and bind complete non-circular inputs.</done>
</task>

<task type="auto">
  <name>Task 2: Atomically bind the finalized ledger into the immutable profile and canonical negotiation transcripts</name>
  <files>docs/compat/profiles/uzel-m0.toml, docs/compat/generated/uzel-m0.md, docs/specs/interpretations/SIR-0001-manifest-build-identity.md, docs/compat/negotiation-vectors.json, docs/knowledge/index.internal.json</files>
  <read_first>
    - docs/ecosystem/upstreams.toml — finalized upstream/fork/pin/patch/contribution identities
    - docs/runtime/capabilities/m0-index.toml — finalized pre-profile capability/fairness ledger and stable digest input
    - docs/knowledge/m0-governance.md, docs/knowledge/terms.toml — decision/learning/disclosure authority and canonical meanings
    - docs/architecture/phase-01-authority.md — source/decision/change authority
    - docs/architecture/phase-01-identity.md — distinct principals, actors, roles, identities, instances, and profiles
    - docs/security/phase-01-threat-model.md — scoped residual risks and validation ownership
    - docs/plans/uzel-product-incubation-v4-2026-08-10/08-DECISIONS-LEARNING.md — decision/learning/term/index/disclosure contracts
    - docs/plans/uzel-product-incubation-v4-2026-08-10/09-PRODUCTION-MATURITY.md — capability, fairness, visibility, education, and maturity closure
    - docs/plans/uzel-product-incubation-v4-2026-08-10/templates/COMPAT-PROFILE.toml — immutable profile field contract
    - docs/plans/uzel-product-incubation-v4-2026-08-10/templates/SPEC-INTERPRETATION.md — SIR field contract
    - docs/plans/uzel-product-incubation-v4-2026-08-10/templates/PROFILE-TRANSITION.toml — later transition linkage, not mutable-profile authority
    - evidence/phase-01/current-source-citations.json, evidence/phase-01/current-package-verdict.json, evidence/phase-01/current-native-baseline.json — exact source/package/native identities
  </read_first>
  <action>Implement D-06, D-08, D-09, D-10, D-26, D-29, and D-30 only after Task 1's ledger validates with `finalized = true`; compute and freeze its whole-file SHA-256 before creating any profile byte. Stop if the ledger, upstream registry, source/tree, package/native evidence, or tool identity drifts. Open SIR-0001 for manifest/exact-build ambiguity using the exact `phase1-sir-v1` Markdown template: one canonical fenced JSON block with exactly `schema`, `sir_id`, `title`, `status`, `source_records`, `interpretations`, `stakeholders`, `dissent`, `decision`, `evidence`, `compatibility_effects`, `rollback_plan`, `human_authority`, `revisit_trigger`, and `phase_2_gate`. Source records bind path/object pointer/object SHA-256; interpretations are nonempty unique IDs with claim, supporting/opposing source IDs, compatibility effect, rollback effect, and selected boolean; exactly one is selected and equals `decision.selected_interpretation_id`. Dissent binds stakeholder/interpretation/rationale/disposition without deletion. Decision binds rationale/confidence and explicitly chosen/unchosen IDs; evidence rows bind retained path/digest; human authority is nonempty; revisit names concrete trigger/owner; Phase-2 gate is exact `bounded_non_normative_interpretation` and cannot claim upstream law. Validate every path/pointer/digest and all ID/set equalities, not headings alone.

In a task-owned staging directory, create the immutable `uzel-m0` profile bound to exact source/tree, package/native evidence, upstream-registry digest, schema/format floors, build/runtime/tool identities, the already-finalized capability-set ID and ledger SHA-256, applicable claims/status, limitations, and evidence. Before creating profile bytes, parse the finalized ledger, require boolean `finalized = true`, a stable non-empty `capability_set_id`, and its whole-file SHA-256; bind both ID and digest into profile and every transcript. Add an explicit `package_binding` contract with two non-aliased digest domains: `profile_sha256` is computed later from exact source-profile bytes, while `exact_packaged_profile_bytes_sha256` may be computed only by independently opening and reading a canonical absolute installed RCP file—not a checkout/staging/profile path. Give them distinct hash-scheme/source fields; never copy or assume equality from source-profile hash. For a packaged Plan-02 verdict, treat its actual `realized_store_path` as the package output root, not the RCP file and not a package identity. Require exactly one Plan-02 declared installable output for verified packaging. At Plan-02's exact `current-output-inventory.json` `source_head`, query that selected Nix output's source/package metadata attribute `uzelRcpRelativePath` with `nix eval --raw --no-write-lock-file` against the exact local Git revision; absence, ambiguity, evaluation failure, an absolute/traversing/empty value, or source-head mismatch blocks verified packaging. Record the selected output attribute, source head, authority enum `nix-output-metadata:uzelRcpRelativePath`, exact eval argv, and SHA-256 of raw eval bytes. Require profile `installed_rcp_relative_path` to equal that evaluated source-declared value; it is never authority itself. Resolve the RCP below the realized output root at that derived path; require exactly one canonical regular non-symlink match, record output root/relative path/absolute installed file, and independently open/hash that file. Bind only fields Plan 02 actually creates: exact declared installable outputs, build-evidence digest, executable-discovery digest, clean-environment-probe digest, realized output root, and verdict-file digest; label the separately derived Nix metadata fields as Plan-05 authority rather than invented Plan-02 evidence. Verified packaging also records canonical absolute staging root, proves the installed file and every resolved parent remain below the realized output root and outside checkout/staging trees, and records read-evidence SHA-256. If checkout-independent package evidence exists, set status `verified_packaged` and bind real 64-hex packaged digest. `blocked_not_yet_packaged` is valid only when the parsed Plan-02 verdict is exactly `not_yet_packaged`; bind the SHA-256 of those exact verdict-file bytes as `plan_02_package_verdict_sha256`, require the verdict's empty declared-output set, set owner `phase_2`, omit packaged digest/path keys entirely (not JSON/TOML null or empty), record required evidence/next trigger, and forbid packaged-runtime acceptance. `discovery_unavailable` and `package_build_failed` are blocking failures and cannot be relabelled or satisfy package readiness. RCP never contains its own digest; source-declared install metadata plus Plan-02 output/evidence identities bind exact installed bytes. No key named `profile_sha256` may occur anywhere recursively in profile TOML; profile also must not bind transcript/index needing that hash. Compute staged source-profile SHA-256 only after bytes finalize, generate `uzel-m0.md` solely from TOML, and mark Markdown non-normative with source profile ID/hash.

The preceding Nix command contract is tightened to exact argv `nix eval --offline --raw --no-write-lock-file ...`. Offline mode is mandatory and no registry/network fallback is admitted. Capture integer exit status plus stdout/stderr as Task-1 evidence-registry artifacts. Package binding names exact `source_rcp_path_eval_stdout_artifact_id` and `source_rcp_path_eval_stderr_artifact_id`; both resolve to retained registry rows and their bytes equal the command capture, while `source_rcp_path_eval_sha256` equals the stdout registry digest. Verified packaging requires exit zero and the exact stdout hash; absence, command unavailability, or any nonzero exit blocks packaging rather than becoming `not_yet_packaged`.

Build D-09/D-26 canonical negotiation transcripts in `negotiation-vectors.json`: require a nonempty, uniquely identified transcript set; canonicalize exact `negotiation_input`, `negotiation_outcome`, and `admission_facts` objects with UTF-8 JSON, sorted keys, and compact separators, then compute `canonical_transcript_sha256` from the canonical object containing exactly those three facts. Recompute that digest during validation; never accept a merely well-formed supplied digest. Every vector, including every required positive and negative kind, names exactly one existing `transcript_id`; no vector is detached from canonical admission facts. Bind those hashed facts—not un-hashed transcript siblings—to exact `profile_id`, independently computed `profile_sha256`, `exact_build_identity_sha256`, `capability_set_id`, `capability_ledger_sha256`, ordered nonempty `session_principals`, `instance_id`, `local_profile_id`, `session_id`, positive integer `generation`, and package-RCP status; every duplicated convenience field outside the facts must equal its hashed fact. A blocked transcript has no `verified_package_rcp_sha256` key and cannot produce packaged-runtime acceptance. Require positive vector kinds exactly covering `exact_match`, `supported_floor`, `optional_capability`, `interoperability`, `clean_room`, and `local_patch`; preserve honest synthetic/non-independent labels. Require negative kinds for profile/build/principal/package bytes/package digest/package diagnostics/manifest/schema/version/required-capability/downgrade/mid-session/stale/local-patch mismatches. Missing/malformed/denied required capability rejects before guest code; optional absence yields explicit unsupported/degraded without authority widening. Every vector, positive and negative, embeds exact `execution_receipt` plus its recomputed canonical `execution_receipt_sha256`; the enclosing tracked negotiation JSON retains the receipt plus exact base64 stdout/stderr bytes without an undeclared side artifact. The receipt uses `phase1-vector-execution-receipt-v2` and binds exact test/vector ID, shell-free argv, absolute component-symlink-free task staging cwd, sorted registry-backed input artifact IDs, queue `negotiation`, the vector's transcript ID, integer exit, pass/fail/blocked status, base64 stdout/stderr plus recomputed byte digests, guest-code-start boolean, exposed-domain set, and structured observed result exactly equal to the vector. Reject explicit shell launchers, rerun every vector with `subprocess.run(argv, cwd=cwd, shell=False, capture_output=True)`, and require actual exit/stdout/stderr exact equality; the authored receipt cannot assert execution. Only zero-exit `pass` satisfies a positive or expected-rejection vector. Every negative additionally names regular component-symlink-free mismatch evidence and requires `reject_before_guest`, `guest_code_started=false`, and zero exposed domains; 64-hex alone is not evidence. Mid-session replacement terminates/relaunches new session. Each vector records declarations, inputs, expected/observed result, transcript/evidence digest, limitations, cleanup, cancellation, and restart behavior.

Generate internal JSON index from every finalized Task-1 record plus staged profile/SIR/transcript records. Before classification, deterministically enumerate every parsed JSON/TOML object in the exact Plan-01–05 `files_modified` source set (excluding the internal index itself) that carries a stable `id`, `claim_id`, `row_id`, `threat_id`, or `upstream_id` plus either a blocker marker (`phase1_blocker: true`, nonempty blocker object, `blocking: true`, or `classification: phase_2_blocker`) or a critical replacement marker (critical true plus nonempty replacement object or status `replaced`, `proven_current`, or `retired_no_longer_claimed`). Emit exactly one `phase-1-authoritative-transition-source-manifest` internal record whose payload has schema `phase1-authoritative-transition-source-manifest-v1` and canonical sorted rows containing source path, `json`/`toml` format, RFC-6901 object pointer, stable ID-field name/value, status-field name/value, canonical source-object SHA-256, and required category. Recompute this manifest directly from exact source files; no generated index field chooses membership. Require a global one-to-one mapping between every authoritative manifest row and one non-manifest internal record through `authoritative_source_key`; deleting either side, duplicating a binding, or omitting/changing source markers fails before classification.

Every internal record carries stable unique `id`, object `payload`, canonical `payload_sha256`, disclosure classification, evidence availability, limitations, derived `source_record_category`, boolean `transition_required`, and `source_record_sha256` computed from canonical UTF-8 JSON of the whole record with only `source_record_sha256` omitted. The internal top level is exactly `schema` plus `records`, with schema `phase1-internal-knowledge-index-v1`; each record has an exact category-specific schema, never unknown fields. Derive category from authoritative source facts using this fixed precedence, never an index-author choice: an `EDGE-*` Plan-01–05 frontmatter ID is `assumption`; a `PROHIB-*` ID is `prohibition`; an upstream-ID/state Cartesian member is `upstream_state`; a Phase-1 threat-register ID is `residual_threat`; an authoritative manifest blocker row is `blocker_source`; an authoritative manifest replacement row is `replacement_claim`; every remaining record is `informational`. Map those categories bijectively to `assumptions`, `prohibitions`, `upstream_states`, `residual_threats`, `blocker_sources`, `replacement_claims`, and `not_applicable` respectively; `transition_required` is true exactly for non-informational categories. `not_applicable` is therefore allowed only for derived informational records and cannot hide any authoritative blocker/replacement row. For every blocker/replacement packet row, copy the manifest's source format, object pointer, ID/status field names, canonical source-object digest, and authoritative source key; packet status/ID must equal the resolved source object's exact fields.

Add a distinct `phase-1-packet-row-source-manifest` internal record with schema `phase1-packet-row-source-manifest-v1`. It covers every transition-required record exactly once with sorted exact rows `record_id`, `source_path`, `source_format`, `object_pointer`, `source_object_sha256`, and `required_category`. Formats are only `json`, `toml`, or `text-slice`; JSON/TOML pointers resolve canonically, while a `text-slice` pointer is exactly `lines:&lt;start&gt;-&lt;end&gt;` over immutable E bytes and hashes those exact UTF-8 source lines including newlines. Category is taken only from this manifest row and must agree with the independently derived finite authority universe; packet fields copy this exact row. Add one `phase-1-transition-source-inventory` record whose payload has schema `phase1-transition-source-inventory-v1` and a sorted `sources` array covering every other internal record ID exactly once with its required mapped disposition and, only for informational rows, a nonempty reason. Derive, do not hand-select, mandatory rows: assumptions/prohibitions equal all `EDGE-*`/`PROHIB-*` frontmatter IDs in Plans 01-01 through 01-05; upstream-state rows equal the Cartesian product of every registry upstream ID and the seven lifecycle state keys; residual threats equal every threat ID in the Phase-1 threat register; blocker/replacement rows equal the authoritative manifest categories. For each residual threat, preserve exact nonempty source `severity`, `disposition`, `residual_risk`, and `owner`, plus exact source `status` when present. The exact closed severity enum is `low|medium|high|critical|unknown`; the exact closed status/disposition enum is `resolved|accepted|open|unresolved|unmitigated|blocked|unknown`. Define source nonblocking only when severity is exactly `low` or `medium`, disposition is exactly `resolved` or `accepted`, and absent optional status or present status is exactly `resolved` or `accepted`; source blocking is the exact complement. Thus unknown, out-of-enum, wrong-case, empty, or otherwise malformed values always block, and only explicit known-safe values can become bounded-P2 eligible. No generated index or later packet overlay may resolve or downgrade that source classification. Both manifest/internal-record and inventory/referenced-record relationships must be bijective: no orphan, duplicate, omitted source object, category/disposition mismatch, or packet row lacking authoritative source.

Add one stable `phase-1-review-prerequisite-inventory` internal record whose exact payload has schema `phase1-review-prerequisite-inventory-v1`, source path `evidence/phase-01/review-baseline.json`, exact source-byte SHA-256, and stage IDs exactly `local_coderabbit` and `github_codex` copied from that source's authoritative `stages` order. This fixed inventory—not any optional packet-row `category` or `classification` tag—is the sole membership authority for `review_prerequisites_valid`; it also binds the owner-authorized overlapping order `implementation_agents`, `push_exact_pr_sha`, `local_coderabbit_and_github_codex_may_overlap`, `join_both_dispositions`. Add one stable `phase-01-closeout-teaching-witness` prerequisite-template record with schema `phase1-closeout-teaching-v1`; nonempty planned decision/profile/negotiation/terminology/upstream/learning/education/visibility delta schemas; curated learning rows whose source bytes recompute their digest; and teaching-witness rows containing exact `id`, shell-free `argv`, expected exit/stdout/stderr identities, observed values, status, and retained evidence path/digest. Execute every argv with `subprocess.run(argv, shell=False, capture_output=True)` in the task-owned staging root and require captured exit/stdout/stderr to equal the row before accepting `pass`; a stored `status` never substitutes for execution. These existing internal-index records are bounded machine-verifiable closeout prerequisites/templates, not proof of the later actual human decision or completed teaching, so no sixth Task-2 file is added; Plan 06 must include the exact internal index in E.

Generate `uzel-m0.md` by one exact renderer: UTF-8 bytes `# Uzel M0 Compatibility Profile\n\nNon-normative projection.\n\nProfile-SHA256: &lt;sha256&gt;\n\n```toml\n` + the exact profile TOML bytes + `\n```\n`, rejecting a profile that already ends with a newline ambiguity by normalizing the renderer input to exactly one terminal newline. Validation recomputes the entire Markdown byte string; hash presence alone is insufficient. Validate all five task-owned files/cross-digests as one staging set, including package binding, exact positive/negative vector taxonomy, authoritative source manifests, transition inventory, review-prerequisite inventory, closeout witness, then install projections/SIR/vectors/internal index and publish immutable profile last. Once published, changes require new ID/transition. On failure publish no profile; retain checksummed diagnostics and restart only with identical inputs.</action>
  <verify>
    <automated>python3 -c 'import hashlib,json,os,re,tomllib; from pathlib import Path; c=tomllib.load(open("docs/runtime/capabilities/m0-index.toml","rb")); p=tomllib.load(open("docs/compat/profiles/uzel-m0.toml","rb")); b=p["package_binding"]; verdict_path=Path("evidence/phase-01/current-package-verdict.json"); verdict=json.loads(verdict_path.read_text()); verdict_sha=hashlib.sha256(verdict_path.read_bytes()).hexdigest(); hx=lambda x:isinstance(x,str) and re.fullmatch(r"[0-9a-f]{64}",x); ledger_sha=hashlib.sha256(Path("docs/runtime/capabilities/m0-index.toml").read_bytes()).hexdigest(); assert c.get("finalized") is True and isinstance(c.get("capability_set_id"),str) and c["capability_set_id"]; assert p["capability_ledger"]["capability_set_id"]==c["capability_set_id"] and p["capability_ledger"]["sha256"]==ledger_sha; assert b["source_profile_hash_scheme"]=="sha256-exact-source-profile-bytes-v1" and b["packaged_profile_hash_scheme"]=="sha256-exact-packaged-rcp-bytes-v1" and b["runtime_normalization_before_hash"]=="forbidden" and b["startup_mismatch"]=="fail_closed" and "source_profile_sha256" not in b; verified=b["status"]=="verified_packaged"; blocked=b["status"]=="blocked_not_yet_packaged"; assert verified or blocked; assert (not blocked) or (verdict["verdict"]=="not_yet_packaged" and verdict.get("declared_installable_outputs")==[] and b["plan_02_package_verdict_sha256"]==verdict_sha and b["implementation_owner"]=="phase_2" and all(k not in b for k in ("exact_packaged_profile_bytes_sha256","canonical_installed_rcp_path","installed_rcp_relative_path")) and b.get("required_evidence") and b.get("next_trigger")); path=Path(b.get("canonical_installed_rcp_path","/not-packaged")); assert (not verified) or (path.is_absolute() and path==path.resolve() and path.is_file() and not path.is_symlink() and hashlib.sha256(path.read_bytes()).hexdigest()==b["exact_packaged_profile_bytes_sha256"] and hx(b.get("installed_read_evidence_sha256")) and b.get("realized_store_path") and b.get("installed_rcp_relative_path"))' &amp;&amp; python3 -m json.tool docs/compat/negotiation-vectors.json &gt;/dev/null &amp;&amp; python3 -m json.tool docs/knowledge/index.internal.json &gt;/dev/null &amp;&amp; jq --arg profile_sha "$(sha256sum docs/compat/profiles/uzel-m0.toml | cut -d " " -f1)" -e 'all(.transcripts[]; (.profile_sha256==$profile_sha) and (.capability_set_id|type=="string") and (.capability_ledger_sha256|test("^[0-9a-f]{64}$")) and (((.package_rcp_status=="verified_packaged") and (.verified_package_rcp_sha256|test("^[0-9a-f]{64}$")) and (.package_rcp_digest_source=="installed_packaged_rcp_bytes")) or ((.package_rcp_status=="blocked_not_yet_packaged") and (has("verified_package_rcp_sha256")|not) and (.packaged_runtime_accepted==false))) and (.canonical_transcript_sha256|test("^[0-9a-f]{64}$"))) and all(.vectors[]|select(.polarity=="negative"); (.observed=="reject_before_guest") and (.guest_code_started==false) and (.exposed_domains|length==0) and (.mismatch_rejection_evidence_sha256==.evidence_sha256) and (.evidence_sha256|test("^[0-9a-f]{64}$")))' docs/compat/negotiation-vectors.json &gt;/dev/null &amp;&amp; jq -e '([.vectors[]|select(.polarity=="positive")|.kind]|sort) == (["exact_match","supported_floor","optional_capability","interoperability","clean_room","local_patch"]|sort) and ([.vectors[]|select(.polarity=="negative")|.kind]|sort) == (["profile_id_mismatch","profile_hash_mismatch","exact_build_mismatch","session_principal_mismatch","package_rcp_bytes_mismatch","package_rcp_digest_mismatch","package_diagnostics_mismatch","missing_manifest","malformed_manifest","incompatible_required_capability","implicit_required_downgrade","mid_session_profile_replacement","stale_transcript","unsupported_version_skew","local_patch_divergence"]|sort) and all(.vectors[]; .polarity=="positive" or .polarity=="negative")' docs/compat/negotiation-vectors.json &gt;/dev/null &amp;&amp; python3 -c 'import hashlib,json,re,tomllib; from pathlib import Path; profile=tomllib.load(open("docs/compat/profiles/uzel-m0.toml","rb")); binding=profile["package_binding"]; enc=lambda x:json.dumps(x,sort_keys=True,separators=(",",":"),ensure_ascii=False).encode();
def keys(x):
 return ([k for k,v in x.items()]+[y for v in x.values() for y in keys(v)]) if isinstance(x,dict) else ([y for v in x for y in keys(v)] if isinstance(x,list) else [])
assert "profile_sha256" not in keys(profile); verdict_path=Path("evidence/phase-01/current-package-verdict.json"); verdict=json.loads(verdict_path.read_text())
if binding["status"]=="verified_packaged":
 checkout=Path.cwd().resolve(); staging=Path(binding["task_owned_staging_root"]); realized=Path(verdict["realized_store_path"]); installed=Path(binding["canonical_installed_rcp_path"]); relative=Path(binding["installed_rcp_relative_path"]); assert verdict["verdict"]=="packaged" and staging.is_absolute() and staging==staging.resolve() and realized.is_absolute() and realized==realized.resolve() and realized.exists() and relative!=Path() and not relative.is_absolute() and ".." not in relative.parts and installed.is_absolute() and installed==installed.resolve()==(realized/relative).resolve() and installed.is_file() and not installed.is_symlink() and installed.is_relative_to(realized) and checkout not in installed.parents and staging not in installed.parents; assert binding["realized_store_path"]==verdict["realized_store_path"] and binding["plan_02_declared_installable_outputs"]==verdict["declared_installable_outputs"] and binding["plan_02_build_evidence_sha256"]==verdict["build_evidence_sha256"] and binding["plan_02_executable_discovery_evidence_sha256"]==verdict["executable_discovery_evidence_sha256"] and binding["plan_02_clean_environment_probe_evidence_sha256"]==verdict["clean_environment_probe_evidence_sha256"] and binding["plan_02_package_verdict_sha256"]==hashlib.sha256(verdict_path.read_bytes()).hexdigest() and binding["exact_packaged_profile_bytes_sha256"]==hashlib.sha256(installed.read_bytes()).hexdigest()
elif binding["status"]=="blocked_not_yet_packaged":
 assert verdict["verdict"]=="not_yet_packaged" and verdict.get("declared_installable_outputs")==[] and binding["plan_02_package_verdict_sha256"]==hashlib.sha256(verdict_path.read_bytes()).hexdigest() and all(k not in binding for k in ("exact_packaged_profile_bytes_sha256","canonical_installed_rcp_path","installed_rcp_relative_path"))
else: raise AssertionError("invalid package binding status")
d=json.load(open("docs/compat/negotiation-vectors.json")); profile_sha=hashlib.sha256(Path("docs/compat/profiles/uzel-m0.toml").read_bytes()).hexdigest()
transcript_ids=[t["transcript_id"] for t in d["transcripts"]]; assert transcript_ids and len(transcript_ids)==len(set(transcript_ids)) and d["vectors"] and all(v.get("transcript_id") in transcript_ids for v in d["vectors"])
for t in d["transcripts"]:
 facts={k:t[k] for k in ("negotiation_input","negotiation_outcome","admission_facts")}; assert all(isinstance(v,dict) for v in facts.values()) and t["canonical_transcript_sha256"]==hashlib.sha256(enc(facts)).hexdigest() and t["profile_sha256"]==profile_sha
idx=json.load(open("docs/knowledge/index.internal.json")); records=idx["records"]; assert all(r["source_record_sha256"]==hashlib.sha256(enc({k:v for k,v in r.items() if k!="source_record_sha256"})).hexdigest() for r in records); by_id={r["id"]:r for r in records}; assert len(by_id)==len(records); packet_names={"blocker_sources","replacement_claims","assumptions","prohibitions","residual_threats","upstream_states"}; inventories=[r for r in records if r["id"]=="phase-1-transition-source-inventory"]; assert len(inventories)==1; inv=inventories[0]["payload"]; assert set(inv)=={"schema","sources"} and inv["schema"]=="phase1-transition-source-inventory-v1" and isinstance(inv["sources"],list); source_ids=[x["record_id"] for x in inv["sources"]]; expected_ids=set(by_id)-{"phase-1-transition-source-inventory"}; assert source_ids==sorted(expected_ids) and len(source_ids)==len(set(source_ids)); dispositions=packet_names|{"not_applicable"}; assert all(set(x)==({"record_id","disposition"} if x["disposition"] in packet_names else {"record_id","disposition","reason"}) and x["disposition"] in dispositions and (x["disposition"]!="not_applicable" or isinstance(x["reason"],str) and x["reason"]) for x in inv["sources"]); plans=[Path(f".planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-0{i}-PLAN.md").read_text().split("---",2)[1] for i in range(1,6)]; assumption_ids={x for text in plans for x in re.findall(r"(?m)^\s+- id: (EDGE-[A-Z0-9-]+)$",text)}; prohibition_ids={x for text in plans for x in re.findall(r"(?m)^\s+- id: (PROHIB-[A-Z0-9-]+)$",text)}; upstream=tomllib.load(open("docs/ecosystem/upstreams.toml","rb"))["upstream_records"]; lifecycle={"local_patch","thread","pr_accepted","pr_merged","release_contains_change","uzel_adoption","patch_removal"}; upstream_ids={str(u.get("id"))+":"+s for u in upstream for s in lifecycle}; structure=json.load(open("docs/architecture/phase-01-structure.json")); threat_ids={x["id"] for x in structure["threats"]}; category_to_disposition={"assumption":"assumptions","prohibition":"prohibitions","upstream_state":"upstream_states","residual_threat":"residual_threats","blocker_source":"blocker_sources","replacement_claim":"replacement_claims","informational":"not_applicable"}
def objects(x):
 if isinstance(x,dict):
  yield x
  for v in x.values(): yield from objects(v)
 elif isinstance(x,list):
  for v in x: yield from objects(v)
def derived_category(record):
 rid=record["id"]; nodes=list(objects(record["payload"])); blocker=any(x.get("phase1_blocker") is True or x.get("blocking") is True or x.get("classification")=="phase_2_blocker" or isinstance(x.get("blocker"),dict) and bool(x["blocker"]) for x in nodes); replacement=any(x.get("critical") is True and (isinstance(x.get("replacement"),dict) and bool(x["replacement"]) or x.get("status") in ("replaced","proven_current","retired_no_longer_claimed")) for x in nodes); return "assumption" if rid in assumption_ids else "prohibition" if rid in prohibition_ids else "upstream_state" if rid in upstream_ids else "residual_threat" if rid in threat_ids else "blocker_source" if blocker else "replacement_claim" if replacement else "informational"
packet_records=[]
for item in inv["sources"]:
 record=by_id[item["record_id"]]; payload=record["payload"]; category=derived_category(record); expected=category_to_disposition[category]; assert record.get("source_record_category")==category and record.get("transition_required") is (category!="informational") and item["disposition"]==expected
 if expected in packet_names:
  row=payload["packet_row"]; assert row["id"]==record["id"]; packet_records.append((expected,row))
assert {r["id"] for n,r in packet_records if n=="assumptions"}==assumption_ids and {r["id"] for n,r in packet_records if n=="prohibitions"}==prohibition_ids; assert {r["id"] for n,r in packet_records if n=="upstream_states"}==upstream_ids; assert {r["id"] for n,r in packet_records if n=="residual_threats"}==threat_ids; threats=structure["threats"]; severity_enum={"low","medium","high","critical","unknown"}; state_enum={"resolved","accepted","open","unresolved","unmitigated","blocked","unknown"}; assert all(all(isinstance(x.get(k),str) and x[k].strip() for k in ("severity","disposition","residual_risk","owner")) and x["severity"] in severity_enum and x["disposition"] in state_enum and ("status" not in x or isinstance(x["status"],str) and x["status"] in state_enum) for x in threats); threat_by={x["id"]:x for x in threats}; residual={r["id"]:r for n,r in packet_records if n=="residual_threats"}; fields=("severity","disposition","residual_risk","owner"); assert set(residual)==set(threat_by) and all(all(residual[i].get(k)==threat_by[i][k] for k in fields) and ("status" not in threat_by[i] or residual[i].get("status")==threat_by[i]["status"]) for i in threat_by); nonblocking=lambda x:x.get("severity") in {"low","medium"} and x.get("disposition") in {"resolved","accepted"} and ("status" not in x or x.get("status") in {"resolved","accepted"}); blocking=lambda x:not nonblocking(x); assert {x["id"] for x in threats if blocking(x)}=={i for i,x in residual.items() if blocking(x)}; assert {n for n,_ in packet_records}==packet_names; close=[r for r in records if r["id"]=="phase-01-closeout-teaching-witness"]; assert len(close)==1; cp=close[0]["payload"]; delta_keys=("decision_deltas","profile_deltas","negotiation_deltas","terminology_deltas","upstream_deltas","learning_deltas","education_deltas","visibility_deltas"); assert cp["closeout_schema"]=="phase1-closeout-teaching-v1" and all(isinstance(cp[k],list) and cp[k] for k in delta_keys) and isinstance(cp["curated_learnings"],list) and cp["curated_learnings"] and all(x.get("id") and x.get("source") and re.fullmatch(r"[0-9a-f]{64}",x.get("evidence_sha256","")) and x.get("lesson") for x in cp["curated_learnings"]) and isinstance(cp["teaching_witnesses"],list) and cp["teaching_witnesses"] and all(x.get("command") and x.get("expected") and x.get("observed") and x.get("status") in ("pass","fail","blocked") and re.fullmatch(r"[0-9a-f]{64}",x.get("evidence_sha256","")) for x in cp["teaching_witnesses"])' &amp;&amp; rg -q 'SIR-0001' docs/specs/interpretations/SIR-0001-manifest-build-identity.md &amp;&amp; rg -q "$(sha256sum docs/compat/profiles/uzel-m0.toml | cut -d " " -f1)" docs/compat/generated/uzel-m0.md</automated>
    <automated>python3 -c 'import hashlib,json,pathlib,subprocess,tomllib; b=tomllib.load(open("docs/compat/profiles/uzel-m0.toml","rb"))["package_binding"]; reg={x["artifact_id"]:x for x in tomllib.load(open("docs/runtime/capabilities/m0-index.toml","rb"))["evidence_registry"]}
if b["status"]=="verified_packaged":
 i=json.load(open("evidence/phase-01/current-output-inventory.json")); o=i["declared_installable_outputs"]; root=pathlib.Path.cwd().resolve(); assert len(o)==1; argv=["nix","eval","--offline","--raw","--no-write-lock-file",f"git+file://{root}?rev={i['"'"'source_head'"'"']}#{o[0]}.passthru.uzelRcpRelativePath"]; run=subprocess.run(argv,capture_output=True); assert run.returncode==0; raw=run.stdout; rel=raw.decode(); rp=pathlib.Path(rel); out_id=b["source_rcp_path_eval_stdout_artifact_id"]; err_id=b["source_rcp_path_eval_stderr_artifact_id"]; assert out_id in reg and err_id in reg and pathlib.Path(reg[out_id]["path"]).read_bytes()==run.stdout and pathlib.Path(reg[err_id]["path"]).read_bytes()==run.stderr and hashlib.sha256(run.stdout).hexdigest()==reg[out_id]["sha256"]==b["source_rcp_path_eval_sha256"] and hashlib.sha256(run.stderr).hexdigest()==reg[err_id]["sha256"]; assert rel and not rp.is_absolute() and ".." not in rp.parts and b["source_rcp_path_authority"]=="nix-output-metadata:uzelRcpRelativePath" and b["source_rcp_path_source_head"]==i["source_head"] and b["source_rcp_path_output_attribute"]==o[0] and b["source_rcp_path_eval_argv"]==argv and b["source_rcp_path_eval_exit_status"]==run.returncode==0 and b["installed_rcp_relative_path"]==rel and pathlib.Path(b["canonical_installed_rcp_path"]).resolve()==(pathlib.Path(b["realized_store_path"])/rp).resolve()'</automated>
    <automated>python3 -c 'import hashlib,json,pathlib,re,tomllib; idx=json.load(open("docs/knowledge/index.internal.json")); records=idx["records"]; ms=[r for r in records if r["id"]=="phase-1-authoritative-transition-source-manifest"]; assert len(ms)==1 and ms[0]["payload"]["schema"]=="phase1-authoritative-transition-source-manifest-v1"; actual=ms[0]["payload"]["rows"]; phase=pathlib.Path(".planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam"); paths=set()
for n in range(1,6):
 front=(phase/f"01-0{n}-PLAN.md").read_text().split("---",2)[1]; paths.update(x.strip()[2:].strip().strip("\"") for x in re.search(r"(?m)^files_modified:\n((?:  - .+\n)+)",front).group(1).splitlines())
paths.discard("docs/knowledge/index.internal.json"); ids=("id","claim_id","row_id","threat_id","upstream_id"); statuses=("status","verdict","disposition","classification"); enc=lambda x:json.dumps(x,sort_keys=True,separators=(",",":"),ensure_ascii=False).encode(); esc=lambda x:str(x).replace("~","~0").replace("/","~1")
def walk(x,p=""):
 if isinstance(x,dict):
  yield p,x
  for k,v in x.items(): yield from walk(v,p+"/"+esc(k))
 elif isinstance(x,list):
  for n,v in enumerate(x): yield from walk(v,p+"/"+str(n))
expected=[]
for source in sorted(paths):
 path=pathlib.Path(source); suffix=path.suffix.lower()
 if suffix not in (".json",".toml"): continue
 data=json.load(open(path)) if suffix==".json" else tomllib.load(open(path,"rb"))
 for pointer,obj in walk(data):
  idf=next((k for k in ids if isinstance(obj.get(k),(str,int))),None); sf=next((k for k in statuses if isinstance(obj.get(k),(str,int,bool))),None); blocker=obj.get("phase1_blocker") is True or obj.get("blocking") is True or obj.get("classification")=="phase_2_blocker" or isinstance(obj.get("blocker"),dict) and bool(obj["blocker"]); replacement=obj.get("critical") is True and (isinstance(obj.get("replacement"),dict) and bool(obj["replacement"]) or obj.get("status") in ("replaced","proven_current","retired_no_longer_claimed")); marked=blocker or replacement
  if marked:
   assert idf is not None and sf is not None, ("marked transition source lacks required stable ID/status",source,pointer); expected.append({"authoritative_source_key":source+"#"+pointer,"source_path":source,"source_format":suffix[1:],"object_pointer":pointer,"id_field":idf,"id_value":obj[idf],"status_field":sf,"status_value":obj[sf],"source_object_sha256":hashlib.sha256(enc(obj)).hexdigest(),"required_category":"blocker_source" if blocker else "replacement_claim"})
expected.sort(key=lambda x:x["authoritative_source_key"]); assert actual==expected and len(actual)==len({x["authoritative_source_key"] for x in actual}); bound=sorted(r["authoritative_source_key"] for r in records if r.get("source_record_category") in ("blocker_source","replacement_claim")); assert bound==[x["authoritative_source_key"] for x in expected] and len(bound)==len(set(bound))'</automated>
    <automated>python3 -c 'import hashlib,json,re,tomllib; from pathlib import Path
def strict(pairs):
 d={}; [(_ for _ in ()).throw(ValueError("duplicate JSON key: "+k)) if k in d else d.__setitem__(k,v) for k,v in pairs]; return d
identity=Path("docs/architecture/phase-01-identity.md").read_text(); blocks=re.findall(r"```phase1-identity-inventory-v1\n([^\n]+)\n```",identity); assert len(blocks)==1; inv=json.loads(blocks[0],object_pairs_hook=strict,parse_constant=lambda x:(_ for _ in ()).throw(ValueError("non-finite JSON constant: "+x))); assert set(inv)=={"schema","identities"} and inv["schema"]=="phase1-identity-inventory-v1" and blocks[0]==json.dumps(inv,sort_keys=True,separators=(",",":"),ensure_ascii=False); citations=json.load(open("evidence/phase-01/current-source-citations.json")); claim_ids={x["id"] for x in citations["claims"]}; identities=inv["identities"]; ids=[x["identity_id"] for x in identities]; assert ids==sorted(set(ids)) and all(set(x)=={"identity_id","kind","status","source_claim_ids"} and x["kind"] in ("principal","actor","role") and isinstance(x["status"],str) and x["status"] and x["source_claim_ids"]==sorted(set(x["source_claim_ids"])) and x["source_claim_ids"] and set(x["source_claim_ids"])&lt;=claim_ids for x in identities); ledger=tomllib.load(open("docs/runtime/capabilities/m0-index.toml","rb")); profile=tomllib.load(open("docs/compat/profiles/uzel-m0.toml","rb")); caps=ledger["capability_definitions"]; cap_ids=[x["capability_id"] for x in caps]; assert cap_ids==sorted(set(cap_ids)) and all(isinstance(x.get("source_claim_ids"),list) and x["source_claim_ids"]==sorted(set(x["source_claim_ids"])) and x["source_claim_ids"] and set(x["source_claim_ids"])&lt;=claim_ids for x in caps); expected=sorted((c,i) for c in cap_ids for i in ids); matrix=ledger["capability_matrix"]; actual=sorted((x["capability_id"],x["identity_id"]) for x in matrix); assert actual==expected and len(actual)==len(set(actual)); digest=hashlib.sha256(json.dumps(matrix,sort_keys=True,separators=(",",":"),ensure_ascii=False).encode()).hexdigest(); assert profile["capability_contract"]["capability_ids"]==cap_ids and profile["capability_contract"]["identity_ids"]==ids and profile["capability_contract"]["matrix_sha256"]==digest'</automated>
    <automated>python3 -c 'import json,re,tomllib; from pathlib import Path
def strict(pairs):
 d={}; [(_ for _ in ()).throw(ValueError("duplicate JSON key: "+k)) if k in d else d.__setitem__(k,v) for k,v in pairs]; return d
raw=Path("evidence/phase-01/current-source-citations.json").read_bytes(); src=json.loads(raw,object_pairs_hook=strict,parse_constant=lambda x:(_ for _ in ()).throw(ValueError("non-finite JSON constant: "+x))); claims=src["claims"]; expected_caps=sorted("source-claim/"+x["id"] for x in claims if x.get("citations") and not x.get("explicit_absence")); assert expected_caps and len(expected_caps)==len(set(expected_caps)); text=Path("docs/architecture/phase-01-identity.md").read_text(); block=re.findall(r"```phase1-identity-inventory-v1\n([^\n]+)\n```",text); assert len(block)==1; inv=json.loads(block[0],object_pairs_hook=strict,parse_constant=lambda x:(_ for _ in ()).throw(ValueError("non-finite JSON constant: "+x))); expected_ids=sorted(("view-identity","viewed-subject","signing-identity","app-instance","profile","runtime","project","workbench-state","actor","role","signer")); actual_ids=[x["identity_id"] for x in inv["identities"]]; assert actual_ids==expected_ids; ledger=tomllib.load(open("docs/runtime/capabilities/m0-index.toml","rb")); actual_caps=[x["capability_id"] for x in ledger["capability_definitions"]]; assert actual_caps==expected_caps; matrix={(x["capability_id"],x["identity_id"]) for x in ledger["capability_matrix"]}; assert matrix=={(c,i) for c in expected_caps for i in expected_ids} and len(ledger["capability_matrix"])==len(matrix); profile=tomllib.load(open("docs/compat/profiles/uzel-m0.toml","rb"))["capability_contract"]; assert profile["capability_ids"]==expected_caps and profile["identity_ids"]==expected_ids'</automated>
    <automated>python3 -c 'import hashlib,json,pathlib,tomllib; idx=json.load(open("docs/knowledge/index.internal.json")); records=idx["records"]; by_id={x["id"]:x for x in records}; manifest=[x for x in records if x["id"]=="phase-1-authoritative-transition-source-manifest"][0]["payload"]["rows"]; mm={x["authoritative_source_key"]:x for x in manifest}; cache={}; enc=lambda x:json.dumps(x,sort_keys=True,separators=(",",":"),ensure_ascii=False).encode()
def unesc(x): return x.replace("~1","/").replace("~0","~")
def resolve(source,fmt,pointer):
 key=(source,fmt)
 if key not in cache: cache[key]=json.load(open(source)) if fmt=="json" else tomllib.load(open(source,"rb"))
 obj=cache[key]
 for token in pointer.split("/")[1:]: token=unesc(token); obj=obj[int(token)] if isinstance(obj,list) else obj[token]
 return obj
for record in records:
 category=record.get("source_record_category")
 if category in ("blocker_source","replacement_claim"):
  m=mm[record["authoritative_source_key"]]; row=record["payload"]["packet_row"]; obj=resolve(m["source_path"],m["source_format"],m["object_pointer"]); exact={"source":m["source_path"],"source_format":m["source_format"],"object_pointer":m["object_pointer"],"id_field":m["id_field"],"status_field":m["status_field"],"source_object_sha256":m["source_object_sha256"],"authoritative_source_key":m["authoritative_source_key"],"required_category":m["required_category"]}; assert all(row.get(k)==v for k,v in exact.items()) and row["id"]==m["id_value"]==obj[m["id_field"]] and row["status"]==m["status_value"]==obj[m["status_field"]] and m["required_category"]==category and hashlib.sha256(enc(obj)).hexdigest()==m["source_object_sha256"]
 elif category=="informational":
  info=record.get("informational_source"); assert isinstance(info,dict) and set(info)=={"source_path","source_format","object_pointer","source_object_sha256"}; obj=resolve(info["source_path"],info["source_format"],info["object_pointer"]); assert hashlib.sha256(enc(obj)).hexdigest()==info["source_object_sha256"]
 else: assert category in ("assumption","prohibition","upstream_state","residual_threat")'</automated>
    <automated>python3 -c 'import json,pathlib,re
def strict(pairs):
 d={}; [(_ for _ in ()).throw(ValueError("duplicate JSON key: "+k)) if k in d else d.__setitem__(k,v) for k,v in pairs]; return d
phase=pathlib.Path(".planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam"); paths=set()
for n in range(1,6):
 front=(phase/f"01-0{n}-PLAN.md").read_text().split("---",2)[1]; paths.update(x.strip()[2:].strip().strip("\"") for x in re.search(r"(?m)^files_modified:\n((?:  - .+\n)+)",front).group(1).splitlines())
for path in sorted(pathlib.Path(x) for x in paths if pathlib.Path(x).suffix.lower()==".json"):
 json.loads(path.read_bytes(),object_pairs_hook=strict,parse_constant=lambda x:(_ for _ in ()).throw(ValueError("non-finite JSON constant: "+x)))'</automated>
    <automated>python3 -c 'import hashlib,json; raw=open("evidence/phase-01/review-baseline.json","rb").read(); review=json.loads(raw); idx=json.load(open("docs/knowledge/index.internal.json")); records=[r for r in idx["records"] if r["id"]=="phase-1-review-prerequisite-inventory"]; assert len(records)==1; payload=records[0]["payload"]; assert set(payload)=={"schema","source_path","source_sha256","stage_ids"} and payload["schema"]=="phase1-review-prerequisite-inventory-v1" and payload["source_path"]=="evidence/phase-01/review-baseline.json" and payload["source_sha256"]==hashlib.sha256(raw).hexdigest() and payload["stage_ids"]==review["stages"]==["local_coderabbit","github_codex"] and review["order"]==["implementation_agents","push_exact_pr_sha","local_coderabbit_and_github_codex_may_overlap","join_both_dispositions"]'</automated>
    <automated>python3 -c 'import hashlib,json,pathlib,re,stat,tomllib
root=pathlib.Path.cwd().resolve(); profile_path=pathlib.Path("docs/compat/profiles/uzel-m0.toml"); profile_raw=profile_path.read_bytes().rstrip(b"\n")+b"\n"; profile_sha=hashlib.sha256(profile_raw).hexdigest(); expected=b"# Uzel M0 Compatibility Profile\n\nNon-normative projection.\n\nProfile-SHA256: "+profile_sha.encode()+b"\n\n```toml\n"+profile_raw+b"```\n"; assert pathlib.Path("docs/compat/generated/uzel-m0.md").read_bytes()==expected; profile=tomllib.loads(profile_raw.decode()); ledger=tomllib.load(open("docs/runtime/capabilities/m0-index.toml","rb")); data=json.load(open("docs/compat/negotiation-vectors.json")); tids=[x["transcript_id"] for x in data["transcripts"]]; assert tids==sorted(set(tids)); required={"profile_id","profile_sha256","exact_build_identity_sha256","capability_set_id","capability_ledger_sha256","session_principals","instance_id","local_profile_id","session_id","generation","package_rcp_status"}
for t in data["transcripts"]:
 facts={k:t[k] for k in ("negotiation_input","negotiation_outcome","admission_facts")}; values={}
 for key in required:
  found=[obj[key] for obj in facts.values() if key in obj]; assert len(found)==1; values[key]=found[0]
 assert values["profile_sha256"]==profile_sha and values["capability_set_id"]==ledger["capability_set_id"] and values["capability_ledger_sha256"]==hashlib.sha256(pathlib.Path("docs/runtime/capabilities/m0-index.toml").read_bytes()).hexdigest() and re.fullmatch(r"[0-9a-f]{64}",values["exact_build_identity_sha256"]) and isinstance(values["session_principals"],list) and values["session_principals"] and all(isinstance(x,str) and x for x in values["session_principals"]) and type(values["generation"]) is int and values["generation"]&gt;0; assert all(k not in t or t[k]==v for k,v in values.items())
neg=[x for x in data["vectors"] if x["polarity"]=="negative"]; assert neg
for x in neg:
 assert x["observed"]=="reject_before_guest" and x["guest_code_started"] is False and x["exposed_domains"]==[] and re.fullmatch(r"[0-9a-f]{64}",x["evidence_sha256"]); rel=pathlib.PurePosixPath(x["evidence_path"]); assert not rel.is_absolute() and ".." not in rel.parts; cur=root
 for part in rel.parts: cur=cur/part; assert not stat.S_ISLNK(cur.lstat().st_mode)
 assert cur.is_file() and stat.S_ISREG(cur.stat().st_mode) and hashlib.sha256(cur.read_bytes()).hexdigest()==x["evidence_sha256"]'</automated>
    <automated>python3 -c 'import base64,hashlib,json,pathlib,re,subprocess,tomllib
def strict(pairs):
 d={}; [(_ for _ in ()).throw(ValueError("duplicate JSON key: "+k)) if k in d else d.__setitem__(k,v) for k,v in pairs]; return d
data=json.load(open("docs/compat/negotiation-vectors.json"),object_pairs_hook=strict,parse_constant=lambda q:(_ for _ in ()).throw(ValueError("non-finite JSON constant: "+q))); transcripts={x["transcript_id"] for x in data["transcripts"]}; registry={x["artifact_id"] for x in tomllib.load(open("docs/runtime/capabilities/m0-index.toml","rb"))["evidence_registry"]}; top={"receipt_schema","test_id","argv","cwd","input_artifact_ids","vector_id","queue_id","transcript_id","exit_status","status","stdout_base64","stdout_sha256","stderr_base64","stderr_sha256","guest_code_started","exposed_domains","observed_result"}; shells={"sh","bash","zsh","fish","cmd","powershell","pwsh"}
for x in data["vectors"]:
 assert x["transcript_id"] in transcripts and isinstance(x["argv"],list) and x["argv"] and x["input_artifact_ids"]==sorted(set(x["input_artifact_ids"])) and x["input_artifact_ids"] and set(x["input_artifact_ids"])&lt;=registry and re.fullmatch(r"[0-9a-f]{64}",x["execution_receipt_sha256"]); r=x["execution_receipt"]; raw=json.dumps(r,sort_keys=True,separators=(",",":"),ensure_ascii=False).encode(); cwd=pathlib.Path(r["cwd"]); stdout=base64.b64decode(r["stdout_base64"],validate=True); stderr=base64.b64decode(r["stderr_base64"],validate=True); assert cwd.is_absolute() and cwd.is_dir() and cwd==cwd.resolve() and all(not p.is_symlink() for p in (cwd,*cwd.parents)); assert hashlib.sha256(raw).hexdigest()==x["execution_receipt_sha256"] and set(r)==top and r["receipt_schema"]=="phase1-vector-execution-receipt-v2" and r["test_id"]==r["vector_id"]==x["vector_id"] and r["queue_id"]=="negotiation" and r["transcript_id"]==x["transcript_id"] and r["argv"]==x["argv"] and all(isinstance(a,str) and a for a in r["argv"]) and pathlib.Path(r["argv"][0]).name not in shells and r["input_artifact_ids"]==x["input_artifact_ids"] and type(r["exit_status"]) is int and r["status"] in ("pass","fail","blocked") and r["stdout_sha256"]==hashlib.sha256(stdout).hexdigest() and r["stderr_sha256"]==hashlib.sha256(stderr).hexdigest() and type(r["guest_code_started"]) is bool and r["exposed_domains"]==sorted(set(r["exposed_domains"])) and all(isinstance(v,str) and v for v in r["exposed_domains"]) and r["observed_result"]==x["observed"] and r["guest_code_started"]==x["guest_code_started"] and r["exposed_domains"]==x["exposed_domains"] and r["status"]=="pass" and r["exit_status"]==0; run=subprocess.run(r["argv"],cwd=r["cwd"],shell=False,capture_output=True); assert run.returncode==r["exit_status"] and run.stdout==stdout and run.stderr==stderr
 assert x["polarity"]!="negative" or (r["observed_result"]=="reject_before_guest" and r["guest_code_started"] is False and r["exposed_domains"]==[])'</automated>
    <automated>python3 -c 'import hashlib,json,pathlib,re,tomllib
def strict(pairs):
 d={}; [(_ for _ in ()).throw(ValueError("duplicate JSON key: "+k)) if k in d else d.__setitem__(k,v) for k,v in pairs]; return d
p=pathlib.Path("docs/specs/interpretations/SIR-0001-manifest-build-identity.md"); text=p.read_text(); blocks=re.findall(r"```phase1-sir-v1\n([^\n]+)\n```",text); assert len(blocks)==1; s=json.loads(blocks[0],object_pairs_hook=strict,parse_constant=lambda q:(_ for _ in ()).throw(ValueError("non-finite JSON constant: "+q))); assert blocks[0]==json.dumps(s,sort_keys=True,separators=(",",":"),ensure_ascii=False); top={"schema","sir_id","title","status","source_records","interpretations","stakeholders","dissent","decision","evidence","compatibility_effects","rollback_plan","human_authority","revisit_trigger","phase_2_gate"}; assert set(s)==top and s["schema"]=="phase1-sir-v1" and s["sir_id"]=="SIR-0001" and s["title"] and s["status"] in ("accepted","provisional","blocked") and s["human_authority"] and s["phase_2_gate"]=="bounded_non_normative_interpretation"; enc=lambda x:json.dumps(x,sort_keys=True,separators=(",",":"),ensure_ascii=False).encode(); source_ids=[]
for x in s["source_records"]:
 assert set(x)=={"source_id","path","format","object_pointer","source_object_sha256"} and x["format"] in ("json","toml","text-slice"); raw=pathlib.Path(x["path"]).read_bytes(); obj=json.loads(raw) if x["format"]=="json" else tomllib.loads(raw.decode()) if x["format"]=="toml" else b"".join(raw.splitlines(keepends=True)[int(re.fullmatch(r"lines:(\d+)-(\d+)",x["object_pointer"]).group(1))-1:int(re.fullmatch(r"lines:(\d+)-(\d+)",x["object_pointer"]).group(2))]);
 if x["format"]!="text-slice":
  for token in x["object_pointer"].split("/")[1:]: obj=obj[int(token)] if isinstance(obj,list) else obj[token.replace("~1","/").replace("~0","~")]
 assert hashlib.sha256(obj if isinstance(obj,bytes) else enc(obj)).hexdigest()==x["source_object_sha256"]; source_ids.append(x["source_id"])
assert source_ids==sorted(set(source_ids)) and source_ids and isinstance(s["dissent"],list) and s["dissent"] and isinstance(s["evidence"],list) and s["evidence"]; ids=[x["interpretation_id"] for x in s["interpretations"]]; assert ids==sorted(set(ids)) and len(ids)&gt;=2 and sum(x["selected"] is True for x in s["interpretations"])==1
for x in s["interpretations"]: assert set(x)=={"interpretation_id","claim","supporting_source_ids","opposing_source_ids","compatibility_effect","rollback_effect","selected"} and x["claim"] and x["supporting_source_ids"]==sorted(set(x["supporting_source_ids"])) and set(x["supporting_source_ids"]+x["opposing_source_ids"])&lt;=set(source_ids) and type(x["selected"]) is bool
d=s["decision"]; assert set(d)=={"selected_interpretation_id","chosen_ids","unchosen_ids","rationale","confidence"} and d["selected_interpretation_id"]==next(x["interpretation_id"] for x in s["interpretations"] if x["selected"]) and d["chosen_ids"]==[d["selected_interpretation_id"]] and sorted(d["chosen_ids"]+d["unchosen_ids"])==ids and d["confidence"] in ("low","medium","high") and d["rationale"]; assert all(set(x)=={"stakeholder","interpretation_id","rationale","disposition"} and x["interpretation_id"] in ids and x["rationale"] and x["disposition"] in ("retained","resolved","deferred") for x in s["dissent"]); assert s["stakeholders"]==sorted(set(s["stakeholders"])) and s["stakeholders"] and set(x["stakeholder"] for x in s["dissent"])&lt;=set(s["stakeholders"]); assert all(set(x)=={"path","sha256"} and hashlib.sha256(pathlib.Path(x["path"]).read_bytes()).hexdigest()==x["sha256"] for x in s["evidence"]); assert isinstance(s["compatibility_effects"],list) and s["compatibility_effects"] and s["rollback_plan"] and set(s["revisit_trigger"])=={"owner","condition"} and all(s["revisit_trigger"].values())'</automated>
    <automated>python3 -c 'import hashlib,json,pathlib,re,tomllib
idx=json.load(open("docs/knowledge/index.internal.json")); assert set(idx)=={"schema","records"} and idx["schema"]=="phase1-internal-knowledge-index-v1"; records=idx["records"]; by={x["id"]:x for x in records}; assert len(by)==len(records); enc=lambda x:json.dumps(x,sort_keys=True,separators=(",",":"),ensure_ascii=False).encode(); base={"id","payload","payload_sha256","disclosure","evidence_availability","limitations","source_record_category","transition_required","source_record_sha256"}; categories={"assumption","prohibition","upstream_state","residual_threat","blocker_source","replacement_claim","informational"}
for x in records:
 extra={"authoritative_source_key"} if x["source_record_category"] in ("blocker_source","replacement_claim") else ({"informational_source"} if x["source_record_category"]=="informational" else set()); assert set(x)==base|extra and x["source_record_category"] in categories and x["disclosure"] in ("public","redacted","internal","embargoed") and isinstance(x["payload"],dict) and isinstance(x["limitations"],list) and isinstance(x["evidence_availability"],str) and x["evidence_availability"] and x["transition_required"] is (x["source_record_category"]!="informational") and x["payload_sha256"]==hashlib.sha256(enc(x["payload"])).hexdigest() and x["source_record_sha256"]==hashlib.sha256(enc({k:v for k,v in x.items() if k!="source_record_sha256"})).hexdigest()
mr=by["phase-1-packet-row-source-manifest"]; assert mr["payload"]["schema"]=="phase1-packet-row-source-manifest-v1"; rows=mr["payload"]["rows"]; assert [x["record_id"] for x in rows]==sorted(x["id"] for x in records if x["transition_required"]); mm={x["record_id"]:x for x in rows}; assert len(mm)==len(rows)
def unesc(x): return x.replace("~1","/").replace("~0","~")
for rid,m in mm.items():
 assert set(m)=={"record_id","source_path","source_format","object_pointer","source_object_sha256","required_category"} and m["required_category"]==by[rid]["source_record_category"] and m["source_format"] in ("json","toml","text-slice") and re.fullmatch(r"[0-9a-f]{64}",m["source_object_sha256"]); raw=pathlib.Path(m["source_path"]).read_bytes()
 if m["source_format"]=="text-slice":
  z=re.fullmatch(r"lines:(\d+)-(\d+)",m["object_pointer"]); assert z and int(z.group(1))&gt;0 and int(z.group(2))&gt;=int(z.group(1)); obj=b"".join(raw.splitlines(keepends=True)[int(z.group(1))-1:int(z.group(2))]); digest=hashlib.sha256(obj).hexdigest()
 else:
  obj=json.loads(raw) if m["source_format"]=="json" else tomllib.loads(raw.decode())
  for token in m["object_pointer"].split("/")[1:]: token=unesc(token); obj=obj[int(token)] if isinstance(obj,list) else obj[token]
  digest=hashlib.sha256(enc(obj)).hexdigest()
 assert digest==m["source_object_sha256"]; row=by[rid]["payload"]["packet_row"]; assert row["source"]==m["source_path"] and all(row[k]==m[k] for k in ("source_format","object_pointer","source_object_sha256","required_category"))'</automated>
    <automated>python3 -c 'import hashlib,json,pathlib,re,stat,subprocess
idx=json.load(open("docs/knowledge/index.internal.json")); cp=[x["payload"] for x in idx["records"] if x["id"]=="phase-01-closeout-teaching-witness"]; assert len(cp)==1; cp=cp[0]; root=pathlib.Path.cwd().resolve(); keys={"id","command","argv","expected","observed","expected_exit_status","expected_stdout_sha256","expected_stderr_sha256","observed_exit_status","observed_stdout_sha256","observed_stderr_sha256","status","evidence_path","evidence_sha256"}
for x in cp["curated_learnings"]:
 p=pathlib.Path(x["source"]); assert p.is_file() and hashlib.sha256(p.read_bytes()).hexdigest()==x["evidence_sha256"]
for x in cp["teaching_witnesses"]:
 assert set(x)==keys and isinstance(x["argv"],list) and x["argv"] and all(isinstance(a,str) and a for a in x["argv"]) and type(x["expected_exit_status"]) is int and x["expected"]==x["observed"] and x["status"]=="pass"; p=pathlib.Path(x["evidence_path"]); assert p.is_file() and hashlib.sha256(p.read_bytes()).hexdigest()==x["evidence_sha256"]; run=subprocess.run(x["argv"],shell=False,capture_output=True); out=hashlib.sha256(run.stdout).hexdigest(); err=hashlib.sha256(run.stderr).hexdigest(); assert run.returncode==x["expected_exit_status"]==x["observed_exit_status"] and out==x["expected_stdout_sha256"]==x["observed_stdout_sha256"] and err==x["expected_stderr_sha256"]==x["observed_stderr_sha256"]'</automated>
  </verify>
  <acceptance_criteria>
    - Every Plan-01 through Plan-05 JSON source or generated artifact rejects duplicate keys recursively and rejects `NaN`/`Infinity` constants before authoritative manifest/index acceptance; malformed or non-RFC JSON fails closed.
    - Profile creation starts only after the finalized capability ledger exists; its exact whole-file digest is bound into the immutable profile.
    - Generated Markdown is exclusively Task-2-owned, deterministic from the profile TOML, hash-labelled, and never independently normative.
    - Source-profile hash and packaged-RCP digest have distinct schemes/sources and are never aliases; packaged digest comes only from installed bytes, while `blocked_not_yet_packaged` requires and digest-binds Plan 02's exact `not_yet_packaged` verdict, omits packaged keys, and cannot mask discovery/build failure.
    - Profile has no recursive `profile_sha256` key; verified installed RCP is canonical, absolute, regular, non-symlink, and outside checkout/staging trees.
    - Package binding derives RCP relative path by exact-head evaluation of selected output's `uzelRcpRelativePath` Nix metadata, requires profile path equality below Plan-02's realized output root, independently hashes installed bytes, and fails closed on missing/ambiguous/drifting metadata.
    - SIR records disagreement and chosen/unchosen interpretations without claiming universal conformance.
    - Every canonical transcript binds profile ID/hash, exact-build identity, finalized-ledger digest, and ordered session principals.
    - Every canonical transcript digest is recomputed from exactly canonical negotiation input, outcome, and admission facts.
    - Required capability absence/malformed/denial and implicit downgrade reject before guest code; optional absence is explicit, exposes no extra authority, and cannot silently replace required semantics.
    - Package-RCP binding/diagnostic mismatch, stale session/generation, and mid-session profile replacement reject before guest code/domain exposure; positive/skew/interop/clean-room/local-patch cases retain honest availability and limitations.
    - Packaged RCP path and package/provenance/manifest/compiled identities equal Plan-02 verdict fields; bytes are independently hashed.
    - Internal index is exclusively Task-2-owned and covers finalized Task-1/Task-2 records; it is sole source for Plan 06 Task 1 public projection.
    - Internal index contains nonempty canonical transcript bindings, a directly recomputed authoritative JSON/TOML source-object manifest bijective with every blocker/replacement record, fixed category-to-disposition mapping, and exhaustive transition inventory where deletion or `not_applicable` cannot hide mandatory rows.
    - Review-prerequisite membership is fixed by the exact review-baseline source digest and two-stage inventory; optional packet-row tags have no authority, and the two approved lanes may overlap.
    - Any blocker/replacement-marked source object lacking a supported stable ID or status field fails closed instead of disappearing from the authoritative manifest.
    - EDGE-M0-05-UNCLASSIFIED and PROHIB-M0-05-FAIRNESS-INDEPENDENCE remain flagged for human and GitHub Codex review.
  </acceptance_criteria>
  <done>The immutable profile is published last after binding the pre-existing ledger digest; canonical D-09/D-26 transcripts and mismatch-before-guest evidence close negotiation, and the internal index covers the finalized set.</done>
</task>

</tasks>

## Source Coverage Audit

| Source type | Item | Coverage | Plan/task |
|---|---|---|---|
| GOAL | Preserve POC/incident evidence and produce separate honest historical, package, native, authority, delivery, compatibility, maturity, and knowledge baselines | PLANNED | 01-01 through 01-07 |
| REQ | M0-01 | PLANNED | 01-01 Tasks 1-2 |
| REQ | M0-02 | PLANNED | 01-02 Tasks 1-2 |
| REQ | M0-03 | PLANNED | 01-03 Tasks 1-2 |
| REQ | M0-04 | PLANNED | 01-04 Tasks 1-2 |
| REQ | M0-05 | PLANNED | 01-05 Tasks 1-2; 01-06 Tasks 1-2; 01-07 Tasks 1-3 |
| RESEARCH | Exact-source replay, fixed closure, hostile egress denial, bounded attempts, preserved WIP evidence | PLANNED | 01-01 Tasks 1-2; authority rechecked live |
| RESEARCH | Separate package/native verdicts, `not_yet_packaged`, state paths, measurements | PLANNED | 01-02 Tasks 1-2; drift-prone facts re-inventoried at execution |
| RESEARCH | Source authority, identities, formats, instance/profile, storage, trust, ASVS-L1 threats | PLANNED | 01-03 Tasks 1-2; source claims rechecked at one head |
| RESEARCH | Pinned tools/commands, claim coverage, CI/review maturity, CodeRabbit attempt/result and GitHub Codex exact-SHA review | PLANNED | 01-04 Tasks 1-2; tool facts measured live at execution |
| RESEARCH | Upstreams, ledger-before-profile finalization, immutable profiles, SIR, canonical negotiation transcripts, capability/fairness, knowledge/visibility/learning | PLANNED | 01-05 Tasks 1-2; 01-06 Tasks 1-2; 01-07 Tasks 1-3; current authority is 07/08/09 |
| REVIEW | Phase-1 closeout deltas, curated learnings, education/visibility and executable teaching witness before E | PLANNED | 01-05 Task 2 internal-index record; frozen by 01-06 Task 2 |
| CONTEXT | D-01 through D-05 | PLANNED | 01-03 Tasks 1-2; 01-05 Task 1 |
| CONTEXT | D-06 through D-11 | PLANNED | 01-04 Tasks 1-2; 01-05 Tasks 1-2 |
| CONTEXT | D-12 through D-19 | PLANNED | 01-01 Tasks 1-2 |
| CONTEXT | D-20 through D-24 | PLANNED | 01-01 Tasks 1-2; 01-02 Tasks 1-2; 01-03 Tasks 1-2 |
| CONTEXT | D-25 through D-28 | PLANNED | 01-03 Task 2; 01-04 Tasks 1-2; 01-05 Tasks 1-2 |
| CONTEXT | D-29 through D-31 | PLANNED | All seven plans; 01-06 freezes E and 01-07 commits P, gates approve, and records decision |

No deferred idea is planned. Stale `01-RESEARCH.md`/`01-PATTERNS.md` facts are evidence leads only; each drift-prone executable fact is rechecked at execution. Spec-less fallback markers are `EDGE_ABSENT=1` and `PROHIB_ABSENT=1`; its five assumptions remain flagged with one-to-one requirement equality. The five retained prohibitions are descriptor-less bespoke transparency/authority/review/fairness constraints with `flagged: true` and `status: unverified`; generic canon-referral security/compliance candidates are omitted from this probe result.

## Artifact Ownership and Boundedness

- Task 1 exclusively owns four source registries plus canonical validated-pack upstream-contribution ledger, its append-only STATUS evidence block, and the frozen upstream-discovery observation: upstreams, finalized capability/fairness ledger, governance, terms, contribution evidence, status handoff, and remote/submodule observation. It publishes no profile or generated projection.
- Task 2 exclusively owns five closure artifacts: immutable profile TOML, its generated Markdown view, SIR-0001, canonical transcript/vector JSON, and the internal knowledge index. It reads the finalized ledger and publishes the profile last.
- The executor exclusively owns the declared `01-05-SUMMARY.md`; tasks do not edit it.
- Plan 06 owns public projection/evidence-set E; Plan 07 owns packet P, exact-P review/human checkpoint, decision, and final-SHA protocol.

This plan's twelve Phase-1 evidence/contract artifacts remain one cohesive bounded M0-05 compatibility slice: seven prerequisite authority/contribution/status/discovery records and five atomic compatibility/negotiation closure artifacts. No file is shared between tasks, no source/runtime code is modified, and each generated projection has exactly one normative input and one owner.

## Artifacts Produced

- `docs/ecosystem/upstreams.toml` — upstream/fork/pin/patch/contribution registry.
- `docs/compat/profiles/uzel-m0.toml` — immutable evidence-bound M0 profile.
- `docs/compat/generated/uzel-m0.md` — deterministic human-readable profile projection.
- `docs/specs/interpretations/SIR-0001-manifest-build-identity.md` — manifest/exact-build interpretation record.
- `docs/compat/negotiation-vectors.json` — positive/negative/skew/interop/clean-room/local-patch vectors.
- `docs/runtime/capabilities/m0-index.toml` — per-principal capability/admission/fairness ledger.
- `docs/knowledge/m0-governance.md` — decisions, learning, visibility, upstream, and milestone governance.
- `docs/knowledge/terms.toml` — source-grounded term registry.
- `docs/knowledge/index.internal.json` — internal evidence/knowledge index.
- `uzel-poc-validated-pack/STATUS.md` — append-only Phase-1 upstream-baseline evidence block preserving active POC status.
- `evidence/phase-01/upstream-discovery-observations.json` — immutable retained Git remote/submodule discovery observation; registry validation never trusts mutable live remotes.

<threat_model>
Security validation scope: OWASP ASVS Level 1, limited to registry, negotiation/admission, compatibility, and upstream boundaries.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Upstream/fork/spec identities to local registry | Remote names, revisions, ownership, licences, and local patches can drift or be spoofed. |
| Finalized capability ledger/registry/SIR to immutable compatibility profile | Pre-existing ledger and interpretive decisions enter a profile that must not bind future data or be silently mutated. |
| Canonical negotiation transcript to guest admission | Untrusted manifests, versions, peers, patches, exact-build identity, and session principals must be bound and checked before guest code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-01 | Spoofing/Tampering | upstream and fork registry | high | mitigate | Record canonical identities, exact pins/tree/lock digests, owners, drift, local patches, dedicated worktree/base/head, and human publication authority. |
| T-05-02 | Tampering/Repudiation | ledger, immutable profile, and SIR | high | mitigate | Finalize/hash ledger first; bind exact digest into staged profile; deterministic projection; explicit dissent; publish profile last; new-ID transition rather than mutation. |
| T-05-03 | Spoofing/Elevation | canonical transcript and guest admission | high | mitigate | Bind profile ID/hash, exact-build identity, ledger digest, and session principals; mismatch vectors prove rejection before guest code; retain unavailable-peer truth. |
| T-05-04 | Denial of Service | malformed/skewed negotiation inputs | medium | mitigate | Bounded vector execution, deterministic rejection, cancellation/cleanup evidence, no uncontrolled retry. |
| T-05-SC | Tampering | existing upstream/runtime dependencies | high | mitigate | No installs/upgrades; validate exact existing pins and Plan-02 closure identities, and stop registry/profile generation on drift. |
</threat_model>

<verification>
Run Tasks 1 and 2 in order. Prove Task 1's finalized immutable ledger bytes exist before Task 2 computes digest; prove published profile binds that exact digest; prove package-to-RCP binding/exposure fields and D-09/D-26 transcripts bind profile/build/session identities; prove downgrade/package/session mismatch fails closed before guest code. Confirm no public index, packet, decision, review claim, or Phase-2 authority is created; Plans 06-07 own those later gates.
</verification>

<success_criteria>
- M0-05 compatibility closure produces immutable, evidence-linked ecosystem/compatibility/capability/knowledge artifacts ready for Plans 06-07.
- Finalized capability/fairness ledger precedes immutable profile binding; generated projections cannot bind or claim future data.
- Canonical negotiation transcripts bind profile ID/hash, exact-build identity, ledger digest, and session principals; mismatch evidence proves rejection before guest execution.
- Interop, skew, clean-room, exact-build, and local-patch results remain honest about unavailable or unproven evidence.
- Uzel shell/product-service ownership, replaceable canonical engine/provider boundary, and dedicated jodobear upstream contribution workflow remain explicit; no upstream contact/publication occurs.
- EDGE-M0-05-UNCLASSIFIED and PROHIB-M0-05-FAIRNESS-INDEPENDENCE remain flagged unresolved/unverified for Plan 07 human/GitHub Codex review.
- Plan completes autonomously without creating or implying any Phase-2 transition authority.
</success_criteria>

<output>
Create `.planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-05-SUMMARY.md` when done.
</output>
