---
phase: 01-slice-ref-01-poc-replay-accepted-napp-seam
plan: 05
type: execute
wave: 5
depends_on:
  - "01-04"
files_modified:
  - docs/ecosystem/upstreams.toml
  - docs/runtime/capabilities/m0-index.toml
  - docs/knowledge/m0-governance.md
  - docs/knowledge/terms.toml
  - docs/compat/profiles/uzel-m0.toml
  - docs/compat/generated/uzel-m0.md
  - docs/specs/interpretations/SIR-0001-manifest-build-identity.md
  - docs/compat/negotiation-vectors.json
  - docs/knowledge/index.internal.json
  - .planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-05-SUMMARY.md
autonomous: true
requirements:
  - M0-05
user_setup: []
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: medium
must_haves:
  truths:
    - "D-01 and D-03: programme authority is traceable; Uzel shell/product-service owns product workflow while the canonical engine/provider remains replaceable behind an explicit private adapter and upstream relationships."
    - "D-06, D-08 through D-10, and D-26: ecosystem, compatibility, SIR, negotiation, capability, fairness, knowledge, terms, visibility, and learning artifacts form one evidence-linked closure."
    - "D-08, D-09, and D-26: source-profile hash and installed packaged-RCP digest are separate non-aliased identities; verified packages bind independently read bytes before guest code, while `not_yet_packaged` explicitly omits the packaged digest and blocks packaged-runtime acceptance."
    - "D-08: the finalized per-principal capability/fairness ledger exists before Task 2 computes its whole-file digest and creates the immutable compatibility profile or any generated projection."
    - "D-11: upstream work has explicit ownership, dedicated jodobear fork/branch/worktree policy, contribution record, named human submitter/approver, truthful AI/authorship/signoff/DCO/CLA/public-text dispositions, and human publication authority."
    - "D-11 and D-26: every guest-influenced queue has both global and per-principal limits, a named anti-starvation owner/policy, and executable noisy/quiet, many-principal, and adversarial pressure vectors."
    - "D-29 and D-30: exact-build identity, interop/version-skew, local-patch status, evidence, limitations, disagreement, and negative results are first-class."
    - "D-31: this autonomous compatibility-closure plan creates no Phase-2 decision or transition authority; evidence freeze and transition closeout remain isolated in dependent Plans 06-07."
  artifacts:
    - path: docs/ecosystem/upstreams.toml
      provides: "Source-of-truth upstream registry, ownership, pins, fork/worktree policy, contribution state, and drift"
    - path: docs/compat/profiles/uzel-m0.toml
      provides: "Immutable evidence-bound compatibility profile atomically finalized after the capability-ledger digest exists"
    - path: docs/specs/interpretations/SIR-0001-manifest-build-identity.md
      provides: "Spec interpretation record for manifest and exact-build identity"
    - path: docs/compat/negotiation-vectors.json
      provides: "Canonical transcript gate plus positive and pre-guest-code rejection vectors for identity/build/principal/capability/skew mismatches"
    - path: docs/runtime/capabilities/m0-index.toml
      provides: "Per-principal capability/admission/fairness ledger with evidence and residual gaps"
    - path: docs/knowledge/m0-governance.md
      provides: "Decision, disagreement, learning, visibility, education, upstream, and closeout workflow"
    - path: docs/knowledge/index.internal.json
      provides: "Task-2-owned internal index over finalized ledger/profile/SIR/transcript/governance records"
  key_links:
    - from: docs/ecosystem/upstreams.toml
      to: docs/compat/profiles/uzel-m0.toml
      via: "Every compatibility dependency binds to exact registry IDs and immutable upstream/source identities"
      pattern: "upstream_id"
    - from: docs/runtime/capabilities/m0-index.toml
      to: docs/compat/profiles/uzel-m0.toml
      via: "Task 1 finalizes immutable ledger bytes; Task 2 computes their digest before it creates and publishes the immutable profile"
      pattern: "capability_ledger_sha256"
    - from: docs/specs/interpretations/SIR-0001-manifest-build-identity.md
      to: docs/compat/negotiation-vectors.json
      via: "Normative ambiguity and exact-build policy become executable positive/negative/skew/local-patch vectors"
      pattern: "SIR-0001"
    - from: docs/compat/profiles/uzel-m0.toml
      to: docs/compat/negotiation-vectors.json
      via: "Each transcript binds source profile ID/hash plus separate packaged-RCP status/digest, exact-build identity, finalized ledger digest, and session principals before admission"
      pattern: "canonical_transcript_sha256"
  assumptions:
    - id: EDGE-M0-05-UNCLASSIFIED
      requirement: M0-05
      category: unclassified
      statement: "The spec-less edge probe could not classify M0-05; manual review must confirm registry completeness, immutable-profile transitions, exact-build ambiguity, negotiation/skew/local-patch cases, per-principal capability/fairness, disclosure projections, learning closure, and human transition authority cover every material ecosystem edge."
      status: unresolved
      flagged: true
  prohibitions:
    - id: PROHIB-M0-05-FAIRNESS-INDEPENDENCE
      requirement: M0-05
      category: values
      statement: "MUST NOT claim universal conformance, manufacture an independent peer result, contact or publish upstream without human authority, conceal asymmetric principal treatment, or advance automatically into Phase 2."
      status: unverified
      flagged: true
---

## Phase Goal

**As a** Uzel owner, **I want to** preserve the current POC and incident evidence while separating historical replay truth from current evidence, **so that** Phase 1 can reset execution without inventing a green baseline.

<objective>
Close the ecosystem, compatibility, exact-build interpretation, negotiation, capability, fairness, upstream, visibility, and learning baselines as immutable inputs for dependent transition closeout.

Purpose: Satisfy the autonomous compatibility-closure portion of M0-05 with immutable evidence-linked governance artifacts and no transition decision, upstream contact, public claim, or product implementation.

Output: Ecosystem/compatibility/SIR/negotiation/capability/knowledge registries and internal index ready for Plan 06 evidence freeze and Plan 07 transition closeout.
</objective>

<execution_context>
@/home/at/.codex/gsd-core/workflows/execute-plan.md
@/home/at/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-CONTEXT.md
@docs/plans/uzel-product-incubation-v4-2026-08-10/00-GSD-INGEST.md
@docs/plans/uzel-product-incubation-v4-2026-08-10/02-PRODUCT-ARCHITECTURE.md
@docs/plans/uzel-product-incubation-v4-2026-08-10/03-ROADMAP.md
@docs/plans/uzel-product-incubation-v4-2026-08-10/04-DELIVERY-QUALITY.md
@docs/plans/uzel-product-incubation-v4-2026-08-10/07-ECOSYSTEM-UPSTREAM.md
@docs/plans/uzel-product-incubation-v4-2026-08-10/08-DECISIONS-LEARNING.md
@docs/plans/uzel-product-incubation-v4-2026-08-10/09-PRODUCTION-MATURITY.md
@.planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-03-SUMMARY.md
@.planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-04-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Trace one principal and upstream into the finalized capability, fairness, and governance ledgers</name>
  <files>docs/ecosystem/upstreams.toml, docs/runtime/capabilities/m0-index.toml, docs/knowledge/m0-governance.md, docs/knowledge/terms.toml</files>
  <read_first>
    - .planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-CONTEXT.md — D-01, D-03, D-06, D-08, D-09, D-11, D-26, D-29 through D-31
    - docs/plans/uzel-product-incubation-v4-2026-08-10/07-ECOSYSTEM-UPSTREAM.md — registry, fork/worktree, patch, contribution, and drift authority
    - docs/plans/uzel-product-incubation-v4-2026-08-10/08-DECISIONS-LEARNING.md — SIR, disagreement, immutable decision, and visibility contracts
    - docs/plans/uzel-product-incubation-v4-2026-08-10/09-PRODUCTION-MATURITY.md — capability, fairness, visibility, education, and maturity closure
    - docs/plans/uzel-product-incubation-v4-2026-08-10/templates/UPSTREAM-REGISTRY.toml — registry field contract
    - docs/plans/uzel-product-incubation-v4-2026-08-10/templates/CAPABILITY-LEDGER.md — per-principal capability/admission/fairness field contract
    - docs/plans/uzel-product-incubation-v4-2026-08-10/templates/TERM-REGISTRY.toml — term-registry field contract
    - docs/architecture/phase-01-authority.md, docs/architecture/phase-01-identity.md — owners, principals, actors, roles, instances, profiles, and non-conflation rules
    - evidence/phase-01/current-source-citations.json, evidence/phase-01/current-package-verdict.json, evidence/phase-01/current-native-baseline.json — current source/package/native evidence identities
    - evidence/phase-01/ci-baseline.json, evidence/phase-01/review-baseline.json — delivery maturity and SHA-bound review evidence
  </read_first>
  <action>Implement D-01, D-03, D-06, D-08, D-10, D-11, D-26, D-29, and D-30 as the prerequisite tracer for profile finalization. Inventory every current upstream dependency, fork, specification, schema, build input, engine seam, and maintained local patch using exact canonical URL/repository, source-of-truth authority, upstream/fork owner, pinned revision and tree/lock identity, consumed path, licence/status, drift, divergence, local-patch state, contribution suitability, and evidence digest. For every material local patch or upstream interaction, add a machine-readable record that keeps `local_patch`, `thread`, `pr_accepted`, `pr_merged`, `release_contains_change`, `uzel_adoption`, and `patch_removal` as seven separate keyed state objects. Every state object records its own status, SHA-256 evidence, owner, and next trigger even when blocked or not applicable; no state, missing field, or shared evidence pointer implies another state. Record the required dedicated branch in the corresponding jodobear fork, a dedicated clean worktree, exact upstream base/head, tests, contribution-policy review, and update to `uzel-poc-validated-pack/docs/08-upstream-contributions.md`. Also record a named human submitter and named human approver (or explicit `blocked_unassigned`), repository AI-contribution policy, truthful AI assistance, authorship, signoff, DCO, CLA, public-text-review and disclosure dispositions. Contacting, pushing, or publishing remains separately human-authorized later work.

Create and finalize the M0 capability index before any compatibility profile exists: enumerate every declared current capability and distinct principal/actor/role from the identity baseline, then record each pair's requested, advertised, supportable, denied, failed, unavailable, or not-applicable state; whether it is runtime-required or guest-optional; authority; source/test/evidence; constraints; side effect; audit signal; cancellation/retry behavior; limitation; and residual risk. Do not insert a profile ID/hash or negotiation transcript that does not yet exist. Build one exhaustive `guest_influenced_queues` inventory covering every queue/cache/batch/retry/subscription/composition/diagnostics buffer. Give every queue a stable `queue_id`; named admission principal; numeric global and per-principal item, byte, in-flight, wakeup, and retained-result limits; typed overload behavior; named anti-starvation owner; scheduling algorithm or bounded-wait policy; privacy-safe metrics; cancellation/cleanup; and crash/restart behavior. Require exactly one admission-class row per queue ID, with no orphan or duplicate queue. For every queue ID add machine-readable noisy-producer/quiet-peer, many-principal, adversarial-pressure, cancellation, and overload vectors with bounded expected outcomes and evidence digests; aggregate vectors or a soak alone are insufficient. Add fairness rows comparing like-for-like principals and inputs, explaining asymmetric treatment and unmeasured cases without inventing multi-user guarantees.

Record the M0/M1 acquisition or commission plan for a separately authored clean-room peer: owner, acquisition route, allowed public-kit/black-box inputs, prohibited Uzel internals/private tests/implementation coaching, provenance requirements, target journey, evidence due phase, current status, and blocking effect. Until acquired it is exactly `blocked_no_independent_peer`; this blocks L4 runtime-composability promotion and M5, not M0 or Phase 2, and the Uzel-authored external-source fixture remains explicitly non-independent. The finalized ledger exposes a stable capability-set ID and immutable bytes; after Task 1 succeeds, Task 2 computes their whole-file SHA-256, treats those bytes as immutable input, and forces ledger review plus a new profile candidate on any drift. In the governance document define immutable decision/SIR updates, dissent, correction, supersession, milestone closeout, learning extraction, teaching/education ownership, visibility tiers, disclosure, provenance, restart/cancellation, Uzel shell/product-service direction with a replaceable canonical engine/provider adapter, independent-peer acquisition/blocking, and the dedicated jodobear upstream contribution workflow. Populate the term registry from programme authority without silent redefinition. Validate the three TOML registries, unique IDs, cross-links, source/evidence digests, separate upstream lifecycle states, contribution-policy dispositions, bounds/anti-starvation fields and pressure-vector coverage before atomic replacement. On cancellation retain only independently validated task-owned artifacts, mark ledger finalization incomplete, and refuse Task 2.</action>
  <verify>
    <automated>python3 -c 'import re,tomllib; u=tomllib.load(open("docs/ecosystem/upstreams.toml","rb")); c=tomllib.load(open("docs/runtime/capabilities/m0-index.toml","rb")); tomllib.load(open("docs/knowledge/terms.toml","rb")); hex64=lambda x:isinstance(x,str) and re.fullmatch(r"[0-9a-f]{64}",x); required_states={"local_patch","thread","pr_accepted","pr_merged","release_contains_change","uzel_adoption","patch_removal"}; records=u.get("upstream_records",[]); assert records and all(set(r["states"]) == required_states and all(s.get("status") and hex64(s.get("evidence_sha256")) and s.get("owner") and s.get("next_trigger") for s in r["states"].values()) and all(r.get(k) for k in ("human_submitter","human_approver","ai_assistance","repository_ai_policy","authorship","signoff","dco","cla","public_text_review")) for r in records); queues=c.get("guest_influenced_queues",[]); admissions=c.get("admission_classes",[]); qids=[q["queue_id"] for q in queues]; aids=[a["queue_id"] for a in admissions]; limit_keys=("global_item_limit","per_principal_item_limit","global_byte_limit","per_principal_byte_limit","global_in_flight_limit","per_principal_in_flight_limit","global_wakeup_limit","per_principal_wakeup_limit","global_retained_result_limit","per_principal_retained_result_limit"); assert qids and len(qids)==len(set(qids)) and sorted(qids)==sorted(aids) and len(aids)==len(set(aids)); assert all(a.get("admission_principal") and all(isinstance(a.get(k),int) and a[k]&gt;=0 for k in limit_keys) and a.get("typed_overload") and a.get("anti_starvation_owner") and a.get("anti_starvation_policy") and a.get("privacy_safe_metrics") and a.get("cancellation_cleanup") and a.get("crash_restart") for a in admissions); required_kinds={"noisy_quiet","many_principal","adversarial_pressure","cancellation","overload"}; vectors=c.get("pressure_vectors",[]); assert all({v["kind"] for v in vectors if v.get("queue_id")==q} == required_kinds and all(v.get("bounded_expected_outcome") and hex64(v.get("evidence_sha256")) for v in vectors if v.get("queue_id")==q) for q in qids); p=c["independent_peer"]; assert p["status"] == "blocked_no_independent_peer" and p["blocks_l4_runtime_composability"] and p["blocks_m5"] and not p["blocks_phase_2"] and p.get("acquisition_owner") and p.get("acquisition_route")' &amp;&amp; rg -q 'jodobear' docs/ecosystem/upstreams.toml &amp;&amp; rg -q 'dedicated.*worktree|worktree.*dedicated' docs/ecosystem/upstreams.toml &amp;&amp; rg -q 'finalized = true' docs/runtime/capabilities/m0-index.toml &amp;&amp; rg -q 'capability_set_id' docs/runtime/capabilities/m0-index.toml &amp;&amp; rg -q 'uzel-poc-validated-pack/docs/08-upstream-contributions.md' docs/knowledge/m0-governance.md &amp;&amp; rg -q 'human.*authoriz|authoriz.*human' docs/knowledge/m0-governance.md</automated>
  </verify>
  <acceptance_criteria>
    - One principal/capability path links identity, source evidence, fairness/admission status, stable capability-set ID, and finalized immutable ledger bytes before profile creation.
    - Registry covers upstream/fork ownership, pins, drift/divergence/local patches, dedicated contribution branch/worktree, policy, tests, named human submitter/approver, AI/authorship/signoff/DCO/CLA/public-text dispositions, and human publication authority.
    - Upstream records keep local patch, thread, acceptance, merge, release, exact Uzel adoption, and patch removal as exactly seven keyed states; every state independently has status/evidence/owner/trigger and no state implies another.
    - Every guest-influenced queue has exactly one admission row, no orphan/duplicate ID, all global/per-principal item/byte/in-flight/wakeup/retained-result bounds, and owned anti-starvation behavior proven by all five per-queue pressure-vector kinds.
    - Every current capability is recorded per distinct principal with required/optional semantics, honest support/failure/unavailable state, and like-for-like fairness comparison.
    - Independent-peer acquisition has an owner, clean-room input/provenance rules, target journey and honest `blocked_no_independent_peer` record; it blocks L4/M5 rather than being manufactured or silently deleted.
    - Ledger finalization contains no profile ID/hash or transcript reference to data created by Task 2.
    - Governance/terms/upstream records have single ownership, provenance, cancellation, restart, learning, visibility, and human authority rules.
  </acceptance_criteria>
  <done>The upstream, capability/fairness, governance, and term registries are finalized; Task 2 can now hash immutable ledger bytes and bind complete non-circular inputs.</done>
</task>

<task type="auto">
  <name>Task 2: Atomically bind the finalized ledger into the immutable profile and canonical negotiation transcripts</name>
  <files>docs/compat/profiles/uzel-m0.toml, docs/compat/generated/uzel-m0.md, docs/specs/interpretations/SIR-0001-manifest-build-identity.md, docs/compat/negotiation-vectors.json, docs/knowledge/index.internal.json</files>
  <read_first>
    - docs/ecosystem/upstreams.toml — finalized upstream/fork/pin/patch/contribution identities
    - docs/runtime/capabilities/m0-index.toml — finalized pre-profile capability/fairness ledger and stable digest input
    - docs/knowledge/m0-governance.md, docs/knowledge/terms.toml — decision/learning/disclosure authority and canonical meanings
    - docs/architecture/phase-01-authority.md — source/decision/change authority
    - docs/architecture/phase-01-identity.md — distinct principals, actors, roles, identities, instances, and profiles
    - docs/security/phase-01-threat-model.md — scoped residual risks and validation ownership
    - docs/plans/uzel-product-incubation-v4-2026-08-10/08-DECISIONS-LEARNING.md — decision/learning/term/index/disclosure contracts
    - docs/plans/uzel-product-incubation-v4-2026-08-10/09-PRODUCTION-MATURITY.md — capability, fairness, visibility, education, and maturity closure
    - docs/plans/uzel-product-incubation-v4-2026-08-10/templates/COMPAT-PROFILE.toml — immutable profile field contract
    - docs/plans/uzel-product-incubation-v4-2026-08-10/templates/SPEC-INTERPRETATION.md — SIR field contract
    - docs/plans/uzel-product-incubation-v4-2026-08-10/templates/PROFILE-TRANSITION.toml — later transition linkage, not mutable-profile authority
    - evidence/phase-01/current-source-citations.json, evidence/phase-01/current-package-verdict.json, evidence/phase-01/current-native-baseline.json — exact source/package/native identities
  </read_first>
  <action>Implement D-06, D-08, D-09, D-10, D-26, D-29, and D-30 only after Task 1's ledger validates with `finalized = true`; compute and freeze its whole-file SHA-256 before creating any profile byte. Stop if the ledger, upstream registry, source/tree, package/native evidence, or tool identity drifts. Open SIR-0001 for manifest/exact-build ambiguity with sources, competing interpretations, stakeholders, dissent, chosen and unchosen readings, compatibility/rollback effects, evidence, confidence, revisit trigger, and human authority; never portray the SIR as upstream specification law.

In a task-owned staging directory, create the immutable `uzel-m0` profile bound to exact source/tree, package/native evidence, upstream-registry digest, schema/format floors, build/runtime/tool identities, the already-finalized capability-set ID and ledger SHA-256, applicable claims/status, limitations, and evidence. Before creating profile bytes, parse the finalized ledger, require boolean `finalized = true`, a stable non-empty `capability_set_id`, and its whole-file SHA-256; bind both ID and digest into profile and every transcript. Add an explicit `package_binding` contract with two non-aliased digest domains: `profile_sha256` is computed later from exact source-profile bytes, while `exact_packaged_profile_bytes_sha256` may be computed only by independently opening and reading a canonical absolute installed RCP path—not a checkout/staging/profile path. Give them distinct hash-scheme/source fields; never copy or assume equality from source-profile hash. Verified packaging records canonical installed path, regular-file/no-symlink result, read-evidence SHA-256, package identity/manifest association, provenance association, compiled expected-digest identity, and independently read bytes digest. If checkout-independent package evidence exists, set status `verified_packaged` and bind real 64-hex packaged digest. If verdict is `not_yet_packaged`, set `blocked_not_yet_packaged`, owner `phase_2`, omit packaged digest key entirely (not JSON/TOML null or empty), record required evidence/next trigger, and forbid packaged-runtime acceptance. RCP never contains its own digest; external package manifest/compiled expectation bind exact bytes. Profile must not contain its own source-file hash or bind transcript/index needing that hash. Compute staged source-profile SHA-256 only after bytes finalize, generate `uzel-m0.md` solely from TOML, and mark Markdown non-normative with source profile ID/hash.

Build D-09/D-26 canonical negotiation transcripts in `negotiation-vectors.json`: canonicalize negotiation input/outcome, then bind every transcript digest to profile ID, independently computed source `profile_sha256`, exact-build identity digest, finalized ledger ID/digest, ordered session principals, instance/local-profile/session/generation, package-RCP status, and—only when verified—independently computed packaged digest before admission. A blocked transcript has no `verified_package_rcp_sha256` key and cannot produce packaged-runtime acceptance. Require positive vector kinds exactly covering `exact_match`, `supported_floor`, `optional_capability`, `interoperability`, `clean_room`, and `local_patch`; preserve honest synthetic/non-independent labels. Require negative kinds for profile/build/principal/package bytes/package digest/package diagnostics/manifest/schema/version/required-capability/downgrade/mid-session/stale/local-patch mismatches. Missing/malformed/denied required capability rejects before guest code; optional absence yields explicit unsupported/degraded without authority widening. Every negative produces content-addressed evidence with `reject_before_guest`, `guest_code_started=false`, and zero exposed domains. Mid-session replacement terminates/relaunches new session. Each vector records declarations, inputs, expected/observed result, transcript/evidence digest, limitations, cleanup, cancellation, and restart behavior.

Generate internal JSON index from finalized Task-1 records plus staged profile/SIR/transcript records. Every internal record carries stable unique `id`, object `payload`, canonical `payload_sha256`, canonical whole-record/source digest, and disclosure classification so Plan 06 can verify projection against source bytes rather than trust copied public metadata. Validate all five task-owned files/cross-digests as one staging set, including package binding, exact positive/negative vector taxonomy, then install projections/SIR/vectors/internal index and publish immutable profile last. Once published, changes require new ID/transition. On failure publish no profile; retain checksummed diagnostics and restart only with identical inputs.</action>
  <verify>
    <automated>python3 -c 'import hashlib,os,re,tomllib; from pathlib import Path; c=tomllib.load(open("docs/runtime/capabilities/m0-index.toml","rb")); p=tomllib.load(open("docs/compat/profiles/uzel-m0.toml","rb")); b=p["package_binding"]; hx=lambda x:isinstance(x,str) and re.fullmatch(r"[0-9a-f]{64}",x); ledger_sha=hashlib.sha256(Path("docs/runtime/capabilities/m0-index.toml").read_bytes()).hexdigest(); assert c.get("finalized") is True and isinstance(c.get("capability_set_id"),str) and c["capability_set_id"]; assert p["capability_ledger"]["capability_set_id"]==c["capability_set_id"] and p["capability_ledger"]["sha256"]==ledger_sha; assert b["source_profile_hash_scheme"]=="sha256-exact-source-profile-bytes-v1" and b["packaged_profile_hash_scheme"]=="sha256-exact-packaged-rcp-bytes-v1" and b["runtime_normalization_before_hash"]=="forbidden" and b["startup_mismatch"]=="fail_closed" and "source_profile_sha256" not in b; verified=b["status"]=="verified_packaged"; blocked=b["status"]=="blocked_not_yet_packaged"; assert verified or blocked; assert (not blocked) or (b["implementation_owner"]=="phase_2" and "exact_packaged_profile_bytes_sha256" not in b and b.get("required_evidence") and b.get("next_trigger")); path=Path(b.get("canonical_installed_rcp_path","/not-packaged")); assert (not verified) or (path.is_absolute() and path.is_file() and not path.is_symlink() and hashlib.sha256(path.read_bytes()).hexdigest()==b["exact_packaged_profile_bytes_sha256"] and hx(b.get("installed_read_evidence_sha256")) and b.get("package_identity") and hx(b.get("package_manifest_sha256")) and b.get("package_association") and b.get("provenance_association"))' &amp;&amp; python3 -m json.tool docs/compat/negotiation-vectors.json &gt;/dev/null &amp;&amp; python3 -m json.tool docs/knowledge/index.internal.json &gt;/dev/null &amp;&amp; jq --arg profile_sha "$(sha256sum docs/compat/profiles/uzel-m0.toml | cut -d " " -f1)" -e 'all(.transcripts[]; (.profile_sha256==$profile_sha) and (.capability_set_id|type=="string") and (.capability_ledger_sha256|test("^[0-9a-f]{64}$")) and (((.package_rcp_status=="verified_packaged") and (.verified_package_rcp_sha256|test("^[0-9a-f]{64}$")) and (.package_rcp_digest_source=="installed_packaged_rcp_bytes")) or ((.package_rcp_status=="blocked_not_yet_packaged") and (has("verified_package_rcp_sha256")|not) and (.packaged_runtime_accepted==false))) and (.canonical_transcript_sha256|test("^[0-9a-f]{64}$"))) and all(.vectors[]|select(.polarity=="negative"); (.observed=="reject_before_guest") and (.guest_code_started==false) and (.exposed_domains|length==0) and (.mismatch_rejection_evidence_sha256|test("^[0-9a-f]{64}$")))' docs/compat/negotiation-vectors.json &gt;/dev/null &amp;&amp; jq -e '([.vectors[]|select(.polarity=="positive")|.kind] as $p | ["exact_match","supported_floor","optional_capability","interoperability","clean_room","local_patch"] | all(.[]; . as $required | $p | index($required)!=null)) and ([.vectors[]|select(.polarity=="negative")|.kind] as $n | ["profile_id_mismatch","profile_hash_mismatch","exact_build_mismatch","session_principal_mismatch","package_rcp_bytes_mismatch","package_rcp_digest_mismatch","package_diagnostics_mismatch","missing_manifest","malformed_manifest","incompatible_required_capability","implicit_required_downgrade","mid_session_profile_replacement","stale_transcript","unsupported_version_skew","local_patch_divergence"] | all(.[]; . as $required | $n | index($required)!=null))' docs/compat/negotiation-vectors.json &gt;/dev/null &amp;&amp; python3 -c 'import hashlib,json,re; d=json.load(open("docs/knowledge/index.internal.json")); rs=d["records"]; enc=lambda x:json.dumps(x,sort_keys=True,separators=(",",":"),ensure_ascii=False).encode(); hx=lambda x:isinstance(x,str) and re.fullmatch(r"[0-9a-f]{64}",x); assert len({r["id"] for r in rs})==len(rs) and all(isinstance(r["payload"],dict) and hashlib.sha256(enc(r["payload"])).hexdigest()==r["payload_sha256"] and hx(r["source_record_sha256"]) and r["disclosure"] in ("public","redacted","internal") for r in rs)' &amp;&amp; rg -q 'SIR-0001' docs/specs/interpretations/SIR-0001-manifest-build-identity.md &amp;&amp; rg -q "$(sha256sum docs/compat/profiles/uzel-m0.toml | cut -d " " -f1)" docs/compat/generated/uzel-m0.md</automated>
  </verify>
  <acceptance_criteria>
    - Profile creation starts only after the finalized capability ledger exists; its exact whole-file digest is bound into the immutable profile.
    - Generated Markdown is exclusively Task-2-owned, deterministic from the profile TOML, hash-labelled, and never independently normative.
    - Source-profile hash and packaged-RCP digest have distinct schemes/sources and are never aliases; packaged digest comes only from installed bytes, while `not_yet_packaged` explicitly omits it and remains `blocked_not_yet_packaged` with owner/evidence/trigger.
    - Package binding explicitly defines exact packaged RCP path/bytes, external manifest and compiled digest binding, startup fail-closed verification, trusted diagnostic exposure, and honest Phase-2 implementation ownership when blocked.
    - SIR records disagreement and chosen/unchosen interpretations without claiming universal conformance.
    - Every canonical transcript binds profile ID/hash, exact-build identity, finalized-ledger digest, and ordered session principals.
    - Required capability absence/malformed/denial and implicit downgrade reject before guest code; optional absence is explicit, exposes no extra authority, and cannot silently replace required semantics.
    - Package-RCP binding/diagnostic mismatch, stale session/generation, and mid-session profile replacement reject before guest code/domain exposure; positive/skew/interop/clean-room/local-patch cases retain honest availability and limitations.
    - Internal index is exclusively Task-2-owned and covers finalized Task-1/Task-2 records; it is sole source for Plan 06 Task 1 public projection.
    - EDGE-M0-05-UNCLASSIFIED and PROHIB-M0-05-FAIRNESS-INDEPENDENCE remain flagged for human and GitHub Codex review.
  </acceptance_criteria>
  <done>The immutable profile is published last after binding the pre-existing ledger digest; canonical D-09/D-26 transcripts and mismatch-before-guest evidence close negotiation, and the internal index covers the finalized set.</done>
</task>

</tasks>

## Source Coverage Audit

| Source type | Item | Coverage | Plan/task |
|---|---|---|---|
| GOAL | Preserve POC/incident evidence and produce separate honest historical, package, native, authority, delivery, compatibility, maturity, and knowledge baselines | COVERED | 01-01 through 01-07 |
| REQ | M0-01 | COVERED | 01-01 Tasks 1-2 |
| REQ | M0-02 | COVERED | 01-02 Tasks 1-2 |
| REQ | M0-03 | COVERED | 01-03 Tasks 1-2 |
| REQ | M0-04 | COVERED | 01-04 Tasks 1-2 |
| REQ | M0-05 | COVERED | 01-05 Tasks 1-2; 01-06 Tasks 1-2; 01-07 Tasks 1-3 |
| RESEARCH | Exact-source replay, fixed closure, hostile egress denial, bounded attempts, preserved WIP evidence | COVERED | 01-01 Tasks 1-2; authority rechecked live |
| RESEARCH | Separate package/native verdicts, `not_yet_packaged`, state paths, measurements | COVERED | 01-02 Tasks 1-2; drift-prone facts re-inventoried at execution |
| RESEARCH | Source authority, identities, formats, instance/profile, storage, trust, ASVS-L1 threats | COVERED | 01-03 Tasks 1-2; source claims rechecked at one head |
| RESEARCH | Pinned tools/commands, claim coverage, CI/review maturity, CodeRabbit attempt/result and GitHub Codex exact-SHA review | COVERED | 01-04 Tasks 1-2; tool facts measured live at execution |
| RESEARCH | Upstreams, ledger-before-profile finalization, immutable profiles, SIR, canonical negotiation transcripts, capability/fairness, knowledge/visibility/learning | COVERED | 01-05 Tasks 1-2; 01-06 Tasks 1-2; 01-07 Tasks 1-3; current authority is 07/08/09 |
| CONTEXT | D-01 through D-05 | COVERED | 01-03 Tasks 1-2; 01-05 Task 1 |
| CONTEXT | D-06 through D-11 | COVERED | 01-04 Tasks 1-2; 01-05 Tasks 1-2 |
| CONTEXT | D-12 through D-19 | COVERED | 01-01 Tasks 1-2 |
| CONTEXT | D-20 through D-24 | COVERED | 01-01 Tasks 1-2; 01-02 Tasks 1-2; 01-03 Tasks 1-2 |
| CONTEXT | D-25 through D-28 | COVERED | 01-03 Task 2; 01-04 Tasks 1-2; 01-05 Tasks 1-2 |
| CONTEXT | D-29 through D-31 | COVERED | All seven plans; 01-06 freezes E and 01-07 commits P, gates approve, and records decision |

No deferred idea is planned. Stale `01-RESEARCH.md`/`01-PATTERNS.md` facts are evidence leads only; each drift-prone executable fact is rechecked at execution. Spec-less fallback markers are `EDGE_ABSENT=1` and `PROHIB_ABSENT=1`; its five assumptions remain flagged with one-to-one requirement equality. The five retained prohibitions are descriptor-less bespoke transparency/authority/review/fairness constraints with `flagged: true` and `status: unverified`; generic canon-referral security/compliance candidates are omitted from this probe result.

## Artifact Ownership and Boundedness

- Task 1 exclusively owns four source registries: upstreams, finalized capability/fairness ledger, governance, and terms. It publishes no profile or generated projection.
- Task 2 exclusively owns five closure artifacts: immutable profile TOML, its generated Markdown view, SIR-0001, canonical transcript/vector JSON, and the internal knowledge index. It reads the finalized ledger and publishes the profile last.
- The executor exclusively owns the declared `01-05-SUMMARY.md`; tasks do not edit it.
- Plan 06 owns public projection/evidence-set E; Plan 07 owns packet P, exact-P review/human checkpoint, decision, and final-SHA protocol.

This plan's nine Phase-1 evidence/contract artifacts remain one cohesive bounded M0-05 compatibility slice: four prerequisite authority registries and five atomic compatibility/negotiation closure artifacts. No file is shared between tasks, no source/runtime code is modified, and each generated projection has exactly one normative input and one owner.

## Artifacts Produced

- `docs/ecosystem/upstreams.toml` — upstream/fork/pin/patch/contribution registry.
- `docs/compat/profiles/uzel-m0.toml` — immutable evidence-bound M0 profile.
- `docs/compat/generated/uzel-m0.md` — deterministic human-readable profile projection.
- `docs/specs/interpretations/SIR-0001-manifest-build-identity.md` — manifest/exact-build interpretation record.
- `docs/compat/negotiation-vectors.json` — positive/negative/skew/interop/clean-room/local-patch vectors.
- `docs/runtime/capabilities/m0-index.toml` — per-principal capability/admission/fairness ledger.
- `docs/knowledge/m0-governance.md` — decisions, learning, visibility, upstream, and milestone governance.
- `docs/knowledge/terms.toml` — source-grounded term registry.
- `docs/knowledge/index.internal.json` — internal evidence/knowledge index.

<threat_model>
Security validation scope: OWASP ASVS Level 1, limited to registry, negotiation/admission, compatibility, and upstream boundaries.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Upstream/fork/spec identities to local registry | Remote names, revisions, ownership, licences, and local patches can drift or be spoofed. |
| Finalized capability ledger/registry/SIR to immutable compatibility profile | Pre-existing ledger and interpretive decisions enter a profile that must not bind future data or be silently mutated. |
| Canonical negotiation transcript to guest admission | Untrusted manifests, versions, peers, patches, exact-build identity, and session principals must be bound and checked before guest code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-01 | Spoofing/Tampering | upstream and fork registry | high | mitigate | Record canonical identities, exact pins/tree/lock digests, owners, drift, local patches, dedicated worktree/base/head, and human publication authority. |
| T-05-02 | Tampering/Repudiation | ledger, immutable profile, and SIR | high | mitigate | Finalize/hash ledger first; bind exact digest into staged profile; deterministic projection; explicit dissent; publish profile last; new-ID transition rather than mutation. |
| T-05-03 | Spoofing/Elevation | canonical transcript and guest admission | high | mitigate | Bind profile ID/hash, exact-build identity, ledger digest, and session principals; mismatch vectors prove rejection before guest code; retain unavailable-peer truth. |
| T-05-04 | Denial of Service | malformed/skewed negotiation inputs | medium | mitigate | Bounded vector execution, deterministic rejection, cancellation/cleanup evidence, no uncontrolled retry. |
| T-05-SC | Tampering | existing upstream/runtime dependencies | high | mitigate | No installs/upgrades; validate exact existing pins and Plan-02 closure identities, and stop registry/profile generation on drift. |
</threat_model>

<verification>
Run Tasks 1 and 2 in order. Prove Task 1's finalized immutable ledger bytes exist before Task 2 computes digest; prove published profile binds that exact digest; prove package-to-RCP binding/exposure fields and D-09/D-26 transcripts bind profile/build/session identities; prove downgrade/package/session mismatch fails closed before guest code. Confirm no public index, packet, decision, review claim, or Phase-2 authority is created; Plans 06-07 own those later gates.
</verification>

<success_criteria>
- M0-05 compatibility closure produces immutable, evidence-linked ecosystem/compatibility/capability/knowledge artifacts ready for Plans 06-07.
- Finalized capability/fairness ledger precedes immutable profile binding; generated projections cannot bind or claim future data.
- Canonical negotiation transcripts bind profile ID/hash, exact-build identity, ledger digest, and session principals; mismatch evidence proves rejection before guest execution.
- Interop, skew, clean-room, exact-build, and local-patch results remain honest about unavailable or unproven evidence.
- Uzel shell/product-service ownership, replaceable canonical engine/provider boundary, and dedicated jodobear upstream contribution workflow remain explicit; no upstream contact/publication occurs.
- EDGE-M0-05-UNCLASSIFIED and PROHIB-M0-05-FAIRNESS-INDEPENDENCE remain flagged unresolved/unverified for Plan 07 human/GitHub Codex review.
- Plan completes autonomously without creating or implying any Phase-2 transition authority.
</success_criteria>

<output>
Create `.planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-05-SUMMARY.md` when done.
</output>
