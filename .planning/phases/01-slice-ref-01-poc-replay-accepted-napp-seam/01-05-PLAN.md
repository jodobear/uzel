---
phase: 01-slice-ref-01-poc-replay-accepted-napp-seam
plan: 05
status: superseded
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - evidence/phase-01/debian13-acceptance.md
  - scripts/ref-debian-check.py
autonomous: false
requirements:
  - REF-01
  - REF-04
user_setup:
  - service: debian-13-gui
    why: "Direct visible WebKitGTK acceptance requires an x86_64 Debian 13 desktop/VM with systemd, a graphical session, at least 8 GiB RAM, and about 15 GiB free disk."
    dashboard_config:
      - task: "Make exact production baseline 19519c378c2e775c6ad4b042cfd9aadd89f766b9 available in a separate Debian checkout; do not use the shared planning worktree."
        location: "Operator-controlled Debian 13 graphical host"
must_haves:
  truths:
    - "REF-01/D-06: A human directly sees the merged rich profile/follow fixtures and exact-build composition on Debian 13, or records a precise failure without changing architecture."
    - "REF-04: Automated Debian real-WebKit evidence passes before interactive acceptance and remains distinct from Chromium and generic Weston smoke."
    - "The operator verifies exact builds, profile/follow rendering, source-bound selection, layout controls, one NMP runtime, bounded diagnostics, and clean daemon shutdown."
    - "No selected identity, invoke key, secret, host-private path, or raw unredacted log is committed in the acceptance record."
    - "D-17/D-18: This human/platform gate remains one repository-qualified issue/PR and one review sequence; failure does not reopen PR 30 or trigger adapter/Nix/CI/Social work."
    - "Review #31/#32/#33: A fixed-schema validator checks exact Debian/build/marker/evidence/checklist fields and staged-record redaction; every failure path still closes the window, reaps the daemon group, retains a raw-log hash, and records failure."
    - "D-18: After human evidence and validation produce a candidate commit, execute-plan records a machine-checkable pending review handoff and stops; no push, PR, merge, remote-review claim, or publication occurs without separate authorization."
  artifacts:
    - path: evidence/phase-01/debian13-acceptance.md
      provides: "Exact-head Debian environment, automated marker, visible checklist, cleanup result, and pass/fail evidence"
    - path: scripts/ref-debian-check.py
      provides: "Fixed-schema, exact-field, checklist, cleanup, and staged-redaction validator with negative self-tests"
  key_links:
    - from: evidence/phase-01/debian13-acceptance.md
      to: DEBIAN13-LIVE-TEST.md
      via: "Operator follows the committed automated and visible checklist at the exact production baseline"
      pattern: "DEBIAN13_LIVE_SMOKE_OK"
    - from: evidence/phase-01/debian13-acceptance.md
      to: evidence/phase-01/replay-manifest.json
      via: "Record cites the current fixture/native baseline but does not relabel it as Debian evidence"
      pattern: "replay-manifest.json"
  assumptions:
    - "[FLAGGED-UNVERIFIED] REF-01 edge classification remains unresolved; this gate covers only the documented merged rich-profile/follow path on the named Debian host."
    - "[FLAGGED-UNVERIFIED] REF-04 edge classification remains unresolved; other distributions, display stacks, hardware, and future Napp adapter behavior are not inferred."
  prohibitions:
    - "[FLAGGED-UNVERIFIED] No private Napp substitute or expanded legacy protocol."
    - "[FLAGGED-UNVERIFIED] No duplicated NMP, runtime, Nostr, or store authority."
    - "[FLAGGED-UNVERIFIED] No evidence-class conflation or fabricated metrics."
    - "[FLAGGED-UNVERIFIED] No pull-in of Nix packaging, CI architecture, Social rebuild, multi-instance implementation, or parked capabilities."
---

## Phase Goal

**As a** Uzel maintainer/reviewer, **I want to** replay current POC evidence and apply a fail-closed Napp admission gate, **so that** visible value survives without duplicating authority.

<objective>
Deliver repository-qualified issue slice `jodobear/uzel — REF-01E: Debian 13 visible acceptance` as one human/platform evidence PR.

Purpose: Close the remaining direct visual gate for merged Work 07 behavior while keeping automated native evidence, human observation, and architecture changes separate, per D-06, D-17, and D-18.
Output: One redacted exact-head Debian 13 acceptance record. No production, dependency, package, CI, Napp adapter, or historical-status change.
</objective>

<execution_context>
@/home/at/.codex/gsd-core/workflows/execute-plan.md
@/home/at/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-CONTEXT.md
@.planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-REAUDIT.md
@.planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-RESEARCH.md
@.planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-01-SUMMARY.md
</context>

## Artifacts this phase produces

- `evidence/phase-01/debian13-acceptance.md`; human/platform evidence only, no code symbols.
- `scripts/ref-debian-check.py`; Python standard-library parser/validator for the record's canonical JSON block and staged bytes.
- Operator-host `.artifacts/debian13-live/<UTC-run-id>/`; raw automated logs retained on the Debian host, referenced by redacted identifier/hash rather than copied with private paths.

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Run and record Debian 13 visible acceptance</name>
  <read_first>
    DEBIAN13-LIVE-TEST.md
    scripts/debian13-setup.sh
    scripts/debian13-live-test.sh
    scripts/linux-run-smoke.sh
    evidence/phase-01/replay-manifest.json
    evidence/phase-01/work-07-preservation.md
    uzel-poc-validated-pack/STATUS.md
  </read_first>
  <files>evidence/phase-01/debian13-acceptance.md, scripts/ref-debian-check.py</files>
  <action>Human-only reason: the current agent environment cannot observe a physical/virtual Debian graphical session. On a separate x86_64 Debian 13 systemd desktop/VM, use a clean checkout detached at production baseline `19519c378c2e775c6ad4b042cfd9aadd89f766b9`; do not switch the shared planning workspace. Follow `DEBIAN13-LIVE-TEST.md`. First run `bash scripts/debian13-setup.sh --check`; review and explicitly approve any exact package/Nix/cache changes before using the documented install path. Run `bash scripts/debian13-live-test.sh headless` and require both emitted markers `DEBIAN13_LIVE_SMOKE_OK` and `DEBIAN13_EVIDENCE_OK`; retain environment/prebuild/run/Uzel/Weston logs on the operator host. Then run interactive mode from the graphical session. Verify window ready state, profile aggregate `b6637ff3e90e13b110bafbbaf00d166e7f27e2ebd845d038630b0ba942732b26`, follow aggregate `c452783958683672fdb7b7e8987ac01ffcc125a984bfdda9cee1d3d524e166d7`, one operator-supplied read identity without recording it, NMP-backed follow rows, source-bound follow-to-profile selection, complete profile presentation, side-by-side/stacked/focus/resize/fullscreen controls, one NMP runtime and exact sessions in Developer diagnostics, bounded envelope/resource evidence, and clean private-daemon stop after window close.

Create `evidence/phase-01/debian13-acceptance.md` with exactly one marker-delimited canonical JSON record. Fixed schema fields are `schema`, `result`, `uzel_commit`, `production_tree`, `os`, `toolchain`, `markers`, `exact_builds`, `evidence`, `checklist`, `cleanup`, `failure`, `limitations`, `observed_at`, and `operator_attestation`. Require `schema: uzel.debian13-acceptance/v1`; exact Uzel commit/tree; `os.id=debian`, `version_id=13`, `arch=x86_64`, kernel/desktop/display values; exact tool versions; both marker records; both aggregate hashes; a redacted run ID plus SHA-256 for environment/prebuild/run/Uzel/Weston logs; and checklist IDs `window_ready`, `exact_builds`, `one_read_context`, `nmp_follows`, `source_bound_selection`, `profile_complete`, `layout_controls`, `one_runtime_sessions`, `bounded_diagnostics`, and `window_daemon_cleanup`, each `pass|fail` with no missing/extra/duplicate IDs. Pass requires every item and cleanup pass. Failure requires smallest step, raw-log SHA-256, and cleanup outcome.

Create `scripts/ref-debian-check.py` using Python standard library to parse the canonical block, recompute exact schema/values/counts, and self-test pass/fail/redaction cases. Reject any key/value containing selected identity material, `npub1`, `nsec1`, `nostrconnect://`, `nbunksec`, Tauri invoke-key material, secret/token/password fields, username/hostname/MAC/IP fields, absolute home/workspace/private paths, embedded raw log content, screenshots, or unintended personal-data fields; hashes, fixed Git commits, kernel/tool versions, redacted run IDs, and enum values remain allowed. Immediately before commit, stage only the two declared files and run validator against their exact index blobs so working-tree/staged substitution cannot bypass redaction.

Failure cleanup is unconditional. On first falsifier stop later assertions/source work, close the Uzel window if open, forward TERM to the active process group, wait the documented five-second grace, force/reap remaining descendants, confirm daemon/socket/process-group absence, retain operator-host raw logs and their hashes, then write `result: fail` before returning. A cleanup failure remains a failed acceptance and is recorded; it never prevents the failure record. Do not change architecture, fixtures, Napp seam, package, CI, or legacy `STATUS.md`.</action>
  <instructions>Use the separate Debian 13 graphical host only. Verify exact detached commit, run setup inventory, complete headless smoke, complete the documented interactive checklist, close the window, confirm daemon cleanup, redact the record, then return the pass/fail signal. Do not operate on the shared planning worktree.</instructions>
  <verification>Automated checks below validate record completeness and pass status; human check confirms observations were direct, identity/private host data was omitted, and cleanup was seen.</verification>
  <verify>
    <automated>python3 -c 'from pathlib import Path; compile(Path("scripts/ref-debian-check.py").read_text(encoding="utf-8"), "scripts/ref-debian-check.py", "exec")' &amp;&amp; python3 scripts/ref-debian-check.py self-test &amp;&amp; python3 scripts/ref-debian-check.py validate --record evidence/phase-01/debian13-acceptance.md --expected-result pass --expected-commit 19519c378c2e775c6ad4b042cfd9aadd89f766b9 --expected-tree ca34ad1a26beefc73657f90b142df3be415ff2a5 --staged</automated>
    <human-check>Confirm the visible checklist was observed directly on Debian 13, the selected identity and private host details are absent, and closing the window stopped the private daemon.</human-check>
  </verify>
  <acceptance_criteria>Validator proves exact Debian 13/x86_64 and production commit/tree, both emitted markers, both exact-build hashes, tool/environment/run evidence hashes, ten fixed checklist passes, no failed item, direct attestation, and staged-byte redaction. Any falsifier blocks pass but still records raw-log hash and unconditional window/daemon/process-group cleanup without source changes.</acceptance_criteria>
  <done>REF-01E direct Debian acceptance is recorded as pass; Work 07 human gate may be reconciled in new evidence but historical STATUS remains unchanged.</done>
  <resume-signal>Reply `approved-pass` after the redacted record is available for agent validation/commit, or `failed: <smallest falsifying step>` with retained run identifier/hash and cleanup result.</resume-signal>
</task>

</tasks>

<post_execution_review_gate>
## D-18 candidate handoff and separately authorized ship/review gate

- **Execute-plan ownership:** After the human checkpoint returns and the redacted record passes staged validation, capture the full 40-character plan-start HEAD as `candidate_base` and the final task commit as `candidate_head`. In `01-05-SUMMARY.md`, write one `review_handoff` object with `schema: uzel.review-handoff/v1`, `slice: ref-01e`, both SHAs, the exact task `<verify>` command/status as `affected_local_gates`, `review_evidence_path: evidence/phase-01/reviews/ref-01e-&lt;candidate_head&gt;.md`, `external_review_status: pending-separate-authorization`, and `publication_status: not-authorized`. Stop there: execution does not push, open/update a PR, merge, or claim any external review passed.
- **Evidence owner and order:** A separately authorized `$gsd-review`/`$gsd-ship` workflow creates the head-specific evidence record and fills these ordered stages, each with exact base/head plus live command output, check URL, or evidence hash: `affected_local_gates=pass`; `local_coderabbit=pass` from one full `coderabbit review --committed --base-commit &lt;base&gt;` on the immutable pair; `remote_codex=pass`; `in_scope_fixes=none|batched`; any required affected-gate/review reruns; `final_github_coderabbit=pass` on the final exact head; `unresolved_blockers=[]`. No status may be claimed from planned or stale evidence, and no selected identity, secret, credential, signer material, invoke key, raw log, private host data/path, screenshot, or unrelated private metadata may be uploaded.
- **Invalidation:** Any candidate-head change creates a new head-specific evidence path, invalidates all later review evidence tied to the old head, and restarts at affected local gates plus full local CodeRabbit before remote Codex/final GitHub CodeRabbit. Unchanged base/head and semantic inputs do not rerun expensive gates or repeat the already-admitted human observation unless the evidence inputs changed.
- **Authority boundary:** Publication, PR creation/update, and merge each require separate user authorization. Phase 3 later automates/enforces CI and exact-head aggregation; this Phase 1 contract only records the candidate/pending stop and the ordered ship gate.
</post_execution_review_gate>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator Debian host → repository evidence | Human observation and host logs cross into a committed record and require redaction/provenance. |
| untrusted napplet frame → trusted shell/Tauri | Visible acceptance must preserve source binding and deny raw network, native bridge, host paths, secrets, and caller principal. |
| Uzel runtime → NMP | One runtime/NMP owner supplies profile/follow truth; the UI and evidence record must not create another store. |
| automated headless result → human visible result | Native markers cannot substitute for direct visible behavior, and human observation cannot substitute for exact automated denial/cleanup proof. |

## STRIDE Threat Register — ASVS L1, block high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Spoofing / Repudiation | Debian acceptance record | high | mitigate | Fixed-schema validator binds result to exact commit/tree, OS/toolchain, both markers/build hashes, log hashes, ten checklist IDs, cleanup, and operator attestation. |
| T-05-02 | Information disclosure | identity and host logs | high | mitigate | Validator scans exact staged bytes/keys/values and negative fixtures for selected identity, signer/secret/invoke material, host-private details/paths, raw logs, screenshots, and personal data. |
| T-05-03 | Elevation | hostile frame/native boundary | high | mitigate | Require exact headless markers for source binding, denied raw egress/native invoke, zero sentinel/native calls, and visible one-runtime diagnostics. |
| T-05-04 | Tampering | exact builds and fixture evidence | high | mitigate | Use detached production commit, locked scripts/toolchain, both exact-build hashes, and raw evidence hashes; no fixture repin. |
| T-05-05 | Denial of service | daemon/process cleanup | medium | mitigate | On success or first falsifier, close window, TERM/grace/KILL/reap process group, confirm daemon/socket/liveness state, preserve log hash, and record cleanup before return. |
| T-05-06 | Elevation / Tampering | Napp/NMP ownership | high | mitigate | Human gate changes no adapter/store/protocol; one NMP runtime remains visible and no private fallback is introduced. |
</threat_model>

<verification>
- Human prerequisite is a separate x86_64 Debian 13 graphical host at exact production commit.
- Headless script produces exact native markers and retained redacted evidence before interactive work.
- All visible checklist items and cleanup are directly observed and recorded.
- Automated/native and human/visible evidence remain distinct.
- Acceptance record contains no selected identity, secret, invoke key, private host path, or unrelated personal data.
</verification>

<success_criteria>
- Debian 13 directly displays merged rich profile/follow composition at exact current-green pins.
- Source-bound profile selection, one NMP runtime, exact sessions, diagnostics, layout, and cleanup behave as documented.
- Failed acceptance stops at exact evidence; it never authorizes architecture or adapter changes.
- Legacy Work 07 file stays historical; new evidence carries the current human outcome.
- Phase still remains conditional on Napp qualification and later REF-01D parity.
</success_criteria>

## Review Feedback Disposition

| Review item | Status | Reason / executable disposition |
|---:|---|---|
| 31 | Accepted | Validator scans exact staged schema keys/values for identity, signer/secret/invoke material, host-private data/paths, raw logs, screenshots, and personal data. |
| 32 | Accepted | Fixed canonical JSON schema enforces Debian 13/x86_64, exact commit/tree/builds, both markers, run/log hashes, environment/toolchain, ten checklist passes, and zero failures. |
| 33 | Accepted | First falsifier stops later assertions but always executes window/process-group/daemon cleanup, retains raw-log hash, and writes failed result before return. |
| D-18 checker | Accepted | After the explicit human gate, executor records exact candidate base/head and pending handoff; separately authorized review/ship fills one redacted head-bound ordered evidence record, with head-change invalidation and no automatic publication. |

<output>
Create `.planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-05-SUMMARY.md` when done. Record repository-qualified issue/branch/PR only if already available, exact Debian-tested commit/environment, redacted evidence path/hash, checklist result, cleanup, limitations, any smallest falsifier, and the required `review_handoff` pending-stop object without performing external publication.
</output>
