---
phase: 01-slice-ref-01-poc-replay-accepted-napp-seam
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - evidence/phase-01/ownership-disposition.json
  - evidence/phase-01/state-retention.json
  - evidence/phase-01/resource-bounds.json
  - scripts/ref-evidence-check.py
  - scripts/phase-01-measure.sh
  - evidence/phase-01/measurements/baseline.json
autonomous: true
requirements:
  - REF-03
  - REF-05
  - REF-06
must_haves:
  truths:
    - "REF-05/D-05: Every relevant current POC file or state owner is classified with exact repository, commit, tests, disposition, rationale, and revalidation trigger."
    - "REF-03/D-13/D-14: One Uzel instance and one active read profile are the accepted baseline; NMP remains the only owner of Nostr events, queries, relays, freshness, provenance, signing, and publication."
    - "D-10/D-15: Stored and transient state records its writable owner, durability, schema/version, retention, recovery, migration trigger, cancellation/cleanup behavior, and preserved-Poc rollback."
    - "REF-06/D-07/D-08/D-09: Startup, ready/first-frame, local profile, process/WebView pressure, resource flow, queue, cancellation, and recovery measurements are raw/reproducible or explicitly unavailable, with build/cache/runtime modes kept separate."
    - "D-05/D-08: Evidence is admitted only when every exact-commit source/test anchor resolves to its expected literal and every measured value is reproduced from a confined hash-verified raw output; unavailable records carry concrete reasons and no samples."
  artifacts:
    - path: evidence/phase-01/ownership-disposition.json
      provides: "File-level Uzel/Napp/upstream/compatibility/obsolete classification"
    - path: evidence/phase-01/state-retention.json
      provides: "Writable-owner, durability, retention, recovery, and migration ledger"
    - path: evidence/phase-01/resource-bounds.json
      provides: "Exact-source surface, envelope, queue, retry, URL, state, session, and resource ceilings"
    - path: scripts/ref-evidence-check.py
      provides: "Phase-specific source-anchor, test-coverage, disposition, and measurement-provenance validator"
    - path: scripts/phase-01-measure.sh
      provides: "Bounded single-instance and representative measurement probe"
    - path: evidence/phase-01/measurements/baseline.json
      provides: "Mode-separated raw samples or honest unavailable results"
  key_links:
    - from: evidence/phase-01/ownership-disposition.json
      to: crates/napd/src/runner.rs
      via: "Classifies current runtime adapter and its tests without declaring private protocol as accepted Napp API"
      pattern: "compatibility-only"
    - from: evidence/phase-01/state-retention.json
      to: crates/napd/src/runner.rs
      via: "Anchors bounded product state separately from NMP canonical data"
      pattern: "uzel-state.json"
    - from: evidence/phase-01/resource-bounds.json
      to: apps/uzel/public/trusted-shell/trusted-shell.js
      via: "Records source identifier, literal limit, owner, cleanup, and validating test"
      pattern: "source_anchor"
    - from: scripts/phase-01-measure.sh
      to: evidence/phase-01/replay-manifest.json
      via: "Reuses Plan 01 exact evidence head/modes instead of rerunning or relabeling proof"
      pattern: "replay-manifest.json"
    - from: scripts/ref-evidence-check.py
      to: evidence/phase-01/ownership-disposition.json
      via: "Resolves every source/test anchor at its exact Git commit and enforces the Phase-01 ownership/disposition matrix"
      pattern: "source_anchor"
  assumptions:
    - "[FLAGGED-UNVERIFIED] REF-05 edge classification is unresolved; the disposition schema is Phase 1 evidence, not a future Napp public contract."
    - "[FLAGGED-UNVERIFIED] REF-06 edge classification is unresolved; representative single samples establish method and current observation, not p50/p95 economics."
    - "[FLAGGED-UNVERIFIED] REF-03 remains limited to one instance and one active read profile; multi-instance and multi-profile isolation are unproved."
  prohibitions:
    - "[FLAGGED-UNVERIFIED] No private Napp substitute or expanded legacy protocol."
    - "[FLAGGED-UNVERIFIED] No duplicated NMP, runtime, Nostr, or store authority."
    - "[FLAGGED-UNVERIFIED] No evidence-class conflation or fabricated metrics."
    - "[FLAGGED-UNVERIFIED] No pull-in of Nix packaging, CI architecture, Social rebuild, multi-instance implementation, or parked capabilities."
---

## Phase Goal

**As a** Uzel maintainer/reviewer, **I want to** replay current POC evidence and apply a fail-closed Napp admission gate, **so that** visible value survives without duplicating authority.

<objective>
Deliver repository-qualified issue slice `jodobear/uzel — REF-01B: classify POC ownership, storage, retention, bounds, and pressure` as one bounded PR.

Purpose: Turn exact current source into a durable ownership/state/resource contract and a small honest baseline without extraction, migration, new storage, or architecture work, per D-05, D-07 through D-10, D-13 through D-15, D-17, and D-18.
Output: Three machine-readable ledgers, one bounded measurement probe, and one baseline record. Production Rust, Tauri, Svelte, napplet, fixture, protocol, dependency, and lock files remain unchanged.
</objective>

<execution_context>
@/home/at/.codex/gsd-core/workflows/execute-plan.md
@/home/at/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-CONTEXT.md
@.planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-REAUDIT.md
@.planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-RESEARCH.md
@.planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-PATTERNS.md
@.planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-01-SUMMARY.md
</context>

## Artifacts this phase produces

- `evidence/phase-01/ownership-disposition.json`; no code symbols.
- `evidence/phase-01/state-retention.json`; no code symbols.
- `evidence/phase-01/resource-bounds.json`; no code symbols.
- `scripts/ref-evidence-check.py`; small Phase-01-only Python standard-library validator for ledgers and measurements.
- `scripts/phase-01-measure.sh`; new shell entry point with internal functions `record_measure`, `probe_instance`, and `stop_probe`.
- `evidence/phase-01/measurements/baseline.json`; tracked method/result index.
- `.artifacts/phase-01/measurements/<run-id>/`; ignored raw logs/environment samples referenced from the baseline.

<tasks>

<task type="auto">
  <name>Task 1: Classify file ownership, state retention, and exact resource bounds</name>
  <read_first>
    evidence/phase-01/replay-manifest.json
    evidence/POC_HANDOFF.md
    uzel-poc-validated-pack/docs/02-architecture.md
    uzel-poc-validated-pack/docs/03-provisional-design.md
    uzel-poc-validated-pack/docs/08-upstream-contributions.md
    crates/napd/src/runner.rs
    crates/napd/src/server.rs
    crates/napd/src/resource.rs
    crates/napd-protocol/src/lib.rs
    apps/uzel-napd/src/main.rs
    apps/uzel/src-tauri/src/main.rs
    apps/uzel/src/App.svelte
    apps/uzel/public/trusted-shell/trusted-shell.js
    apps/uzel/public/trusted-shell/trusted-shell-surface-host.js
    scripts/check-boundaries.sh
    scripts/check-napplet-imports.mjs
  </read_first>
  <files>evidence/phase-01/ownership-disposition.json, evidence/phase-01/state-retention.json, evidence/phase-01/resource-bounds.json, scripts/ref-evidence-check.py</files>
  <action>Build the three JSON ledgers from exact source at the replayed head. Every `source_anchor`, `tests`, `validated_by`, evidence reference, or value written in `file:line` form must carry an adjacent exact `repository`, 40-character `commit`, repository-relative `path`, positive `line`, `expected_literal`, and named `fact`; no bare path/line assertion is admissible. `ownership-disposition.json` gets one row per relevant product/runtime/protocol/trusted-host/fixture/contract/napplet/test/harness element with those anchors plus `disposition`, `acceptance`, `current_owner`, `authority`, `public_contract`, `rationale`, and `revalidate_when`. Allowed disposition semantics are exact: `retained-uzel` stays Uzel product/trusted-host ownership and cannot claim Nostr authority; `consumed-napp-contract` is Napp-owned and must remain `unaccepted` with no Uzel-private symbol while D-01 is unmet; `neutral-upstream-candidate` records current Uzel ownership, proposed upstream owner, and unaccepted handoff; `compatibility-only` is private/non-public and names adapter-parity retirement evidence; `obsolete` requires exact proof but authorizes no removal in this plan. `state-retention.json` records NMP canonical data, runner `uzel-state.json`, runtime/NMP stores, XDG/socket state, Svelte preferences, object URLs, pending/retry state, fixture/build artifacts, and absent OS service state with writable owner, authority, durability, schema/version, retention, invalidation, recovery, cleanup, migration trigger, rollback, scope, and anchored evidence. Enforce NMP ownership for all Nostr query/relay/store/freshness/provenance/signer/publication facts; Uzel rows may hold only bounded product/UI state. Lock acceptance to one Uzel instance/endpoint and one active read profile per D-13. `resource-bounds.json` records actual identifier, literal value, anchored source, owner, anchored validating test/harness, named fact, and terminal cleanup for every evidenced surface, review, event, envelope, frame, replay, state-file, operation-ID, query, retry, subscription, object-URL, resource-byte, timeout, and process bound. Do not infer values, generalize individual `resource.bytes` proof into whole-operation `bytesMany`, add a store, or edit production.

Create complete `scripts/ref-evidence-check.py` in this task using only Python standard library plus existing `git`. Keep it specific to the three fixed ledgers and Phase-01 baseline; no schema/plugin framework. Its `ledgers` command recursively finds every anchor and `file:line` reference, rejects traversal/absolute paths, verifies the exact commit object, reads source with `git show &lt;commit&gt;:&lt;path&gt;`, and requires the declared line to contain the exact expected literal. For each referenced test, require an existing test/harness path, an anchored Rust `fn`, Node `test`/`scenarioTest`, or shell marker/check declaration, and a `coverage_literal` inside that declaration's bounded body which asserts the row's named fact; encode the finite Phase-01 fact vocabulary (`ownership-boundary`, `source-binding`, `native-denial`, `exact-build`, `replay-idempotency`, `cancellation`, `cleanup`, `state-retention`, `state-rollback`, `lifecycle-recovery`, `instance-collision`, `socket-safety`, `resource-bound`, `resource-flow`, `fixture-integrity`) with allowed source/test file classes and reject mismatched fact/test kinds. Enforce the disposition/current-owner/authority/acceptance/public-contract matrix above, the one-instance/one-active-profile scope, NMP canonical categories, non-empty rationale/revalidation/cleanup fields, literal numeric bound equality, and replay-manifest head linkage. Fail on a missing/moved literal, nonexistent or non-covering test, unknown disposition/fact, private protocol labeled public, unaccepted contract labeled consumed, or unsupported ownership combination.</action>
  <verify>
    <automated>python3 -c 'from pathlib import Path; compile(Path("scripts/ref-evidence-check.py").read_text(encoding="utf-8"), "scripts/ref-evidence-check.py", "exec")' &amp;&amp; python3 scripts/ref-evidence-check.py ledgers --repo . --replay-manifest evidence/phase-01/replay-manifest.json --ownership evidence/phase-01/ownership-disposition.json --state evidence/phase-01/state-retention.json --bounds evidence/phase-01/resource-bounds.json</automated>
  </verify>
  <acceptance_criteria>Every ledger row and every `file:line`/source/test anchor is resolved at its exact commit and expected literal; every referenced test/harness contains an allowed declaration and coverage literal for the named fact. Ownership/disposition semantics are validator-admitted. NMP remains sole Nostr authority; Uzel state stays bounded product/UI state; unknown future Napp contracts remain unaccepted; one-instance/one-profile scope is explicit; every literal limit names cleanup and proof.</acceptance_criteria>
  <done>REF-03/05 ownership, state, retention, rollback, and resource ceilings are inspectable without source extraction or duplicate truth.</done>
</task>

<task type="auto">
  <name>Task 2: Measure a bounded single-instance current baseline</name>
  <read_first>
    evidence/phase-01/replay-manifest.json
    evidence/phase-01/state-retention.json
    evidence/phase-01/resource-bounds.json
    scripts/phase-01-replay.sh
    scripts/linux-run-smoke.sh
    apps/uzel/tests/ui/acceptance.test.mjs
    apps/uzel-napd/src/main.rs
    crates/napd/src/runner.rs
    crates/napd/src/resource.rs
  </read_first>
  <files>scripts/phase-01-measure.sh, evidence/phase-01/measurements/baseline.json</files>
  <action>Create strict `scripts/phase-01-measure.sh` with an explicit output root, bounded timeouts, signal forwarding, and process-group cleanup. Enter the same locked flake shell for every repository probe: use `nix develop --command pnpm ...` for package scripts and `nix develop --command cargo ...` for direct Rust probes; record argv and reject ambient tool invocation. Reuse Plan 01 run IDs only as functional observations linked to the already-validated replay manifest; never copy them into numeric measurement samples. Take one representative named sample where the current host supports a reproducible method for clean startup-to-ready, first visible frame, local profile render, idle daemon CPU/RSS, WebKit process pressure, resource flow, queue/cancellation, lifecycle recovery, and second-instance collision against the same explicit socket/XDG root. Keep `dependency-cache`, `build`, `fixture`, and `release-runtime` records separate. The collision probe must start one controlled fixture daemon, record its ready/health marker, attempt a second against the same endpoint with a bounded deadline, capture the exact active-socket refusal and nonzero exit, prove the first daemon remains healthy after refusal, then idempotently stop both/process groups and verify socket cleanup; it must not claim supported multi-instance behavior.

For each measurement write `metric`, `unit` or null, allowed `mode`, `method`, `result`, `command` argv, exact environment/head, candidate null, raw-output path/hash, limitation, and result-specific fields. Store every measurement raw file canonically below `.artifacts/phase-01/measurements`; a `measured` sample is an object containing value plus raw file, line, and deterministic extractor/capture, so `ref-evidence-check.py baseline` can re-read that line and reproduce the value. `dependency-cache` and `build` duration may not be labeled runtime; startup/frame/profile/CPU/RSS/WebKit metrics require `release-runtime`; fixture observations cannot become runtime latency. `unavailable` requires an allowed reason code, concrete method/host reason, attempted or blocked command, empty samples, and raw probe output when attempted. `failed` requires a falsifier and raw output. Extend no validator code in this task: run its already-defined `baseline` command to resolve every raw path below the measurement root, recompute SHA-256, reproduce every numeric sample, enforce metric/mode/unit/result consistency, reject p50/p95 or any sample without raw extraction, verify timeout/cancel/recovery records, and require the collision object's ready, second-instance-refused, first-still-healthy, process-reaped, idempotent-stop, and socket-removed evidence anchors. If WebView memory, visible profile timing, or operator-host service state lacks a named observable method, record unavailable rather than a numeric claim per D-08. Do not translate test counts, Nix realization, shell materialization, build duration, or qualitative observations into runtime performance. Retain falsifiers and stop; no production fix is authorized.</action>
  <verify>
    <automated>bash -n scripts/phase-01-measure.sh &amp;&amp; python3 scripts/ref-evidence-check.py baseline --repo . --replay-manifest evidence/phase-01/replay-manifest.json --manifest evidence/phase-01/measurements/baseline.json --artifact-root .artifacts/phase-01/measurements</automated>
  </verify>
  <acceptance_criteria>Every required metric has validator-recomputed raw output/hash and either reproducible extracted samples or an honest unavailable/failed record with concrete reason. Mode/result/unit semantics cannot relabel build, fixture, or qualitative output as runtime measurement. Single-instance refusal, first-daemon health, deadline/cancel behavior, idempotent stop, process reaping, and socket cleanup are raw-evidenced. No synthetic sample, p50/p95, multi-instance, multi-profile, package, or CI claim is introduced.</acceptance_criteria>
  <done>REF-06 has a small current baseline with correct mode separation; REF-03 single-instance/read-profile limits and cleanup are explicit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source/test corpus → evidence ledgers | Exact constants, owners, tests, and limitations must survive classification without invented semantics. |
| measurement harness → daemon/WebKit processes | Probes cross process, XDG, socket, and cleanup boundaries and must remain bounded/cancellable. |
| Uzel product state → NMP canonical state | Uzel may retain bounded UI/read-selection state but may not become another Nostr/freshness/store owner. |
| untrusted frame → resource/native boundary | Ledgers must preserve source binding and denial of raw network, native bridge, host paths, secrets, and caller principal. |

## STRIDE Threat Register — ASVS L1, block high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | ownership/state/bounds ledgers | high | mitigate | Phase-specific validator resolves exact-commit source/test anchors and coverage literals, then enforces literal values, owners, disposition/acceptance semantics, and revalidation triggers; inferred future Napp ownership fails. |
| T-02-02 | Elevation / Information disclosure | NMP and Uzel stores | high | mitigate | Declare NMP sole Nostr authority and prohibit new profile/follow/event/freshness stores, secrets, signer state, or caller-selected principals. |
| T-02-03 | Denial of service | instance/pressure probes | high | mitigate | Use explicit ephemeral XDG/socket roots, deadlines, process groups, signal forwarding, idempotent stop, and post-cleanup liveness checks. |
| T-02-04 | Spoofing / Elevation | frame/resource classification | high | mitigate | Anchor source-created frame binding, runtime surface identity, HTTPS/resource limits, native denial, and object-URL cleanup to existing tests. |
| T-02-05 | Repudiation | performance measurements | medium | mitigate | Store method, locked-shell argv, environment, exact head, confined raw path/hash, deterministic sample extractor, result, limitations, and unavailable reason; validator reproduces samples and rejects evidence-class conversion. |
| T-02-06 | Tampering | future migration/rollback | medium | mitigate | Record preserved-Poc pin/adapter revert as D-15 rollback and forbid dual write/store or compatibility retirement before parity. |
</threat_model>

<verification>
- `scripts/ref-evidence-check.py ledgers` resolves every exact-commit source/test anchor, validates test coverage literals, and enforces ownership/disposition semantics for all three ledgers.
- `bash -n scripts/phase-01-measure.sh` passes.
- `scripts/ref-evidence-check.py baseline` validates raw paths/hashes, exact extraction, mode/result consistency, unavailable reasons, collision health/refusal, deadline/cancel, and cleanup for startup, frame, profile, process, WebView, resource, queue/cancel, recovery, and collision categories.
- `git diff --name-only` contains only six declared evidence/harness files; no runtime/state/store source changed.
</verification>

<success_criteria>
- Maintainer can answer who owns each current POC file/state/resource mechanism and when it must be revalidated.
- NMP and runtime authority remain singular; Uzel persistence stays bounded and product-facing.
- One-instance/one-active-profile baseline and observed collision behavior are explicit without new implementation.
- Measurements are reproducible, mode-correct, and honest about unsupported observations.
- D-15 rollback remains preserved current POC pin/adapter revert; no compatibility code is retired.
</success_criteria>

<output>
Create `.planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-02-SUMMARY.md` when done. Record repository-qualified issue/branch/PR if available, exact source head, ledger/measurement paths, limitations, falsifiers, and next probe.
</output>
