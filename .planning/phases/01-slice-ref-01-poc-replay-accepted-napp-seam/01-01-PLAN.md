---
phase: 01-slice-ref-01-poc-replay-accepted-napp-seam
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - evidence/phase-01/incident-state.json
  - evidence/phase-01/historical-claim-ledger.json
  - evidence/phase-01/historical-tool-inventory.json
  - evidence/phase-01/exact-closure-plan.json
  - evidence/phase-01/replay-manifest.json
  - evidence/phase-01/replay-claim-impact.json
  - evidence/phase-01/replay-artifacts.sha256
  - evidence/phase-01/b185ad1-disposition.md
  - scripts/phase-01-validate-incident.sh
  - graphify-out/
  - .planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-01-SUMMARY.md
autonomous: false
requirements:
  - M0-01
user_setup: []
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-12 through D-19 and D-22: b185ad1, its parent, blocked worktree, safety ref, and portable checksummed archive remain intact while one bounded exact-source replay produces an allowed honest verdict."
    - "D-13: historical replay remains a distinct evidence class; it neither repairs nor substitutes for current-source or current package evidence."
    - "D-14 through D-17: every replay command has a source/lock/integrity origin, fixed closure, reviewed lifecycle policy, disposable state roots, and demonstrated external-egress denial."
    - "D-16: the fixed closure and reviewed lifecycle policy fail closed on any unclassified or mutable input."
    - "D-18 and 01-BASELINE-REPLAY.md: one inventory, one realization path, one replay attempt, and at most one evidence-proven harness/isolation correction bound the work."
    - "D-19 and D-30: every unavailable, failed, or superseded critical claim has independent current-source replacement evidence or remains an explicit Phase-1 blocker."
    - "D-31: no task starts until Prompt 02 independent review passes for the exact plan head and the human authorizes Phase-1 execution."
  artifacts:
    - path: evidence/phase-01/incident-state.json
      provides: "Immutable incident, worktree, safety-ref, archive, and planning-head inventory"
    - path: evidence/phase-01/historical-tool-inventory.json
      provides: "Claim-linked package-manager, lock, Vite, conformance-tool, script, service, and lifecycle-script origins"
    - path: evidence/phase-01/exact-closure-plan.json
      provides: "Single fixed-closure realization, isolation, network, attempt-budget, cleanup, restart, and cancellation contract"
    - path: evidence/phase-01/replay-manifest.json
      provides: "Exact-source replay input, raw-evidence digest, egress proof, and top-level verdict"
    - path: evidence/phase-01/replay-claim-impact.json
      provides: "Claim-by-claim reproduced, replaced, retired, unproven, or blocking status"
    - path: evidence/phase-01/b185ad1-disposition.md
      provides: "Evidence-based final disposition without deleting or rewriting preserved incident objects"
  key_links:
    - from: evidence/phase-01/historical-tool-inventory.json
      to: evidence/phase-01/exact-closure-plan.json
      via: "Every selected byte/tool/script identity is admitted from the inventory; no unclassified command reaches materialization"
      pattern: "inventory_sha256"
    - from: evidence/phase-01/exact-closure-plan.json
      to: evidence/phase-01/replay-manifest.json
      via: "Replay records the selected plan digest, attempt number, sandbox roots, egress probe, and exact argv"
      pattern: "closure_plan_sha256"
    - from: evidence/phase-01/replay-manifest.json
      to: evidence/phase-01/replay-claim-impact.json
      via: "Top-level and per-command outcomes dispose every historical claim"
      pattern: "claim_id"
    - from: evidence/phase-01/replay-claim-impact.json
      to: evidence/phase-01/b185ad1-disposition.md
      via: "Final WIP disposition cites replay and replacement evidence, never plan intent alone"
      pattern: "evidence_sha256"
  assumptions:
    - id: EDGE-M0-01-UNCLASSIFIED
      requirement: M0-01
      category: unclassified
      statement: "The spec-less edge probe could not classify M0-01; manual review must confirm that incident preservation, exact-closure replay, critical-claim replacement, and final disposition criteria cover every material historical claim."
      status: unresolved
      flagged: true
  prohibitions:
    - id: PROHIB-M0-01-TRANSPARENCY
      requirement: M0-01
      category: transparency
      statement: "MUST NOT rewrite preserved incident history, mutate historical source/locks, or present unavailable/failed replay evidence as a green historical claim."
      status: unverified
      flagged: true
---

## Phase Goal

**As a** Uzel owner, **I want to** preserve the current POC and incident evidence while separating historical replay truth from current evidence, **so that** Phase 1 can reset execution without inventing a green baseline.

<objective>
Reconcile the blocked Phase-1 incident and execute one bounded, hostile-egress-denied replay from exact historical source and fixed dependency identities, ending in an evidence-based b185ad1 disposition per D-12 through D-19 and D-22.

Purpose: Satisfy M0-01 without product behavior, dependency, historical-source, lock, worktree, ref, or archive mutation.

Output: Immutable incident/tool/claim inventories, one exact-closure plan, one replay verdict and claim-impact table, raw-artifact checksums, and final disposition record.
</objective>

<execution_context>
@/home/at/.codex/gsd-core/workflows/execute-plan.md
@/home/at/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-CONTEXT.md
@.planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-REORIENTATION.md
@docs/plans/uzel-product-incubation-v4-2026-08-10/00-GSD-INGEST.md
@docs/plans/uzel-product-incubation-v4-2026-08-10/01-BASELINE-REPLAY.md
@docs/plans/uzel-product-incubation-v4-2026-08-10/04-DELIVERY-QUALITY.md
@docs/plans/uzel-product-incubation-v4-2026-08-10/06-START-RUNBOOK.md
@docs/plans/uzel-product-incubation-v4-2026-08-10/prompts/02-review-phase-1.md
@package.json
@pnpm-lock.yaml
@Cargo.toml
@Cargo.lock
@flake.nix
@flake.lock
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Trace one critical historical claim from preserved incident object to an admitted exact closure</name>
  <precondition>The applicable approved Prompt 02 path for the exact Phase-1 plan-set Git head records no unresolved blocking finding—local CodeRabbit pass plus green GitHub Codex, or a recorded pre-finding CodeRabbit rate limit plus green GitHub Codex—and the human has explicitly authorized Phase-1 implementation. Before any task write, resolve both Git objects and exact refs in the current repository; resolve the original blocked worktree from `git worktree list --porcelain` by branch `refs/heads/gsd/phase-01-plan-01-replay` plus HEAD `b185ad1b8d9d034d151406b12aa189f5a6be970f`; and require `UZEL_INCIDENT_ARCHIVE_DIR` to name the readable restored archive defined by `06-START-RUNBOOK.md`: its outer `SHA256SUMS` has exactly the runbook's eight paths, verifies every retained byte including `blocked-worktree-v2`, and its bundle and metadata retain the exact digests in `01-REORIENTATION.md`. If any coordinate is absent, ambiguous, dirty, or mismatched, stop for incident-evidence restoration before creating an output.</precondition>
  <files>evidence/phase-01/incident-state.json, evidence/phase-01/historical-claim-ledger.json, evidence/phase-01/historical-tool-inventory.json, evidence/phase-01/exact-closure-plan.json, scripts/phase-01-validate-incident.sh</files>
  <read_first>
    - .planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-CONTEXT.md — D-12 through D-19, D-22, D-27, D-29, D-31
    - .planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-REORIENTATION.md — content-addressed incident identities, last-observed locators, and restoration precondition
    - docs/plans/uzel-product-incubation-v4-2026-08-10/01-BASELINE-REPLAY.md — incident, tool classification, closure, isolation, attempt, and verdict contracts
    - docs/plans/uzel-product-incubation-v4-2026-08-10/prompts/02-review-phase-1.md — exact pre-execution independent-review criteria
    - package.json, pnpm-lock.yaml, Cargo.toml, Cargo.lock, flake.nix, flake.lock — current comparison inputs only; historical inputs must be read from the recorded revision
  </read_first>
  <action>Implement D-12, D-14, D-17, D-18, and D-22 as a production-quality evidence tracer. Treat paths in `01-REORIENTATION.md` only as last-observed hints: verify `b185ad1b8d9d034d151406b12aa189f5a6be970f` and parent `431e37af5ca86196dbaf08a534a0a7626c4ae32c` with `git cat-file`/`git rev-parse`; require the exact safety ref; uniquely resolve the registered blocked worktree by branch+HEAD; resolve archive files only below the literal directory supplied in `UZEL_INCIDENT_ARCHIVE_DIR`; reject symlinks, non-regular files, extra matches, path traversal, and digest drift. Eliminate archive TOCTOU before any checksum or bundle verification: open the archive root and `SHA256SUMS` with directory-FD-relative `openat`/`O_NOFOLLOW`, reject unsafe metadata, parse the manifest from that opened FD, then open each of the exact eight runbook-manifest targets -- `b185ad1.bundle`, `b185ad1-metadata.txt`, `blocked-worktree-v2/SHA256SUMS`, `blocked-worktree-v2/blocked-status-v2.z`, `blocked-worktree-v2/blocked-unstaged.patch`, `blocked-worktree-v2/blocked-staged.patch`, `blocked-worktree-v2/blocked-untracked.z`, and `blocked-worktree-v2/blocked-untracked.tar` -- using directory-FD-relative traversal that applies `O_NOFOLLOW`/directory checks to every path component. Copy bytes from those already-open regular-file FDs into the corresponding safe relative paths in a private task-owned `0700` snapshot directory, write the outer manifest bytes read from its FD, fsync files and directories, and record source-FD metadata plus SHA-256 while copying. Verify `sha256sum -c` and `git bundle verify` only against this immutable snapshot; never reopen or trust the mutable supplied archive paths after snapshot creation. Require exactly eight outer-manifest rows with 64-lowercase-hex digests and the exact sorted runbook entry set above; reject absolute names, empty/`.`/`..` components, traversal, duplicates, symlinks in any component, non-regular targets, root escapes, missing nested snapshot bytes, or any extra entry. Verify the nested `blocked-worktree-v2/SHA256SUMS` as an exact five-row manifest for the five blocked-worktree snapshot files before accepting the outer archive. Retain the snapshot only for task verification under disposable evidence storage; report its method, source FD inode/device/mode, copy digests, parsed entries, and verification results in `incident-state.json`. Capture `git show --format=fuller --stat`, `git worktree list --porcelain`, ref identity, current Phase-1 head/tree, and blocked-worktree staged/unstaged/untracked state without modifying any ref or worktree. In `incident-state.json`, record the exact restoration identity as structured fields: WIP commit/tree and parent commit/tree; SHA-256 of the parent-to-WIP four-path binary diff; the exact four WIP artifact rows from `git ls-tree` and `git show`, with path, mode, blob ID, content SHA-256, and role (`observed_evidence`, `claims_assumptions_manifest`, `replay_harness`, or `validator_tests`); safety-ref full name and target; blocked-worktree branch, HEAD, tree, and zero staged/unstaged/untracked counts; archive coordinate source `UZEL_INCIDENT_ARCHIVE_DIR`; the exact eight outer archive entry paths plus `SHA256SUMS`; fixed bundle/metadata SHA-256 values from `01-REORIENTATION.md`; computed outer-manifest and nested-manifest SHA-256 values; every outer and nested path/digest pair; and snapshot-only outer-checksum, inner-checksum, and bundle-verification results. Reject any missing or extra parent-to-WIP path, so the preserved harness, validator/tests, evidence, and assumptions cannot be silently replaced by metadata status. The archive's blocked-worktree snapshot is preserved historical evidence, while the registered worktree must also be verified independently by exact HEAD/tree and current clean index/worktree facts; record both evidence classes without treating the archive snapshot as proof of current cleanliness. Create `scripts/phase-01-validate-incident.sh` as executable, no-network, fail-closed validator: live-resolve safety ref and registered worktree, construct the O_NOFOLLOW FD snapshot, verify both checksum manifests and bundle bytes, and compare each live/snapshot coordinate to `incident-state.json`; it must reject any mismatch. Write atomic JSON records, replacing only a task-owned temporary file after schema validation; never overwrite prior raw evidence. Build a claim ledger whose `required_categories` is exactly `source_bound_guest_boundary`, `package_manager`, `lockfile`, `node_source`, `rust_source`, `nix_source`, `vite`, `conformance`, `repository_script`, `environment`, `generated_input`, `port_or_socket`, `lifecycle_script`, `native_dependency`, and `command`; every category has at least one unique claim. Add exactly one tool-linked claim per historical inventory `tool_id` using immutable `source_tool_id`; no missing, duplicate, or extra source-tool link is allowed. Every claim has unique nonempty `claim_id`, exact category, boolean criticality, and source identity. Inspect the exact historical revision using `git show`/`git ls-tree`, not the current worktree, and classify package manager, lockfiles, Node/Rust/Nix sources, Vite, conformance tooling, scripts, environment, generated inputs, ports/sockets, lifecycle scripts, native dependencies, and every command using only the six D-14 origin enums. Require a nonempty `tools` array whose unique entries each have an allowed origin, exact historical evidence path/blob/content digest, and `closure_id`; include explicit categories `vite`, `conformance`, `lifecycle_script`, `command`, and `lockfile`, and require every tool's closure_id to exist in the sole selected closure. Select exactly one closure path from fixed identities. Record fixed-byte admission, reviewed lifecycle-script allowlist or empty list, public-DNS/external-egress denial, enumerated loopback/Unix fixtures, disposable HOME/XDG/cache/temp/build roots, one-attempt budget, one conditional harness-only correction, idempotent cleanup, restart refusal on input-digest drift, and INT/TERM cancellation evidence. In `exact-closure-plan.json`, define nonempty unique `network.local_fixtures[]` rows with `fixture_id`, kind `loopback_tcp` or `unix_socket`, and exact endpoint, plus exactly three unique `network.probes[]` rows with IDs `external_egress`, `public_dns`, and `local_fixture`. Each probe fixes the exact nonempty argv, target, admitted historical `tool_id`, selected `closure_id`, and expected status; target must occur literally in argv. The local probe additionally names a fixture ID and target exactly equal to that fixture endpoint; egress/DNS probes have no fixture ID and their targets must not equal any local endpoint. No Task-2 probe command or target may differ from this admitted inventory. Stop before materialization if any required tool identity, lock integrity, historical revision, WIP parent, original worktree, archive FD snapshot, snapshot checksum, or isolation control is ambiguous.</action>
  <verify>
    <automated>test -x scripts/phase-01-validate-incident.sh && scripts/phase-01-validate-incident.sh --incident evidence/phase-01/incident-state.json && python3 -m json.tool evidence/phase-01/incident-state.json &gt;/dev/null &amp;&amp; python3 -m json.tool evidence/phase-01/historical-claim-ledger.json &gt;/dev/null &amp;&amp; python3 -m json.tool evidence/phase-01/historical-tool-inventory.json &gt;/dev/null &amp;&amp; python3 -m json.tool evidence/phase-01/exact-closure-plan.json &gt;/dev/null &amp;&amp; jq -e '(.wip.commit == "b185ad1b8d9d034d151406b12aa189f5a6be970f") and (.wip.tree == "0e5635a6c2a843438b418de51b3f1240d6d4f52f") and (.wip.parent == "431e37af5ca86196dbaf08a534a0a7626c4ae32c") and (.wip.parent_tree == "54aebc9bbcdacbc44581ee5b9ead0b62e285c68f") and (.wip.binary_diff_sha256 == "47f2036a0dfd1a0fae699d5ec3be3be5d8d81a56c5673c0d591afe58490dfb6b") and (([.wip.artifacts[] | [.path,.mode,.blob,.sha256,.role]] | sort) == [["evidence/phase-01/replay-evidence.jsonl","100644","95835ccba37d7aae5d45273813babcb332866e3a","71623dbf4d34f9491d070cdd13b332801ce519d574d5603c5334750bc5800af6","observed_evidence"],["evidence/phase-01/replay-manifest.json","100644","34d50ef45ea6b43ede7e0872130f65e9ace3cdb2","f631b56358083c9481506ff3dc89ccd7373391da7e0cd926a3ec01e12464938b","claims_assumptions_manifest"],["scripts/phase-01-replay.sh","100755","e543e4125794eae59722ddf12935fa7b8f3cfd8a","469af9c3423c61263887979df70b2588678161c2757b6ad61f669ed5cac6f1d4","replay_harness"],["scripts/ref-replay-check.py","100644","1473a55e15fd868f5eb356056ff80460996bbf91","9b02b950852e76d9e9bbf6c53718a371ba82e9c1c4f65f2d6166625eb4668eac","validator_tests"]]) and (.safety_ref.name == "refs/heads/wip/phase-1-replay-b185ad1") and (.safety_ref.target == .wip.commit) and (.blocked_worktree.branch == "refs/heads/gsd/phase-01-plan-01-replay") and (.blocked_worktree.head == .wip.commit) and (.blocked_worktree.tree == .wip.tree) and (.blocked_worktree.staged_count == 0) and (.blocked_worktree.unstaged_count == 0) and (.blocked_worktree.untracked_count == 0) and (.archive.coordinate_source == "UZEL_INCIDENT_ARCHIVE_DIR") and (.archive.snapshot.method == "openat_o_nofollow_fd_copy") and (.archive.snapshot.verified_only_from_snapshot == true) and (.archive.snapshot.root_mode == "0700") and ((.archive.snapshot.source_fds | type == "array" and length == 9) and ([.archive.snapshot.source_fds[].name] | sort) == (["SHA256SUMS"] + (["b185ad1.bundle","b185ad1-metadata.txt","blocked-worktree-v2/SHA256SUMS","blocked-worktree-v2/blocked-status-v2.z","blocked-worktree-v2/blocked-unstaged.patch","blocked-worktree-v2/blocked-staged.patch","blocked-worktree-v2/blocked-untracked.z","blocked-worktree-v2/blocked-untracked.tar"] | sort) | sort) and all(.archive.snapshot.source_fds[]; (.device | type == "number") and (.inode | type == "number") and (.mode | type == "string") and (.sha256 | test("^[0-9a-f]{64}$")))) and ((.archive.snapshot.parsed_entries | sort) == (["b185ad1.bundle","b185ad1-metadata.txt","blocked-worktree-v2/SHA256SUMS","blocked-worktree-v2/blocked-status-v2.z","blocked-worktree-v2/blocked-unstaged.patch","blocked-worktree-v2/blocked-staged.patch","blocked-worktree-v2/blocked-untracked.z","blocked-worktree-v2/blocked-untracked.tar"] | sort)) and ((.archive.snapshot.outer_entries | map(.path) | sort) == (["b185ad1.bundle","b185ad1-metadata.txt","blocked-worktree-v2/SHA256SUMS","blocked-worktree-v2/blocked-status-v2.z","blocked-worktree-v2/blocked-unstaged.patch","blocked-worktree-v2/blocked-staged.patch","blocked-worktree-v2/blocked-untracked.z","blocked-worktree-v2/blocked-untracked.tar"] | sort)) and all(.archive.snapshot.outer_entries[]; (.sha256 | test("^[0-9a-f]{64}$"))) and (.archive.snapshot.manifest_fd_sha256 | test("^[0-9a-f]{64}$")) and (.archive.snapshot.nested_manifest_fd_sha256 | test("^[0-9a-f]{64}$")) and (.archive.snapshot.inner_checksum == "pass") and ((.archive.snapshot.inner_entries | map(.path) | sort) == ["blocked-staged.patch","blocked-status-v2.z","blocked-unstaged.patch","blocked-untracked.tar","blocked-untracked.z"]) and all(.archive.snapshot.inner_entries[]; (.sha256 | test("^[0-9a-f]{64}$"))) and (.archive.bundle.name == "b185ad1.bundle") and (.archive.bundle.sha256 == "2690ff85ed2d561af3592833d6741b92e7cedfb0afdf343db1d140bfde0cba37") and (.archive.metadata.name == "b185ad1-metadata.txt") and (.archive.metadata.sha256 == "879f4185d74a33f935ff35464f7bea7215326bfb0d38603edf4b5731136899db") and (.archive.outer_checksum == "pass") and (.archive.bundle_verify == "pass")' evidence/phase-01/incident-state.json &gt;/dev/null &amp;&amp; jq -e '(.tools | type == "array" and length &gt; 0) and (([.tools[].tool_id] | length) == ([.tools[].tool_id] | unique | length)) and ([.tools[].category] | contains(["vite","conformance","lifecycle_script","command","lockfile"])) and all(.tools[]; (.origin | IN("workspace_package_or_binary","repository_script","nix_app_or_derivation","exact_lockfile_dependency","external_historical_dependency_with_integrity_evidence","unavailable_historical_tool")) and (.evidence.path | type == "string" and length &gt; 0) and (.evidence.blob | test("^[0-9a-f]{40}$")) and (.evidence.sha256 | test("^[0-9a-f]{64}$")) and (.closure_id | type == "string" and length &gt; 0))' evidence/phase-01/historical-tool-inventory.json &gt;/dev/null &amp;&amp; python3 -c 'import hashlib,json,pathlib,subprocess; d=json.load(open("evidence/phase-01/historical-tool-inventory.json")); h="b185ad1b8d9d034d151406b12aa189f5a6be970f"; assert d["tools"]; assert all(not (p:=pathlib.PurePosixPath(x["evidence"]["path"])).is_absolute() and ".." not in p.parts and subprocess.check_output(["git","rev-parse",h+":"+x["evidence"]["path"]],text=True).strip()==x["evidence"]["blob"] and hashlib.sha256(subprocess.check_output(["git","show",h+":"+x["evidence"]["path"]])).hexdigest()==x["evidence"]["sha256"] for x in d["tools"])' &amp;&amp; python3 -c 'import json; l=json.load(open("evidence/phase-01/historical-claim-ledger.json")); t=json.load(open("evidence/phase-01/historical-tool-inventory.json")); required={"source_bound_guest_boundary","package_manager","lockfile","node_source","rust_source","nix_source","vite","conformance","repository_script","environment","generated_input","port_or_socket","lifecycle_script","native_dependency","command"}; claims=l["claims"]; assert set(l.get("required_categories",[]))==required and claims and all(isinstance(x.get("claim_id"),str) and x["claim_id"] and x.get("category") in required and isinstance(x.get("critical"),bool) for x in claims) and len({x["claim_id"] for x in claims})==len(claims) and {x["category"] for x in claims}==required and any(x["category"]=="source_bound_guest_boundary" and x["critical"] is True for x in claims); linked=[x["source_tool_id"] for x in claims if x.get("source_tool_id") is not None]; tool_ids={x["tool_id"] for x in t["tools"]}; assert len(linked)==len(set(linked)) and set(linked)==tool_ids' &amp;&amp; jq --slurpfile tools evidence/phase-01/historical-tool-inventory.json -e '(.closures | type == "array" and length == 1) and ([.closures[].id] | unique) as $ids | all($tools[0].tools[]; .closure_id as $id | $ids | index($id) != null)' evidence/phase-01/exact-closure-plan.json &gt;/dev/null &amp;&amp; jq -e '(.attempt_budget.inventory == 1) and (.attempt_budget.realization_paths == 1) and (.attempt_budget.replay_attempts == 1) and (.attempt_budget.harness_corrections_max == 1) and (.network.external_egress == "deny") and (.network.public_dns == "deny") and (.source_mutation == false) and (.lock_mutation == false)' evidence/phase-01/exact-closure-plan.json &gt;/dev/null &amp;&amp; python3 -c 'import json; p=json.load(open("evidence/phase-01/exact-closure-plan.json")); t=json.load(open("evidence/phase-01/historical-tool-inventory.json")); closures=p["closures"]; assert len(closures)==1; closure_id=closures[0]["id"]; tools={x["tool_id"]:x for x in t["tools"]}; fixtures=p["network"]["local_fixtures"]; assert fixtures and all(isinstance(x.get("fixture_id"),str) and x["fixture_id"] and x.get("kind") in ("loopback_tcp","unix_socket") and isinstance(x.get("endpoint"),str) and x["endpoint"] for x in fixtures) and len({x["fixture_id"] for x in fixtures})==len(fixtures) and len({x["endpoint"] for x in fixtures})==len(fixtures); fby={x["fixture_id"]:x for x in fixtures}; local_endpoints={x["endpoint"] for x in fixtures}; probes=p["network"]["probes"]; assert len(probes)==3 and {x.get("id") for x in probes}=={"external_egress","public_dns","local_fixture"}; by={x["id"]:x for x in probes}; assert all(isinstance(x.get("argv"),list) and x["argv"] and all(isinstance(v,str) and v for v in x["argv"]) and isinstance(x.get("target"),str) and x["target"] and x["target"] in x["argv"] and x.get("tool_id") in tools and x.get("closure_id")==closure_id and tools[x["tool_id"]].get("closure_id")==closure_id for x in probes); assert by["external_egress"].get("expected_status")=="denied" and by["public_dns"].get("expected_status")=="denied" and by["external_egress"].get("fixture_id") is None and by["public_dns"].get("fixture_id") is None and by["external_egress"]["target"] not in local_endpoints and by["public_dns"]["target"] not in local_endpoints; local=by["local_fixture"]; assert local.get("expected_status")=="reachable" and local.get("fixture_id") in fby and local["target"]==fby[local["fixture_id"]]["endpoint"]' </automated>
  </verify>
  <acceptance_criteria>
    - The WIP, parent, blocked worktree, safety ref, bundle, archive, inner snapshot, current Phase-1 head/tree, and dirty-state facts are immutable and content-addressed.
    - Each historical claim links to exact original evidence and criticality; no claim survives by plan assertion.
    - Vite and the conformance tool are classified from exact historical source before any materialization command exists.
    - Closure, lifecycle, network, fixture, disposable-state, cleanup, restart, cancellation, and attempt-budget fields are explicit and fail closed.
    - EDGE-M0-01-UNCLASSIFIED remains flagged for manual coverage review; it is not silently dismissed.
  </acceptance_criteria>
  <done>One critical claim is traceable end-to-end from the preserved incident object through exact tool/lock identities into the single admissible replay plan, and the complete inventory is ready for the bounded attempt.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh Graphify in the immediate graph-only child commit</name>
  <files>graphify-out/</files>
  <read_first>
    - AGENTS.md — mandatory post-code Graphify refresh and separate-commit rule
    - graphify-out/manifest.json — current graph root, generator identity, and prior indexed state
    - scripts/phase-01-validate-incident.sh — executable code added by the immediately preceding Task-1 implementation commit
  </read_first>
  <action>Run only after Task 1 has been committed and the worktree is clean. Capture that current HEAD as `implementation_commit`; require its changed-path set to contain `scripts/phase-01-validate-incident.sh` and to contain no `graphify-out/` path. Run exactly `graphify update .` once. Require a nonempty changed-path set and require every tracked or untracked changed path to be canonically below `graphify-out/`; fail closed on any source, evidence, plan, lock, configuration, or other path. Stage only `graphify-out/`, recheck that the staged set is nonempty and contains only regular Graphify-output paths with no symlink component, then create the separate commit with exact subject `chore(graph): refresh after phase-01 incident validator`. Immediately verify that this graph commit has exactly one parent equal to `implementation_commit`, that its complete changed-path set is nonempty and only below `graphify-out/`, and that the worktree is clean. Do not amend, squash, or combine this commit with Task 1 or Task 3. The graph commit records derived code-index state only; it does not change replay inputs, evidence authority, current-source identity, or E/P acceptance. Record both commit IDs, exact command, exit status, and graph-only changed paths in `01-01-SUMMARY.md` when the plan summary is written.</action>
  <verify>
    <automated>test "$(git log -1 --format=%s)" = "chore(graph): refresh after phase-01 incident validator" &amp;&amp; test "$(git rev-list --parents -n 1 HEAD | awk '{print NF}')" -eq 2 &amp;&amp; python3 -c 'import pathlib,subprocess; head=subprocess.check_output(["git","rev-parse","HEAD"],text=True).strip(); parent=subprocess.check_output(["git","rev-parse","HEAD^"],text=True).strip(); paths=subprocess.check_output(["git","diff-tree","--no-commit-id","--name-only","-r",head],text=True).splitlines(); parent_paths=subprocess.check_output(["git","diff-tree","--no-commit-id","--name-only","-r",parent],text=True).splitlines(); assert paths and all((q:=pathlib.PurePosixPath(x)).parts and q.parts[0]=="graphify-out" and ".." not in q.parts for x in paths); assert "scripts/phase-01-validate-incident.sh" in parent_paths and not any((q:=pathlib.PurePosixPath(x)).parts and q.parts[0]=="graphify-out" for x in parent_paths)' &amp;&amp; test -z "$(git status --porcelain)"</automated>
  </verify>
  <acceptance_criteria>
    - The Graphify refresh commit is the immediate single-parent child of Task 1's implementation commit.
    - The graph commit changes a nonempty set of paths exclusively below `graphify-out/`; no code, evidence, plan, lock, or configuration path is mixed into it.
    - The exact refresh command, disposition, parent, graph commit, and changed paths are retained for the plan summary without changing E/P evidence identity.
  </acceptance_criteria>
  <done>The executable validator is represented by one immediately following, separately owned, graph-only refresh commit.</done>
</task>

<task type="auto">
  <name>Task 3: Execute the bounded replay, replace or block every critical claim, and record final WIP disposition</name>
  <reversibility rating="costly">The disposition becomes later cleanup authority; correcting a wrong record requires coordinated incident, claim, and closeout updates, but this task removes no preserved object.</reversibility>
  <files>.gitignore, evidence/phase-01/replay-manifest.json, evidence/phase-01/replay-claim-impact.json, evidence/phase-01/replay-artifacts.sha256, evidence/phase-01/b185ad1-disposition.md</files>
  <read_first>
    - evidence/phase-01/incident-state.json — immutable preserved-object coordinates and hashes
    - evidence/phase-01/historical-claim-ledger.json — complete claim set and criticality
    - evidence/phase-01/historical-tool-inventory.json — admitted command/tool identities
    - evidence/phase-01/exact-closure-plan.json — sole permitted realization and replay contract
    - docs/plans/uzel-product-incubation-v4-2026-08-10/01-BASELINE-REPLAY.md — allowed verdicts and replacement-evidence rule
    - docs/plans/uzel-product-incubation-v4-2026-08-10/04-DELIVERY-QUALITY.md — historical replay supply-chain and evidence discipline
  </read_first>
  <action>Implement D-13, D-15 through D-19, D-22, D-29, and D-30. Realize only the selected fixed closure: a fetch stage may obtain only missing lock/integrity/hash-bound bytes, then all lifecycle scripts, materialization, and replay run with public DNS and external egress denied; only the inventory's loopback/Unix fixtures may exist. Create a disposable exact-source checkout and disposable HOME/XDG/cache/temp/build roots outside the active Phase-1 worktree. Disable dependency lifecycle scripts unless the exact reviewed allowlist is non-empty; log each allowlisted script identity, argv, bounded writes, and output hash. Before creating raw evidence, add exactly one anchored `/evidence/phase-01/raw/replay/` rule to the task-owned tracked `.gitignore`; reject a duplicate/conflicting rule, require the literal raw root to pass `git check-ignore`, and require it absent from `git ls-files`. Canonically contain every replay, claim, blocker, replacement, disposition, and network-probe evidence path below that root and reject every symlink component before reading. Record exactly three `network.probes` rows with IDs `external_egress`, `public_dns`, and `local_fixture`; each repeats its exact admitted `argv`, target, tool ID, closure ID, expected status, and optional fixture ID from `exact-closure-plan.json`, then adds observed status, integer exit status, artifact ID/path, and SHA-256. Reject any missing/extra probe or field mismatch. External egress and public DNS must record denied with nonzero integer exit; the declared local fixture must record reachable with integer exit zero and target exactly equal to its admitted enumerated fixture endpoint. Execute the source, conformance, and hostile-boundary commands supported by the exact baseline in exactly one replay attempt. A harness/isolation correction may be applied at most once before that replay begins; if the need is discovered only after the replay starts, record the resulting honest verdict and do not run a second replay. Record `replay_attempt_count = 1`, `harness_correction_count` as zero or one, and the correction diff/rationale when used. On INT/TERM, kill owned children, retain completed logs/checksums, mark `cancelled`, and refuse resume unless all immutable input hashes match. Store raw redacted evidence outside tracked source. Give `replay-manifest.json` one nonempty sole `artifacts[]` registry with unique nonempty artifact ID, unique path, SHA-256, kind, owner kind, and owner ID. Every replay evidence producer -- each network probe, environment observation, replay command stdout/stderr/result/cleanup, lifecycle-script observation, verdict evidence, variance/limitation observation when present, harness correction when present, cancellation when present, and final cleanup -- has unique nonempty `evidence_owner_kind`/`evidence_owner_id` and nonempty unique `artifact_ids`; every referenced artifact repeats that exact owner, and the registry ID set must equal the reference set bijectively with no duplicate ownership reference, orphan, or unknown ID. Owner kinds and exact required artifact-kind sets are: `network_probe` -> `network_probe`; `environment_observation` -> `environment`; every `replay_command` -> all four of `stdout`, `stderr`, `result`, and `cleanup`; `lifecycle_script` -> `lifecycle_script`; `verdict_evidence` -> `verdict`; `variance_observation` -> `variance_or_limitation`; `harness_correction` -> `harness_correction`; `cancellation` -> `cancellation`; and `final_cleanup` -> `final_cleanup`. Reject missing or extra kinds per producer. `replay-artifacts.sha256` is the exact sorted path/digest projection of that full registry, with neither omitted nor extra rows and byte/digest parity. Every claim-impact and disposition path/digest must resolve to exactly one registry row; no free evidence path outside this registry may confer a claim, blocker, replacement, probe, command, cleanup, cancellation, correction, or disposition. Give every unreplaced critical outcome explicit `phase1_blocker: true` and a nonempty `blocker` object `{code,reason,evidence_path,evidence_sha256}`; no implicit blocker status. Derive `b185ad1-disposition.md` from replay-manifest plus every claim-impact row, with exactly one enum `retained|amended|superseded_with_provenance|discarded_with_reason`, one or more exact `Evidence: <repo-relative-path> <sha256>` rows whose safe regular-file bytes match, and nonempty limitations. Write the manifest atomically. Choose exactly one allowed top-level verdict. Populate exactly one impact row for every unique `claim_id` in `historical-claim-ledger.json`, with no duplicate, missing, or extra IDs; an unavailable/failed/superseded critical claim is green only when current-source evidence independently replaces it, otherwise mark the Phase-1 blocker. Derive one final WIP disposition from `retained`, `amended`, `superseded_with_provenance`, or `discarded_with_reason`, citing evidence digests and limitations. Do not delete, amend, cherry-pick, reset, merge, prune, or repurpose the WIP, ref, archive, or blocked worktree in this task.</action>
  <verify>
    <automated>python3 -m json.tool evidence/phase-01/replay-manifest.json &gt;/dev/null &amp;&amp; python3 -m json.tool evidence/phase-01/replay-claim-impact.json &gt;/dev/null &amp;&amp; jq -e '(.verdict | IN("reproduced","reproduced_with_variance","unavailable_exact_closure","unavailable_platform","failed_behavior","superseded_test"))' evidence/phase-01/replay-manifest.json &gt;/dev/null &amp;&amp; jq -e '(.network.external_egress_probe == "denied") and (.network.public_dns == "denied") and (.source_tree_before == .source_tree_after) and (.lock_hashes_before == .lock_hashes_after) and (.replay_attempt_count == 1) and (.harness_correction_count | IN(0,1))' evidence/phase-01/replay-manifest.json &gt;/dev/null &amp;&amp; python3 -c 'import hashlib,json,pathlib,re,subprocess; m=json.load(open("evidence/phase-01/replay-manifest.json")); plan=json.load(open("evidence/phase-01/exact-closure-plan.json")); root=pathlib.Path("evidence/phase-01/raw/replay"); ignore=pathlib.Path(".gitignore").read_text().splitlines(); ignored=subprocess.check_output(["git","check-ignore","-v",str(root)],text=True).strip().split("\t",1)[0].rsplit(":",2); assert ignore.count("/evidence/phase-01/raw/replay/")==1 and len(ignored)==3 and ignored[0]==".gitignore" and ignored[2]=="/evidence/phase-01/raw/replay/" and subprocess.run(["git","ls-files","--error-unmatch",str(root)],stdout=subprocess.DEVNULL,stderr=subprocess.DEVNULL).returncode!=0; root_abs=root.resolve(); expected={x["id"]:x for x in plan["network"]["probes"]}; probes=m["network"]["probes"]; assert len(expected)==len(probes)==3 and set(expected)=={x.get("id") for x in probes}=={"external_egress","public_dns","local_fixture"}; by={x["id"]:x for x in probes}; fixed=("argv","target","tool_id","closure_id","expected_status","fixture_id"); assert all(all(x.get(k)==expected[x["id"]].get(k) for k in fixed) and isinstance(x.get("artifact_id"),str) and x["artifact_id"] and x.get("artifact_ids")==[x["artifact_id"]] and x.get("evidence_owner_kind")=="network_probe" and x.get("evidence_owner_id")==x["id"] and isinstance(x.get("artifact_path"),str) and re.fullmatch(r"[0-9a-f]{64}",x.get("artifact_sha256","")) for x in probes); assert all(by[k].get("status")=="denied" and isinstance(by[k].get("exit_status"),int) and not isinstance(by[k].get("exit_status"),bool) and by[k]["exit_status"]!=0 for k in ("external_egress","public_dns")) and by["local_fixture"].get("status")=="reachable" and isinstance(by["local_fixture"].get("exit_status"),int) and not isinstance(by["local_fixture"].get("exit_status"),bool) and by["local_fixture"]["exit_status"]==0; artifacts=m["artifacts"]; assert artifacts and all(isinstance(x.get("artifact_id"),str) and x["artifact_id"] and isinstance(x.get("path"),str) and x["path"] and re.fullmatch(r"[0-9a-f]{64}",x.get("sha256","")) and isinstance(x.get("kind"),str) and x["kind"] and isinstance(x.get("owner_kind"),str) and x["owner_kind"] and isinstance(x.get("owner_id"),str) and x["owner_id"] for x in artifacts) and len({x["artifact_id"] for x in artifacts})==len(artifacts) and len({x["path"] for x in artifacts})==len(artifacts); aby={x["artifact_id"]:x for x in artifacts}; producers=list(probes)+[m["environment_observation"]]+m["commands"]+m.get("lifecycle_scripts",[])+[m["verdict_evidence"]]+m.get("variance_observations",[])+[m["cleanup"]]; producers+=([m["harness_correction"]] if m.get("harness_correction_count")==1 else []); producers+=([m["cancellation"]] if m.get("cancellation") is not None else []); assert m["commands"] and len({(x.get("evidence_owner_kind"),x.get("evidence_owner_id")) for x in producers})==len(producers) and all(isinstance(x.get("evidence_owner_kind"),str) and x["evidence_owner_kind"] and isinstance(x.get("evidence_owner_id"),str) and x["evidence_owner_id"] and isinstance(x.get("artifact_ids"),list) and x["artifact_ids"] and len(x["artifact_ids"])==len(set(x["artifact_ids"])) for x in producers); refs=[(aid,x["evidence_owner_kind"],x["evidence_owner_id"]) for x in producers for aid in x["artifact_ids"]]; assert len(refs)==len({aid for aid,k,o in refs})==len(artifacts) and {aid for aid,k,o in refs}==set(aby) and all(aby[aid]["owner_kind"]==k and aby[aid]["owner_id"]==o for aid,k,o in refs); required_kinds={"network_probe":{"network_probe"},"environment_observation":{"environment"},"replay_command":{"stdout","stderr","result","cleanup"},"lifecycle_script":{"lifecycle_script"},"verdict_evidence":{"verdict"},"variance_observation":{"variance_or_limitation"},"harness_correction":{"harness_correction"},"cancellation":{"cancellation"},"final_cleanup":{"final_cleanup"}}; assert all(x["evidence_owner_kind"] in required_kinds and {aby[aid]["kind"] for aid in x["artifact_ids"]}==required_kinds[x["evidence_owner_kind"]] for x in producers); assert all((z:=aby[x["artifact_id"]])["path"]==x["artifact_path"] and z["sha256"]==x["artifact_sha256"] for x in probes); assert all(not (q:=pathlib.Path(x["path"])).is_absolute() and ".." not in q.parts and q.resolve().is_relative_to(root_abs) and all(not pathlib.Path(*q.parts[:n]).is_symlink() for n in range(1,len(q.parts)+1)) and q.is_file() and hashlib.sha256(q.read_bytes()).hexdigest()==x["sha256"] for x in artifacts)' &amp;&amp; jq --slurpfile ledger evidence/phase-01/historical-claim-ledger.json -e '([.claims[].claim_id] | length) &gt; 0 and (([.claims[].claim_id] | length) == ([.claims[].claim_id] | unique | length)) and (([$ledger[0].claims[].claim_id] | length) == ([$ledger[0].claims[].claim_id] | unique | length)) and (([.claims[].claim_id] | sort) == ([$ledger[0].claims[].claim_id] | sort)) and all(.claims[]; . as $impact | any($ledger[0].claims[]; (.claim_id == $impact.claim_id) and (.critical == $impact.critical) and (.category == $impact.category) and (.source_tool_id == $impact.source_tool_id)) and ((.critical != true) or (.status == "reproduced") or ((.status == "replaced") and (.replacement.evidence_sha256 | type == "string") and (.replacement.evidence_sha256 | length == 64)) or (.phase1_blocker == true)))' evidence/phase-01/replay-claim-impact.json &gt;/dev/null &amp;&amp; python3 -c 'import hashlib,json,pathlib,re; d=json.load(open("evidence/phase-01/replay-claim-impact.json")); m=json.load(open("evidence/phase-01/replay-manifest.json")); root=pathlib.Path("evidence/phase-01/raw/replay").resolve(); artifacts=m["artifacts"]; by_path={x["path"]:x for x in artifacts}; assert len(by_path)==len(artifacts); lines=pathlib.Path("evidence/phase-01/replay-artifacts.sha256").read_text().splitlines(); parsed=[(z.group(2),z.group(1)) for line in lines if (z:=re.fullmatch(r"([0-9a-f]{64})  (.+)",line))]; expected=sorted((x["path"],x["sha256"]) for x in artifacts); assert len(parsed)==len(lines)==len(expected) and sorted(parsed)==expected; assert all(not (q:=pathlib.Path(path)).is_absolute() and ".." not in q.parts and q.resolve().is_relative_to(root) and all(not pathlib.Path(*q.parts[:n]).is_symlink() for n in range(1,len(q.parts)+1)) and q.is_file() and hashlib.sha256(q.read_bytes()).hexdigest()==digest for path,digest in parsed); assert all(sum((bool(x.get("status")=="replaced" and isinstance(x.get("replacement"),dict) and not x.get("evidence_path") and x.get("phase1_blocker") is not True),bool(x.get("phase1_blocker") is True and x.get("status")!="replaced" and isinstance(x.get("blocker"),dict) and not x.get("evidence_path")),bool(x.get("status")!="replaced" and x.get("phase1_blocker") is not True and isinstance(x.get("evidence_path"),str) and x["evidence_path"] and isinstance(x.get("evidence_sha256"),str))))==1 for x in d["claims"]); refs=[(x["replacement"].get("evidence_path"),x["replacement"].get("evidence_sha256")) if x.get("status")=="replaced" else (x["blocker"].get("evidence_path"),x["blocker"].get("evidence_sha256")) if x.get("phase1_blocker") is True else (x.get("evidence_path"),x.get("evidence_sha256")) for x in d["claims"]]; assert len(refs)==len(d["claims"]) and all(isinstance(path,str) and path in by_path and re.fullmatch(r"[0-9a-f]{64}",digest or "") and by_path[path]["sha256"]==digest for path,digest in refs); assert all(x.get("phase1_blocker") is not True or (isinstance(x.get("blocker",{}).get("code"),str) and x["blocker"]["code"] and isinstance(x["blocker"].get("reason"),str) and x["blocker"]["reason"]) for x in d["claims"]); disposition=pathlib.Path("evidence/phase-01/b185ad1-disposition.md").read_text(); cited=re.findall(r"(?m)^Evidence: ([^ \r\n]+) ([0-9a-f]{64})$",disposition); assert cited and all(path in by_path and by_path[path]["sha256"]==digest for path,digest in cited)' &amp;&amp; sha256sum -c evidence/phase-01/replay-artifacts.sha256 &amp;&amp; grep -Eq '^Disposition: (retained|amended|superseded_with_provenance|discarded_with_reason)$' evidence/phase-01/b185ad1-disposition.md &amp;&amp; grep -Eq '^Limitations: .+$' evidence/phase-01/b185ad1-disposition.md</automated>
  </verify>
  <acceptance_criteria>
    - Replay uses the exact admitted network-probe argv, tools, closure, targets, and local fixture plus the one admitted inventory/path/attempt budget; placeholder commands cannot prove isolation.
    - The tracked exact ignore rule owns the task raw root, and the sole replay artifact registry, producer references, checksum manifest, claim evidence, probes, and disposition have exact non-orphaned byte parity.
    - Source/tree and every historical lock digest are byte-identical before/after; no ambient/global/dynamic/substitute tool enters the manifest.
    - Verdict is one allowed enum and carries exact environment, command, raw-evidence digest, variance/limitation, cleanup, and cancellation fields.
    - Every critical claim is reproduced, independently replaced with a current-source evidence digest, or explicitly blocks Phase 1.
    - Final b185ad1 disposition is evidence-derived and does not perform the later cleanup it may authorize.
    - PROHIB-M0-01-TRANSPARENCY remains flagged until independent review verifies the records against raw evidence.
  </acceptance_criteria>
  <done>M0-01 has one honest historical verdict, claim-by-claim replacement/blocker table, preserved raw-evidence checksums, and an evidence-based final b185ad1 disposition; any missing critical replacement remains an explicit blocker.</done>
</task>

</tasks>

## Artifacts Produced

- `evidence/phase-01/incident-state.json` — preserved incident and current planning state.
- `evidence/phase-01/historical-claim-ledger.json` — exact historical claim provenance.
- `evidence/phase-01/historical-tool-inventory.json` — command/tool/lock/lifecycle origins.
- `evidence/phase-01/exact-closure-plan.json` — one fixed replay path and safety contract.
- `evidence/phase-01/replay-manifest.json` — bounded replay result.
- `evidence/phase-01/replay-claim-impact.json` — replacement/blocker matrix.
- `evidence/phase-01/replay-artifacts.sha256` — raw retained artifact checksums.
- `evidence/phase-01/b185ad1-disposition.md` — final evidence-based WIP disposition.

<threat_model>
Security validation scope: OWASP ASVS Level 1, limited to the replay/evidence boundaries declared below.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Active Phase-1 worktree to preserved blocked worktree/archive | Read-only forensic inspection crosses into evidence that must not be mutated or cleaned. |
| Fixed package/Nix stores to disposable replay checkout | Supply-chain bytes and lifecycle code enter the replay only after lock/integrity/hash admission. |
| Replay namespace to host/network | Untrusted historical scripts/tests may reach only named loopback/Unix fixtures; public DNS and external egress are denied. |
| Raw replay output to tracked evidence | Potential paths, keys, content, and tool noise must be redacted while hashes retain provenance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-01 | Tampering | b185ad1 worktree/ref/archive | high | mitigate | Verify commit/parent/ref/bundle/outer+inner checksums before and after; use read-only Git probes; prohibit cleanup/ref mutation. |
| T-01-02 | Tampering/Elevation | fixed closure and lifecycle scripts | high | mitigate | Admit only exact lock/integrity/hash identities, disable scripts by default, review any allowlist, isolate disposable state, deny public DNS/external egress. |
| T-01-03 | Repudiation | replay verdict and claim replacement | medium | mitigate | Bind exact argv/environment/input hashes/raw artifact digests to one enum verdict and require a per-claim evidence/blocker row. |
| T-01-04 | Information Disclosure | raw logs and evidence export | medium | mitigate | Use synthetic fixtures, redact secrets/paths where needed, retain private raw evidence outside tracked source, and publish only checksums plus bounded extracts. |
| T-01-SC | Tampering | existing pnpm/Cargo/Nix closure materialization | high | mitigate | No new package is selected; exact historical identities must pass the lock/integrity audit before materialization, with no dynamic download or substitution. |
</threat_model>

<verification>
Run both task automated gates, confirm current `git worktree list --porcelain` still names the blocked worktree at b185ad1, confirm the safety ref and archive checksums still match, and compare tracked source/lock hashes with the incident inventory. Honest non-green verdicts pass artifact validation but keep Phase 1 blocked when critical replacement evidence is absent.
</verification>

<success_criteria>
- M0-01 is fully covered by D-12 through D-19 and D-22 evidence.
- Historical replay, current-source evidence, and current package/native evidence remain separate classes.
- No product, dependency, source, lock, fixture, blocked-worktree, ref, or archive mutation occurs.
- Independent Prompt 02 review and human execution authorization are hard preconditions, not inferred from plan existence.
</success_criteria>

<output>
Create `.planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-01-SUMMARY.md` when done.
</output>
