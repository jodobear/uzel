---
phase: 01-slice-ref-01-poc-replay-accepted-napp-seam
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase-01-replay.sh
  - scripts/ref-replay-check.py
  - evidence/phase-01/replay-manifest.json
  - evidence/phase-01/work-07-preservation.md
autonomous: true
requirements:
  - REF-01
  - REF-02
  - REF-03
  - REF-04
must_haves:
  truths:
    - "REF-01/D-04: A maintainer can replay fixture, deterministic Chromium, real Weston/WebKit, and clean relocated-target modes only from merged production baseline 19519c378c2e775c6ad4b042cfd9aadd89f766b9; Plan 01 captures one execution-head commit, validates that recorded commit against the finite allowlist at replay start and end, rejects concurrent drift, and treats later phase commits as non-retroactive so they cannot replace product identity or invalidate the finalized manifest."
    - "REF-02: Source-created-surface binding, raw-network denial, native-bridge denial, path/secret denial, and runtime-owned principal selection remain independently evidenced."
    - "REF-03/D-10: Review, confirmation, launch, lost-reply recovery, cancellation, restart reconciliation, cleanup, and rollback retain bounded typed outcomes."
    - "REF-04: Chromium and Weston/WebKit results remain separate evidence classes, each bound to exact source, fixtures, command, environment, raw output, and result."
    - "D-08/D-11: A replay or later seam claim fails when the product commit/tree, source inventory, lock/pin inventory, fixture inventory/hashes, or required mode differs from the approved baseline, or when evidence is missing, failed, unavailable, unhashed, marker-incomplete, path-escaped, or cleanup-incomplete; an arbitrary reachable commit is never admissible."
    - "D-06: Work 07 is reconciled to merged production commit 19519c378c2e775c6ad4b042cfd9aadd89f766b9 without rewriting the legacy status or closing Debian acceptance."
  artifacts:
    - path: scripts/phase-01-replay.sh
      provides: "Bounded, mode-separated replay orchestration for existing POC commands"
    - path: scripts/ref-replay-check.py
      provides: "Phase-specific fixed-baseline, execution-head equivalence, manifest, raw-artifact, marker, provenance, and cleanup validator"
    - path: evidence/phase-01/replay-manifest.json
      provides: "Separate production-baseline and execution-head identities plus baseline-bound results and raw-evidence hashes by evidence class"
    - path: evidence/phase-01/work-07-preservation.md
      provides: "Dated source-truth reconciliation for merged PR 30 and unresolved Debian gate"
  key_links:
    - from: scripts/phase-01-replay.sh
      to: package.json
      via: "Invokes existing pinned check, test, conformance, Chromium, and native-smoke commands without duplicating suites"
      pattern: "pnpm (check|test|test:conformance|test:ui|smoke:linux)"
    - from: evidence/phase-01/replay-manifest.json
      to: .artifacts/phase-01/replay
      via: "Each mode stores raw output path plus SHA-256 instead of embedding mutable summaries"
      pattern: "raw_output"
    - from: scripts/ref-replay-check.py
      to: evidence/phase-01/replay-manifest.json
      via: "Pins production admission to 19519c378c2e775c6ad4b042cfd9aadd89f766b9, binds replay to the execution-head commit captured at Plan 01 start/end, proves that recorded commit changes only the finite planning/harness/evidence allowlist, and recomputes source/lock/fixture/raw hashes before marker and cleanup gates without substituting a later repository tip"
      pattern: "19519c378c2e775c6ad4b042cfd9aadd89f766b9"
    - from: evidence/phase-01/work-07-preservation.md
      to: uzel-poc-validated-pack/STATUS.md
      via: "References historical status and records a dated reconciliation without modifying it"
      pattern: "19519c378c2e775c6ad4b042cfd9aadd89f766b9"
  assumptions:
    - "[FLAGGED-UNVERIFIED] REF-01 edge classification is unresolved; replay proves only the named merged-POC paths and exact pins."
    - "[FLAGGED-UNVERIFIED] REF-02 boundary behavior beyond the existing created-frame source mapping remains unclassified."
    - "[FLAGGED-UNVERIFIED] REF-02 adjacency behavior between multiple created surfaces remains unresolved beyond current composition tests."
    - "[FLAGGED-UNVERIFIED] REF-02 empty-input behavior remains unresolved beyond existing bounded-envelope rejection tests."
    - "[FLAGGED-UNVERIFIED] REF-02 ordering behavior remains unresolved beyond current hostile-marker and lifecycle ordering assertions."
    - "[FLAGGED-UNVERIFIED] REF-02 precision behavior remains unresolved beyond exact fixture, source, and marker matching."
    - "[FLAGGED-UNVERIFIED] REF-02 concurrency behavior remains unresolved beyond current bounded surface, queue, resource, and cancellation tests."
    - "[FLAGGED-UNVERIFIED] REF-03 edge classification is unresolved; Phase 1 accepts only one instance and one active read profile."
    - "[FLAGGED-UNVERIFIED] REF-04 edge classification is unresolved; unsupported platform evidence must be recorded as unavailable."
  prohibitions:
    - "[FLAGGED-UNVERIFIED] No private Napp substitute or expanded legacy protocol."
    - "[FLAGGED-UNVERIFIED] No duplicated NMP, runtime, Nostr, or store authority."
    - "[FLAGGED-UNVERIFIED] No evidence-class conflation or fabricated metrics."
    - "[FLAGGED-UNVERIFIED] No pull-in of Nix packaging, CI architecture, Social rebuild, multi-instance implementation, or parked capabilities."
---

## Phase Goal

**As a** Uzel maintainer/reviewer, **I want to** replay current POC evidence and apply a fail-closed Napp admission gate, **so that** visible value survives without duplicating authority.

<objective>
Deliver repository-qualified issue slice `jodobear/uzel — REF-01A: replay merged Uzel POC from clean/relocated checkout` as one bounded PR.

Purpose: Prove the preserved POC remains green and relocatable before any extraction, repin, or adapter work, per D-04, D-06, D-10, D-11, D-15, D-17, and D-18.
Output: A reusable replay harness, one machine-readable exact-evidence manifest that distinguishes the fixed production baseline from the planning/execution head, and a dated Work 07 reconciliation. No Uzel, Tauri, Rust, Svelte, fixture, contract, or lockfile production change.
</objective>

<execution_context>
@/home/at/.codex/gsd-core/workflows/execute-plan.md
@/home/at/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-CONTEXT.md
@.planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-REAUDIT.md
@.planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-RESEARCH.md
@.planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-PATTERNS.md
</context>

## Artifacts this phase produces

- `scripts/phase-01-replay.sh`; new shell entry point with internal functions `run_replay_mode`, `record_replay`, and `clean_replay`.
- `scripts/ref-replay-check.py`; small Phase-01-only Python standard-library validator for replay evidence admission.
- `evidence/phase-01/replay-manifest.json`; tracked replay index for `fixture`, `renderer`, `native`, and `relocated` modes, with separate fixed production-baseline and dynamic execution-head objects.
- `.artifacts/phase-01/replay/<run-id>/`; ignored raw logs and per-run environment records referenced by hash from the manifest.
- `evidence/phase-01/work-07-preservation.md`; dated historical reconciliation, not a replacement for legacy `STATUS.md`.

## Fixed replay admission identity

- Production commit: `19519c378c2e775c6ad4b042cfd9aadd89f766b9`; this constant is not supplied by a manifest or CLI argument.
- Production Git tree: `ca34ad1a26beefc73657f90b142df3be415ff2a5`.
- Canonical `git ls-tree -r` source-inventory SHA-256: `559e2feea95390521d41320bcf5372c383dcac5989286eb3b92acde5e50c3282`.
- Canonical pin inventory SHA-256: `7d3d832934050579848a61b7bf7c1ff1fd1168ec2c885312278c0a1082294565`, over sorted committed `Cargo.toml`, `Cargo.lock`, `package.json`, `pnpm-lock.yaml`, `pnpm-workspace.yaml`, `flake.nix`, `flake.lock`, `rust-toolchain.toml`, `deno.lock`, and `compatibility.lock` tree records at the production commit.
- Canonical committed `fixtures/` inventory SHA-256: `2ec342bc8dd888345bdd170ac28e0faf3524f33d064c9b5d2786e118ca1b234e`; manifest also stores every fixture path, mode, Git blob OID, and content SHA-256.
- Execution-head source-equivalence allowlist: `.planning/**`, `scripts/phase-01-replay.sh`, `scripts/ref-replay-check.py`, `evidence/phase-01/replay-manifest.json`, and `evidence/phase-01/work-07-preservation.md`. No other added, modified, deleted, renamed, copied, mode-changed, or symlink path is equivalent to the production baseline.

<tasks>

<task type="tracer">
  <name>Task 1: Replay one exact POC path through every existing evidence layer</name>
  <read_first>
    package.json
    flake.nix
    flake.lock
    Cargo.toml
    Cargo.lock
    apps/uzel/tests/ui/README.md
    apps/uzel/tests/ui/acceptance.test.mjs
    scripts/linux-run-smoke.sh
    scripts/check-pinned-assets.sh
    apps/uzel/public/trusted-shell/trusted-shell.js
    apps/uzel/src-tauri/src/main.rs
    crates/napd/src/runner.rs
    crates/napd/src/server.rs
    crates/napd/src/fixtures.rs
  </read_first>
  <files>scripts/phase-01-replay.sh, scripts/ref-replay-check.py, evidence/phase-01/replay-manifest.json</files>
  <action>Create `scripts/phase-01-replay.sh` as strict Bash with explicit `--mode fixture|renderer|native|relocated|all` and `--output` arguments. Hard-code `PRODUCTION_BASELINE=19519c378c2e775c6ad4b042cfd9aadd89f766b9`; do not accept a baseline/head override from CLI, environment, or manifest. Resolve and record the committed planning/execution head separately with `git rev-parse HEAD^{commit}`. Before any product command, require the production commit object, tree `ca34ad1a26beefc73657f90b142df3be415ff2a5`, source-inventory digest, pin-inventory digest, and fixture-inventory digest listed above. Materialize product input only with `git worktree add --detach` at the fixed baseline under the confined run root; fixture, renderer, and native commands run from that clean detached baseline worktree, never the shared working tree or execution-head tree. Every repository command must enter the locked flake shell: fixture mode uses the exact argv forms `nix develop --command pnpm check`, `nix develop --command pnpm test`, `nix develop --command pnpm test:conformance`, `nix develop --command pnpm lint`, `nix develop --command pnpm fallow`, and `nix develop --command pnpm format:check`; renderer uses `nix develop --command pnpm test:ui`; native uses `nix develop --command pnpm smoke:linux`; any direct Cargo probe uses `nix develop --command cargo ...`. Never assume ambient `pnpm` or `cargo`, install/update dependencies, or duplicate existing tests. Relocated mode sets `RELOCATED_WORKTREE=$RUN_ROOT/relocated/worktree`, creates a second detached worktree at the same fixed baseline, then runs `(cd "$RELOCATED_WORKTREE" &amp;&amp; env CARGO_TARGET_DIR="$RUN_ROOT/cargo-target" XDG_CACHE_HOME="$RUN_ROOT/xdg-cache" XDG_DATA_HOME="$RUN_ROOT/xdg-data" XDG_RUNTIME_DIR="$RUN_ROOT/xdg-runtime" TMPDIR="$RUN_ROOT/tmp" UZEL_SMOKE_ARTIFACT_DIR="$RUN_ROOT/native-failed" UZEL_SMOKE_EVIDENCE_DIR="$RUN_ROOT/native-evidence" nix develop --command pnpm smoke:linux)`. Create per-run directories with safe permissions, record resolved values, scan generated metadata/logs for the old checkout path from the re-audit, require no tracked baseline-worktree drift before or after each mode, and clean worktrees/targets through bounded traps. Main/shared-worktree tracked or untracked content, including user changes, is never copied, hashed, listed, or used as product, lock, pin, fixture, or environment identity. Native and relocated modes require the exact `LINUX_RUN_SMOKE_OK` line and all `LINUX_SMOKE_MARKER` names emitted by `report_marker_state`, including ordered hostile evidence. Renderer records `metadata.json`, configured scenario outcomes, clean `cleanupErrors`, and reaped browser descendants. Fixture records each locked-shell step, exit code, `BOUNDARIES_OK`, and `PINNED_ASSETS_OK`.

Execution-head binding contract: capture `git rev-parse HEAD^{commit}` once at replay start. Require the shared checkout's committed HEAD to equal that captured commit before and after every mode and immediately before final manifest admission; any concurrent commit/tip drift during the replay window fails. Reject any manifest or run record that names tracked or untracked dirty shared-worktree content as product source, lock, pin, fixture, or environment identity/input. After the manifest is finalized, historical validation binds to its recorded `execution_head` commit/tree and does not substitute or require equality with the repository's then-current tip, so commits made by later phase plans do not retroactively invalidate Plan 01 evidence. Extend `self-test-baseline` to prove concurrent pre-finalization tip drift and dirty shared-worktree inputs fail, while a post-finalization later-phase commit leaves validation bound to the previously captured execution commit.

Create `scripts/ref-replay-check.py` as a small Phase-01-specific validator using only Python standard library plus existing `git`. Encode the fixed commit, tree, inventory digests, pin-path matcher, and execution-head allowlist from `Fixed replay admission identity` as constants; none are manifest-provided policy. Define top-level manifest objects `production_baseline` and `execution_head`: production records the fixed commit/tree, canonical source and pin inventory digests, and every committed fixture path/mode/blob/content SHA-256; execution records the captured committed head/tree, asserts the baseline is its ancestor, records the exact allowlisted committed diff, and sets `working_tree_identity` to `excluded`. Derive both identities only from Git commit objects. Compute the captured execution commit's product inventory by removing only the finite allowlist from `git ls-tree -r` output and require byte-for-byte identity with the baseline inventory; inspect name-status and raw tree modes so additions, modifications, deletions, renames, copies, symlinks, and executable-bit changes outside the allowlist fail. A reachable 40-character commit is insufficient. Reject a recorded execution head that changes any product source, package/config input, lock, exact-pin manifest, fixture path/content/mode, `crates/napd/src/fixtures.rs`, `scripts/check-pinned-assets.sh`, `scripts/linux-run-smoke.sh`, or `uzel-poc-validated-pack/compatibility.lock`, even if the manifest and raw evidence are internally self-consistent. Require every run's `product_commit` and `product_tree` to equal the fixed baseline while its separate `execution_head` equals the top-level captured execution identity.

Complete the manifest contract with top-level artifact root and exactly one current result for `fixture`, `renderer`, `native`, and `relocated`; per run require exact argv/env, raw-output records, result, reason/limitation, marker records, timeout/cancel/signal state, and cleanup state. Require a hashed raw environment JSON with architecture, kernel, Nix, rustc, Cargo, Node, pnpm, flake-lock, Cargo-lock, pnpm-lock, baseline commit/tree, and execution-head fields. Resolve every raw path canonically beneath `.artifacts/phase-01/replay`, reject escapes and missing/non-regular files, and recompute SHA-256. Encode `self-test-baseline` using temporary Git objects to prove: an allowlist-only planning/harness/evidence descendant is admitted; arbitrary reachable source drift is rejected; lock drift is rejected; fixture add/modify/delete/rename/mode drift is rejected; and dirty shared-worktree content does not alter or enter either identity. Encode result-specific pass gates: fixture requires the complete locked-shell command set, zero exits, `BOUNDARIES_OK`, and `PINNED_ASSETS_OK`; renderer requires every scenario declared by the baseline `apps/uzel/tests/ui/playwright.config.mjs` with passed outcomes, matching baseline Git SHA, empty cleanup errors, and all owned browser processes exited; native and relocated require the exact smoke success line, every named marker from baseline source present, hostile ordering, and clean process teardown; relocated additionally requires the isolated target/env record, old-path scan absence, detached-worktree removal, and target cleanup. Every unavailable record requires a non-empty reason code, concrete reason, blocked command, and host/tool fact; every failed record retains raw output and cleanup. Missing, failed, or unavailable required modes may be stored honestly per D-08 but must make `--claim replay` exit nonzero. Require timeout/cancel/signal/cleanup records for every attempted run and reject a pass if a deadline fired, cancellation/cleanup was unaccounted for, a child remained alive, baseline tracked state drifted, or a marker was absent. Keep this validator local to REF-01. Write raw stdout/stderr and environment records under `.artifacts/phase-01/replay/&lt;run-id&gt;/`, redact native invoke-key material, hash each file, then atomically update the manifest. On contradiction, retain the failing record, exit nonzero, and make no production edit per D-11. This tracer proves one exact-build review/confirmation/launch/composition path from the approved product baseline through fixtures, Rust, trusted host, Chromium, Tauri/WebKit, denial, cancellation, restart, and cleanup while separately identifying the planning/execution commit.</action>
  <verify>
    <automated>bash -n scripts/phase-01-replay.sh &amp;&amp; python3 -c 'from pathlib import Path; compile(Path("scripts/ref-replay-check.py").read_text(encoding="utf-8"), "scripts/ref-replay-check.py", "exec")' &amp;&amp; python3 scripts/ref-replay-check.py self-test-baseline --repo . &amp;&amp; python3 scripts/ref-replay-check.py --repo . --manifest evidence/phase-01/replay-manifest.json --artifact-root .artifacts/phase-01/replay --claim replay</automated>
  </verify>
  <acceptance_criteria>All four evidence classes execute from fixed baseline `19519c378c2e775c6ad4b042cfd9aadd89f766b9` and carry validator-admitted baseline tree/source/lock/pin/fixture identity, separate captured execution-head identity, environment, raw hashes, markers, deadlines, cancel state, and cleanup. The captured execution commit is checked against the finite allowlist at replay start/end; concurrent replay-window tip drift and dirty shared-worktree inputs fail, while commits made after Plan 01 finalization do not retroactively invalidate the manifest. Allowlist-only planning/harness/evidence commits pass source equivalence; any other recorded execution commit or source/lock/pin/fixture drift fails. Unsupported modes remain honest `unavailable` records but cannot satisfy the replay claim. Any failed, unavailable, missing, escaped, unhashed, marker-incomplete, or cleanup-incomplete record blocks replay and seam work.</acceptance_criteria>
  <done>REF-01/02/03/04 production baseline is replayable from clean and relocated paths; manifest binds to its captured, finite-allowlist-admitted execution head; concurrent replay-window drift and dirty shared-worktree inputs are rejected; later phase tips do not rewrite historical admission.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile Work 07 without rewriting historical evidence</name>
  <read_first>
    uzel-poc-validated-pack/AGENTS.md
    uzel-poc-validated-pack/STATUS.md
    uzel-poc-validated-pack/docs/08-upstream-contributions.md
    evidence/POC_HANDOFF.md
    evidence/phase-01/replay-manifest.json
  </read_first>
  <files>evidence/phase-01/work-07-preservation.md</files>
  <action>Create a dated reconciliation record per D-06. Record the exact legacy file and its statement that Work 07/PR 30 is active, the source-backed merged production commit `19519c378c2e775c6ad4b042cfd9aadd89f766b9`, the exact planning/execution head, the replay-manifest path/hash, and the still-open Debian 13 visible acceptance. Preserve review, Debian, cancellation, restart, cleanup, and rollback gates as independent statuses. Do not edit `uzel-poc-validated-pack/STATUS.md`, merge/close/reopen external state, or claim Debian acceptance. If replay found a falsifier, record it as the next smallest probe and stop rather than changing production. Scope this evidence and resulting commit/PR only to repository-qualified issue REF-01A per D-17; apply D-18 review once to the immutable semantic candidate, not once per raw replay run.</action>
  <verify>
    <automated>test -s evidence/phase-01/work-07-preservation.md &amp;&amp; rg -q '19519c378c2e775c6ad4b042cfd9aadd89f766b9' evidence/phase-01/work-07-preservation.md &amp;&amp; rg -q 'Debian 13.*(open|unresolved|pending)' evidence/phase-01/work-07-preservation.md &amp;&amp; rg -q 'uzel-poc-validated-pack/STATUS.md' evidence/phase-01/work-07-preservation.md &amp;&amp; git diff --exit-code -- uzel-poc-validated-pack/STATUS.md</automated>
  </verify>
  <acceptance_criteria>Historical claim, merged source truth, current replay evidence, and unresolved human gate coexist without overwriting one another.</acceptance_criteria>
  <done>Work 07/PR 30 source state is reconciled; Debian acceptance remains explicitly open; no historical or external state was mutated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted napplet frame → trusted shell | Browser messages must bind to the host-created frame before any surface or principal reaches runtime code. |
| trusted shell/Tauri → private daemon | No raw frame network, Tauri bridge, host path, secret, or caller-selected principal crosses this boundary. |
| replay harness → fixed production baseline and later execution head | Product commands must use committed baseline `19519c378c2e775c6ad4b042cfd9aadd89f766b9`; later planning/harness commits are separate provenance and must satisfy the finite source-equivalence allowlist. |
| runtime → NMP | One runtime/NMP owner retains Nostr query, relay, store, freshness, provenance, signer, and publication truth. |

## STRIDE Threat Register — ASVS L1, block high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Spoofing / Elevation | trusted-shell message routing | high | mitigate | Replay existing `MessageEvent.source` binding, bounded-envelope checks, runtime-derived surface identity, and caller-principal denial before accepting REF-02 evidence. |
| T-01-02 | Elevation / Information disclosure | hostile frame and Tauri host | high | mitigate | Require separate Chromium and real-WebKit markers for denied raw egress, native invoke, paths, secrets, sentinel contact, and broad frame authority. |
| T-01-03 | Tampering | baseline source, locks, fixtures, execution head, raw artifacts, and relocated target | high | mitigate | Validator hard-codes the merged commit/tree/inventory digests, executes product commands in detached baseline worktrees, permits only the finite planning/harness/evidence diff, recomputes lock/pin/fixture/raw hashes, confines artifact paths, and rejects arbitrary reachable heads or stale absolute metadata. |
| T-01-04 | Denial of service | replay, cancellation, restart, cleanup | medium | mitigate | Use bounded timeouts, signal forwarding, idempotent stop/cancel, snapshot reconciliation, process-group cleanup, and retained failure logs. |
| T-01-05 | Tampering / Elevation | Napp/NMP ownership boundary | high | mitigate | This plan performs no adapter or private fallback change; it preserves one runtime/NMP authority and records current POC evidence only. |
| T-01-06 | Repudiation | Work 07 and replay evidence | medium | mitigate | Store exact heads, commands, timestamps, raw hashes, results, and limitations; preserve the historical file unchanged. |
</threat_model>

<verification>
- `bash -n scripts/phase-01-replay.sh` passes.
- `scripts/ref-replay-check.py self-test-baseline` proves allowlist-only planning/harness changes pass while arbitrary reachable source, lock, and fixture drift, dirty shared-worktree inputs, and concurrent pre-finalization tip drift fail; a post-finalization phase commit leaves validation bound to the captured execution commit.
- `scripts/ref-replay-check.py --claim replay` requires fixed baseline commit/tree/source/lock/pin/fixture identity, validates the recorded captured execution head against the finite allowlist instead of substituting the current tip, and admits fixture, renderer, native, and relocated modes without evidence-class substitution.
- Every pass has its exact marker set and clean deadline/cancel/process/worktree record; unavailable or failed evidence is retained but blocks the replay claim.
- Work 07 reconciliation names merged commit and open Debian gate while `git diff --exit-code -- uzel-poc-validated-pack/STATUS.md` passes.
- `git diff --name-only` for this plan contains only the four declared files; any falsified production behavior stops for a new scoped plan.
</verification>

<success_criteria>
- One real merged-production-baseline POC path is replayed across every existing layer before expansion; later planning/execution commits cannot redefine the product under test.
- Exact-build, source binding, hostile denial, cancellation, restart, reconciliation, and cleanup evidence is replayable from raw artifacts.
- Clean/relocated target proof cannot be confused with warm shared-target proof.
- Product/source/lock/pin/fixture drift, arbitrary recorded execution commits, concurrent replay-window tip drift, and dirty shared-worktree inputs fail admission; later phase commits do not retroactively invalidate the finalized captured-head evidence.
- Work 07 source truth is reconciled without claiming Debian acceptance or mutating historical/external state.
- Phase completion remains conditional on Napp qualification and later adapter parity.
</success_criteria>

## Multi-Source Coverage Audit

| Source | ID | Feature / decision | Plan | Status | Notes |
|--------|----|--------------------|------|--------|-------|
| GOAL | — | Replay/classify merged POC, admit authority, qualify Napp, then conditionally prove parity | 01–05 | COVERED-CONDITIONAL | Current lanes planned; REF-01D parity remains dependency-gated. |
| REQ | REF-01 | Exact-build review, confirmation, launch, rich profile/follows, composition | 01, 05 | COVERED | Current POC replay plus Debian visible evidence. |
| REQ | REF-02 | Source-created-surface binding and denied frame authority | 01 | COVERED | Existing boundary replay; adapter parity remains conditional. |
| REQ | REF-03 | Restart/recovery without duplicate runtime or Nostr truth | 01, 02 | COVERED | Replay plus state/retention ledger. |
| REQ | REF-04 | Chromium and Weston/WebKit evidence | 01, 05 | COVERED | Separate deterministic/native and Debian lanes. |
| REQ | REF-05 | Durable ownership classification | 02, 03 | COVERED | File/state/bounds map plus canonical authority admission. |
| REQ | REF-06 | Small reproducible lifecycle/resource baseline | 02 | COVERED | Raw measurements or honest unavailable records. |
| REQ | REF-07 | Accepted seam only; dependency record until qualified | 03, 04 | COVERED-GATE | Fail-closed handoff now; adapter parity explicitly blocked. |
| RESEARCH | A | Clean/relocated replay | 01 | COVERED | Tracer plan. |
| RESEARCH | B | Ownership, state, retention, bounds, measurements | 02 | COVERED | Evidence-only plan. |
| RESEARCH | C | Root product-document admission | 03 | COVERED | Documentation-only plan. |
| RESEARCH | Napp | Candidate qualification/handoff | 04 | COVERED | Read-only fail-closed plan. |
| RESEARCH | D | Narrow adapter parity | NONE | EXCLUDED-BLOCKED | Requires committed authority set plus qualifying exact candidate. |
| RESEARCH | E | Debian visible acceptance | 05 | COVERED | Human/platform checkpoint. |
| CONTEXT | D-01 | One exact reachable committed candidate; costly to change | 04 | COVERED-GATE | Qualification only; no integration. |
| CONTEXT | D-02 | Candidate failure stops only qualification/adaptation | 04 | COVERED | Other lanes remain independent. |
| CONTEXT | D-03 | Napp runtime authority and NMP sole Nostr owner | 02, 03, 04 | COVERED | Ownership/admission/handoff all preserve boundary. |
| CONTEXT | D-04 | Replay POC before extraction/replacement | 01 | COVERED | Leading tracer. |
| CONTEXT | D-05 | File-level ownership disposition | 02 | COVERED | Machine-readable map. |
| CONTEXT | D-06 | Reconcile merged PR 30; Debian still open | 01, 05 | COVERED | Historical status preserved. |
| CONTEXT | D-07 | Reproducible lifecycle/resource measurements | 02 | COVERED | Named methods and raw evidence. |
| CONTEXT | D-08 | Honest unavailable measurements | 02 | COVERED | No estimation. |
| CONTEXT | D-09 | Separate evidence/measurement modes | 01, 02 | COVERED | Separate records. |
| CONTEXT | D-10 | Typed bounded lifecycle outcomes | 01, 02 | COVERED | Replay and ledger. |
| CONTEXT | D-11 | Stop on smallest falsifier | 01, 04 | COVERED | Failure retained; no source workaround. |
| CONTEXT | D-12 | Dedicated upstream branch and contribution ledger | 04 | COVERED | Handoff records upstream owner; no local reusable fix. |
| CONTEXT | D-13 | One instance and one active read profile baseline | 02 | COVERED | Collision scope recorded, no expansion. |
| CONTEXT | D-14 | NMP canonical; bounded Uzel product/UI state only | 02 | COVERED | State/retention ledger. |
| CONTEXT | D-15 | One-axis migration and preserved-Poc rollback | 01, 02, 04 | COVERED-GATE | Current baseline/handoff records revert path; adapter omitted. |
| CONTEXT | D-16 | Proposed root pack must be audited/committed | 03 | COVERED | Authority admission plan. |
| CONTEXT | D-17 | One issue and bounded PR per plan | 01–05 | COVERED | Each objective names one repository-qualified slice. |
| CONTEXT | D-18 | Existing review sequence once per candidate | 01–05 | COVERED | Plans preserve sequence; CI enforcement deferred. |

<output>
Create `.planning/phases/01-slice-ref-01-poc-replay-accepted-napp-seam/01-01-SUMMARY.md` when done. Record repository-qualified issue/branch/PR if available, exact tested head, evidence paths, falsifiers, and next probe without copying the programme pack.
</output>
