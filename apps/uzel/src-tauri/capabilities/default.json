{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum authority for the empty Uzel shell",
  "windows": ["main"],
  "permissions": ["core:default"]
}
