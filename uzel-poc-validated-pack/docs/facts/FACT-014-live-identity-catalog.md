# FACT-014 — public identity, resource, and naddr catalog flow

- **Claim:** The Linux POC can load the supplied public identity and a signed napplet coordinate without browser network authority or product-owned Nostr selection.
- **Classification:** verified fact
- **Exact source/pin:** `jodobear/nampplets@d533a63d519c14470f900323958509cdea1c6479`, including fork PRs [#2](https://github.com/jodobear/nampplets/pull/2), [#3](https://github.com/jodobear/nampplets/pull/3), and [#4](https://github.com/jodobear/nampplets/pull/4); `pablof7z/nmp@005dc2a5f12aa414961b313d05ebb021934e385c`; released NAP packages remain pinned by `compatibility.lock`.
- **Probe/command:** `pnpm smoke`, which invokes `cargo test -p napd runner::tests::public_identity_profile_follows_and_picture_cross_only_native_providers -- --ignored --exact` and rejects output unless exactly one test runs and that exact test reports `ok`; exact ignored live test `public_naddr_reviews_confirms_and_launches_exact_single_file`; full locked workspace tests; strict Clippy; Unix-socket lost-response tests; browser click tests with console inspection and screenshots; real Weston/WebKit `pnpm smoke:linux`.
- **Observed result:** `npub16c9a45p5dr6l3jzmrvgdh9m7xy994tatxd6sm7kmxaygkq4lertsfnacfm` decoded to `d60bdad03468f5f8c85b1b10db977e310a5aafab33750dfadb37488b02bfc8d7`. NMP returned a canonical named kind-0 profile and at least 100 unique direct follows. The profile's HTTPS image crossed the native NAP-RESOURCE provider and returned an image MIME plus non-trivial bytes; WebKit received only an object URL. The successful `pnpm smoke` output contained `running 1 test` and the exact named test's `ok` line, preventing a stale filter from passing with zero tests. Restart with no relay lanes returned the same cached profile. The signed Good Morning Protocol naddr resolved to the expected author, d-tag, event, and aggregate; exact bytes verified, required capability denial blocked install, approval installed and launched the frozen build, and closing stopped it. Repeated confirmation mapped the second launch to its newly created runtime session rather than an older matching exact build. Browser clicks proved synchronous mount refusal and asynchronous child environment rejection both stop the new session, restore the base workspace, and preserve base 2/2 readiness. Every accepted post-launch snapshot must refine to exactly one newly created running session with the frozen coordinate; any other newly created sessions are stopped before refusal. If the post-launch projection itself is refused and therefore exposes no session identifier, the daemon closes the controller and clears its surface map so the unidentifiable session and all other runtime work terminate fail-closed. A post-launch verified-document refusal or UTF-8 failure stops its identified runtime session before returning. If the IPC asset transfer fails after the daemon returns a surface, the client sends `StopFixture`; if that cleanup also fails, Tauri returns a structured error containing the surface token and the shell enters a compact locked recovery state. A forced failed retry kept identity/catalog controls disabled and the exact token visible; the next idempotent stop succeeded and restored the workspace. Identity-change teardown now retains every base-pane surface until its stop response succeeds. Playwright forced one profile stop response to fail, observed the exact token in a compact locked recovery card while the follow stop completed, clicked Retry, and proved the same token was stopped idempotently before two fresh panes reached 2/2 readiness. Review cancellation removes local state only after confirmed runtime cancellation or the exact terminal `stale-review` result, leaving any retryable refusal tracked. Surface stop and review cancellation are idempotent when the daemon already removed their state. Mocked-browser lost-response probes proved that the first failed loaded-napplet stop keeps the loaded lock, the first failed cancellation keeps the exact review visible, and each second Close succeeds. Review and confirmation now carry client operation IDs retained across ambiguous protocol failures. The daemon keeps a bounded 64-entry exact request/response replay cache, rejects an operation ID reused with different input, and returns the original review token or launched surface after a lost response rather than executing the catalog operation twice. Real Unix-socket tests dropped two review responses before a later caller retry and dropped the first confirmation response before the automatic retry; every retry carried the identical operation ID and the confirmation fetched the single returned surface. A fresh renderer owns no prior daemon surfaces: native startup reconciliation cancels any stale hostile sentinel, stops every surface in the initial daemon snapshot once, and takes a second authoritative snapshot. A lost stop response is accepted only when the second snapshot proves that exact surface absent. Any remaining or newly observed token is shown in a compact recovery card and identity/catalog controls remain locked until Retry produces an empty second snapshot. Svelte teardown also sends best-effort exact-token stops and review cancellation, while the next startup remains the authoritative recovery boundary. Browser fault injection kept the exact orphan token visible across a failed retry, kept both identity and napplet loading disabled, and reported zero console errors. Superseding an active-profile request with a followed-profile request re-enables Refresh only when the newest request settles. While any cleanup or arbitrary napplet remains open, identity switching and another catalog load are explicitly disabled. The boundary scanner admits object URLs only through the unshadowed native `URL.createObjectURL` binding and rejects reassignment of native object-URL methods. Real Weston/WebKit smoke again reported daemon, shell, exact-build, NAP-SHELL, artifact, source-binding, hostile-network, sentinel, native-bridge, user-mode, and compositor success markers.
- **Ownership:** Uzel supplies bounded secure indexer/app relay lanes, public read-key selection, product-level Blossom fallback-server configuration, Linux HTTPS resource transport, UI review/consent, and daemon IPC. The reusable `crates/napd` resource adapter consumes configured Blossom servers but does not choose product endpoints. NMP owns relay subscriptions, reconnects, NIP-65 discovery, canonical replacement, persistence, and evidence. Nampplets owns NAP providers, permission review, catalog resolution, exact artifact verification, installation, lifecycle, and NAP-RESOURCE public-network/MIME/size/redirect/DNS-pin policy.
- **Rejected assumptions:** `EXACT DOMAINS AVAILABLE` was not useful user language and was replaced with explicit unavailable capability names or `All requested capabilities ready`. A fallback relay is not an app-content lane. Relay hints in an naddr are resolution evidence, not caller-owned routing truth. Configured Blossom servers are fallback sources, not an HTTPS-origin allowlist: ordinary public HTTPS resource URLs remain admissible subject to upstream NAP-RESOURCE policy. Browser `img.src` must not receive the remote HTTPS URL; only NAP-RESOURCE-returned object URLs are admitted by the boundary scanner.
- **Limit:** the current Linux rasterizer returns a typed refusal for SVG. The tested supplied profile image is JPEG; this fact does not claim SVG rendering.
- **Revalidate when:** any nampplets/NMP/NAP pin, operator relay lane, resource transport policy, naddr resolver, permission flow, or trusted-shell boundary changes.
