{
  "name": "uzel-workspace",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "packageManager": "pnpm@10.8.0",
  "engines": {
    "node": ">=22.12 <23"
  },
  "pnpm": {
    "overrides": {
      "@napplet/conformance": "0.14.0",
      "@napplet/core": "0.29.0",
      "playwright": "1.62.0"
    }
  },
  "devDependencies": {
    "@napplet/cli": "0.0.0",
    "fallow": "3.9.1"
  },
  "scripts": {
    "dev": "bash scripts/dev.sh",
    "build": "pnpm --filter './napplets/*' build && pnpm --filter @uzel/shell build && cargo build --workspace",
    "check": "pnpm --filter './napplets/*' build && pnpm --filter @uzel/shell check && pnpm --filter @uzel/shell build && cargo check --workspace",
    "test": "node --test contracts/*.test.mjs scripts/verify-external-napplet-corpus.test.mjs && bash scripts/verify-external-napplet-corpus.sh && bash scripts/verify-external-napplet-corpus.test.sh && pnpm --filter './napplets/*' test && pnpm --filter @uzel/shell test && cargo test --workspace --exclude uzel && cargo test -p uzel --no-default-features && bash scripts/check-pinned-assets.sh",
    "test:ui": "pnpm --filter @uzel/shell test:ui",
    "test:conformance": "pnpm --filter @jodobear/follow-list test:conformance && pnpm --filter @jodobear/profile-card test:conformance",
    "lint": "cargo clippy --workspace --all-targets -- -D warnings && pnpm check:boundaries",
    "check:boundaries": "bash scripts/check-boundaries.sh",
    "format:check": "cargo fmt --all -- --check",
    "docs:check": "python3 uzel-poc-validated-pack/scripts/audit_docs.py",
    "fallow": "fallow --root . --config config/fallow.jsonc --format json --quiet --fail-on-issues",
    "smoke": "bash scripts/smoke.sh",
    "smoke:linux": "bash scripts/linux-run-smoke.sh",
    "smoke:fedora": "bash scripts/fedora-run-smoke.sh",
    "smoke:debian13": "bash scripts/debian13-live-test.sh headless",
    "dev:debian13": "bash scripts/debian13-live-test.sh interactive",
    "smoke:debian": "bash scripts/debian-build-smoke.sh"
  }
}
